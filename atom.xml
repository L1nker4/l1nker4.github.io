<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>l1nker4&#39;s Blog</title>
  
  <subtitle>Talk is cheap,show me the code.</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://l1n.wang/"/>
  <updated>2020-08-05T09:43:26.469Z</updated>
  <id>https://l1n.wang/</id>
  
  <author>
    <name>L1nker4</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>TreeMap源码分析</title>
    <link href="https://l1n.wang/2020/08/java-treemap/"/>
    <id>https://l1n.wang/2020/08/java-treemap/</id>
    <published>2020-08-05T01:17:29.000Z</published>
    <updated>2020-08-05T09:43:26.469Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p><code>TreeMap</code>底层通过红黑树实现，在查询性能上能达到<code>O(logn)</code>，由于使用红黑树结构进行存储，所以<code>TreeMap</code>的元素都是有序的。同时，这也是一个非线程安全的<code>Map</code>，无法在并发环境下使用。</p><p><code>TreeMap</code>继承自<code>AbstractMap</code>，该类Map接口的抽象实现。实现了 <code>NavigableMap</code>、<code>Cloneable</code>和 <code>Serializable</code>接口。其中<code>NavigableMap</code>继承自<code>SortedMap</code>，这保证了<code>TreeMap</code>的有序性。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TreeMap</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt;    <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractMap</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt;    <span class="hljs-keyword">implements</span> <span class="hljs-title">NavigableMap</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt;, <span class="hljs-title">Cloneable</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span></span></code></pre></div><h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><p><code>TreeMap</code>采用红黑树进行构建，红黑树是一种自平衡二叉查找树。插入、删除、查找的时间复杂度为<code>O(logn)</code>。与另一个自平衡二叉查找树<code>AVL Tree</code>相比，红黑树以减少旋转操作牺牲部分平衡性，但是其整体性能优于<code>AVL Tree</code>。</p><p>有关红黑树的定义如下（摘自wikipedia）：</p><ol><li>节点是红色或黑色。</li><li>根是黑色。</li><li>所有叶子都是黑色（叶子是NIL节点）。</li><li>每个红色节点必须有两个黑色的子节点。（从每个叶子到根的所有路径上不能有两个连续的红色节点。）</li><li>从任一节点到其每个叶子的所有简单路径都包含相同数目的黑色节点。</li></ol><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/java/treemap/rbtree-construction.png" srcset="/img/loading.gif" alt="红黑树结构示意图（摘自Wikipedia）"></p><p><code>TreeMap</code>中树节点的定义如下：</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Entry</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title">Map</span>.<span class="hljs-title">Entry</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt; </span>&#123;        K key;        V value;        Entry&lt;K,V&gt; left;        Entry&lt;K,V&gt; right;        Entry&lt;K,V&gt; parent;        <span class="hljs-keyword">boolean</span> color = BLACK;                Entry(K key, V value, Entry&lt;K,V&gt; parent) &#123;            <span class="hljs-keyword">this</span>.key = key;            <span class="hljs-keyword">this</span>.value = value;            <span class="hljs-keyword">this</span>.parent = parent;        &#125;                <span class="hljs-function"><span class="hljs-keyword">public</span> K <span class="hljs-title">getKey</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> key;        &#125;                <span class="hljs-function"><span class="hljs-keyword">public</span> V <span class="hljs-title">getValue</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> value;        &#125;            <span class="hljs-function"><span class="hljs-keyword">public</span> V <span class="hljs-title">setValue</span><span class="hljs-params">(V value)</span> </span>&#123;            V oldValue = <span class="hljs-keyword">this</span>.value;            <span class="hljs-keyword">this</span>.value = value;            <span class="hljs-keyword">return</span> oldValue;        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">equals</span><span class="hljs-params">(Object o)</span> </span>&#123;            <span class="hljs-keyword">if</span> (!(o <span class="hljs-keyword">instanceof</span> Map.Entry))                <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;            Map.Entry&lt;?,?&gt; e = (Map.Entry&lt;?,?&gt;)o;            <span class="hljs-keyword">return</span> valEquals(key,e.getKey()) &amp;&amp; valEquals(value,e.getValue());        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">hashCode</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">int</span> keyHash = (key==<span class="hljs-keyword">null</span> ? <span class="hljs-number">0</span> : key.hashCode());            <span class="hljs-keyword">int</span> valueHash = (value==<span class="hljs-keyword">null</span> ? <span class="hljs-number">0</span> : value.hashCode());            <span class="hljs-keyword">return</span> keyHash ^ valueHash;        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">toString</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> key + <span class="hljs-string">"="</span> + value;        &#125;    &#125;</code></pre></div><h2 id="成员变量"><a href="#成员变量" class="headerlink" title="成员变量"></a>成员变量</h2><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">//TreeMap中用来确定顺序的comparator</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Comparator&lt;? <span class="hljs-keyword">super</span> K&gt; comparator;<span class="hljs-comment">//树的根节点</span>   <span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> Entry&lt;K,V&gt; root;   <span class="hljs-comment">//树的大小</span>   <span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> <span class="hljs-keyword">int</span> size = <span class="hljs-number">0</span>;   <span class="hljs-comment">//结构变化计数器</span>   <span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> <span class="hljs-keyword">int</span> modCount = <span class="hljs-number">0</span>;<span class="hljs-comment">//EntrySet</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> EntrySet entrySet;   <span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> KeySet&lt;K&gt; navigableKeySet;   <span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> NavigableMap&lt;K,V&gt; descendingMap;<span class="hljs-comment">//SubMapIterator中fence == null时，key的值</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Object UNBOUNDED = <span class="hljs-keyword">new</span> Object();<span class="hljs-comment">//RB Tree的颜色变量</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> RED   = <span class="hljs-keyword">false</span>;   <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> BLACK = <span class="hljs-keyword">true</span>;</code></pre></div><h2 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h2><p>共有四个构造方法：</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">TreeMap</span><span class="hljs-params">()</span> </span>&#123;        comparator = <span class="hljs-keyword">null</span>;    &#125;    <span class="hljs-comment">//传入comparator</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">TreeMap</span><span class="hljs-params">(Comparator&lt;? <span class="hljs-keyword">super</span> K&gt; comparator)</span> </span>&#123;        <span class="hljs-keyword">this</span>.comparator = comparator;    &#125;    <span class="hljs-comment">//传入map</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">TreeMap</span><span class="hljs-params">(Map&lt;? extends K, ? extends V&gt; m)</span> </span>&#123;        comparator = <span class="hljs-keyword">null</span>;        putAll(m);    &#125;   <span class="hljs-comment">//传入有序map</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">TreeMap</span><span class="hljs-params">(SortedMap&lt;K, ? extends V&gt; m)</span> </span>&#123;        comparator = m.comparator();        <span class="hljs-keyword">try</span> &#123;            buildFromSorted(m.size(), m.entrySet().iterator(), <span class="hljs-keyword">null</span>, <span class="hljs-keyword">null</span>);        &#125; <span class="hljs-keyword">catch</span> (java.io.IOException cannotHappen) &#123;        &#125; <span class="hljs-keyword">catch</span> (ClassNotFoundException cannotHappen) &#123;        &#125;    &#125;</code></pre></div><h2 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h2><h3 id="左旋"><a href="#左旋" class="headerlink" title="左旋"></a>左旋</h3><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/java/treemap/rbtree-rotateLeft.png" srcset="/img/loading.gif" alt="左旋操作"></p><p>上图中的5失衡，需要对该节点进行左旋进行修复。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">/** From CLR */</span>    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">rotateLeft</span><span class="hljs-params">(Entry&lt;K,V&gt; p)</span> </span>&#123;        <span class="hljs-keyword">if</span> (p != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//r为右子树节点</span>            Entry&lt;K,V&gt; r = p.right;           <span class="hljs-comment">//p的右子树换成它子的左孩子节点</span>            p.right = r.left;            <span class="hljs-comment">//如果p的右孩子节点不为空</span>            <span class="hljs-keyword">if</span> (r.left != <span class="hljs-keyword">null</span>)                <span class="hljs-comment">//将该节点的父节指向p</span>                r.left.parent = p;            <span class="hljs-comment">//r的父节点指向p的父结点</span>            r.parent = p.parent;            <span class="hljs-comment">//判断旋转的p节点是否为树的根节点</span>            <span class="hljs-keyword">if</span> (p.parent == <span class="hljs-keyword">null</span>)                <span class="hljs-comment">//如果是，将根节点设置为r</span>                root = r;            <span class="hljs-comment">//如果失衡节点p是父节点的左孩子节点</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (p.parent.left == p)                <span class="hljs-comment">//将父节点的左孩子节点设置为r</span>                p.parent.left = r;            <span class="hljs-keyword">else</span>                <span class="hljs-comment">//失衡节点是父节点的右孩子节点</span>                p.parent.right = r;            <span class="hljs-comment">//将r节点的左子树设置为失衡节点p</span>            r.left = p;            p.parent = r;        &#125;    &#125;</code></pre></div><h3 id="右旋"><a href="#右旋" class="headerlink" title="右旋"></a>右旋</h3><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/java/treemap/rbtree-rorateRight.jpg" srcset="/img/loading.gif" alt="右旋操作"></p><p>上图中的10失衡，需要对该节点进行右旋进行修复。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">rotateRight</span><span class="hljs-params">(Entry&lt;K,V&gt; p)</span> </span>&#123;    <span class="hljs-comment">//失衡节点传入不为空</span>        <span class="hljs-keyword">if</span> (p != <span class="hljs-keyword">null</span>) &#123;           <span class="hljs-comment">//l为失衡节点的左子树</span>            Entry&lt;K,V&gt; l = p.left;            <span class="hljs-comment">//将失衡节点的左孩子节点指向它左子树的左孩子节点</span>            p.left = l.right;            <span class="hljs-comment">//l的右子树不为空，将右子树的父指针指向p</span>            <span class="hljs-keyword">if</span> (l.right != <span class="hljs-keyword">null</span>) l.right.parent = p;            <span class="hljs-comment">//l升级为原来p节点的地位</span>            l.parent = p.parent;            <span class="hljs-comment">//如果原来的p节点为根节点，将l设置为根节点</span>            <span class="hljs-keyword">if</span> (p.parent == <span class="hljs-keyword">null</span>)                root = l;            <span class="hljs-comment">//如果p是父节点的右孩子，则将其父节点的右孩子设置为l</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (p.parent.right == p)                p.parent.right = l;            <span class="hljs-keyword">else</span> p.parent.left = l;            <span class="hljs-comment">//设置l的右节点为p，右旋完成</span>            l.right = p;            p.parent = l;        &#125;    &#125;</code></pre></div><h2 id="API"><a href="#API" class="headerlink" title="API"></a>API</h2><h3 id="get"><a href="#get" class="headerlink" title="get"></a>get</h3><p><code>get</code>通过调用<code>getEntry</code>获取对应的<code>entry</code>。返回的是<code>entry.value</code>。查找逻辑较为简单，是<code>BST</code>经典查询代码。代码注释如下：</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> V <span class="hljs-title">get</span><span class="hljs-params">(Object key)</span> </span>&#123;        Entry&lt;K,V&gt; p = getEntry(key);        <span class="hljs-keyword">return</span> (p==<span class="hljs-keyword">null</span> ? <span class="hljs-keyword">null</span> : p.value);&#125;<span class="hljs-function"><span class="hljs-keyword">final</span> Entry&lt;K,V&gt; <span class="hljs-title">getEntry</span><span class="hljs-params">(Object key)</span> </span>&#123;        <span class="hljs-comment">// Offload comparator-based version for sake of performance</span>        <span class="hljs-keyword">if</span> (comparator != <span class="hljs-keyword">null</span>)            <span class="hljs-keyword">return</span> getEntryUsingComparator(key);    <span class="hljs-comment">//不允许key为空</span>        <span class="hljs-keyword">if</span> (key == <span class="hljs-keyword">null</span>)            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> NullPointerException();        <span class="hljs-meta">@SuppressWarnings</span>(<span class="hljs-string">"unchecked"</span>)            Comparable&lt;? <span class="hljs-keyword">super</span> K&gt; k = (Comparable&lt;? <span class="hljs-keyword">super</span> K&gt;) key;        Entry&lt;K,V&gt; p = root;    <span class="hljs-comment">//从根节点开始找</span>        <span class="hljs-keyword">while</span> (p != <span class="hljs-keyword">null</span>) &#123;                        <span class="hljs-keyword">int</span> cmp = k.compareTo(p.key);            <span class="hljs-comment">//比key大，往左子树找</span>            <span class="hljs-keyword">if</span> (cmp &lt; <span class="hljs-number">0</span>)                p = p.left;            <span class="hljs-comment">//比key小，往右子树找</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (cmp &gt; <span class="hljs-number">0</span>)                p = p.right;            <span class="hljs-keyword">else</span>                <span class="hljs-keyword">return</span> p;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre></div><h3 id="put"><a href="#put" class="headerlink" title="put"></a>put</h3><p><code>put</code>方法首先检查是否已经存在该<code>key</code>，如果有则覆盖，没有则构造新节点进行插入。插入后调用<code>fixAfterInsertion</code>进行红黑树的修复。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> V <span class="hljs-title">put</span><span class="hljs-params">(K key, V value)</span> </span>&#123;        Entry&lt;K,V&gt; t = root;    <span class="hljs-comment">//根节点为空</span>        <span class="hljs-keyword">if</span> (t == <span class="hljs-keyword">null</span>) &#123;            compare(key, key); <span class="hljs-comment">// type (and possibly null) check</span>            root = <span class="hljs-keyword">new</span> Entry&lt;&gt;(key, value, <span class="hljs-keyword">null</span>);            size = <span class="hljs-number">1</span>;            modCount++;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;        <span class="hljs-keyword">int</span> cmp;        Entry&lt;K,V&gt; parent;        <span class="hljs-comment">// split comparator and comparable paths</span>        Comparator&lt;? <span class="hljs-keyword">super</span> K&gt; cpr = comparator;    <span class="hljs-comment">//comparator不为空</span>        <span class="hljs-keyword">if</span> (cpr != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//遍历找到与该key相等的节点，覆盖旧值</span>            <span class="hljs-keyword">do</span> &#123;                parent = t;                cmp = cpr.compare(key, t.key);                <span class="hljs-keyword">if</span> (cmp &lt; <span class="hljs-number">0</span>)                    t = t.left;                <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (cmp &gt; <span class="hljs-number">0</span>)                    t = t.right;                <span class="hljs-keyword">else</span>                    <span class="hljs-keyword">return</span> t.setValue(value);            &#125; <span class="hljs-keyword">while</span> (t != <span class="hljs-keyword">null</span>);        &#125;        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//comparator为空</span>            <span class="hljs-keyword">if</span> (key == <span class="hljs-keyword">null</span>)                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> NullPointerException();            <span class="hljs-meta">@SuppressWarnings</span>(<span class="hljs-string">"unchecked"</span>)                Comparable&lt;? <span class="hljs-keyword">super</span> K&gt; k = (Comparable&lt;? <span class="hljs-keyword">super</span> K&gt;) key;            <span class="hljs-keyword">do</span> &#123;                parent = t;                cmp = k.compareTo(t.key);                <span class="hljs-keyword">if</span> (cmp &lt; <span class="hljs-number">0</span>)                    t = t.left;                <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (cmp &gt; <span class="hljs-number">0</span>)                    t = t.right;                <span class="hljs-keyword">else</span>                    <span class="hljs-keyword">return</span> t.setValue(value);            &#125; <span class="hljs-keyword">while</span> (t != <span class="hljs-keyword">null</span>);        &#125;    <span class="hljs-comment">//没有相同的key，构造新Entry进行插入</span>        Entry&lt;K,V&gt; e = <span class="hljs-keyword">new</span> Entry&lt;&gt;(key, value, parent);        <span class="hljs-keyword">if</span> (cmp &lt; <span class="hljs-number">0</span>)            parent.left = e;        <span class="hljs-keyword">else</span>            parent.right = e;    <span class="hljs-comment">//插入后修复</span>        fixAfterInsertion(e);        size++;        modCount++;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;</code></pre></div><h3 id="remove"><a href="#remove" class="headerlink" title="remove"></a>remove</h3><p><code>remove</code>方法首先调用<code>getEntry</code>获取需要删除的entry，调用<code>deleteEntry</code>进行删除。红黑树的删除逻辑与二叉查找树相类似，可以分为两种情况：</p><ol><li>待删除节点P的左右子树都为空，则直接删除</li><li>待删除节点P的左右子树非空，用P的后继节点代替P</li></ol><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> V <span class="hljs-title">remove</span><span class="hljs-params">(Object key)</span> </span>&#123;        Entry&lt;K,V&gt; p = getEntry(key);        <span class="hljs-keyword">if</span> (p == <span class="hljs-keyword">null</span>)            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        V oldValue = p.value;        deleteEntry(p);        <span class="hljs-keyword">return</span> oldValue;    &#125;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">deleteEntry</span><span class="hljs-params">(Entry&lt;K,V&gt; p)</span> </span>&#123;        modCount++;        size--;        <span class="hljs-comment">//上述的第二种情况，找到P的后继节点代替它</span>        <span class="hljs-keyword">if</span> (p.left != <span class="hljs-keyword">null</span> &amp;&amp; p.right != <span class="hljs-keyword">null</span>) &#123;            Entry&lt;K,V&gt; s = successor(p);            p.key = s.key;            p.value = s.value;            p = s;        &#125; <span class="hljs-comment">// p has 2 children</span>        <span class="hljs-comment">// Start fixup at replacement node, if it exists.</span>        Entry&lt;K,V&gt; replacement为 = (p.left != <span class="hljs-keyword">null</span> ? p.left : p.right);<span class="hljs-comment">//replacement用来代替删除节点</span>        <span class="hljs-keyword">if</span> (replacement != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// Link replacement to parent</span>            replacement.parent = p.parent;            <span class="hljs-comment">//p没有父节点</span>            <span class="hljs-keyword">if</span> (p.parent == <span class="hljs-keyword">null</span>)                <span class="hljs-comment">//说明它是根节点，直接将replacement设置为根节点。</span>                root = replacement;            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (p == p.parent.left)                p.parent.left  = replacement;            <span class="hljs-keyword">else</span>                p.parent.right = replacement;            <span class="hljs-comment">// Null out links so they are OK to use by fixAfterDeletion.</span>            p.left = p.right = p.parent = <span class="hljs-keyword">null</span>;            <span class="hljs-comment">// 进行删除后修复</span>            <span class="hljs-keyword">if</span> (p.color == BLACK)                fixAfterDeletion(replacement);        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (p.parent == <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// return if we are the only node.</span>            root = <span class="hljs-keyword">null</span>;        &#125; <span class="hljs-keyword">else</span> &#123; <span class="hljs-comment">//  No children. Use self as phantom replacement and unlink.</span>            <span class="hljs-keyword">if</span> (p.color == BLACK)                fixAfterDeletion(p);            <span class="hljs-keyword">if</span> (p.parent != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">if</span> (p == p.parent.left)                    p.parent.left = <span class="hljs-keyword">null</span>;                <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (p == p.parent.right)                    p.parent.right = <span class="hljs-keyword">null</span>;                p.parent = <span class="hljs-keyword">null</span>;            &#125;        &#125;    &#125;</code></pre></div><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文介绍了<code>TreeMap</code>的数据结构上的实现，并介绍了红黑树的基本概念，并对增删改查的接口做了简要介绍，但是并未深入探究修复的接口（<code>fixAfterDeletion</code>和<code>fixAfterInsertion</code>）。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;&lt;code&gt;TreeMap&lt;/code&gt;底层通过红黑树实现，在查询性能上能达到&lt;code&gt;O(logn)&lt;/code&gt;，由于使用红黑树结构进
      
    
    </summary>
    
      <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
      <category term="集合框架" scheme="https://l1n.wang/categories/Java/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/"/>
    
    
      <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
      <category term="红黑树" scheme="https://l1n.wang/tags/%E7%BA%A2%E9%BB%91%E6%A0%91/"/>
    
  </entry>
  
  <entry>
    <title>HashMap源码分析</title>
    <link href="https://l1n.wang/2020/08/java-hashmap/"/>
    <id>https://l1n.wang/2020/08/java-hashmap/</id>
    <published>2020-08-02T09:15:18.000Z</published>
    <updated>2020-08-03T09:00:27.426Z</updated>
    
    <content type="html"><![CDATA[<h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>​        <code>HashMap</code>是<code>Map</code>接口基于哈希的实现，在查询性能上，时间复杂度接近<code>O(1)</code>。在<code>JDK 7</code>中采用了数组+链表的数据结构，在<code>JDK 8</code>后，底层数据结构转变为<code>数组+链表+红黑树</code>，也就是当出现Hash冲突时，链表长度过长，转为红黑树存储节点提高搜索效率。<code>HashMap</code>为非线程安全的类，多线程处理时会出现问题。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HashMap</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractMap</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt;    <span class="hljs-keyword">implements</span> <span class="hljs-title">Map</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt;, <span class="hljs-title">Cloneable</span>, <span class="hljs-title">Serializable</span></span></code></pre></div><p><code>HashMap</code> 继承了 <code>AbstractMap</code> ，该类提供了Map接口的抽象实现，并提供了一些方法的基本实现。实现了 <code>Map</code>、<code>Cloneable</code>和 <code>Serializable</code>接口。</p><h2 id="成员变量"><a href="#成员变量" class="headerlink" title="成员变量"></a>成员变量</h2><ul><li>loadFactor：该变量控制table数组存放数据的疏密程度，越趋向1时，数组中存放的数据越多越密。链表的长度会增加，因此会导致查找效率变低。该值越小，则数组中存放的数据越少，越稀疏，则会导致空间利用率下降。默认值0.75是较好的默认值。</li><li>threshold：当前 <code>HashMap</code>所能容纳键值对数量的最大值，超过这个值，则需扩容。<strong>threshold = capacity * loadFactor</strong><ul><li>默认容量为16，默认负载因子为0.75，当存放的数据达到16 * 0.75时，需要进行扩容。</li></ul></li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">//存储元素的数组，大小为2的幂次</span><span class="hljs-keyword">transient</span> Node&lt;K,V&gt;[] table;<span class="hljs-comment">//存放具体元素的集</span>   <span class="hljs-keyword">transient</span> Set&lt;Map.Entry&lt;K,V&gt;&gt; entrySet;<span class="hljs-comment">//已经存放了的数组大小</span>   <span class="hljs-keyword">transient</span> <span class="hljs-keyword">int</span> size;<span class="hljs-comment">//结构修改的计数器</span>   <span class="hljs-keyword">transient</span> <span class="hljs-keyword">int</span> modCount;<span class="hljs-comment">//临界值，实际大小超过该值，则进行扩容</span>   <span class="hljs-keyword">int</span> threshold;<span class="hljs-comment">//负载因子</span>   <span class="hljs-keyword">final</span> <span class="hljs-keyword">float</span> loadFactor;<span class="hljs-comment">//默认初始容量：16</span><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> DEFAULT_INITIAL_CAPACITY = <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">4</span>; <span class="hljs-comment">// aka 16</span><span class="hljs-comment">//最大容量</span>   <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> MAXIMUM_CAPACITY = <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">30</span>;   <span class="hljs-comment">//默认的负载因子</span>   <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">float</span> DEFAULT_LOAD_FACTOR = <span class="hljs-number">0.75f</span>;   <span class="hljs-comment">//链表节点数大于该阈值，转为红黑树存储</span>   <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> TREEIFY_THRESHOLD = <span class="hljs-number">8</span>;   <span class="hljs-comment">//红黑树节点数小于该值，转为链表存储</span>   <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> UNTREEIFY_THRESHOLD = <span class="hljs-number">6</span>;<span class="hljs-comment">//树化时，检查table数组长度是否大于该值，小于则扩容</span>   <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> MIN_TREEIFY_CAPACITY = <span class="hljs-number">64</span>;</code></pre></div><h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><p>链表状态下的节点，继承自<code>Map.Entry&lt;K,V&gt;</code>。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Node</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title">Map</span>.<span class="hljs-title">Entry</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt; </span>&#123;    <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> hash;    <span class="hljs-keyword">final</span> K key;    V value;    Node&lt;K,V&gt; next;    <span class="hljs-comment">//构造方法</span>    Node(<span class="hljs-keyword">int</span> hash, K key, V value, Node&lt;K,V&gt; next) &#123;        <span class="hljs-keyword">this</span>.hash = hash;        <span class="hljs-keyword">this</span>.key = key;        <span class="hljs-keyword">this</span>.value = value;        <span class="hljs-keyword">this</span>.next = next;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> K <span class="hljs-title">getKey</span><span class="hljs-params">()</span>        </span>&#123; <span class="hljs-keyword">return</span> key; &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> V <span class="hljs-title">getValue</span><span class="hljs-params">()</span>      </span>&#123; <span class="hljs-keyword">return</span> value; &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> String <span class="hljs-title">toString</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-keyword">return</span> key + <span class="hljs-string">"="</span> + value; &#125;    <span class="hljs-comment">//Node的HashCode返回键值的HashCode异或值</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">hashCode</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> Objects.hashCode(key) ^ Objects.hashCode(value);    &#125;<span class="hljs-comment">//设置新值，返回旧值</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> V <span class="hljs-title">setValue</span><span class="hljs-params">(V newValue)</span> </span>&#123;        V oldValue = value;        value = newValue;        <span class="hljs-keyword">return</span> oldValue;    &#125;<span class="hljs-comment">//equals</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">equals</span><span class="hljs-params">(Object o)</span> </span>&#123;        <span class="hljs-keyword">if</span> (o == <span class="hljs-keyword">this</span>)            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        <span class="hljs-keyword">if</span> (o <span class="hljs-keyword">instanceof</span> Map.Entry) &#123;            Map.Entry&lt;?,?&gt; e = (Map.Entry&lt;?,?&gt;)o;            <span class="hljs-keyword">if</span> (Objects.equals(key, e.getKey()) &amp;&amp;                Objects.equals(value, e.getValue()))                <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;&#125;<span class="hljs-comment">//返回key的HashCode值</span><span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">hash</span><span class="hljs-params">(Object key)</span> </span>&#123;    <span class="hljs-keyword">int</span> h;    <span class="hljs-keyword">return</span> (key == <span class="hljs-keyword">null</span>) ? <span class="hljs-number">0</span> : (h = key.hashCode()) ^ (h &gt;&gt;&gt; <span class="hljs-number">16</span>);&#125;<span class="hljs-comment">//如果对象x的类是C，如果C实现了Comparable&lt;C&gt;接口，那么返回C，否则返回null</span><span class="hljs-keyword">static</span> Class&lt;?&gt; comparableClassFor(Object x) &#123;    <span class="hljs-keyword">if</span> (x <span class="hljs-keyword">instanceof</span> Comparable) &#123;        Class&lt;?&gt; c; Type[] ts, as; Type t; ParameterizedType p;        <span class="hljs-keyword">if</span> ((c = x.getClass()) == String<span class="hljs-class">.<span class="hljs-keyword">class</span>) // <span class="hljs-title">bypass</span> <span class="hljs-title">checks</span>            <span class="hljs-title">return</span> <span class="hljs-title">c</span></span>;        <span class="hljs-keyword">if</span> ((ts = c.getGenericInterfaces()) != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; ts.length; ++i) &#123;                <span class="hljs-keyword">if</span> (((t = ts[i]) <span class="hljs-keyword">instanceof</span> ParameterizedType) &amp;&amp;                    ((p = (ParameterizedType)t).getRawType() ==                     Comparable<span class="hljs-class">.<span class="hljs-keyword">class</span>) &amp;&amp;                    (<span class="hljs-title">as</span> </span>= p.getActualTypeArguments()) != <span class="hljs-keyword">null</span> &amp;&amp;                    as.length == <span class="hljs-number">1</span> &amp;&amp; as[<span class="hljs-number">0</span>] == c) <span class="hljs-comment">// type arg is c</span>                    <span class="hljs-keyword">return</span> c;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre></div><p>红黑树状态下的节点，继承自<code>LinkedHashMap.Entry&lt;K,V&gt;</code>，而该类继承自<code>HashMap.Node&lt;K,V&gt;</code>。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TreeNode</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title">LinkedHashMap</span>.<span class="hljs-title">Entry</span>&lt;<span class="hljs-title">K</span>,<span class="hljs-title">V</span>&gt; </span>&#123;        <span class="hljs-comment">//父结点</span>    TreeNode&lt;K,V&gt; parent;  <span class="hljs-comment">// red-black tree links</span>    <span class="hljs-comment">//左孩子节点</span>        TreeNode&lt;K,V&gt; left;    <span class="hljs-comment">//右孩子节点</span>        TreeNode&lt;K,V&gt; right;        TreeNode&lt;K,V&gt; prev;    <span class="hljs-comment">// needed to unlink next upon deletion</span>    <span class="hljs-comment">//颜色</span>        <span class="hljs-keyword">boolean</span> red;        TreeNode(<span class="hljs-keyword">int</span> hash, K key, V val, Node&lt;K,V&gt; next) &#123;            <span class="hljs-keyword">super</span>(hash, key, val, next);        &#125;</code></pre></div><h2 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h2><p>核心的构造方法是第一个，通过调用<code>tableSizeFor</code>为<code>threshold</code>（扩容临界值）赋值。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">HashMap</span><span class="hljs-params">(<span class="hljs-keyword">int</span> initialCapacity, <span class="hljs-keyword">float</span> loadFactor)</span> </span>&#123;       <span class="hljs-comment">//边界检测</span>       <span class="hljs-keyword">if</span> (initialCapacity &lt; <span class="hljs-number">0</span>)           <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">"Illegal initial capacity: "</span> +                                           initialCapacity);       <span class="hljs-comment">//边界检测</span>       <span class="hljs-keyword">if</span> (initialCapacity &gt; MAXIMUM_CAPACITY)           initialCapacity = MAXIMUM_CAPACITY;       <span class="hljs-keyword">if</span> (loadFactor &lt;= <span class="hljs-number">0</span> || Float.isNaN(loadFactor))           <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">"Illegal load factor: "</span> +                                              loadFactor);       <span class="hljs-keyword">this</span>.loadFactor = loadFactor;       <span class="hljs-keyword">this</span>.threshold = tableSizeFor(initialCapacity);   &#125;<span class="hljs-comment">//调用前一个构造方法</span>   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">HashMap</span><span class="hljs-params">(<span class="hljs-keyword">int</span> initialCapacity)</span> </span>&#123;       <span class="hljs-keyword">this</span>(initialCapacity, DEFAULT_LOAD_FACTOR);   &#125;  <span class="hljs-comment">//未传参数时,负载因子设置为默认值</span>   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">HashMap</span><span class="hljs-params">()</span> </span>&#123;       <span class="hljs-keyword">this</span>.loadFactor = DEFAULT_LOAD_FACTOR;   &#125;   <span class="hljs-comment">//传递Map的时候，调用putMapEntries进行批量添加</span>   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">HashMap</span><span class="hljs-params">(Map&lt;? extends K, ? extends V&gt; m)</span> </span>&#123;       <span class="hljs-keyword">this</span>.loadFactor = DEFAULT_LOAD_FACTOR;       putMapEntries(m, <span class="hljs-keyword">false</span>);   &#125;</code></pre></div><h2 id="API"><a href="#API" class="headerlink" title="API"></a>API</h2><h3 id="tableSizeFor"><a href="#tableSizeFor" class="headerlink" title="tableSizeFor"></a>tableSizeFor</h3><p>该方法在初始化时，对<code>threshold</code>赋值，通过位运算找到大于或等于cap的最小的2的幂次方。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">tableSizeFor</span><span class="hljs-params">(<span class="hljs-keyword">int</span> cap)</span> </span>&#123;    <span class="hljs-keyword">int</span> n = cap - <span class="hljs-number">1</span>;    n |= n &gt;&gt;&gt; <span class="hljs-number">1</span>;    n |= n &gt;&gt;&gt; <span class="hljs-number">2</span>;    n |= n &gt;&gt;&gt; <span class="hljs-number">4</span>;    n |= n &gt;&gt;&gt; <span class="hljs-number">8</span>;    n |= n &gt;&gt;&gt; <span class="hljs-number">16</span>;    <span class="hljs-keyword">return</span> (n &lt; <span class="hljs-number">0</span>) ? <span class="hljs-number">1</span> : (n &gt;= MAXIMUM_CAPACITY) ? MAXIMUM_CAPACITY : n + <span class="hljs-number">1</span>;&#125;</code></pre></div><h3 id="hash"><a href="#hash" class="headerlink" title="hash"></a>hash</h3><p>该方法在插入时，计算元素的hash值时调用。<code>hash</code>值为传入key的<code>hashCode</code>与其右移16位的异或值。这被称为<strong>扰动函数</strong>。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">hash</span><span class="hljs-params">(Object key)</span> </span>&#123;        <span class="hljs-keyword">int</span> h;        <span class="hljs-keyword">return</span> (key == <span class="hljs-keyword">null</span>) ? <span class="hljs-number">0</span> : (h = key.hashCode()) ^ (h &gt;&gt;&gt; <span class="hljs-number">16</span>);&#125;</code></pre></div><p><code>hashCode</code>方法，是key的类自带的Hash方法，返回一个int的Hash值，理论上，这个值应当均匀得分布在int的范围内，但是<code>HashMap</code>初始化大小为16，如果让hash映射到16个桶中，通过取模实现十分简单。但是直接取模会造成较多的哈希碰撞。通过扰动函数，增加了随机性，减少了碰撞的几率。</p><p>那么如何通过hash值，获取对应的数组下标呢？在<code>putVal</code>中，获取下标的方法如下：<code>(n - 1) &amp; hash</code>，n是数组大小，上文中，<code>threshold</code>为2<sup>n</sup>，那么<code>n - 1</code><sub>2</sub> = 00….1111，那么通过<code>&amp;</code>运算，会保留下hash的低位。为何用<code>&amp;</code>替代取模运算，主要是位运算的效率远远高于取模运算。这也证明了为何<code>threshold</code>必须要是2的幂次方，通过控制该值，从而达到提高hash映射效率的目的。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">if</span> ((p = tab[i = (n - <span class="hljs-number">1</span>) &amp; hash]) == <span class="hljs-keyword">null</span>)</code></pre></div><h3 id="get"><a href="#get" class="headerlink" title="get"></a>get</h3><p>主要逻辑：</p><ol><li>根据hash找到指定位置的节点</li><li>判断第一个节点的key是否符合要求，符合要求直接返回第一个节点，否则继续查找。</li><li>如果是红黑树结构，通过红黑树查找节点数据并返回</li><li>如果是链表结构，遍历节点查询并返回</li><li>如果没有找到，返回null</li></ol><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> V <span class="hljs-title">get</span><span class="hljs-params">(Object key)</span> </span>&#123;    Node&lt;K,V&gt; e;    <span class="hljs-keyword">return</span> (e = getNode(hash(key), key)) == <span class="hljs-keyword">null</span> ? <span class="hljs-keyword">null</span> : e.value;&#125;<span class="hljs-function"><span class="hljs-keyword">final</span> Node&lt;K,V&gt; <span class="hljs-title">getNode</span><span class="hljs-params">(<span class="hljs-keyword">int</span> hash, Object key)</span> </span>&#123;    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; <span class="hljs-keyword">int</span> n; K k;        <span class="hljs-keyword">if</span> ((tab = table) != <span class="hljs-keyword">null</span> &amp;&amp; (n = tab.length) &gt; <span class="hljs-number">0</span> &amp;&amp;        (first = tab[(n - <span class="hljs-number">1</span>) &amp; hash]) != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">//第一个节点就命中</span>        <span class="hljs-keyword">if</span> (first.hash == hash &amp;&amp; <span class="hljs-comment">// always check first node</span>            ((k = first.key) == key || (key != <span class="hljs-keyword">null</span> &amp;&amp; key.equals(k))))            <span class="hljs-keyword">return</span> first;        <span class="hljs-keyword">if</span> ((e = first.next) != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//判断是否为红黑树节点</span>            <span class="hljs-keyword">if</span> (first <span class="hljs-keyword">instanceof</span> TreeNode)                <span class="hljs-keyword">return</span> ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);            <span class="hljs-comment">//如果是链表，就遍历链表找到相应的数据</span>            <span class="hljs-keyword">do</span> &#123;                <span class="hljs-keyword">if</span> (e.hash == hash &amp;&amp;                    ((k = e.key) == key || (key != <span class="hljs-keyword">null</span> &amp;&amp; key.equals(k))))                    <span class="hljs-keyword">return</span> e;            &#125; <span class="hljs-keyword">while</span> ((e = e.next) != <span class="hljs-keyword">null</span>);        &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre></div><h3 id="put"><a href="#put" class="headerlink" title="put"></a>put</h3><p>主要逻辑：</p><ol><li>桶数组table为空，通过<code>resize()</code>进行扩容。</li><li>查找要插入的键值对是否已经存在，存在的话，用新值替换旧值。</li><li>如果不存在，将键值对插入链表尾部，并根据长度判断是否将链表转换成红黑树。</li><li>判断键值对数量是否大于阈值，大于的话进行扩容操作。</li></ol><p>需要注意的是，<code>treeifyBin</code>方法在进行树化前，进行了检查。如果小于<code>MIN_TREEIFY_CAPACITY</code>，则进行扩容，不进行树化。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (tab == <span class="hljs-keyword">null</span> || (n = tab.length) &lt; MIN_TREEIFY_CAPACITY)</code></pre></div><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> V <span class="hljs-title">put</span><span class="hljs-params">(K key, V value)</span> </span>&#123;        <span class="hljs-keyword">return</span> putVal(hash(key), key, value, <span class="hljs-keyword">false</span>, <span class="hljs-keyword">true</span>);    &#125;<span class="hljs-function"><span class="hljs-keyword">final</span> V <span class="hljs-title">putVal</span><span class="hljs-params">(<span class="hljs-keyword">int</span> hash, K key, V value, <span class="hljs-keyword">boolean</span> onlyIfAbsent,                   <span class="hljs-keyword">boolean</span> evict)</span> </span>&#123;        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="hljs-keyword">int</span> n, i;        <span class="hljs-keyword">if</span> ((tab = table) == <span class="hljs-keyword">null</span> || (n = tab.length) == <span class="hljs-number">0</span>)            <span class="hljs-comment">//如果未初始化，进行数组初始化，赋予初始容量</span>            n = (tab = resize()).length;    <span class="hljs-comment">//通过hash找到下标，如果该位置为空</span>        <span class="hljs-keyword">if</span> ((p = tab[i = (n - <span class="hljs-number">1</span>) &amp; hash]) == <span class="hljs-keyword">null</span>)            <span class="hljs-comment">//直接将数据存储进去</span>            tab[i] = newNode(hash, key, value, <span class="hljs-keyword">null</span>);        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//发生hash碰撞</span>            Node&lt;K,V&gt; e; K k;            <span class="hljs-comment">//如果插入的key和当前key相同，将e指向当前键值对</span>            <span class="hljs-keyword">if</span> (p.hash == hash &amp;&amp;                ((k = p.key) == key || (key != <span class="hljs-keyword">null</span> &amp;&amp; key.equals(k))))                e = p;            <span class="hljs-comment">//如果当前节点类型是红黑树节点，使用红黑树进行插入</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (p <span class="hljs-keyword">instanceof</span> TreeNode)                e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(<span class="hljs-keyword">this</span>, tab, hash, key, value);            <span class="hljs-keyword">else</span> &#123;                <span class="hljs-comment">//链表的情况</span>                <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> binCount = <span class="hljs-number">0</span>; ; ++binCount) &#123;                    <span class="hljs-comment">//将新节点放到链表的末尾</span>                    <span class="hljs-keyword">if</span> ((e = p.next) == <span class="hljs-keyword">null</span>) &#123;                        p.next = newNode(hash, key, value, <span class="hljs-keyword">null</span>);                        <span class="hljs-comment">//如果链表长度达到红黑树化的阈值，将链表转化成红黑树</span>                        <span class="hljs-keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD - <span class="hljs-number">1</span>)                            treeifyBin(tab, hash);                        <span class="hljs-keyword">break</span>;                    &#125;                    <span class="hljs-comment">//如果该key已经存在于链表中，覆盖</span>                    <span class="hljs-keyword">if</span> (e.hash == hash &amp;&amp;                        ((k = e.key) == key || (key != <span class="hljs-keyword">null</span> &amp;&amp; key.equals(k))))                        <span class="hljs-keyword">break</span>;                    p = e;                &#125;            &#125;            <span class="hljs-comment">//e不为空说明值已经插入成功</span>            <span class="hljs-keyword">if</span> (e != <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// existing mapping for key</span>                V oldValue = e.value;                <span class="hljs-comment">//onlyIfAbsent控制是否替换原来的value</span>                <span class="hljs-keyword">if</span> (!onlyIfAbsent || oldValue == <span class="hljs-keyword">null</span>)                    e.value = value;                afterNodeAccess(e);                <span class="hljs-keyword">return</span> oldValue;            &#125;        &#125;        ++modCount;    <span class="hljs-comment">//扩容检测</span>        <span class="hljs-keyword">if</span> (++size &gt; threshold)            resize();        afterNodeInsertion(evict);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;</code></pre></div><h3 id="remove"><a href="#remove" class="headerlink" title="remove"></a>remove</h3><p>主要逻辑：</p><ol><li>判断第一个节点是否是需删除节点，如果是，将节点存储下来。</li><li>如果节点是红黑树节点，通过调用<code>getTreeNode</code>找到需删除节点，存储下来。</li><li>如果是链表，遍历获取到需删除节点。</li><li>删除节点，并进行修复工作</li></ol><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> V <span class="hljs-title">remove</span><span class="hljs-params">(Object key)</span> </span>&#123;    Node&lt;K,V&gt; e;    <span class="hljs-keyword">return</span> (e = removeNode(hash(key), key, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">false</span>, <span class="hljs-keyword">true</span>)) == <span class="hljs-keyword">null</span> ?        <span class="hljs-keyword">null</span> : e.value;&#125;<span class="hljs-function"><span class="hljs-keyword">final</span> Node&lt;K,V&gt; <span class="hljs-title">removeNode</span><span class="hljs-params">(<span class="hljs-keyword">int</span> hash, Object key, Object value,                           <span class="hljs-keyword">boolean</span> matchValue, <span class="hljs-keyword">boolean</span> movable)</span> </span>&#123;    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="hljs-keyword">int</span> n, index;    <span class="hljs-keyword">if</span> ((tab = table) != <span class="hljs-keyword">null</span> &amp;&amp; (n = tab.length) &gt; <span class="hljs-number">0</span> &amp;&amp;        (p = tab[index = (n - <span class="hljs-number">1</span>) &amp; hash]) != <span class="hljs-keyword">null</span>) &#123;        Node&lt;K,V&gt; node = <span class="hljs-keyword">null</span>, e; K k; V v;        <span class="hljs-comment">//如果键与第一个节点相等，则该节点是需删除节点</span>        <span class="hljs-keyword">if</span> (p.hash == hash &amp;&amp;            ((k = p.key) == key || (key != <span class="hljs-keyword">null</span> &amp;&amp; key.equals(k))))            node = p;        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> ((e = p.next) != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//如果是红黑树节点，调用红黑树的查找方法找到需删除节点</span>            <span class="hljs-keyword">if</span> (p <span class="hljs-keyword">instanceof</span> TreeNode)                node = ((TreeNode&lt;K,V&gt;)p).getTreeNode(hash, key);            <span class="hljs-keyword">else</span> &#123;                <span class="hljs-keyword">do</span> &#123;                    <span class="hljs-comment">//遍历链表，找到需删除节点</span>                    <span class="hljs-keyword">if</span> (e.hash == hash &amp;&amp;                        ((k = e.key) == key ||                         (key != <span class="hljs-keyword">null</span> &amp;&amp; key.equals(k)))) &#123;                        node = e;                        <span class="hljs-keyword">break</span>;                    &#125;                    p = e;                &#125; <span class="hljs-keyword">while</span> ((e = e.next) != <span class="hljs-keyword">null</span>);            &#125;        &#125;        <span class="hljs-comment">//删除节点，并修复链表或红黑树</span>        <span class="hljs-keyword">if</span> (node != <span class="hljs-keyword">null</span> &amp;&amp; (!matchValue || (v = node.value) == value ||                             (value != <span class="hljs-keyword">null</span> &amp;&amp; value.equals(v)))) &#123;            <span class="hljs-keyword">if</span> (node <span class="hljs-keyword">instanceof</span> TreeNode)                ((TreeNode&lt;K,V&gt;)node).removeTreeNode(<span class="hljs-keyword">this</span>, tab, movable);            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (node == p)                tab[index] = node.next;            <span class="hljs-keyword">else</span>                p.next = node.next;            ++modCount;            --size;            afterNodeRemoval(node);            <span class="hljs-keyword">return</span> node;        &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre></div><h3 id="resize"><a href="#resize" class="headerlink" title="resize"></a>resize</h3><p><code>HashMap</code>的table数组长度为2的幂，阈值大小 = <code>capacity * load factor</code>，<code>Node</code>数量超过阈值，进行扩容。</p><p>主要逻辑：</p><ol><li>计算新的桶数组容量<code>newCap</code>和新阈值<code>newThr</code>。<code>newCap</code>为原来的两倍，<code>newThr</code>为原来的两倍。</li><li>根据<code>newCap</code>创建新的桶数组，初始化新的桶数组。</li><li>将键值对节点重新映射到新的桶数组中，如果是红黑树节点，则需要拆分红黑树，如果是普通节点，则节点按照顺序进行分组。</li></ol><p>需要注意的是：<code>resize</code>十分消耗性能，日常开发需要尽量避免。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">final</span> Node&lt;K,V&gt;[] resize() &#123;    Node&lt;K,V&gt;[] oldTab = table;    <span class="hljs-keyword">int</span> oldCap = (oldTab == <span class="hljs-keyword">null</span>) ? <span class="hljs-number">0</span> : oldTab.length;    <span class="hljs-keyword">int</span> oldThr = threshold;    <span class="hljs-keyword">int</span> newCap, newThr = <span class="hljs-number">0</span>;    <span class="hljs-comment">//如果table不为空，说明已经被初始化</span>    <span class="hljs-keyword">if</span> (oldCap &gt; <span class="hljs-number">0</span>) &#123;        <span class="hljs-comment">//如果table的容量超过最大值，不进行扩容</span>        <span class="hljs-keyword">if</span> (oldCap &gt;= MAXIMUM_CAPACITY) &#123;            threshold = Integer.MAX_VALUE;            <span class="hljs-keyword">return</span> oldTab;        &#125;        <span class="hljs-comment">//将容量变为原来的两倍，阈值变为原来的两倍</span>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> ((newCap = oldCap &lt;&lt; <span class="hljs-number">1</span>) &lt; MAXIMUM_CAPACITY &amp;&amp;                 oldCap &gt;= DEFAULT_INITIAL_CAPACITY)            newThr = oldThr &lt;&lt; <span class="hljs-number">1</span>; <span class="hljs-comment">// double threshold</span>    &#125;    <span class="hljs-comment">//newCap = threshold</span>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (oldThr &gt; <span class="hljs-number">0</span>)        newCap = oldThr;    <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">//阈值为默认容量与默认负载因子乘积</span>        newCap = DEFAULT_INITIAL_CAPACITY;        newThr = (<span class="hljs-keyword">int</span>)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);    &#125;    <span class="hljs-comment">//新阈值为0时，按照默认公式重新算</span>    <span class="hljs-keyword">if</span> (newThr == <span class="hljs-number">0</span>) &#123;        <span class="hljs-keyword">float</span> ft = (<span class="hljs-keyword">float</span>)newCap * loadFactor;        newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (<span class="hljs-keyword">float</span>)MAXIMUM_CAPACITY ?                  (<span class="hljs-keyword">int</span>)ft : Integer.MAX_VALUE);    &#125;    <span class="hljs-comment">//赋值</span>    threshold = newThr;    <span class="hljs-meta">@SuppressWarnings</span>(&#123;<span class="hljs-string">"rawtypes"</span>,<span class="hljs-string">"unchecked"</span>&#125;)    <span class="hljs-comment">//创建新的桶数组</span>        Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])<span class="hljs-keyword">new</span> Node[newCap];    table = newTab;    <span class="hljs-keyword">if</span> (oldTab != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> j = <span class="hljs-number">0</span>; j &lt; oldCap; ++j) &#123;            Node&lt;K,V&gt; e;            <span class="hljs-comment">//如果旧的桶数组不为空，就遍历桶数组，并将键值对映射到新的桶数组</span>            <span class="hljs-keyword">if</span> ((e = oldTab[j]) != <span class="hljs-keyword">null</span>) &#123;                oldTab[j] = <span class="hljs-keyword">null</span>;                <span class="hljs-keyword">if</span> (e.next == <span class="hljs-keyword">null</span>)                    newTab[e.hash &amp; (newCap - <span class="hljs-number">1</span>)] = e;                <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (e <span class="hljs-keyword">instanceof</span> TreeNode)                    <span class="hljs-comment">//如果是红黑树节点，进行拆分</span>                    ((TreeNode&lt;K,V&gt;)e).split(<span class="hljs-keyword">this</span>, newTab, j, oldCap);                <span class="hljs-keyword">else</span> &#123; <span class="hljs-comment">// 链表节点情况</span>                    Node&lt;K,V&gt; loHead = <span class="hljs-keyword">null</span>, loTail = <span class="hljs-keyword">null</span>;                    Node&lt;K,V&gt; hiHead = <span class="hljs-keyword">null</span>, hiTail = <span class="hljs-keyword">null</span>;                    Node&lt;K,V&gt; next;                    <span class="hljs-keyword">do</span> &#123;                        <span class="hljs-comment">//遍历链表并进行按序分组</span>                        next = e.next;                        <span class="hljs-keyword">if</span> ((e.hash &amp; oldCap) == <span class="hljs-number">0</span>) &#123;                            <span class="hljs-keyword">if</span> (loTail == <span class="hljs-keyword">null</span>)                                loHead = e;                            <span class="hljs-keyword">else</span>                                loTail.next = e;                            loTail = e;                        &#125;                        <span class="hljs-keyword">else</span> &#123;                            <span class="hljs-keyword">if</span> (hiTail == <span class="hljs-keyword">null</span>)                                hiHead = e;                            <span class="hljs-keyword">else</span>                                hiTail.next = e;                            hiTail = e;                        &#125;                    &#125; <span class="hljs-keyword">while</span> ((e = next) != <span class="hljs-keyword">null</span>);                    <span class="hljs-comment">//将分组后的链表映射到新的桶数组中</span>                    <span class="hljs-keyword">if</span> (loTail != <span class="hljs-keyword">null</span>) &#123;                        loTail.next = <span class="hljs-keyword">null</span>;                        newTab[j] = loHead;                    &#125;                    <span class="hljs-keyword">if</span> (hiTail != <span class="hljs-keyword">null</span>) &#123;                        hiTail.next = <span class="hljs-keyword">null</span>;                        newTab[j + oldCap] = hiHead;                    &#125;                &#125;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> newTab;&#125;</code></pre></div><h2 id="红黑树退化"><a href="#红黑树退化" class="headerlink" title="红黑树退化"></a>红黑树退化</h2><p>在<code>TreeNode.split</code>方法和中，有一段代码对是否需要进行退化进行了判断。如果树节点个数小于<code>6</code>，则会退化为链表，至于该阈值与树化阈值<code>（UNTREEIFY_THRESHOLD 与 TREEIFY_THRESHOLD）</code>不等的原因，主要为了避免桶数组中的某个节点在该值附近震荡，从而导致频繁的树化和链表化。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (loHead != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">if</span> (lc &lt;= UNTREEIFY_THRESHOLD)                    tab[index] = loHead.untreeify(map);                <span class="hljs-keyword">else</span> &#123;                    tab[index] = loHead;                    <span class="hljs-keyword">if</span> (hiHead != <span class="hljs-keyword">null</span>) <span class="hljs-comment">// (else is already treeified)</span>                        loHead.treeify(tab);                &#125;            &#125;            <span class="hljs-keyword">if</span> (hiHead != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">if</span> (hc &lt;= UNTREEIFY_THRESHOLD)                    tab[index + bit] = hiHead.untreeify(map);                <span class="hljs-keyword">else</span> &#123;                    tab[index + bit] = hiHead;                    <span class="hljs-keyword">if</span> (loHead != <span class="hljs-keyword">null</span>)                        hiHead.treeify(tab);                &#125;            &#125;</code></pre></div><p>此外，在<code>TreeNode.removeTreeNode</code>中，删除红黑树节点之前，如果满足以下条件，也会进行链表化再进行删除：</p><ul><li>树的左子树为空</li><li>树的右子树为空</li><li>树的左孙子节点为空</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (root == <span class="hljs-keyword">null</span> || root.right == <span class="hljs-keyword">null</span> ||                (rl = root.left) == <span class="hljs-keyword">null</span> || rl.left == <span class="hljs-keyword">null</span>) &#123;                tab[index] = first.untreeify(map);  <span class="hljs-comment">// too small</span>                <span class="hljs-keyword">return</span>;&#125;</code></pre></div><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>本文对 JDK 8 中的 <code>HashMap</code> 的源代码进行了简要分析，主要为增删改查接口的内部实现机制以及扩容原理。</p><p>HashMap内部基于数组实现的，数组每个元素称为一个桶(bucket)，当存储的键值对数量超过阈值时，还会进行扩容操作，HashMap中的键值对会重新Hash到新位置。当桶中节点数超过阈值，则会进行树化，如果删除导致低于阈值，则会进行链表化。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;概述&quot;&gt;&lt;a href=&quot;#概述&quot; class=&quot;headerlink&quot; title=&quot;概述&quot;&gt;&lt;/a&gt;概述&lt;/h2&gt;&lt;p&gt;​        &lt;code&gt;HashMap&lt;/code&gt;是&lt;code&gt;Map&lt;/code&gt;接口基于哈希的实现，在查询性能上，时间复杂度接近
      
    
    </summary>
    
      <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
      <category term="集合框架" scheme="https://l1n.wang/categories/Java/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/"/>
    
    
      <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
  </entry>
  
  <entry>
    <title>LinkedList源码分析</title>
    <link href="https://l1n.wang/2020/08/java-linkedlist/"/>
    <id>https://l1n.wang/2020/08/java-linkedlist/</id>
    <published>2020-08-01T05:59:58.000Z</published>
    <updated>2020-08-01T08:56:52.723Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p><code>LinkedList</code>底层采用双向链表结构实现，所以在存储元素，并不需要扩容机制，但是需要额外的空间存储指针，头插和尾插的时间复杂度为<code>O(1)</code>，指定位置插入的时间复杂度为<code>O(n)</code>，<code>LinkedList</code>是非线程安全的集合。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">LinkedList</span>&lt;<span class="hljs-title">E</span>&gt;    <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractSequentialList</span>&lt;<span class="hljs-title">E</span>&gt;    <span class="hljs-keyword">implements</span> <span class="hljs-title">List</span>&lt;<span class="hljs-title">E</span>&gt;, <span class="hljs-title">Deque</span>&lt;<span class="hljs-title">E</span>&gt;, <span class="hljs-title">Cloneable</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span></span>&#123;</code></pre></div><p><code>LinkedList</code>继承了<code>AbstractSequentialList</code>。该类提供了一套基于顺序访问的接口。</p><p>实现了<code>List</code>接口和<code>Deque</code>接口，使得<code>LinkedList</code>同时具备了<code>List</code>和双端队列的特性。</p><p><code>LinkedList</code>实现了<code>Serializable</code>接口，表明<code>ArrayList</code>支持序列化。</p><p><code>LinkedList</code>实现了<code>Cloneable</code>接口，能被克隆。</p><h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><p>Node节点包括数据，前驱节点和后继节点。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Node</span>&lt;<span class="hljs-title">E</span>&gt; </span>&#123;        E item;        Node&lt;E&gt; next;        Node&lt;E&gt; prev;        Node(Node&lt;E&gt; prev, E element, Node&lt;E&gt; next) &#123;            <span class="hljs-keyword">this</span>.item = element;            <span class="hljs-keyword">this</span>.next = next;            <span class="hljs-keyword">this</span>.prev = prev;        &#125;    &#125;</code></pre></div><h2 id="成员变量"><a href="#成员变量" class="headerlink" title="成员变量"></a>成员变量</h2><p>分别是链表长度，头节点，尾节点。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">transient</span> <span class="hljs-keyword">int</span> size = <span class="hljs-number">0</span>;<span class="hljs-keyword">transient</span> Node&lt;E&gt; first;<span class="hljs-keyword">transient</span> Node&lt;E&gt; last;</code></pre></div><h2 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h2><p>构造方法有两种，注释如下：</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">//空构造方法</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">LinkedList</span><span class="hljs-params">()</span> </span>&#123;   &#125;   <span class="hljs-comment">//传入集合，调用addAll进行添加</span>   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">LinkedList</span><span class="hljs-params">(Collection&lt;? extends E&gt; c)</span> </span>&#123;       <span class="hljs-keyword">this</span>();       addAll(c);   &#125;</code></pre></div><h2 id="API"><a href="#API" class="headerlink" title="API"></a>API</h2><h3 id="linkFirst"><a href="#linkFirst" class="headerlink" title="linkFirst"></a>linkFirst</h3><p>将元素添加到头部。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">linkFirst</span><span class="hljs-params">(E e)</span> </span>&#123;        <span class="hljs-keyword">final</span> Node&lt;E&gt; f = first;    <span class="hljs-comment">//新节点的前驱节点为null，后继节点为原来的头节点</span>        <span class="hljs-keyword">final</span> Node&lt;E&gt; newNode = <span class="hljs-keyword">new</span> Node&lt;&gt;(<span class="hljs-keyword">null</span>, e, f);        first = newNode;    <span class="hljs-comment">//如果头节点为空</span>        <span class="hljs-keyword">if</span> (f == <span class="hljs-keyword">null</span>)            <span class="hljs-comment">//新插入的既是头节点，也是尾节点。</span>            last = newNode;        <span class="hljs-keyword">else</span>            <span class="hljs-comment">//将原来头节点的前驱指针指向新节点</span>            f.prev = newNode;        size++;        modCount++;    &#125;</code></pre></div><h3 id="linkLast"><a href="#linkLast" class="headerlink" title="linkLast"></a>linkLast</h3><p>将元素添加到尾部。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">linkLast</span><span class="hljs-params">(E e)</span> </span>&#123;        <span class="hljs-keyword">final</span> Node&lt;E&gt; l = last;    <span class="hljs-comment">//新节点的后继节点为null，前驱节点为原来的尾节点</span>        <span class="hljs-keyword">final</span> Node&lt;E&gt; newNode = <span class="hljs-keyword">new</span> Node&lt;&gt;(l, e, <span class="hljs-keyword">null</span>);        last = newNode;    <span class="hljs-comment">//如果尾节点为空,直接将新节点设置为头节点</span>        <span class="hljs-keyword">if</span> (l == <span class="hljs-keyword">null</span>)            first = newNode;        <span class="hljs-keyword">else</span>            <span class="hljs-comment">//否则将原来的尾节点后继指向新节点</span>            l.next = newNode;        size++;        modCount++;    &#125;</code></pre></div><h3 id="linkBefore"><a href="#linkBefore" class="headerlink" title="linkBefore"></a>linkBefore</h3><p>在一个非空节点前插入元素。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">linkBefore</span><span class="hljs-params">(E e, Node&lt;E&gt; succ)</span> </span>&#123;        <span class="hljs-comment">// assert succ != null;</span>        <span class="hljs-keyword">final</span> Node&lt;E&gt; pred = succ.prev;    <span class="hljs-comment">//插入节点的前驱为succ的前驱，后继节点为succ</span>        <span class="hljs-keyword">final</span> Node&lt;E&gt; newNode = <span class="hljs-keyword">new</span> Node&lt;&gt;(pred, e, succ);    <span class="hljs-comment">//设置succ的前驱指针</span>        succ.prev = newNode;    <span class="hljs-comment">//如果succ的前驱节点为空</span>        <span class="hljs-keyword">if</span> (pred == <span class="hljs-keyword">null</span>)            <span class="hljs-comment">//新插入的节点为头节点</span>            first = newNode;        <span class="hljs-keyword">else</span>            <span class="hljs-comment">//否则succ的前驱节点的后继指针指向新节点</span>            pred.next = newNode;        size++;        modCount++;    &#125;</code></pre></div><h3 id="unlinkFirst"><a href="#unlinkFirst" class="headerlink" title="unlinkFirst"></a>unlinkFirst</h3><p>移除头节点。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> E <span class="hljs-title">unlinkFirst</span><span class="hljs-params">(Node&lt;E&gt; f)</span> </span>&#123;        <span class="hljs-comment">// assert f == first &amp;&amp; f != null;</span>        <span class="hljs-keyword">final</span> E element = f.item;    <span class="hljs-comment">//获取头节点的下一个元素</span>        <span class="hljs-keyword">final</span> Node&lt;E&gt; next = f.next;    <span class="hljs-comment">//方便GC</span>        f.item = <span class="hljs-keyword">null</span>;        f.next = <span class="hljs-keyword">null</span>;    <span class="hljs-comment">//first指针指向next节点</span>        first = next;    <span class="hljs-comment">//如果链表只有一个节点</span>        <span class="hljs-keyword">if</span> (next == <span class="hljs-keyword">null</span>)            <span class="hljs-comment">//删除后为空，将尾指针置空</span>            last = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">else</span>            <span class="hljs-comment">//否则将next的前置置为空</span>            next.prev = <span class="hljs-keyword">null</span>;    <span class="hljs-comment">//设置size和modCount</span>        size--;        modCount++;        <span class="hljs-keyword">return</span> element;    &#125;</code></pre></div><h3 id="unlinkLast"><a href="#unlinkLast" class="headerlink" title="unlinkLast"></a>unlinkLast</h3><p>移除尾节点。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> E <span class="hljs-title">unlinkLast</span><span class="hljs-params">(Node&lt;E&gt; l)</span> </span>&#123;        <span class="hljs-comment">// assert l == last &amp;&amp; l != null;</span>        <span class="hljs-keyword">final</span> E element = l.item;        <span class="hljs-keyword">final</span> Node&lt;E&gt; prev = l.prev;    <span class="hljs-comment">//方便GC</span>        l.item = <span class="hljs-keyword">null</span>;        l.prev = <span class="hljs-keyword">null</span>;    <span class="hljs-comment">//last指针指向原来尾节点的前一个</span>        last = prev;    <span class="hljs-comment">//如果前一个为空</span>        <span class="hljs-keyword">if</span> (prev == <span class="hljs-keyword">null</span>)            <span class="hljs-comment">//说明现在没有节点，头节点置空</span>            first = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">else</span>            <span class="hljs-comment">//否则将尾节点的next置为空</span>            prev.next = <span class="hljs-keyword">null</span>;        size--;        modCount++;        <span class="hljs-keyword">return</span> element;    &#125;</code></pre></div><h3 id="unlink"><a href="#unlink" class="headerlink" title="unlink"></a>unlink</h3><p>移除一个非空节点。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function">E <span class="hljs-title">unlink</span><span class="hljs-params">(Node&lt;E&gt; x)</span> </span>&#123;        <span class="hljs-comment">// assert x != null;</span>        <span class="hljs-keyword">final</span> E element = x.item;        <span class="hljs-keyword">final</span> Node&lt;E&gt; next = x.next;        <span class="hljs-keyword">final</span> Node&lt;E&gt; prev = x.prev;<span class="hljs-comment">//如果x是头节点，将头指针指向x的next</span>        <span class="hljs-keyword">if</span> (prev == <span class="hljs-keyword">null</span>) &#123;            first = next;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//不是的话，将x的前驱指针指向它的后继节点</span>            <span class="hljs-comment">//并将x的前去指针置空</span>            prev.next = next;            x.prev = <span class="hljs-keyword">null</span>;        &#125;<span class="hljs-comment">//如果后继节点为空，说明删除的节点是尾节点</span>        <span class="hljs-keyword">if</span> (next == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//last指向前一个</span>            last = prev;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//处理next的前驱指针</span>            next.prev = prev;            x.next = <span class="hljs-keyword">null</span>;        &#125;        x.item = <span class="hljs-keyword">null</span>;        size--;        modCount++;        <span class="hljs-keyword">return</span> element;    &#125;</code></pre></div><h3 id="node"><a href="#node" class="headerlink" title="node"></a>node</h3><p>根据下标返回节点。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function">Node&lt;E&gt; <span class="hljs-title">node</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> </span>&#123;        <span class="hljs-comment">//如果下标小于size/2，从头节点开始遍历</span>        <span class="hljs-keyword">if</span> (index &lt; (size &gt;&gt; <span class="hljs-number">1</span>)) &#123;            Node&lt;E&gt; x = first;            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; index; i++)                x = x.next;            <span class="hljs-keyword">return</span> x;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//从尾部开始遍历</span>            Node&lt;E&gt; x = last;            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = size - <span class="hljs-number">1</span>; i &gt; index; i--)                x = x.prev;            <span class="hljs-keyword">return</span> x;        &#125;    &#125;</code></pre></div><h3 id="indexOf"><a href="#indexOf" class="headerlink" title="indexOf"></a>indexOf</h3><p>返回元素第一次出现的下标。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">indexOf</span><span class="hljs-params">(Object o)</span> </span>&#123;        <span class="hljs-keyword">int</span> index = <span class="hljs-number">0</span>;        <span class="hljs-keyword">if</span> (o == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//o为null时，找到第一个null节点</span>            <span class="hljs-keyword">for</span> (Node&lt;E&gt; x = first; x != <span class="hljs-keyword">null</span>; x = x.next) &#123;                <span class="hljs-keyword">if</span> (x.item == <span class="hljs-keyword">null</span>)                    <span class="hljs-keyword">return</span> index;                index++;            &#125;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//否则从头开始找与o匹配的</span>            <span class="hljs-keyword">for</span> (Node&lt;E&gt; x = first; x != <span class="hljs-keyword">null</span>; x = x.next) &#123;                <span class="hljs-keyword">if</span> (o.equals(x.item))                    <span class="hljs-keyword">return</span> index;                index++;            &#125;        &#125;        <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;    &#125;</code></pre></div><h3 id="lastIndexOf"><a href="#lastIndexOf" class="headerlink" title="lastIndexOf"></a>lastIndexOf</h3><p>找到最后一个匹配的下标。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">lastIndexOf</span><span class="hljs-params">(Object o)</span> </span>&#123;        <span class="hljs-keyword">int</span> index = size;    <span class="hljs-comment">//从尾部开始找</span>        <span class="hljs-keyword">if</span> (o == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">for</span> (Node&lt;E&gt; x = last; x != <span class="hljs-keyword">null</span>; x = x.prev) &#123;                index--;                <span class="hljs-keyword">if</span> (x.item == <span class="hljs-keyword">null</span>)                    <span class="hljs-keyword">return</span> index;            &#125;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">for</span> (Node&lt;E&gt; x = last; x != <span class="hljs-keyword">null</span>; x = x.prev) &#123;                index--;                <span class="hljs-keyword">if</span> (o.equals(x.item))                    <span class="hljs-keyword">return</span> index;            &#125;        &#125;        <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;    &#125;</code></pre></div><h3 id="add"><a href="#add" class="headerlink" title="add"></a>add</h3><p>add方法调用<code>linkLast</code>，将元素添加到链表。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">add</span><span class="hljs-params">(E e)</span> </span>&#123;        linkLast(e);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">linkLast</span><span class="hljs-params">(E e)</span> </span>&#123;        <span class="hljs-keyword">final</span> Node&lt;E&gt; l = last;        <span class="hljs-keyword">final</span> Node&lt;E&gt; newNode = <span class="hljs-keyword">new</span> Node&lt;&gt;(l, e, <span class="hljs-keyword">null</span>);        last = newNode;        <span class="hljs-keyword">if</span> (l == <span class="hljs-keyword">null</span>)            first = newNode;        <span class="hljs-keyword">else</span>            l.next = newNode;        size++;        modCount++;    &#125;</code></pre></div><h3 id="addAll"><a href="#addAll" class="headerlink" title="addAll"></a>addAll</h3><p>将传入的集合从指定下标开始插入。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">addAll</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index, Collection&lt;? extends E&gt; c)</span> </span>&#123;    <span class="hljs-comment">//检查index</span>        checkPositionIndex(index);<span class="hljs-comment">//存到数组中</span>        Object[] a = c.toArray();        <span class="hljs-keyword">int</span> numNew = a.length;    <span class="hljs-comment">//检验是否为空数组</span>        <span class="hljs-keyword">if</span> (numNew == <span class="hljs-number">0</span>)            <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;        Node&lt;E&gt; pred, succ;    <span class="hljs-comment">//如果插入位置为尾部</span>        <span class="hljs-keyword">if</span> (index == size) &#123;            <span class="hljs-comment">//succ指向空 pred指向尾节点</span>            succ = <span class="hljs-keyword">null</span>;            pred = last;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//不为空则通过node方法找到指定下标的节点</span>            succ = node(index);            pred = succ.prev;        &#125;<span class="hljs-comment">//遍历插入数据</span>        <span class="hljs-keyword">for</span> (Object o : a) &#123;            <span class="hljs-meta">@SuppressWarnings</span>(<span class="hljs-string">"unchecked"</span>) E e = (E) o;            Node&lt;E&gt; newNode = <span class="hljs-keyword">new</span> Node&lt;&gt;(pred, e, <span class="hljs-keyword">null</span>);            <span class="hljs-comment">//如果插入位置在链表头部</span>            <span class="hljs-keyword">if</span> (pred == <span class="hljs-keyword">null</span>)                <span class="hljs-comment">//头指针设置新节点</span>                first = newNode;            <span class="hljs-keyword">else</span>                pred.next = newNode;            pred = newNode;        &#125;<span class="hljs-comment">//如果从尾部开始插入</span>        <span class="hljs-keyword">if</span> (succ == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//重置尾节点</span>            last = pred;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//否则将插入的链表与原来的链表连接起来</span>            pred.next = succ;            succ.prev = pred;        &#125;        size += numNew;        modCount++;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;</code></pre></div><h3 id="peek"><a href="#peek" class="headerlink" title="peek"></a>peek</h3><p>peek方法返回链表的头节点。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> E <span class="hljs-title">peek</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">final</span> Node&lt;E&gt; f = first;        <span class="hljs-keyword">return</span> (f == <span class="hljs-keyword">null</span>) ? <span class="hljs-keyword">null</span> : f.item;    &#125;</code></pre></div><h3 id="poll"><a href="#poll" class="headerlink" title="poll"></a>poll</h3><p>poll方法返回头节点，并将头节点删除。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> E <span class="hljs-title">poll</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">final</span> Node&lt;E&gt; f = first;        <span class="hljs-keyword">return</span> (f == <span class="hljs-keyword">null</span>) ? <span class="hljs-keyword">null</span> : unlinkFirst(f);    &#125;</code></pre></div><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><code>LinkedList</code>通过实现<code>List</code>接口与<code>Deque</code>接口，底层数据结构采用了双向链表，同时具备了<code>List</code>，<code>Queue</code>和<code>Stack</code>的特性，本文对其该集合的基本的API实现做了简要分析。关于迭代器并未阐述，其机制与<code>ArrayList</code>类似，通过<code>fail-fast</code>检测在迭代时结构是否发生改变。</p><p>与<code>ArrayList</code>相比，<code>LinkedList</code>的查找性能差于<code>ArrayList</code>，但是其插入和删除性能优于<code>ArrayList</code>，在日常开发中，可以根据业务场景选择合适的集合。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;&lt;code&gt;LinkedList&lt;/code&gt;底层采用双向链表结构实现，所以在存储元素，并不需要扩容机制，但是需要额外的空间存储指针，头插和
      
    
    </summary>
    
      <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
      <category term="集合框架" scheme="https://l1n.wang/categories/Java/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/"/>
    
    
      <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
  </entry>
  
  <entry>
    <title>ArrayList源码分析</title>
    <link href="https://l1n.wang/2020/07/java-arraylist/"/>
    <id>https://l1n.wang/2020/07/java-arraylist/</id>
    <published>2020-07-30T09:59:58.000Z</published>
    <updated>2020-08-01T04:01:26.273Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p><code>ArrayList</code>是<code>List</code>接口的实现类，其底层通过数组实现。当空间不够会通过内部的扩容机制进行扩容。时间复杂度与数组类似，<code>ArrayList</code>是非线程安全的集合，在并发环境下使用会产生错误。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ArrayList</span>&lt;<span class="hljs-title">E</span>&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractList</span>&lt;<span class="hljs-title">E</span>&gt;        <span class="hljs-keyword">implements</span> <span class="hljs-title">List</span>&lt;<span class="hljs-title">E</span>&gt;, <span class="hljs-title">RandomAccess</span>, <span class="hljs-title">Cloneable</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span></span></code></pre></div><p><code>ArrayList</code>继承了<code>AbstractList</code>，实现了<code>List</code>接口，提供了添加、删除、修改、遍历等功能</p><p><code>ArrayList</code>实现了<code>RandomAccess</code>接口，实现该接口表明<code>ArrayList</code>支持快速随机访问。</p><p><code>ArrayList</code>实现了<code>Serializable</code>接口，表明<code>ArrayList</code>支持序列化。</p><p><code>ArrayList</code>实现了<code>Cloneable</code>接口，能被克隆。</p><h2 id="成员变量"><a href="#成员变量" class="headerlink" title="成员变量"></a>成员变量</h2><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">//默认初始化大小</span>   <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> DEFAULT_CAPACITY = <span class="hljs-number">10</span>;<span class="hljs-comment">//传入initialCapacity为0时，elementData指向该变量</span>   <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Object[] EMPTY_ELEMENTDATA = &#123;&#125;;<span class="hljs-comment">//不传initialCapacity时，elementData指向该变量</span>   <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Object[] DEFAULTCAPACITY_EMPTY_ELEMENTDATA = &#123;&#125;;<span class="hljs-comment">//存放数据的数组</span>   <span class="hljs-keyword">transient</span> Object[] elementData;   <span class="hljs-comment">//list大小</span>   <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> size;</code></pre></div><h2 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h2><p><code>ArrayList</code>共有三个构造方法。注释如下：</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">//传递了长度的构造方法</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ArrayList</span><span class="hljs-params">(<span class="hljs-keyword">int</span> initialCapacity)</span> </span>&#123;    <span class="hljs-comment">//边界检查</span>        <span class="hljs-keyword">if</span> (initialCapacity &gt; <span class="hljs-number">0</span>) &#123;            <span class="hljs-keyword">this</span>.elementData = <span class="hljs-keyword">new</span> Object[initialCapacity];        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (initialCapacity == <span class="hljs-number">0</span>) &#123;            <span class="hljs-keyword">this</span>.elementData = EMPTY_ELEMENTDATA;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">"Illegal Capacity: "</span>+                                               initialCapacity);        &#125;    &#125;    <span class="hljs-comment">//无参构造</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ArrayList</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">this</span>.elementData = DEFAULTCAPACITY_EMPTY_ELEMENTDATA;    &#125;    <span class="hljs-comment">//传递集合</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ArrayList</span><span class="hljs-params">(Collection&lt;? extends E&gt; c)</span> </span>&#123;        <span class="hljs-comment">//获取初始值数组</span>        elementData = c.toArray();        <span class="hljs-comment">//如果传入的为非空集合</span>        <span class="hljs-keyword">if</span> ((size = elementData.length) != <span class="hljs-number">0</span>) &#123;            <span class="hljs-comment">// c.toArray可能返回的不是Object[]类型</span>            <span class="hljs-keyword">if</span> (elementData.getClass() != Object[]<span class="hljs-class">.<span class="hljs-keyword">class</span>)                <span class="hljs-title">elementData</span> </span>= Arrays.copyOf(elementData, size, Object[]<span class="hljs-class">.<span class="hljs-keyword">class</span>)</span>;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//传递空集合，将elementData指向空数组</span>            <span class="hljs-keyword">this</span>.elementData = EMPTY_ELEMENTDATA;        &#125;    &#125;</code></pre></div><h2 id="API"><a href="#API" class="headerlink" title="API"></a>API</h2><h3 id="rangeCheck"><a href="#rangeCheck" class="headerlink" title="rangeCheck"></a>rangeCheck</h3><p>下标合法性检查。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">rangeCheck</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> </span>&#123;        <span class="hljs-keyword">if</span> (index &gt;= size)            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IndexOutOfBoundsException(outOfBoundsMsg(index));&#125;</code></pre></div><h3 id="get"><a href="#get" class="headerlink" title="get"></a>get</h3><p>获取对应下标的元素，时间复杂度<code>O(1)</code></p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> E <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> </span>&#123;        rangeCheck(index);        <span class="hljs-keyword">return</span> elementData(index);    &#125;</code></pre></div><h3 id="set"><a href="#set" class="headerlink" title="set"></a>set</h3><p>设置对应下标的元素，返回原数据。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> E <span class="hljs-title">set</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index, E element)</span> </span>&#123;        rangeCheck(index);        E oldValue = elementData(index);        elementData[index] = element;        <span class="hljs-keyword">return</span> oldValue;    &#125;</code></pre></div><h3 id="add"><a href="#add" class="headerlink" title="add"></a>add</h3><p>add有两种方式，一种是尾插，一种是指定位置插入。</p><p>插入中，首先需要检查容量是否足够，不够会进行扩容，扩容机制会在后续介绍。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">add</span><span class="hljs-params">(E e)</span> </span>&#123;        ensureCapacityInternal(size + <span class="hljs-number">1</span>);  <span class="hljs-comment">// Increments modCount!!</span>        elementData[size++] = e;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index, E element)</span> </span>&#123;        rangeCheckForAdd(index);        ensureCapacityInternal(size + <span class="hljs-number">1</span>);  <span class="hljs-comment">// Increments modCount!!</span>    <span class="hljs-comment">//从index开始到末尾，逐个向后移动1位，相当于index处空出一个位置。</span>        System.arraycopy(elementData, index, elementData, index + <span class="hljs-number">1</span>,                         size - index);        elementData[index] = element;        size++;    &#125;</code></pre></div><h3 id="remove"><a href="#remove" class="headerlink" title="remove"></a>remove</h3><p>remove有两种删除方式：一种是根据下标删除，一种是根据对象删除。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> E <span class="hljs-title">remove</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> </span>&#123;        rangeCheck(index);        modCount++;        E oldValue = elementData(index);<span class="hljs-comment">//找到index后一位</span>        <span class="hljs-keyword">int</span> numMoved = size - index - <span class="hljs-number">1</span>;        <span class="hljs-keyword">if</span> (numMoved &gt; <span class="hljs-number">0</span>)            <span class="hljs-comment">//将numMoved开始到结尾向前移动一位</span>            System.arraycopy(elementData, index+<span class="hljs-number">1</span>, elementData, index,                             numMoved);    <span class="hljs-comment">//置空，让其被GC</span>        elementData[--size] = <span class="hljs-keyword">null</span>; <span class="hljs-comment">// clear to let GC do its work</span>        <span class="hljs-keyword">return</span> oldValue;    &#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">remove</span><span class="hljs-params">(Object o)</span> </span>&#123;    <span class="hljs-comment">//检查</span>        <span class="hljs-keyword">if</span> (o == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> index = <span class="hljs-number">0</span>; index &lt; size; index++)                <span class="hljs-comment">//判断空元素</span>                <span class="hljs-keyword">if</span> (elementData[index] == <span class="hljs-keyword">null</span>) &#123;                    <span class="hljs-comment">//fastRemove不检查边界值，不返回删除元素值</span>                    fastRemove(index);                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;                &#125;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> index = <span class="hljs-number">0</span>; index &lt; size; index++)                <span class="hljs-comment">//调用equals检查对象是否符合要求</span>                <span class="hljs-keyword">if</span> (o.equals(elementData[index])) &#123;                    fastRemove(index);                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;                &#125;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;</code></pre></div><h3 id="fastRemove"><a href="#fastRemove" class="headerlink" title="fastRemove"></a>fastRemove</h3><p>不检查边界值，并且不返回删除元素的方式进行删除指定下标的数据。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">fastRemove</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> </span>&#123;        modCount++;        <span class="hljs-keyword">int</span> numMoved = size - index - <span class="hljs-number">1</span>;        <span class="hljs-keyword">if</span> (numMoved &gt; <span class="hljs-number">0</span>)            System.arraycopy(elementData, index+<span class="hljs-number">1</span>, elementData, index,                             numMoved);        elementData[--size] = <span class="hljs-keyword">null</span>; <span class="hljs-comment">// clear to let GC do its work</span>    &#125;</code></pre></div><h3 id="clear"><a href="#clear" class="headerlink" title="clear"></a>clear</h3><p>清空列表。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">clear</span><span class="hljs-params">()</span> </span>&#123;        modCount++;        <span class="hljs-comment">// 将每个元素置空，方便GC回收</span>        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; size; i++)            elementData[i] = <span class="hljs-keyword">null</span>;<span class="hljs-comment">//长度置为0</span>        size = <span class="hljs-number">0</span>;    &#125;</code></pre></div><h3 id="addAll"><a href="#addAll" class="headerlink" title="addAll"></a>addAll</h3><p>将集合添加进列表中。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">addAll</span><span class="hljs-params">(Collection&lt;? extends E&gt; c)</span> </span>&#123;    <span class="hljs-comment">//将集合转为Object数组</span>        Object[] a = c.toArray();        <span class="hljs-keyword">int</span> numNew = a.length;    <span class="hljs-comment">//确保容量足够</span>        ensureCapacityInternal(size + numNew);  <span class="hljs-comment">// Increments modCount</span>    <span class="hljs-comment">//将数组copy到elementData中</span>        System.arraycopy(a, <span class="hljs-number">0</span>, elementData, size, numNew);    <span class="hljs-comment">//更新长度</span>        size += numNew;        <span class="hljs-keyword">return</span> numNew != <span class="hljs-number">0</span>;    &#125;</code></pre></div><h3 id="indexOf"><a href="#indexOf" class="headerlink" title="indexOf"></a>indexOf</h3><p>返回元素在列表中第一个匹配的下标。不存在返回-1。<code>lastIndexOf</code>同理，找到最后匹配的下标。实现原理为<code>for</code>从后往前扫描。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">indexOf</span><span class="hljs-params">(Object o)</span> </span>&#123;    <span class="hljs-comment">//如果传入元素为空</span>        <span class="hljs-keyword">if</span> (o == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">//找到列表中第一个为空的下标，返回</span>            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; size; i++)                <span class="hljs-keyword">if</span> (elementData[i]==<span class="hljs-keyword">null</span>)                    <span class="hljs-keyword">return</span> i;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">//equals找到第一个匹配对象，并返回</span>            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; size; i++)                <span class="hljs-keyword">if</span> (o.equals(elementData[i]))                    <span class="hljs-keyword">return</span> i;        &#125;        <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;    &#125;</code></pre></div><h3 id="removeRange"><a href="#removeRange" class="headerlink" title="removeRange"></a>removeRange</h3><p>删除指定区间的元素。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">removeRange</span><span class="hljs-params">(<span class="hljs-keyword">int</span> fromIndex, <span class="hljs-keyword">int</span> toIndex)</span> </span>&#123;        modCount++;    <span class="hljs-comment">//将指定区间的元素覆盖掉</span>        <span class="hljs-keyword">int</span> numMoved = size - toIndex;        System.arraycopy(elementData, toIndex, elementData, fromIndex,                         numMoved);        <span class="hljs-comment">//将指定区间的元素置空，方便GC</span>        <span class="hljs-keyword">int</span> newSize = size - (toIndex-fromIndex);        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = newSize; i &lt; size; i++) &#123;            elementData[i] = <span class="hljs-keyword">null</span>;        &#125;        size = newSize;    &#125;</code></pre></div><h3 id="retainAll"><a href="#retainAll" class="headerlink" title="retainAll"></a>retainAll</h3><p>retainAll返回该集合与传入集合的交集，其底层通过调用<code>batchRemove</code>实现将所有不包含在传入集合中的元素删除。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">retainAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span> </span>&#123;        Objects.requireNonNull(c);        <span class="hljs-keyword">return</span> batchRemove(c, <span class="hljs-keyword">true</span>);    &#125;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">batchRemove</span><span class="hljs-params">(Collection&lt;?&gt; c, <span class="hljs-keyword">boolean</span> complement)</span> </span>&#123;    <span class="hljs-comment">//获取到当前List的数组</span>        <span class="hljs-keyword">final</span> Object[] elementData = <span class="hljs-keyword">this</span>.elementData;        <span class="hljs-keyword">int</span> r = <span class="hljs-number">0</span>, w = <span class="hljs-number">0</span>;        <span class="hljs-keyword">boolean</span> modified = <span class="hljs-keyword">false</span>;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">//for找出相同的，并将相同的逐个放在elementData</span>            <span class="hljs-keyword">for</span> (; r &lt; size; r++)                <span class="hljs-keyword">if</span> (c.contains(elementData[r]) == complement)                    elementData[w++] = elementData[r];        &#125; <span class="hljs-keyword">finally</span> &#123;            <span class="hljs-comment">//如果 c.contains()抛出异常</span>            <span class="hljs-keyword">if</span> (r != size) &#123;                <span class="hljs-comment">//复制剩余的元素</span>                System.arraycopy(elementData, r,                                 elementData, w,                                 size - r);                <span class="hljs-comment">//w为当前List的length</span>                w += size - r;            &#125;            <span class="hljs-comment">//如果交集长度与原来长度不匹配</span>            <span class="hljs-keyword">if</span> (w != size) &#123;                <span class="hljs-comment">// 删除多余的元素</span>                <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = w; i &lt; size; i++)                    elementData[i] = <span class="hljs-keyword">null</span>;                modCount += size - w;                size = w;                modified = <span class="hljs-keyword">true</span>;            &#125;        &#125;        <span class="hljs-keyword">return</span> modified;    &#125;</code></pre></div><h3 id="trimToSize"><a href="#trimToSize" class="headerlink" title="trimToSize"></a>trimToSize</h3><p>手动缩容</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">trimToSize</span><span class="hljs-params">()</span> </span>&#123;        modCount++;        <span class="hljs-keyword">if</span> (size &lt; elementData.length) &#123;            elementData = (size == <span class="hljs-number">0</span>)              ? EMPTY_ELEMENTDATA              : Arrays.copyOf(elementData, size);        &#125;    &#125;</code></pre></div><h2 id="扩容机制"><a href="#扩容机制" class="headerlink" title="扩容机制"></a>扩容机制</h2><p><code>ensureCapacityInternal</code>提供内部调用，<code>ensureCapacity</code>可提供外部调用。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">ensureCapacityInternal</span><span class="hljs-params">(<span class="hljs-keyword">int</span> minCapacity)</span> </span>&#123;        ensureExplicitCapacity(calculateCapacity(elementData, minCapacity));    &#125;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">ensureExplicitCapacity</span><span class="hljs-params">(<span class="hljs-keyword">int</span> minCapacity)</span> </span>&#123;        modCount++;        <span class="hljs-comment">// 边界检查</span>        <span class="hljs-keyword">if</span> (minCapacity - elementData.length &gt; <span class="hljs-number">0</span>)            grow(minCapacity);    &#125;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">calculateCapacity</span><span class="hljs-params">(Object[] elementData, <span class="hljs-keyword">int</span> minCapacity)</span> </span>&#123;        <span class="hljs-keyword">if</span> (elementData == DEFAULTCAPACITY_EMPTY_ELEMENTDATA) &#123;            <span class="hljs-keyword">return</span> Math.max(DEFAULT_CAPACITY, minCapacity);        &#125;        <span class="hljs-keyword">return</span> minCapacity;    &#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">ensureCapacity</span><span class="hljs-params">(<span class="hljs-keyword">int</span> minCapacity)</span> </span>&#123;        <span class="hljs-keyword">int</span> minExpand = (elementData != DEFAULTCAPACITY_EMPTY_ELEMENTDATA)            <span class="hljs-comment">// any size if not default element table</span>            ? <span class="hljs-number">0</span>            <span class="hljs-comment">// larger than default for default empty table. It's already</span>            <span class="hljs-comment">// supposed to be at default size.</span>            : DEFAULT_CAPACITY;        <span class="hljs-keyword">if</span> (minCapacity &gt; minExpand) &#123;            ensureExplicitCapacity(minCapacity);        &#125;    &#125;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> MAX_ARRAY_SIZE = Integer.MAX_VALUE - <span class="hljs-number">8</span>;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">grow</span><span class="hljs-params">(<span class="hljs-keyword">int</span> minCapacity)</span> </span>&#123;        <span class="hljs-comment">// 原容量</span>        <span class="hljs-keyword">int</span> oldCapacity = elementData.length;    <span class="hljs-comment">//新容量为原容量的1.5倍</span>        <span class="hljs-keyword">int</span> newCapacity = oldCapacity + (oldCapacity &gt;&gt; <span class="hljs-number">1</span>);    <span class="hljs-comment">//判断新容量是否大于需要的最小容量</span>        <span class="hljs-keyword">if</span> (newCapacity - minCapacity &lt; <span class="hljs-number">0</span>)            newCapacity = minCapacity;    <span class="hljs-comment">//比最大容量还大</span>        <span class="hljs-keyword">if</span> (newCapacity - MAX_ARRAY_SIZE &gt; <span class="hljs-number">0</span>)            newCapacity = hugeCapacity(minCapacity);        <span class="hljs-comment">//copy</span>        elementData = Arrays.copyOf(elementData, newCapacity);    &#125;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">hugeCapacity</span><span class="hljs-params">(<span class="hljs-keyword">int</span> minCapacity)</span> </span>&#123;        <span class="hljs-keyword">if</span> (minCapacity &lt; <span class="hljs-number">0</span>) <span class="hljs-comment">// overflow</span>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> OutOfMemoryError();    <span class="hljs-comment">//若minCapacity &gt; MAX_ARRAY_SIZE,将Integer.MAX_VALUE作为新数组大小</span>    <span class="hljs-comment">//否则MAX_ARRAY_SIZE作为新数组大小</span>        <span class="hljs-keyword">return</span> (minCapacity &gt; MAX_ARRAY_SIZE) ?            Integer.MAX_VALUE :            MAX_ARRAY_SIZE;    &#125;</code></pre></div><h2 id="modCount"><a href="#modCount" class="headerlink" title="modCount"></a>modCount</h2><p>在对List的结构进行修改时，都会进行<code>modCount++</code>，该变量记录集合的修改次数。</p><p>在使用迭代器遍历集合的时候同时修改结构时，<code>modCount</code>值会改变，而迭代器中<code>checkForComodification</code>方法是在迭代中检查集合是否发生修改。其原理是比较创建迭代器的时候的<code>modCount</code>与当前modCount是否相同。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">checkForComodification</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">if</span> (modCount != expectedModCount)                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ConcurrentModificationException();        &#125;</code></pre></div><h2 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h2><p><code>next</code>方法会调用<code>checkForComodification</code>方法来判断<code>List</code>迭代过程中是否发生结构上的修改，原理已在上文阐述。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">/**     * An optimized version of AbstractList.Itr     */</span><span class="hljs-keyword">private</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Itr</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Iterator</span>&lt;<span class="hljs-title">E</span>&gt; </span>&#123;        <span class="hljs-keyword">int</span> cursor;       <span class="hljs-comment">// index of next element to return</span>        <span class="hljs-keyword">int</span> lastRet = -<span class="hljs-number">1</span>; <span class="hljs-comment">// index of last element returned; -1 if no such</span>        <span class="hljs-keyword">int</span> expectedModCount = modCount;        Itr() &#123;&#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasNext</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> cursor != size;        &#125;        <span class="hljs-meta">@SuppressWarnings</span>(<span class="hljs-string">"unchecked"</span>)    <span class="hljs-comment">//通过i = cursor自增，逐个获取元素</span>        <span class="hljs-function"><span class="hljs-keyword">public</span> E <span class="hljs-title">next</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-comment">//判断List是否在迭代器创建后发生过修改</span>            checkForComodification();            <span class="hljs-keyword">int</span> i = cursor;            <span class="hljs-keyword">if</span> (i &gt;= size)                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> NoSuchElementException();            Object[] elementData = ArrayList.<span class="hljs-keyword">this</span>.elementData;            <span class="hljs-keyword">if</span> (i &gt;= elementData.length)                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ConcurrentModificationException();            cursor = i + <span class="hljs-number">1</span>;            <span class="hljs-keyword">return</span> (E) elementData[lastRet = i];        &#125;       ......<span class="hljs-meta">@Override</span>        <span class="hljs-meta">@SuppressWarnings</span>(<span class="hljs-string">"unchecked"</span>)        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">forEachRemaining</span><span class="hljs-params">(Consumer&lt;? <span class="hljs-keyword">super</span> E&gt; consumer)</span> </span>&#123;            Objects.requireNonNull(consumer);            <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> size = ArrayList.<span class="hljs-keyword">this</span>.size;            <span class="hljs-keyword">int</span> i = cursor;            <span class="hljs-keyword">if</span> (i &gt;= size) &#123;                <span class="hljs-keyword">return</span>;            &#125;            <span class="hljs-keyword">final</span> Object[] elementData = ArrayList.<span class="hljs-keyword">this</span>.elementData;            <span class="hljs-keyword">if</span> (i &gt;= elementData.length) &#123;                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ConcurrentModificationException();            &#125;            <span class="hljs-keyword">while</span> (i != size &amp;&amp; modCount == expectedModCount) &#123;                consumer.accept((E) elementData[i++]);            &#125;            <span class="hljs-comment">// update once at end of iteration to reduce heap write traffic</span>            cursor = i;            lastRet = i - <span class="hljs-number">1</span>;            checkForComodification();        &#125;            <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">checkForComodification</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">if</span> (modCount != expectedModCount)                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ConcurrentModificationException();        &#125;    &#125;</code></pre></div><h4 id="遍历时删除"><a href="#遍历时删除" class="headerlink" title="遍历时删除"></a>遍历时删除</h4><p>如果在遍历时删除，foreach在编译成字节码时会转成迭代器遍历的方式。运行时会在上述<code>checkForComodification</code>方法中抛出<code>ConcurrentModificationException</code>。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        ArrayList&lt;Integer&gt; list = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();        list.add(<span class="hljs-number">10</span>);        Iterator&lt;Integer&gt; iterator = list.iterator();        <span class="hljs-keyword">for</span> (Integer integer : list) &#123;            <span class="hljs-keyword">if</span> (integer == <span class="hljs-number">10</span>) &#123;                list.remove(integer);            &#125;        &#125;    &#125;</code></pre></div><p>上述机制称为<code>fail-fast</code>机制，这种机制在Java集合类中十分常见，简而言之就是操作前先考虑异常情况，如果发生异常，立即停止操作。</p><h2 id="序列化"><a href="#序列化" class="headerlink" title="序列化"></a>序列化</h2><p><code>ArrayList</code>的<code>elementData</code>添加了<code>transient</code>关键字，所以在序列化的时候<code>elementData</code>并不是序列化的一部分。而<code>ArrayList</code>通过内部更为细致的方法进行了序列化控制。内部序列化方法只保存非空元素，从而达到节约空间的目的。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">writeObject</span><span class="hljs-params">(java.io.ObjectOutputStream s)</span>       <span class="hljs-keyword">throws</span> java.io.IOException</span>&#123;       <span class="hljs-comment">// Write out element count, and any hidden stuff</span>       <span class="hljs-keyword">int</span> expectedModCount = modCount;       s.defaultWriteObject();       <span class="hljs-comment">// Write out size as capacity for behavioural compatibility with clone()</span>       s.writeInt(size);       <span class="hljs-comment">// Write out all elements in the proper order.</span>       <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;size; i++) &#123;           s.writeObject(elementData[i]);       &#125;       <span class="hljs-keyword">if</span> (modCount != expectedModCount) &#123;           <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ConcurrentModificationException();       &#125;   &#125;   <span class="hljs-comment">/**    * Reconstitute the &lt;tt&gt;ArrayList&lt;/tt&gt; instance from a stream (that is,    * deserialize it).    */</span>   <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">readObject</span><span class="hljs-params">(java.io.ObjectInputStream s)</span>       <span class="hljs-keyword">throws</span> java.io.IOException, ClassNotFoundException </span>&#123;       elementData = EMPTY_ELEMENTDATA;       <span class="hljs-comment">// Read in size, and any hidden stuff</span>       s.defaultReadObject();       <span class="hljs-comment">// Read in capacity</span>       s.readInt(); <span class="hljs-comment">// ignored</span>       <span class="hljs-keyword">if</span> (size &gt; <span class="hljs-number">0</span>) &#123;           <span class="hljs-comment">// be like clone(), allocate array based upon size not capacity</span>           <span class="hljs-keyword">int</span> capacity = calculateCapacity(elementData, size);           SharedSecrets.getJavaOISAccess().checkArray(s, Object[]<span class="hljs-class">.<span class="hljs-keyword">class</span>, <span class="hljs-title">capacity</span>)</span>;           ensureCapacityInternal(size);           Object[] a = elementData;           <span class="hljs-comment">// Read in all elements in the proper order.</span>           <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; i&lt;size; i++) &#123;               a[i] = s.readObject();           &#125;       &#125;   &#125;</code></pre></div><h2 id="子列表"><a href="#子列表" class="headerlink" title="子列表"></a>子列表</h2><p><code>subList</code>返回<code>List</code>指定区间的数据，可以看到<code>subList</code>中的操作会影响到父列表。</p><p>其次需要注意的是，<code>subList</code>的返回结果不可强转成<code>ArrayList</code>，否则会抛出<code>ClassCastException</code>，因为<code>subList</code>返回的是<code>ArrayList</code>的内部类<code>SubList</code>。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;E&gt; <span class="hljs-title">subList</span><span class="hljs-params">(<span class="hljs-keyword">int</span> fromIndex, <span class="hljs-keyword">int</span> toIndex)</span> </span>&#123;            subListRangeCheck(fromIndex, toIndex, size);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> SubList(<span class="hljs-keyword">this</span>, offset, fromIndex, toIndex);        &#125;<span class="hljs-keyword">private</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SubList</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractList</span>&lt;<span class="hljs-title">E</span>&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title">RandomAccess</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> AbstractList&lt;E&gt; parent;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> parentOffset;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> offset;        <span class="hljs-keyword">int</span> size;        SubList(AbstractList&lt;E&gt; parent,                <span class="hljs-keyword">int</span> offset, <span class="hljs-keyword">int</span> fromIndex, <span class="hljs-keyword">int</span> toIndex) &#123;            <span class="hljs-keyword">this</span>.parent = parent;            <span class="hljs-keyword">this</span>.parentOffset = fromIndex;            <span class="hljs-keyword">this</span>.offset = offset + fromIndex;            <span class="hljs-keyword">this</span>.size = toIndex - fromIndex;            <span class="hljs-keyword">this</span>.modCount = ArrayList.<span class="hljs-keyword">this</span>.modCount;        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> E <span class="hljs-title">set</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index, E e)</span> </span>&#123;            rangeCheck(index);            checkForComodification();            E oldValue = ArrayList.<span class="hljs-keyword">this</span>.elementData(offset + index);            ArrayList.<span class="hljs-keyword">this</span>.elementData[offset + index] = e;            <span class="hljs-keyword">return</span> oldValue;        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> E <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> </span>&#123;            rangeCheck(index);            checkForComodification();            <span class="hljs-keyword">return</span> ArrayList.<span class="hljs-keyword">this</span>.elementData(offset + index);        &#125;    &#125;</code></pre></div><h2 id="Arrays-asList"><a href="#Arrays-asList" class="headerlink" title="Arrays.asList"></a>Arrays.asList</h2><p>该方法通常用于将一个数组转换成一个List集合，但是需要注意不能使用其修改集合的相关方法，add/remove/clear会抛出<code>UnsupportedOperationException</code>，因为该方法返回的<code>ArrayList</code>是<code>Arrays</code>的一个内部类。其内部没有实现集合的修改方法。</p><p>其次传递的数组必须是对象数组，而不能是基本类型，如果传入基本类型数组，该方法得到的就是数组对象本身，而不是元素。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@SafeVarargs</span>    <span class="hljs-meta">@SuppressWarnings</span>(<span class="hljs-string">"varargs"</span>)    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; <span class="hljs-function">List&lt;T&gt; <span class="hljs-title">asList</span><span class="hljs-params">(T... a)</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ArrayList&lt;&gt;(a);    &#125;</code></pre></div><p>演示：</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        List&lt;String&gt; list = Arrays.asList(<span class="hljs-string">"hello"</span>, <span class="hljs-string">"world"</span>);        list.add(<span class="hljs-string">"yes"</span>);        <span class="hljs-keyword">int</span>[] arr = &#123;<span class="hljs-number">1</span>,<span class="hljs-number">2</span>,<span class="hljs-number">3</span>,<span class="hljs-number">4</span>,<span class="hljs-number">5</span>&#125;;        List&lt;<span class="hljs-keyword">int</span>[]&gt; list1 = Arrays.asList(arr);        System.out.println(list1.get(<span class="hljs-number">2</span>));    &#125;</code></pre></div><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文对<code>ArrayList</code>的成员变量，构造方法，API，扩容机制与其它细节做了简单介绍。并对日常开发中会使用到的一些细节做了简要描述，可以减少在日常开发过程中犯错误的几率。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;&lt;code&gt;ArrayList&lt;/code&gt;是&lt;code&gt;List&lt;/code&gt;接口的实现类，其底层通过数组实现。当空间不够会通过内部的扩容
      
    
    </summary>
    
      <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
      <category term="集合框架" scheme="https://l1n.wang/categories/Java/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/"/>
    
    
      <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
  </entry>
  
  <entry>
    <title>MySQL体系结构和存储引擎</title>
    <link href="https://l1n.wang/2020/07/mysql-structure-and-engine/"/>
    <id>https://l1n.wang/2020/07/mysql-structure-and-engine/</id>
    <published>2020-07-11T08:25:46.000Z</published>
    <updated>2020-07-23T09:44:25.319Z</updated>
    
    <content type="html"><![CDATA[<h2 id="体系结构"><a href="#体系结构" class="headerlink" title="体系结构"></a>体系结构</h2><p>MySQL体系结构如图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/structure/MySQL%E4%BD%93%E7%B3%BB%E7%BB%93%E6%9E%84.png" srcset="/img/loading.gif" alt="MySQL体系结构"></p><p>分别由Client Connectors层、MySQL Server层以及存储引擎层组成。</p><ul><li>Client Connectors层：负责处理客户端的连接请求，与客户端创建连接。</li><li>MySQL Server层：<ul><li>Connection Pool：负责处理和存储数据库与客户端创建的连接，一个线程负责管理一个连接，包括了用户认证模块，就是用户登录身份的认证和鉴权以及安全管理</li><li>Service &amp; utilities：管理服务&amp;工具集，包括备份恢复、安全管理、集群管理、工具</li><li>SQL interface：负责接受客户端发送的各种语句</li><li>Parser：对SQL语句进行语法解析生成解析树</li><li>Optimizer：查询优化器会根据解析树生成执行计划，并选择合适的索引，然后按照执行计划执行SQL并与各个存储引擎交互</li><li>Caches：包括各个存储引擎的缓存部分，例如InnoDB的Buffer Pool</li></ul></li><li>存储引擎层：包括InnoDB，MyISAM以及支持归档的Archive和内存的Memory</li><li>存储引擎底部是物理存储层，包括二进制日志，数据文件，错误日志，慢查询日志，全日志，redo/undo日志</li></ul><p>一条SQL语句的执行过程可以参照如下图示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/structure/SQL%20process.png" srcset="/img/loading.gif" alt="SQL执行流程"></p><ol><li>与MySQL建立连接。</li><li>查询缓存，如果开启了Query。 Cache并且查询缓存中存在该查询语句，则直接将结果返回到客户端，没有开启或缓存未命中则由解析器进行语法语义解析，并生成解析树。</li><li>预处理器生成新的解析树。</li><li>查询优化器进行优化。</li><li>查询执行引擎执行SQL，通过API接口查询物理存储层的数据，并返回结果。</li></ol><p>其中，查询缓存于MySQL 8.0中移除，具体原因：查询缓存往往弊大于利。查询缓存的失效非常频繁，只要有对一个表的更新，这个表上的所有的查询缓存都会被清空。</p><p>MySQL官方博客关于该技术移除的解释<a href="https://mysqlserverteam.com/mysql-8-0-retiring-support-for-the-query-cache/" target="_blank" rel="noopener">：</a></p><h2 id="存储引擎"><a href="#存储引擎" class="headerlink" title="存储引擎"></a>存储引擎</h2><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/structure/%E5%AD%98%E5%82%A8%E5%BC%95%E6%93%8E.png" srcset="/img/loading.gif" alt="MySQL存储引擎"></p><p>在 MySQL 5.6 版本之前，默认的存储引擎都是 MyISAM，但 5.6 版本以后默认的存储引擎就是 InnoDB。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/structure/InnoDB%E7%BB%93%E6%9E%84.png" srcset="/img/loading.gif" alt="InnoDB结构"></p><p>InnoDB上半部分是实例层，位于内存中，下半部分是物理层，位于文件系统中。<br>其中实例层分为线程和内存，InnoDB中重要的线程有Master Thread（主线程），其优先级最高，主要负责调度其他线程，其内部有几个循环：主循环，后台循环，刷新循环，暂停循环，Master Thread 会根据其内部运行的相关状态在各循环间进行切换。</p><p>大部分操作在主循环中完成，其包含1s和10s两种操作：</p><ul><li>1s操作<ul><li>日志缓冲刷新到磁盘（即使事务未提交，也被执行）</li><li>最多可以刷100个新脏页到磁盘</li><li>执行并改变缓冲的操作</li><li>若当前没有用户活动，可以切换到后台循环</li></ul></li><li>10s操作<ul><li>最多可以刷新100个脏页到磁盘</li><li>合并至多5个被改变的缓冲</li><li>日志缓冲刷新到磁盘</li><li>删除无用的Undo页</li><li>刷新100个或10个脏页到磁盘，产生一个检查点</li><li>buf_dump_thread 负责将 buffer pool 中的内容 dump 到物理文件中，以便再次启动 MySQL 时，可以快速加热数据。</li><li>page_cleaner_thread 负责将 buffer pool 中的脏页刷新到磁盘，在 5.6 版本之前没有这个线程，刷新操作都是由主线程完成的，所以在刷新脏页时会非常影响 MySQL 的处理能力，在5.7 版本之后可以通过参数设置开启多个 page_cleaner_thread。</li><li>purge_thread 负责将不再使用的 Undo 日志进行回收。</li><li>read_thread 处理用户的读请求，并负责将数据页从磁盘上读取出来，可以通过参数设置线程数量。</li><li>write_thread 负责将数据页从缓冲区写入磁盘，也可以通过参数设置线程数量，page_cleaner 线程发起刷脏页操作后 write_thread 就开始工作了。</li><li>redo_log_thread 负责把日志缓冲中的内容刷新到 Redo log 文件中。</li><li>insert_buffer_thread 负责把 Insert Buffer 中的内容刷新到磁盘。实例层的内存部分主要包含 InnoDB Buffer Pool，这里包含 InnoDB 最重要的缓存内容。数据和索引页、undo 页、insert buffer 页、自适应 Hash 索引页、数据字典页和锁信息等。additional memory pool 后续已不再使用。Redo buffer 里存储数据修改所产生的 Redo log。double write buffer 是 double write 所需的 buffer，主要解决由于宕机引起的物理写入操作中断，数据页不完整的问题。</li></ul></li></ul><p>物理层在逻辑上分为系统表空间、用户表空间和Redo日志。</p><ul><li>系统表空间有ibdata文件和一些Undo，ibdata文件有insert buffer段、double write段、回滚段、索引段、数据字典段和Undo信息段。</li><li>用户表空间之以<code>.ibd</code>后缀结尾的文件，文件中包含 insert buffer 的 bitmap 页、叶子页（这里存储真正的用户数据）、非叶子页。InnoDB 表是索引组织表，采用 B+ 树组织存储，数据都存储在叶子节点中，分支节点（即非叶子页）存储索引分支查找的数据值。</li><li>Redo日志包括多个Redo文件，这些文件循环使用。当达到一定存储阈值会触发checkpoint刷脏页操作，同时也会在MySQL实例异常宕机后重启，InnoDB表数据自动还原回复过程中使用。</li></ul><p>InnoDB内存结构：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/structure/InnoDB%E5%86%85%E5%AD%98%E7%BB%93%E6%9E%84.png" srcset="/img/loading.gif" alt="内存结构"></p><ul><li>用户读取或者写入的最新数据都存储在 Buffer Pool 中，如果 Buffer Pool 中没有找到则会读取物理文件进行查找，之后存储到 Buffer Pool 中并返回给 MySQL Server。Buffer Pool 采用LRU 机制。</li><li>Redo log 是一个循环复用的文件集，负责记录InnoDB中所有对 Buffer Pool的物理修改日志</li></ul><h3 id="InnoDB和MyIASM有什么区别（网易一面）"><a href="#InnoDB和MyIASM有什么区别（网易一面）" class="headerlink" title="InnoDB和MyIASM有什么区别（网易一面）"></a>InnoDB和MyIASM有什么区别（网易一面）</h3><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/structure/InnoDB%20VS%20MyIASM.png" srcset="/img/loading.gif" alt="image"></p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/structure/InnoDB.png" srcset="/img/loading.gif" alt="image"></p><p>其次，InnoDB性能优于MyIASM，CPU核数与InnoDB读写能力呈线性关系。</p><p>InnoDB 核心要点：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/structure/InnoDB%E8%A6%81%E7%82%B9.png" srcset="/img/loading.gif" alt="InnoDB要点"></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;体系结构&quot;&gt;&lt;a href=&quot;#体系结构&quot; class=&quot;headerlink&quot; title=&quot;体系结构&quot;&gt;&lt;/a&gt;体系结构&lt;/h2&gt;&lt;p&gt;MySQL体系结构如图所示：&lt;br&gt;&lt;img src=&quot;https://blog-1251613845.cos.ap-sha
      
    
    </summary>
    
      <category term="MySQL" scheme="https://l1n.wang/categories/MySQL/"/>
    
    
      <category term="MySQL" scheme="https://l1n.wang/tags/MySQL/"/>
    
  </entry>
  
  <entry>
    <title>Redis底层数据结构-Stream源码分析</title>
    <link href="https://l1n.wang/2020/06/redis-stream/"/>
    <id>https://l1n.wang/2020/06/redis-stream/</id>
    <published>2020-06-27T06:21:30.000Z</published>
    <updated>2020-06-27T06:51:17.169Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>Redis在5.0.0版本中引进了消息队列的功能，该功能由Stream实现，本文主要介绍Stream的相关实现。</p><h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><h2 id="stream"><a href="#stream" class="headerlink" title="stream"></a>stream</h2><p>Stream的实现依赖于Rax树与<code>listpack</code>结构，每个消息流都包含一个Rax树，以消息ID为key，listpack为value存储在Rax树中。其基本结构如下：</p><ul><li>rax：rax存储生产者生产的具体消息，每个消息有唯一ID为键</li><li>length：代表当前stream中消息个数。</li><li>last_id：为当前stream中最后插入的消息ID，stream为空时，该值为0。</li><li>cgroups：存储了当前stream相关的消费组，以消费组组名为键，streamCG为值存储在rax中。<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">stream</span> &#123;</span>    rax *rax;               <span class="hljs-comment">/* The radix tree holding the stream. */</span>    <span class="hljs-keyword">uint64_t</span> length;        <span class="hljs-comment">/* Number of elements inside this stream. */</span>    streamID last_id;       <span class="hljs-comment">/* Zero if there are yet no items. */</span>    rax *cgroups;           <span class="hljs-comment">/* Consumer groups dictionary: name -&gt; streamCG */</span>&#125; stream;</code></pre></div></li></ul><p>一个Stream的基本结构如图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/redis/stream/structure.PNG" srcset="/img/loading.gif" alt="Stream结构"></p><p>每一个listpack都有一个master entry，该结构存储了该listpack待插入消息的所有field。</p><h3 id="streamID"><a href="#streamID" class="headerlink" title="streamID"></a>streamID</h3><p>消息ID的基本结构如下：</p><ul><li>ms：消息创建时的时间</li><li>seq：序号<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">streamID</span> &#123;</span>    <span class="hljs-keyword">uint64_t</span> ms;        <span class="hljs-comment">/* Unix time in milliseconds. */</span>    <span class="hljs-keyword">uint64_t</span> seq;       <span class="hljs-comment">/* Sequence number. */</span>&#125; streamID;</code></pre></div></li></ul><h3 id="streamCG"><a href="#streamCG" class="headerlink" title="streamCG"></a>streamCG</h3><p>每个stream会有多个消费组，每个消费组通过组名称进行唯一标识，同时关联一个streamCG结构。该结构如下：</p><ul><li>last_id：该消费组已经确认的最后一个消息ID</li><li>pel：为该消费组尚未确认的消息，消息ID为键，streamNACK为值，存储在rax树中</li><li>consumers：消费组中的所有消费者，消费者名称为键，streamConsumer为值。<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">streamCG</span> &#123;</span>    streamID last_id;    rax *pel;    rax *consumers;&#125; streamCG;</code></pre></div></li></ul><h3 id="streamConsumer"><a href="#streamConsumer" class="headerlink" title="streamConsumer"></a>streamConsumer</h3><p>消费者结构如下：</p><ul><li>seen_time：为该消费者最后一次活跃的时间</li><li>name：消费者名称，为sds结构</li><li>pel：为该消费者尚未确认的消息，消息ID为键，streamNACK为值，存储在rax树中<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">streamConsumer</span> &#123;</span>    <span class="hljs-keyword">mstime_t</span> seen_time;    sds name;    rax *pel;&#125; streamConsumer;</code></pre></div></li></ul><h3 id="streamNACK"><a href="#streamNACK" class="headerlink" title="streamNACK"></a>streamNACK</h3><p>该结构为未确认消息，streamNACK维护了消费组或消费者尚未确认的消息，消费组中的pel的元素与消费者的pel元素是共享的。该结构如下：</p><ul><li>delivery_time：为该消息最后发送给消费方的时间</li><li>delivery_count：该消息已发送的次数</li><li>consumer：当前归属的消费者<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">streamNACK</span> &#123;</span>    <span class="hljs-keyword">mstime_t</span> delivery_time;    <span class="hljs-keyword">uint64_t</span> delivery_count;    streamConsumer *consumer;&#125; streamNACK;</code></pre></div></li></ul><h3 id="streamIterator"><a href="#streamIterator" class="headerlink" title="streamIterator"></a>streamIterator</h3><p>该结构主要提供遍历功能，基本结构如下：</p><ul><li>stream：当前迭代器正在遍历的消息流</li><li>master_id：为listpack中第一个插入的消息ID（master entry）</li><li>master_fields_count：第一个entry的field个数</li><li>master_fields_start：master entry的field首地址</li><li>master_fields_ptr：记录field的位置</li><li>entry_flags：当前遍历的消息的标志位</li><li>rev：迭代器方向</li><li>start_key,end_key：遍历范围</li><li>ri：rax迭代器，用于遍历rax树中的所有key</li><li>lp：当前的listpack指针</li><li>lp_ele：当前正在遍历的listpack中的元素</li><li>lp_flags：指向翻墙消息的flag域</li><li>field_buf,value_buf：从listpack读取数据的缓存<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">streamIterator</span> &#123;</span>    stream *stream;         <span class="hljs-comment">/* The stream we are iterating. */</span>    streamID master_id;     <span class="hljs-comment">/* ID of the master entry at listpack head. */</span>    <span class="hljs-keyword">uint64_t</span> master_fields_count;       <span class="hljs-comment">/* Master entries # of fields. */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *master_fields_start; <span class="hljs-comment">/* Master entries start in listpack. */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *master_fields_ptr;   <span class="hljs-comment">/* Master field to emit next. */</span>    <span class="hljs-keyword">int</span> entry_flags;                    <span class="hljs-comment">/* Flags of entry we are emitting. */</span>    <span class="hljs-keyword">int</span> rev;                <span class="hljs-comment">/* True if iterating end to start (reverse). */</span>    <span class="hljs-keyword">uint64_t</span> start_key[<span class="hljs-number">2</span>];  <span class="hljs-comment">/* Start key as 128 bit big endian. */</span>    <span class="hljs-keyword">uint64_t</span> end_key[<span class="hljs-number">2</span>];    <span class="hljs-comment">/* End key as 128 bit big endian. */</span>    raxIterator ri;         <span class="hljs-comment">/* Rax iterator. */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *lp;      <span class="hljs-comment">/* Current listpack. */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *lp_ele;  <span class="hljs-comment">/* Current listpack cursor. */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *lp_flags; <span class="hljs-comment">/* Current entry flags pointer. */</span>    <span class="hljs-comment">/* Buffers used to hold the string of lpGet() when the element is     * integer encoded, so that there is no string representation of the     * element inside the listpack itself. */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> field_buf[LP_INTBUF_SIZE];    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> value_buf[LP_INTBUF_SIZE];&#125; streamIterator;</code></pre></div></li></ul><h2 id="listpack"><a href="#listpack" class="headerlink" title="listpack"></a>listpack</h2><p>listpack是一个字符串列表的序列化格式，该结构可用于存储字符串或整型。其主要结构如下图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/redis/stream/listpack.PNG" srcset="/img/loading.gif" alt="listpack基本结构"><br>listpack主要由四部分构成，分别是：</p><ul><li>Total Bytes为整个listpack的空间大小</li><li>Num Elem：listpack的Entry个数，占用两个字节，但是Entry个数大于等于65535时，该值为65535，所以这种情况下获取元素个数，需要遍历整个listpack</li><li>Entry：为每个具体的元素</li><li>End：为listpack的结束标志，占用一个字节，内容为0xFF</li></ul><p>Entry由三部分构成，基本如下：</p><ul><li>Encode：元素的编码方式，占用一个字节</li><li>content：内容</li><li>backlen：记录entry的长度（不包括该字段本身）</li></ul><p>其中编码方式取值如下图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/redis/stream/encode.PNG" srcset="/img/loading.gif" alt="编码取值"></p><p>Stream的消息内容存储在listpack中，消息存储格式是每个字段都是一个entry，而不是键整个消息作为字符串存储的，每个listpack会存储多个消息，具体存储个数由<code>stream-node-max-bytes</code>（listpack节点最大占用字节数，默认4096）和<code>stream-node-max-entries</code>（最大存储元素个数，默认100）决定.</p><p>每个listpack在创建时，会将第一个插入的entry构建成master entry，其基本结构如下所示：<br><div class="hljs"><pre><code class="hljs undefined">count   |   <span class="hljs-type">deleted</span>  |  <span class="hljs-type">num</span>-fields  |   <span class="hljs-type">field</span><span class="hljs-number">-1</span> |   <span class="hljs-type">field</span><span class="hljs-number">-2</span> |   <span class="hljs-type">0</span></code></pre></div></p><p>其中：</p><ul><li>count：为当前listpack中所有未删除的消息个数</li><li>deleted：当前listpack中所有已经删除的消息个数</li><li>num-fields：field个数</li><li>field-N：field域</li><li>0为标识位，再从后向前遍历listpack时使用</li></ul><p>存储一个消息时，如果该消息的field域与master entry完全相同，则不需要再次存储field域</p><h2 id="Rax"><a href="#Rax" class="headerlink" title="Rax"></a>Rax</h2><p>Redis对于Rax的解释为<code>A radix tree implement</code>，基数树的一种实现。Rax中不仅可以存储字符串，也可以为该字符串设置值形成kv结构。其基本结构如下：</p><ul><li>head：指向头节点</li><li>numele：元素个数（key）</li><li>numnodes：节点个数<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">rax</span> &#123;</span>    raxNode *head;    <span class="hljs-keyword">uint64_t</span> numele;    <span class="hljs-keyword">uint64_t</span> numnodes;&#125; rax;</code></pre></div></li></ul><h2 id="raxNode"><a href="#raxNode" class="headerlink" title="raxNode"></a>raxNode</h2><p>raxNode代表Rax树中的一个节点，其基本结构如下所示：</p><ul><li>iskey：表明当前节点是否包含一个key，占用1bit</li><li>isnull：表明当前key对应的value是否为空，占用1bit</li><li>iscompr：表明当前节点是否为压缩节点，占用1bit</li><li>size：压缩节点压缩的字符串长度或者非压缩节点的子节点个数，占用29bit</li><li>data：包含填充字段，同时存储了当前节点包含的字符串以及子节点的指针，key对应的value指针。<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">raxNode</span> &#123;</span>    <span class="hljs-keyword">uint32_t</span> iskey:<span class="hljs-number">1</span>;    <span class="hljs-keyword">uint32_t</span> isnull:<span class="hljs-number">1</span>;    <span class="hljs-keyword">uint32_t</span> iscompr:<span class="hljs-number">1</span>;    <span class="hljs-keyword">uint32_t</span> <span class="hljs-built_in">size</span>:<span class="hljs-number">29</span>;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> data[];&#125; raxNode;</code></pre></div></li></ul><p>其中raxNode分为压缩节点域非压缩节点。主要区别在于非压缩节点的每个字符都有子节点，如果字符个数小于2，都是非压缩节点。</p><h2 id="raxStack"><a href="#raxStack" class="headerlink" title="raxStack"></a>raxStack</h2><p>raxStack结构用于存储从根节点到当前节点的路径，基本结构如下：</p><ul><li>stack：用于记录路径，该指针可能指向static_items或者堆内存</li><li>items,maxitems：代表stack指向的空间的已用空间以及最大空间</li><li>static_items：一个数组，每个元素都是指针，存储路径</li><li>oom：代表当前栈是否出现过内存溢出<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">raxStack</span> &#123;</span>    <span class="hljs-keyword">void</span> **<span class="hljs-built_in">stack</span>;    <span class="hljs-keyword">size_t</span> items, maxitems;    <span class="hljs-keyword">void</span> *static_items[RAX_STACK_STATIC_ITEMS];    <span class="hljs-keyword">int</span> oom;&#125; raxStack;</code></pre></div></li></ul><h2 id="raxIterator"><a href="#raxIterator" class="headerlink" title="raxIterator"></a>raxIterator</h2><p>raxIterator用于遍历Rax树中的所有key，基本结构如下：</p><ul><li>flags：代表当前迭代器标志位,取值如下：<ul><li>RAX_ITER_JUST_SEEKED：当前迭代器指向的元素是刚刚搜索过的，当需要从迭代器中获取元素时，直接返回当前元素并清空标志位。</li><li>RAX_ITER_EOF：代表当前迭代器已经遍历到最后一个节点</li><li>RAX_ITER_SAFE：代表当前迭代器为安全迭代器，可以进行写操作</li></ul></li><li>rt：当前迭代器对应的rax</li><li>key：存储了当前迭代器遍历到的key，该指针指向key_static_string或者堆内存</li><li>data：value值</li><li>key_len,key_max：key指向的空间的已用空间以及最大空间</li><li>key_static_string：key的默认存储空间，key过大时，会使用堆内存</li><li>node：当前key所在的raxNode</li><li>stack：记录了从根节点到当前节点的路径，用于raxNode线上遍历</li><li>node_cb：为节点的回调函数，默认为空<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">raxIterator</span> &#123;</span>    <span class="hljs-keyword">int</span> flags;    rax *rt;                <span class="hljs-comment">/* Radix tree we are iterating. */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *key;     <span class="hljs-comment">/* The current string. */</span>    <span class="hljs-keyword">void</span> *data;             <span class="hljs-comment">/* Data associated to this key. */</span>    <span class="hljs-keyword">size_t</span> key_len;         <span class="hljs-comment">/* Current key length. */</span>    <span class="hljs-keyword">size_t</span> key_max;         <span class="hljs-comment">/* Max key len the current key buffer can hold. */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> key_static_string[RAX_ITER_STATIC_LEN];    raxNode *node;          <span class="hljs-comment">/* Current node. Only for unsafe iteration. */</span>    raxStack <span class="hljs-built_in">stack</span>;         <span class="hljs-comment">/* Stack used for unsafe iteration. */</span>    raxNodeCallback node_cb; <span class="hljs-comment">/* Optional node callback. Normally set to NULL. */</span>&#125; raxIterator;</code></pre></div></li></ul><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>上文主要对Redis Stream的基本结构与其底层数据结构做了简要分析，了解了消息由listpack结构存储，以消息ID为key，listpack为value存储在Rax树中。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;p&gt;Redis在5.0.0版本中引进了消息队列的功能，该功能由Stream实现，本文主要介绍Stream的相关实现。&lt;/p&gt;
&lt;h1 id=&quot;数
      
    
    </summary>
    
      <category term="Redis" scheme="https://l1n.wang/categories/Redis/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/categories/Redis/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/tags/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
      <category term="Redis" scheme="https://l1n.wang/tags/Redis/"/>
    
  </entry>
  
  <entry>
    <title>Redis底层数据结构-Quicklist源码分析</title>
    <link href="https://l1n.wang/2020/06/redis-quicklist/"/>
    <id>https://l1n.wang/2020/06/redis-quicklist/</id>
    <published>2020-06-25T02:34:21.000Z</published>
    <updated>2020-06-25T02:38:46.063Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>quicklist是Redis 3.2中引入的数据结构，其本质是一个双向链表，链表的节点类型是ziplist，当ziplist节点过多，quicklist会退化成双向链表，以提高效率。</p><h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><h2 id="quicklistNode"><a href="#quicklistNode" class="headerlink" title="quicklistNode"></a>quicklistNode</h2><p>该结构为节点类型。</p><ul><li>prev：指向前驱节点</li><li>next：指向后继节点</li><li>zl：指向对应的压缩列表</li><li>sz：压缩列表的大小</li><li>encoding：采用的编码方式，1为原生，2代表使用LZF进行压缩</li><li>container：为节点指向的容器类型，1代表none，2代表ziplist存储数据</li><li>recompress：代表这个节点是否是压缩节点，如果是，则使用压缩节点前先进行解压缩，使用后重新压缩</li><li>attempted_compress：测试时使用<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistNode</span> &#123;</span>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistNode</span> *<span class="hljs-title">prev</span>;</span>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistNode</span> *<span class="hljs-title">next</span>;</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *zl;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> sz;             <span class="hljs-comment">/* ziplist size in bytes */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> count : <span class="hljs-number">16</span>;     <span class="hljs-comment">/* count of items in ziplist */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> encoding : <span class="hljs-number">2</span>;   <span class="hljs-comment">/* RAW==1 or LZF==2 */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> container : <span class="hljs-number">2</span>;  <span class="hljs-comment">/* NONE==1 or ZIPLIST==2 */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> recompress : <span class="hljs-number">1</span>; <span class="hljs-comment">/* was this node previous compressed? */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> attempted_compress : <span class="hljs-number">1</span>; <span class="hljs-comment">/* node can't compress; too small */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> extra : <span class="hljs-number">10</span>; <span class="hljs-comment">/* more bits to steal for future usage */</span>&#125; quicklistNode;</code></pre></div></li></ul><h2 id="quicklistLZF"><a href="#quicklistLZF" class="headerlink" title="quicklistLZF"></a>quicklistLZF</h2><p>如果使用LZF算法进行压缩，节点指向的结构为quicklistLZF，其中sz表示compressed数组所占用字节大小。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistLZF</span> &#123;</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> sz; <span class="hljs-comment">/* LZF size in bytes*/</span>    <span class="hljs-keyword">char</span> compressed[];&#125; quicklistLZF;</code></pre></div></p><h2 id="quicklist"><a href="#quicklist" class="headerlink" title="quicklist"></a>quicklist</h2><ul><li>head：指向头节点</li><li>tail：指向尾节点</li><li>count：quicklist中压缩列表的entry总数</li><li>len：节点个数</li><li>fill：每个节点的ziplist长度，可以通过参数<code>list-max-ziplist-size</code>配置节点所占内存大小</li><li>compress：该值表示两端各有compress个节点不压缩<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklist</span> &#123;</span>    quicklistNode *head;    quicklistNode *tail;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> count;        <span class="hljs-comment">/* total count of all entries in all ziplists */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> len;          <span class="hljs-comment">/* number of quicklistNodes */</span>    <span class="hljs-keyword">int</span> <span class="hljs-built_in">fill</span> : <span class="hljs-number">16</span>;              <span class="hljs-comment">/* fill factor for individual nodes */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> compress : <span class="hljs-number">16</span>; <span class="hljs-comment">/* depth of end nodes not to compress;0=off */</span>&#125; quicklist;</code></pre></div></li></ul><h2 id="quicklistEntry"><a href="#quicklistEntry" class="headerlink" title="quicklistEntry"></a>quicklistEntry</h2><ul><li>quicklist：指向当前元素所在的quicklist</li><li>node：指向当前元素所在的节点</li><li>zi：当前元素所在的ziplist</li><li>value：当前节点的字符串内容</li><li>longval：当前节点的整型值</li><li>sz：节点的大小</li><li>offset：该节点相对于整个ziplist的偏移量，即相当于第几个entry<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistEntry</span> &#123;</span>    <span class="hljs-keyword">const</span> quicklist *quicklist;    quicklistNode *node;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *zi;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *value;    <span class="hljs-keyword">long</span> <span class="hljs-keyword">long</span> longval;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> sz;    <span class="hljs-keyword">int</span> offset;&#125; quicklistEntry;</code></pre></div></li></ul><h2 id="quicklistIter"><a href="#quicklistIter" class="headerlink" title="quicklistIter"></a>quicklistIter</h2><p>用于quicklist遍历的迭代器。</p><ul><li>quicklist：指向对应的quicklist</li><li>current：指向元素所在的节点</li><li>zi：指向元素所在的ziplist</li><li>offset：表示节点在ziplist中的偏移量</li><li>direction：表示迭代器的方向<div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistIter</span> &#123;</span>    <span class="hljs-keyword">const</span> quicklist *quicklist;    quicklistNode *current;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *zi;    <span class="hljs-keyword">long</span> offset; <span class="hljs-comment">/* offset in current ziplist */</span>    <span class="hljs-keyword">int</span> direction;&#125; quicklistIter;</code></pre></div></li></ul><h1 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h1><h2 id="quicklistPushHead"><a href="#quicklistPushHead" class="headerlink" title="quicklistPushHead"></a>quicklistPushHead</h2><p>该接口在头部插入元素。传入参数分别是待插入的quicklist，插入的数据value，大小sz。返回指为0代表没有新建head节点，1代表新建了head节点<br>基本逻辑如下：</p><ul><li>查看quicklist的head节点是否允许插入，可以就利用ziplist的接口进行插入。</li><li>否则新建head节点进行插入。<div class="hljs"><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">quicklistPushHead</span><span class="hljs-params">(quicklist *quicklist, <span class="hljs-keyword">void</span> *value, <span class="hljs-keyword">size_t</span> sz)</span> </span>&#123;    quicklistNode *orig_head = quicklist-&gt;head;    <span class="hljs-comment">//检查头节点是否允许插入</span>    <span class="hljs-keyword">if</span> (likely(            _quicklistNodeAllowInsert(quicklist-&gt;head, quicklist-&gt;<span class="hljs-built_in">fill</span>, sz))) &#123;            <span class="hljs-comment">//使用头节点中的ziplist接口进行插入</span>        quicklist-&gt;head-&gt;zl =            ziplistPush(quicklist-&gt;head-&gt;zl, value, sz, ZIPLIST_HEAD);        quicklistNodeUpdateSz(quicklist-&gt;head);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">//创建新节点进行插入</span>        quicklistNode *node = quicklistCreateNode();        node-&gt;zl = ziplistPush(ziplistNew(), value, sz, ZIPLIST_HEAD);        quicklistNodeUpdateSz(node);        _quicklistInsertNodeBefore(quicklist, quicklist-&gt;head, node);    &#125;    <span class="hljs-comment">//更新对应数据</span>    quicklist-&gt;count++;    quicklist-&gt;head-&gt;count++;    <span class="hljs-keyword">return</span> (orig_head != quicklist-&gt;head);&#125;</code></pre></div></li></ul><h2 id="quicklistReplaceAtIndex"><a href="#quicklistReplaceAtIndex" class="headerlink" title="quicklistReplaceAtIndex"></a>quicklistReplaceAtIndex</h2><p>更改元素需要传入index，基本逻辑是：</p><ul><li>删除原有元素</li><li>插入新元素<div class="hljs"><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">quicklistReplaceAtIndex</span><span class="hljs-params">(quicklist *quicklist, <span class="hljs-keyword">long</span> index, <span class="hljs-keyword">void</span> *data,                            <span class="hljs-keyword">int</span> sz)</span> </span>&#123;    quicklistEntry entry;    <span class="hljs-comment">//检查index位置的元素是否存在</span>    <span class="hljs-keyword">if</span> (likely(quicklistIndex(quicklist, index, &amp;entry))) &#123;        <span class="hljs-comment">//删除</span>        entry.node-&gt;zl = ziplistDelete(entry.node-&gt;zl, &amp;entry.zi);        <span class="hljs-comment">//添加</span>        entry.node-&gt;zl = ziplistInsert(entry.node-&gt;zl, entry.zi, data, sz);        <span class="hljs-comment">//更新sz字段</span>        quicklistNodeUpdateSz(entry.node);        quicklistCompress(quicklist, entry.node);        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125;&#125;</code></pre></div></li></ul><h2 id="quicklistIndex"><a href="#quicklistIndex" class="headerlink" title="quicklistIndex"></a>quicklistIndex</h2><p>该方法主要通过元素的下标查找对应元素，找到index对应数据所在的节点，再调用ziplist的<code>ziplistGet</code>得到Index对应的数据。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-comment">//idx为需要查找的下标, 结果写入entry，返回0代表没找到，1代表找到</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">quicklistIndex</span><span class="hljs-params">(<span class="hljs-keyword">const</span> quicklist *quicklist, <span class="hljs-keyword">const</span> <span class="hljs-keyword">long</span> <span class="hljs-keyword">long</span> idx,                   quicklistEntry *entry)</span> </span>&#123;    quicklistNode *n;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> <span class="hljs-keyword">long</span> accum = <span class="hljs-number">0</span>;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> <span class="hljs-keyword">long</span> index;    <span class="hljs-comment">//idx为负值，表示从尾部向头部的偏移量</span>    <span class="hljs-keyword">int</span> forward = idx &lt; <span class="hljs-number">0</span> ? <span class="hljs-number">0</span> : <span class="hljs-number">1</span>; <span class="hljs-comment">/* &lt; 0 -&gt; reverse, 0+ -&gt; forward */</span>    <span class="hljs-comment">//初始化entry</span>    initEntry(entry);    entry-&gt;quicklist = quicklist;    <span class="hljs-keyword">if</span> (!forward) &#123;        <span class="hljs-comment">//idx小于0，将idx设置为(-idx) - 1，从尾部查找</span>        index = (-idx) - <span class="hljs-number">1</span>;        n = quicklist-&gt;tail;    &#125; <span class="hljs-keyword">else</span> &#123;        index = idx;        n = quicklist-&gt;head;    &#125;    <span class="hljs-keyword">if</span> (index &gt;= quicklist-&gt;count)        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    <span class="hljs-comment">//遍历Node节点，找到对应的index元素</span>    <span class="hljs-keyword">while</span> (likely(n)) &#123;        <span class="hljs-keyword">if</span> ((accum + n-&gt;count) &gt; index) &#123;            <span class="hljs-keyword">break</span>;        &#125; <span class="hljs-keyword">else</span> &#123;            D(<span class="hljs-string">"Skipping over (%p) %u at accum %lld"</span>, (<span class="hljs-keyword">void</span> *)n, n-&gt;count,              accum);            accum += n-&gt;count;            n = forward ? n-&gt;next : n-&gt;prev;        &#125;    &#125;    <span class="hljs-keyword">if</span> (!n)        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    D(<span class="hljs-string">"Found node: %p at accum %llu, idx %llu, sub+ %llu, sub- %llu"</span>, (<span class="hljs-keyword">void</span> *)n,      accum, index, index - accum, (-index) - <span class="hljs-number">1</span> + accum);    <span class="hljs-comment">//计算index所在的ziplist的偏移量</span>    entry-&gt;node = n;    <span class="hljs-keyword">if</span> (forward) &#123;        <span class="hljs-comment">/* forward = normal head-to-tail offset. */</span>        entry-&gt;offset = index - accum;    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">/* reverse = need negative offset for tail-to-head, so undo         * the result of the original if (index &lt; 0) above. */</span>        entry-&gt;offset = (-index) - <span class="hljs-number">1</span> + accum;    &#125;    quicklistDecompressNodeForUse(entry-&gt;node);    entry-&gt;zi = ziplistIndex(entry-&gt;node-&gt;zl, entry-&gt;offset);    <span class="hljs-comment">//利用ziplistGet获取元素</span>    ziplistGet(entry-&gt;zi, &amp;entry-&gt;value, &amp;entry-&gt;sz, &amp;entry-&gt;longval);    <span class="hljs-comment">/* The caller will use our result, so we don't re-compress here.     * The caller can recompress or delete the node as needed. */</span>    <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;&#125;</code></pre></div></p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文主要介绍了Quicklist的基本数据结构，并对该结构的基本增删改查接口做了简要分析。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;p&gt;quicklist是Redis 3.2中引入的数据结构，其本质是一个双向链表，链表的节点类型是ziplist，当ziplist节点过多，qu
      
    
    </summary>
    
      <category term="Redis" scheme="https://l1n.wang/categories/Redis/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/categories/Redis/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/tags/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
      <category term="Redis" scheme="https://l1n.wang/tags/Redis/"/>
    
  </entry>
  
  <entry>
    <title>Redis底层数据结构-Intset源码分析</title>
    <link href="https://l1n.wang/2020/06/redis-intset/"/>
    <id>https://l1n.wang/2020/06/redis-intset/</id>
    <published>2020-06-20T07:28:27.000Z</published>
    <updated>2020-06-20T07:29:07.238Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>整数集合是一个有序的，存储Integer类型数据的结构，当元素为64位有符号整数范围之内时，它是Redis数据结构中有序集合ZSET的底层实现，但是当元素个数超过一定数量时（默认为512），会转为hashtable进行存储，由配置项<code>set-max-intset-entries 512</code>决定。如果向有序集合中添加非整型变量，底层实现也会装欢为hashtable。</p><h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><ul><li>encoding：编码类型，决定每个元素占用几个字节<ul><li>INTSET_ENC_INT16：当元素大小位于<code>INT16_MIN</code>和<code>INT16_MAX</code>之间使用，每个元素占用两个字节。</li><li>INTSET_ENC_INT32：元素大小位于<code>INT16_MAX</code>和<code>INT32_MAX</code>之间或<code>INT32_MIN</code>到<code>INT16_MIN</code>之间，该编码方式占用四个字节。</li><li>INTSET_ENC_INT64：元素大小位于<code>INT32_MAX</code>到<code>INT64_MAX</code>或<code>INT64_MIN</code>到<code>INT32_MIN</code>之间，该编码方式每个元素占用八个字节。</li></ul></li><li>length：元素个数</li><li>contents：存储具体元素的数组<div class="hljs"><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> INTSET_ENC_INT16 (sizeof(int16_t))</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> INTSET_ENC_INT32： (sizeof(int32_t))</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> INTSET_ENC_INT64 (sizeof(int64_t))</span><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">intset</span> &#123;</span>    <span class="hljs-keyword">uint32_t</span> encoding;    <span class="hljs-keyword">uint32_t</span> length;    <span class="hljs-keyword">int8_t</span> contents[];&#125; intset;</code></pre></div></li></ul><h1 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h1><h2 id="intsetFind"><a href="#intsetFind" class="headerlink" title="intsetFind"></a>intsetFind</h2><p>查找算法主要通过二分查找实现。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-comment">/* Determine whether a value belongs to this set */</span><span class="hljs-function"><span class="hljs-keyword">uint8_t</span> <span class="hljs-title">intsetFind</span><span class="hljs-params">(intset *is, <span class="hljs-keyword">int64_t</span> value)</span> </span>&#123;    <span class="hljs-comment">//判断编码方式</span>    <span class="hljs-keyword">uint8_t</span> valenc = _intsetValueEncoding(value);    <span class="hljs-comment">//编码方式如果当前intset的编码方式，直接返回0</span>    <span class="hljs-comment">//否则调用intsetSearch函数进行查找</span>    <span class="hljs-keyword">return</span> valenc &lt;= intrev32ifbe(is-&gt;encoding) &amp;&amp; intsetSearch(is,value,<span class="hljs-literal">NULL</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-keyword">uint8_t</span> <span class="hljs-title">intsetSearch</span><span class="hljs-params">(intset *is, <span class="hljs-keyword">int64_t</span> value, <span class="hljs-keyword">uint32_t</span> *pos)</span> </span>&#123;    <span class="hljs-keyword">int</span> <span class="hljs-built_in">min</span> = <span class="hljs-number">0</span>, <span class="hljs-built_in">max</span> = intrev32ifbe(is-&gt;length)<span class="hljs-number">-1</span>, mid = <span class="hljs-number">-1</span>;    <span class="hljs-keyword">int64_t</span> cur = <span class="hljs-number">-1</span>;    <span class="hljs-comment">/* The value can never be found when the set is empty */</span>    <span class="hljs-comment">//intset如果为空，直接返回0</span>    <span class="hljs-keyword">if</span> (intrev32ifbe(is-&gt;length) == <span class="hljs-number">0</span>) &#123;        <span class="hljs-keyword">if</span> (pos) *pos = <span class="hljs-number">0</span>;        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125; <span class="hljs-keyword">else</span> &#123;        当前元素如果大于当前有序集合最大值或者小于最小值，直接返回<span class="hljs-number">0</span>        <span class="hljs-keyword">if</span> (value &gt; _intsetGet(is,<span class="hljs-built_in">max</span>)) &#123;            <span class="hljs-keyword">if</span> (pos) *pos = intrev32ifbe(is-&gt;length);            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value &lt; _intsetGet(is,<span class="hljs-number">0</span>)) &#123;            <span class="hljs-keyword">if</span> (pos) *pos = <span class="hljs-number">0</span>;            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;        &#125;    &#125;    <span class="hljs-comment">//由于集合有序，采用二分查找</span>    <span class="hljs-keyword">while</span>(<span class="hljs-built_in">max</span> &gt;= <span class="hljs-built_in">min</span>) &#123;        mid = ((<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span>)<span class="hljs-built_in">min</span> + (<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span>)<span class="hljs-built_in">max</span>) &gt;&gt; <span class="hljs-number">1</span>;        cur = _intsetGet(is,mid);        <span class="hljs-keyword">if</span> (value &gt; cur) &#123;            <span class="hljs-built_in">min</span> = mid+<span class="hljs-number">1</span>;        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value &lt; cur) &#123;            <span class="hljs-built_in">max</span> = mid<span class="hljs-number">-1</span>;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">break</span>;        &#125;    &#125;    <span class="hljs-keyword">if</span> (value == cur) &#123;        <span class="hljs-keyword">if</span> (pos) *pos = mid;        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">if</span> (pos) *pos = <span class="hljs-built_in">min</span>;        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125;&#125;</code></pre></div></p><h2 id="intsetAdd"><a href="#intsetAdd" class="headerlink" title="intsetAdd"></a>intsetAdd</h2><p>主要逻辑如下：</p><ul><li>判断插入的值的编码格式，小于当前intset编码格式直接插入，大于则调用<code>intsetUpgradeAndAdd</code>函数进行升级</li><li>上述条件小于时，调用<code>intsetSearch</code>进行查找，如果插入的值已经存在，直接返回。</li><li>不存在则进行扩容操作，<code>intsetResize</code>将intset的长度加一。</li><li>如果插入的位置位于原来元素之间，使用<code>intsetMoveTail</code>进行元素移动，空出一个位置进行插入。</li><li>调用<code>_intsetSet</code>保存元素</li></ul><div class="hljs"><pre><code class="hljs C"><span class="hljs-function">intset *<span class="hljs-title">intsetAdd</span><span class="hljs-params">(intset *is, <span class="hljs-keyword">int64_t</span> value, <span class="hljs-keyword">uint8_t</span> *success)</span> </span>&#123;    <span class="hljs-comment">//获取添加元素的编码值</span>    <span class="hljs-keyword">uint8_t</span> valenc = _intsetValueEncoding(value);    <span class="hljs-keyword">uint32_t</span> pos;    <span class="hljs-keyword">if</span> (success) *success = <span class="hljs-number">1</span>;        <span class="hljs-keyword">if</span> (valenc &gt; intrev32ifbe(is-&gt;encoding)) &#123;        <span class="hljs-comment">//如果该元素编码大于当前intset编码。进行升级</span>        <span class="hljs-keyword">return</span> intsetUpgradeAndAdd(is,value);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">//否则先进行查重，如果已经存在该元素，直接返回（保证有序集合的唯一性）</span>        <span class="hljs-keyword">if</span> (intsetSearch(is,value,&amp;pos)) &#123;            <span class="hljs-keyword">if</span> (success) *success = <span class="hljs-number">0</span>;            <span class="hljs-keyword">return</span> is;        &#125;        <span class="hljs-comment">//扩容</span>        is = intsetResize(is,intrev32ifbe(is-&gt;length)+<span class="hljs-number">1</span>);        <span class="hljs-comment">//如果插入元素位置合法，将该位置往后挪</span>        <span class="hljs-keyword">if</span> (pos &lt; intrev32ifbe(is-&gt;length)) intsetMoveTail(is,pos,pos+<span class="hljs-number">1</span>);    &#125;    <span class="hljs-comment">//保存元素</span>    _intsetSet(is,pos,value);    <span class="hljs-comment">//修改inset的长度，将其加1</span>    is-&gt;length = intrev32ifbe(intrev32ifbe(is-&gt;length)+<span class="hljs-number">1</span>);    <span class="hljs-keyword">return</span> is;&#125;<span class="hljs-function"><span class="hljs-keyword">static</span> intset *<span class="hljs-title">intsetUpgradeAndAdd</span><span class="hljs-params">(intset *is, <span class="hljs-keyword">int64_t</span> value)</span> </span>&#123;    <span class="hljs-keyword">uint8_t</span> curenc = intrev32ifbe(is-&gt;encoding);    <span class="hljs-keyword">uint8_t</span> newenc = _intsetValueEncoding(value);    <span class="hljs-keyword">int</span> length = intrev32ifbe(is-&gt;length);    <span class="hljs-keyword">int</span> prepend = value &lt; <span class="hljs-number">0</span> ? <span class="hljs-number">1</span> : <span class="hljs-number">0</span>;    <span class="hljs-comment">/* First set new encoding and resize */</span>    is-&gt;encoding = intrev32ifbe(newenc);    is = intsetResize(is,intrev32ifbe(is-&gt;length)+<span class="hljs-number">1</span>);    <span class="hljs-comment">/* Upgrade back-to-front so we don't overwrite values.     * Note that the "prepend" variable is used to make sure we have an empty     * space at either the beginning or the end of the intset. */</span>    <span class="hljs-keyword">while</span>(length--)        _intsetSet(is,length+prepend,_intsetGetEncoded(is,length,curenc));    <span class="hljs-comment">/* Set the value at the beginning or the end. */</span>    <span class="hljs-keyword">if</span> (prepend)        _intsetSet(is,<span class="hljs-number">0</span>,value);    <span class="hljs-keyword">else</span>        _intsetSet(is,intrev32ifbe(is-&gt;length),value);    is-&gt;length = intrev32ifbe(intrev32ifbe(is-&gt;length)+<span class="hljs-number">1</span>);    <span class="hljs-keyword">return</span> is;&#125;</code></pre></div><h2 id="intsetRemove"><a href="#intsetRemove" class="headerlink" title="intsetRemove"></a>intsetRemove</h2><p>主要逻辑如下：</p><ul><li>判断新插入值的编码是否小于等于当前intset编码。</li><li>该值是否在intset中存在，如果存在，将intset的值从pos + 1的位置移动到pos位置。</li><li>更新长度字段。</li></ul><div class="hljs"><pre><code class="hljs C"><span class="hljs-function">intset *<span class="hljs-title">intsetRemove</span><span class="hljs-params">(intset *is, <span class="hljs-keyword">int64_t</span> value, <span class="hljs-keyword">int</span> *success)</span> </span>&#123;    <span class="hljs-comment">//获取待删除元素的编码</span>    <span class="hljs-keyword">uint8_t</span> valenc = _intsetValueEncoding(value);    <span class="hljs-keyword">uint32_t</span> pos;    <span class="hljs-keyword">if</span> (success) *success = <span class="hljs-number">0</span>;    <span class="hljs-comment">//如果编码小于等于当前intset编码并且查找到该元素，则进行删除操作</span>    <span class="hljs-keyword">if</span> (valenc &lt;= intrev32ifbe(is-&gt;encoding) &amp;&amp; intsetSearch(is,value,&amp;pos)) &#123;        <span class="hljs-comment">//获取intset长度</span>        <span class="hljs-keyword">uint32_t</span> len = intrev32ifbe(is-&gt;length);        <span class="hljs-comment">/* We know we can delete */</span>        <span class="hljs-keyword">if</span> (success) *success = <span class="hljs-number">1</span>;        <span class="hljs-comment">//调用intsetMoveTail对待删除元素进行覆盖</span>        <span class="hljs-keyword">if</span> (pos &lt; (len<span class="hljs-number">-1</span>)) intsetMoveTail(is,pos+<span class="hljs-number">1</span>,pos);        is = intsetResize(is,len<span class="hljs-number">-1</span>);        is-&gt;length = intrev32ifbe(len<span class="hljs-number">-1</span>);    &#125;    <span class="hljs-keyword">return</span> is;&#125;</code></pre></div><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文主要对Redis中集合的底层数据结构intset结构及其基本操作函数做了简要分析，该结构主要使用场景为集合元素全为整型且元素数量不多（默认小于512）。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;p&gt;整数集合是一个有序的，存储Integer类型数据的结构，当元素为64位有符号整数范围之内时，它是Redis数据结构中有序集合ZSET的底层实
      
    
    </summary>
    
      <category term="Redis" scheme="https://l1n.wang/categories/Redis/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/categories/Redis/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/tags/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
      <category term="Redis" scheme="https://l1n.wang/tags/Redis/"/>
    
  </entry>
  
  <entry>
    <title>Redis底层数据结构-Dict源码分析</title>
    <link href="https://l1n.wang/2020/05/redis-dict/"/>
    <id>https://l1n.wang/2020/05/redis-dict/</id>
    <published>2020-05-29T10:30:20.000Z</published>
    <updated>2020-05-29T10:36:00.463Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>字典别名散列表，是一种用来存储键值对的数据结构。Redis本身就是K-V型数据库，整个数据库就是用字典进行存储的，对Redis的增删改查操作，实际上就是对字典中的数据进行增删改查操作。</p><h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><h2 id="HashTable"><a href="#HashTable" class="headerlink" title="HashTable"></a>HashTable</h2><ul><li>table：指针数组，用于存储键值对，指向的是<code>dictEntry</code>结构体，每个<code>dictEntry</code>存有键值对</li><li>size：table数组的大小</li><li>sizemask：掩码，用来计算键的索引值。值恒为size -1</li><li>used：table数组已存键值对个数</li></ul><p>Hash表的数组容量初始值为4，扩容倍数为当前一倍，所以sizemask大小为<code>3,7,11,31</code>，二进制表示为<code>111111...</code>，在计算索引值时，通过<code>idx = hash &amp; d-&gt;dt[table].sizemask</code>语句计算Hash值与Hash表容量取余，位运算速度快于取余运算。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictht</span> &#123;</span>    dictEntry **table;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> <span class="hljs-built_in">size</span>;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> sizemask;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> used;&#125; dictht;</code></pre></div></p><h2 id="dictEntry"><a href="#dictEntry" class="headerlink" title="dictEntry"></a>dictEntry</h2><p>键值对节点，存放键值对。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictEntry</span> &#123;</span>    <span class="hljs-comment">//键</span>    <span class="hljs-keyword">void</span> *key;    <span class="hljs-keyword">union</span> &#123;        <span class="hljs-comment">//存储值</span>        <span class="hljs-keyword">void</span> *val;        <span class="hljs-keyword">uint64_t</span> u64;        <span class="hljs-comment">//存储过期时间</span>        <span class="hljs-keyword">int64_t</span> s64;        <span class="hljs-keyword">double</span> d;    &#125; v;<span class="hljs-comment">//值，联合体</span>    <span class="hljs-comment">//next指针，Hash冲突时的单链表法</span>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictEntry</span> *<span class="hljs-title">next</span>;</span>&#125; dictEntry;</code></pre></div></p><h2 id="dictType"><a href="#dictType" class="headerlink" title="dictType"></a>dictType</h2><p>存放的是对字典操作的函数指针<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictType</span> &#123;</span>    <span class="hljs-comment">//Hash函数</span>    <span class="hljs-keyword">uint64_t</span> (*hashFunction)(<span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *key);    <span class="hljs-comment">//键对应的复制函数</span>    <span class="hljs-keyword">void</span> *(*keyDup)(<span class="hljs-keyword">void</span> *privdata, <span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *key);    <span class="hljs-comment">//值对应的复制函数</span>    <span class="hljs-keyword">void</span> *(*valDup)(<span class="hljs-keyword">void</span> *privdata, <span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *obj);    <span class="hljs-comment">//键的比对函数</span>    <span class="hljs-keyword">int</span> (*keyCompare)(<span class="hljs-keyword">void</span> *privdata, <span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *key1, <span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *key2);    <span class="hljs-comment">//键的销毁函数</span>    <span class="hljs-keyword">void</span> (*keyDestructor)(<span class="hljs-keyword">void</span> *privdata, <span class="hljs-keyword">void</span> *key);    <span class="hljs-comment">//值得销毁函数</span>    <span class="hljs-keyword">void</span> (*valDestructor)(<span class="hljs-keyword">void</span> *privdata, <span class="hljs-keyword">void</span> *obj);&#125; dictType;</code></pre></div></p><h2 id="dict"><a href="#dict" class="headerlink" title="dict"></a>dict</h2><ul><li>type：字典操作函数指针</li><li>privdata：私有数据，配合tyoe指针指向的函数一起使用</li><li>ht：大小为2的数组，默认使用ht[0]，当字典扩容缩容时进行rehash时，才会用到ht[1]</li><li>rehashidx：标记该字典是否在进行rehash，没进行为-1，用来记录rehash到了哪个元素，值为下标值</li><li>iterators：用来记录当前运行的安全迭代器数，当有安全迭代器，会暂停rehash</li></ul><p>基本结构图如图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/redis/dict/dict.png" srcset="/img/loading.gif" alt="字典结构图"><br><div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dict</span> &#123;</span><span class="hljs-comment">//存放字典的操作函数</span>    dictType *type;    <span class="hljs-comment">//依赖的数据</span>    <span class="hljs-keyword">void</span> *privdata;    <span class="hljs-comment">//Hash表</span>    dictht ht[<span class="hljs-number">2</span>];    <span class="hljs-comment">//rehash标识，默认为-1，代表没有进行rehash操作</span>    <span class="hljs-keyword">long</span> rehashidx;     <span class="hljs-comment">//当前运行的迭代器数</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> iterators;&#125; dict;</code></pre></div></p><h1 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h1><h2 id="dictCreate"><a href="#dictCreate" class="headerlink" title="dictCreate"></a>dictCreate</h2><p>redis-server启动时，会初始化一个空字典用于存储整个数据库的键值对，初始化的主要逻辑如下：</p><ul><li>申请空间</li><li>调用<code>_dictInit</code>完成初始化<div class="hljs"><pre><code class="hljs C"><span class="hljs-function">dict *<span class="hljs-title">dictCreate</span><span class="hljs-params">(dictType *type,        <span class="hljs-keyword">void</span> *privDataPtr)</span></span>&#123;    dict *d = zmalloc(<span class="hljs-keyword">sizeof</span>(*d));    _dictInit(d,type,privDataPtr);    <span class="hljs-keyword">return</span> d;&#125;<span class="hljs-keyword">int</span> _dictInit(dict *d, dictType *type,        <span class="hljs-keyword">void</span> *privDataPtr)&#123;    _dictReset(&amp;d-&gt;ht[<span class="hljs-number">0</span>]);    _dictReset(&amp;d-&gt;ht[<span class="hljs-number">1</span>]);    d-&gt;type = type;    d-&gt;privdata = privDataPtr;    d-&gt;rehashidx = <span class="hljs-number">-1</span>;    d-&gt;iterators = <span class="hljs-number">0</span>;    <span class="hljs-keyword">return</span> DICT_OK;&#125;</code></pre></div></li></ul><h2 id="dictAdd"><a href="#dictAdd" class="headerlink" title="dictAdd"></a>dictAdd</h2><p>添加键值对，主要逻辑如下：</p><ul><li>调用<code>dictAddRaw</code>，添加键</li><li>给返回的新节点设置值（更新val字段）<div class="hljs"><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">dictAdd</span><span class="hljs-params">(dict *d, <span class="hljs-keyword">void</span> *key, <span class="hljs-keyword">void</span> *val)</span></span>&#123;    <span class="hljs-comment">//添加键，字典中键已存在则返回NULL，否则添加新节点，并返回</span>    dictEntry *entry = dictAddRaw(d,key,<span class="hljs-literal">NULL</span>);    <span class="hljs-comment">//键存在则添加错误</span>    <span class="hljs-keyword">if</span> (!entry) <span class="hljs-keyword">return</span> DICT_ERR;    <span class="hljs-comment">//设置值</span>    dictSetVal(d, entry, val);    <span class="hljs-keyword">return</span> DICT_OK;&#125;<span class="hljs-comment">//d为入参字典，key为键，existing为Hash表节点地址</span><span class="hljs-function">dictEntry *<span class="hljs-title">dictAddRaw</span><span class="hljs-params">(dict *d, <span class="hljs-keyword">void</span> *key, dictEntry **existing)</span></span>&#123;    <span class="hljs-keyword">long</span> index;    dictEntry *entry;    dictht *ht;    <span class="hljs-comment">//字典是否在进行rehash操作</span>    <span class="hljs-keyword">if</span> (dictIsRehashing(d)) _dictRehashStep(d);    <span class="hljs-comment">//查找键，找到直接返回-1，并把老节点存放在existing里，否则返回新节点索引值</span>    <span class="hljs-keyword">if</span> ((index = _dictKeyIndex(d, key, dictHashKey(d,key), existing)) == <span class="hljs-number">-1</span>)        <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;，    <span class="hljs-comment">//检测容量</span>    ht = dictIsRehashing(d) ? &amp;d-&gt;ht[<span class="hljs-number">1</span>] : &amp;d-&gt;ht[<span class="hljs-number">0</span>];    <span class="hljs-comment">//申请新节点内存空间，插入哈希表</span>    entry = zmalloc(<span class="hljs-keyword">sizeof</span>(*entry));    entry-&gt;next = ht-&gt;table[index];    ht-&gt;table[index] = entry;    ht-&gt;used++;    <span class="hljs-comment">//存储键信息</span>    dictSetKey(d, entry, key);    <span class="hljs-keyword">return</span> entry;&#125;</code></pre></div></li></ul><h2 id="dictExpand"><a href="#dictExpand" class="headerlink" title="dictExpand"></a>dictExpand</h2><p>扩容操作主要逻辑：</p><ul><li>计算扩容大小（是size的下一个2的幂）</li><li>将新申请的地址存放于ht[1]，并把<code>rehashidx</code>标为1，表示需要进行rehash</li></ul><p>扩容完成后ht[1]中为全新的Hash表，扩容之后，添加操作的键值对全部存放于全新的Hash表中，修改删除查找等操作需要在ht[0]和ht[1]中进行检查。此外还需要将ht[0]中的键值对rehash到ht[1]中。</p><div class="hljs"><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">dictExpand</span><span class="hljs-params">(dict *d, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> <span class="hljs-built_in">size</span>)</span></span>&#123;    <span class="hljs-comment">//如果已存在空间大于传入size，则无效</span>    <span class="hljs-keyword">if</span> (dictIsRehashing(d) || d-&gt;ht[<span class="hljs-number">0</span>].used &gt; <span class="hljs-built_in">size</span>)        <span class="hljs-keyword">return</span> DICT_ERR;    dictht n; <span class="hljs-comment">/* the new hash table */</span>    <span class="hljs-comment">//扩容值为2的幂</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> realsize = _dictNextPower(<span class="hljs-built_in">size</span>);    <span class="hljs-comment">//相同则不扩容</span>    <span class="hljs-keyword">if</span> (realsize == d-&gt;ht[<span class="hljs-number">0</span>].<span class="hljs-built_in">size</span>) <span class="hljs-keyword">return</span> DICT_ERR;    <span class="hljs-comment">//将新的hash表内部变量初始化，并申请对应的内存空间</span>    n.<span class="hljs-built_in">size</span> = realsize;    n.sizemask = realsize<span class="hljs-number">-1</span>;    n.table = zcalloc(realsize*<span class="hljs-keyword">sizeof</span>(dictEntry*));    n.used = <span class="hljs-number">0</span>;    <span class="hljs-comment">//如果当前是空表，就直接存放在ht[0]</span>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].table == <span class="hljs-literal">NULL</span>) &#123;        d-&gt;ht[<span class="hljs-number">0</span>] = n;        <span class="hljs-keyword">return</span> DICT_OK;    &#125;    扩容后的新内存放入ht[<span class="hljs-number">1</span>]    d-&gt;ht[<span class="hljs-number">1</span>] = n;    <span class="hljs-comment">//表示需要进行rehash</span>    d-&gt;rehashidx = <span class="hljs-number">0</span>;    <span class="hljs-keyword">return</span> DICT_OK;&#125;</code></pre></div><h2 id="dictResize"><a href="#dictResize" class="headerlink" title="dictResize"></a>dictResize</h2><p>缩容操作主要通过<code>dictExpand(d, minimal)</code>实现。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> DICT_HT_INITIAL_SIZE     4</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">dictResize</span><span class="hljs-params">(dict *d)</span></span>&#123;    <span class="hljs-keyword">int</span> minimal;    <span class="hljs-comment">//判断是否在rehash</span>    <span class="hljs-keyword">if</span> (!dict_can_resize || dictIsRehashing(d)) <span class="hljs-keyword">return</span> DICT_ERR;    <span class="hljs-comment">//minimal为ht[0]的已使用量</span>    minimal = d-&gt;ht[<span class="hljs-number">0</span>].used;    <span class="hljs-comment">//如果键值对数量 &lt; 4，将缩容至4</span>    <span class="hljs-keyword">if</span> (minimal &lt; DICT_HT_INITIAL_SIZE)        minimal = DICT_HT_INITIAL_SIZE;        <span class="hljs-comment">//调用dictExpand进行缩容</span>    <span class="hljs-keyword">return</span> dictExpand(d, minimal);&#125;</code></pre></div></p><h2 id="Rehash"><a href="#Rehash" class="headerlink" title="Rehash"></a>Rehash</h2><p>Rehash在缩容和扩容时都会触发。执行插入、删除、查找、修改操作前，会判断当前字典是否在rehash，如果在，调用<code>dictRehashStep</code>进行rehash（只对一个节点rehash）。如果服务处于空闲时，也会进行rehash操作（<code>incrementally</code>批量，一次100个节点）<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">dictRehash</span><span class="hljs-params">(dict *d, <span class="hljs-keyword">int</span> n)</span> </span>&#123;    <span class="hljs-keyword">int</span> empty_visits = n*<span class="hljs-number">10</span>; <span class="hljs-comment">/* Max number of empty buckets to visit. */</span>    <span class="hljs-comment">//如果已经rehash结束，直接返回</span>    <span class="hljs-keyword">if</span> (!dictIsRehashing(d)) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;        <span class="hljs-keyword">while</span>(n-- &amp;&amp; d-&gt;ht[<span class="hljs-number">0</span>].used != <span class="hljs-number">0</span>) &#123;        dictEntry *de, *nextde;        <span class="hljs-comment">/* Note that rehashidx can't overflow as we are sure there are more         * elements because ht[0].used != 0 */</span>        assert(d-&gt;ht[<span class="hljs-number">0</span>].<span class="hljs-built_in">size</span> &gt; (<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span>)d-&gt;rehashidx);        <span class="hljs-comment">//找到hash表中不为空的位置</span>        <span class="hljs-keyword">while</span>(d-&gt;ht[<span class="hljs-number">0</span>].table[d-&gt;rehashidx] == <span class="hljs-literal">NULL</span>) &#123;            d-&gt;rehashidx++;            <span class="hljs-keyword">if</span> (--empty_visits == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;        &#125;        <span class="hljs-comment">//de为rehash标识，存放正在进行rehash节点的索引值</span>        de = d-&gt;ht[<span class="hljs-number">0</span>].table[d-&gt;rehashidx];        <span class="hljs-comment">/* Move all the keys in this bucket from the old to the new hash HT */</span>        <span class="hljs-keyword">while</span>(de) &#123;            <span class="hljs-keyword">uint64_t</span> h;            nextde = de-&gt;next;            <span class="hljs-comment">//h</span>            h = dictHashKey(d, de-&gt;key) &amp; d-&gt;ht[<span class="hljs-number">1</span>].sizemask;            <span class="hljs-comment">//插入</span>            de-&gt;next = d-&gt;ht[<span class="hljs-number">1</span>].table[h];            d-&gt;ht[<span class="hljs-number">1</span>].table[h] = de;            <span class="hljs-comment">//更新数据</span>            d-&gt;ht[<span class="hljs-number">0</span>].used--;            d-&gt;ht[<span class="hljs-number">1</span>].used++;            de = nextde;        &#125;        <span class="hljs-comment">//置空</span>        d-&gt;ht[<span class="hljs-number">0</span>].table[d-&gt;rehashidx] = <span class="hljs-literal">NULL</span>;        d-&gt;rehashidx++;    &#125;    <span class="hljs-comment">//检查是否已经rehash过了</span>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].used == <span class="hljs-number">0</span>) &#123;        zfree(d-&gt;ht[<span class="hljs-number">0</span>].table);        d-&gt;ht[<span class="hljs-number">0</span>] = d-&gt;ht[<span class="hljs-number">1</span>];        _dictReset(&amp;d-&gt;ht[<span class="hljs-number">1</span>]);        d-&gt;rehashidx = <span class="hljs-number">-1</span>;        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125;    <span class="hljs-comment">/* More to rehash... */</span>    <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;&#125;</code></pre></div></p><h2 id="dictFind"><a href="#dictFind" class="headerlink" title="dictFind"></a>dictFind</h2><p>查找键的逻辑较为简单，遍历ht[0]和ht[1]。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-function">dictEntry *<span class="hljs-title">dictFind</span><span class="hljs-params">(dict *d, <span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *key)</span></span>&#123;    dictEntry *he;    <span class="hljs-keyword">uint64_t</span> h, idx, table;    <span class="hljs-comment">//字典为空，直接返回</span>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].used + d-&gt;ht[<span class="hljs-number">1</span>].used == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>; <span class="hljs-comment">/* dict is empty */</span>    <span class="hljs-keyword">if</span> (dictIsRehashing(d)) _dictRehashStep(d);    <span class="hljs-comment">//获取键的hash值</span>    h = dictHashKey(d, key);    <span class="hljs-comment">//遍历查找hash表,ht[0]和ht[1]</span>    <span class="hljs-keyword">for</span> (table = <span class="hljs-number">0</span>; table &lt;= <span class="hljs-number">1</span>; table++) &#123;        idx = h &amp; d-&gt;ht[table].sizemask;        he = d-&gt;ht[table].table[idx];        <span class="hljs-comment">//遍历单链表</span>        <span class="hljs-keyword">while</span>(he) &#123;            <span class="hljs-keyword">if</span> (key==he-&gt;key || dictCompareKeys(d, key, he-&gt;key))                <span class="hljs-keyword">return</span> he;            he = he-&gt;next;        &#125;        <span class="hljs-keyword">if</span> (!dictIsRehashing(d)) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;&#125;</code></pre></div></p><h2 id="dictDelete"><a href="#dictDelete" class="headerlink" title="dictDelete"></a>dictDelete</h2><p>删除的主要逻辑如下：</p><ul><li>查找该键是否存在于该字典中</li><li>存在则将节点从单链表中删除</li><li>释放节点内存空间，used减一<div class="hljs"><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">dictDelete</span><span class="hljs-params">(dict *ht, <span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *key)</span> </span>&#123;    <span class="hljs-keyword">return</span> dictGenericDelete(ht,key,<span class="hljs-number">0</span>) ? DICT_OK : DICT_ERR;&#125;<span class="hljs-function"><span class="hljs-keyword">static</span> dictEntry *<span class="hljs-title">dictGenericDelete</span><span class="hljs-params">(dict *d, <span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *key, <span class="hljs-keyword">int</span> nofree)</span> </span>&#123;    <span class="hljs-keyword">uint64_t</span> h, idx;    dictEntry *he, *prevHe;    <span class="hljs-keyword">int</span> table;    <span class="hljs-comment">//如果字典为空直接返回</span>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].used == <span class="hljs-number">0</span> &amp;&amp; d-&gt;ht[<span class="hljs-number">1</span>].used == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;    <span class="hljs-comment">//。如果正在rehash，则调用_dictRehashStep进行rehash一次</span>    <span class="hljs-keyword">if</span> (dictIsRehashing(d)) _dictRehashStep(d);    <span class="hljs-comment">//获取需要删除节点的键Hash值</span>    h = dictHashKey(d, key);    <span class="hljs-comment">//从ht[0]和ht[1]中查找</span>    <span class="hljs-keyword">for</span> (table = <span class="hljs-number">0</span>; table &lt;= <span class="hljs-number">1</span>; table++) &#123;        idx = h &amp; d-&gt;ht[table].sizemask;        he = d-&gt;ht[table].table[idx];        prevHe = <span class="hljs-literal">NULL</span>;        <span class="hljs-comment">//遍历单链表查找</span>        <span class="hljs-keyword">while</span>(he) &#123;            <span class="hljs-keyword">if</span> (key==he-&gt;key || dictCompareKeys(d, key, he-&gt;key)) &#123;                <span class="hljs-comment">//删除节点</span>                <span class="hljs-keyword">if</span> (prevHe)                    prevHe-&gt;next = he-&gt;next;                <span class="hljs-keyword">else</span>                    d-&gt;ht[table].table[idx] = he-&gt;next;                <span class="hljs-keyword">if</span> (!nofree) &#123;                    <span class="hljs-comment">//释放节点内存空间</span>                    dictFreeKey(d, he);                    dictFreeVal(d, he);                    zfree(he);                &#125;                <span class="hljs-comment">//used自减一</span>                d-&gt;ht[table].used--;                <span class="hljs-keyword">return</span> he;            &#125;            prevHe = he;            he = he-&gt;next;        &#125;        <span class="hljs-keyword">if</span> (!dictIsRehashing(d)) <span class="hljs-keyword">break</span>;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>; <span class="hljs-comment">/* not found */</span>&#125;</code></pre></div></li></ul><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文主要对Redis中的字典基本结构做了简要分析，对字典的创建，键值对添加/删除/查找等操作与字典的缩容扩容机制做了简要分析，键值对修改操作主要通过<code>db.c</code>中的<code>dbOverwrite</code>函数调用<code>dictSetVal</code>实现。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;p&gt;字典别名散列表，是一种用来存储键值对的数据结构。Redis本身就是K-V型数据库，整个数据库就是用字典进行存储的，对Redis的增删改查操作
      
    
    </summary>
    
      <category term="Redis" scheme="https://l1n.wang/categories/Redis/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/categories/Redis/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/tags/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
      <category term="Redis" scheme="https://l1n.wang/tags/Redis/"/>
    
  </entry>
  
  <entry>
    <title>Redis底层数据结构-ZipList源码分析</title>
    <link href="https://l1n.wang/2020/05/redis-ziplist/"/>
    <id>https://l1n.wang/2020/05/redis-ziplist/</id>
    <published>2020-05-26T08:58:29.000Z</published>
    <updated>2020-05-29T10:31:39.662Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>压缩列表（ziplist）的本质是一个字节数组，主要是Redis为了节省内存而设计的数据结构。在Redis的有序集合和散列表都使用了ziplist，当有序集合或散列表的元素个数比较少，并且元素都是短字符串时，使用ziplist作为其底层数据结构。</p><p>压缩列表的基本结构基本如下所示：<br><div class="hljs"><pre><code class="hljs undefined"><span class="hljs-tag">&lt;<span class="hljs-name">zlbytes</span>&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-name">zltail</span>&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-name">zllen</span>&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-name">entry</span>&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-name">entry</span>&gt;</span> ... <span class="hljs-tag">&lt;<span class="hljs-name">entry</span>&gt;</span> <span class="hljs-tag">&lt;<span class="hljs-name">zlend</span>&gt;</span></code></pre></div></p><ul><li>uint32_t zlbytes：压缩列表的字节长度，占4个字节</li><li>uint32_t zltail：压缩列表尾元素相对于起始地址的偏移量，占4个字节，方便从列表尾部进行操作</li><li>uint16_t zllen：元素个数，占2个字节，元素个数无法超过2^16-1，只能通过遍历整个列表才能获取到个数</li><li>uint8_t zlend：列表的结尾元素，占1个字节，值为255（0xff）</li><li>entry：列表的元素，可以是字节数组或者整数<ul><li>prevlen：表示前一个元素的字节长度，1~5个字节表示，当前一个元素长度小于254字节，用1个字节表示，大于或等于254字节时，用5个字节表示，该情况下，第一个字节为<code>0xFE</code>，剩余4个字节表示真正长度</li><li>encoding：表示当前元素的编码，编码表示当前存储的是字节数组还是整数</li><li>entry：存储数据内容</li></ul></li></ul><p>encoding选项：<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIP_STR_06B (0 &lt;&lt; 6)</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIP_STR_14B (1 &lt;&lt; 6)</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIP_STR_32B (2 &lt;&lt; 6)</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIP_INT_16B (0xc0 | 0&lt;&lt;4)</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIP_INT_32B (0xc0 | 1&lt;&lt;4)</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIP_INT_64B (0xc0 | 2&lt;&lt;4)</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIP_INT_24B (0xc0 | 3&lt;&lt;4)</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIP_INT_8B 0xfe</span></code></pre></div></p><p>下面是一个包含两个元素的ziplist，存储的数据为字符串“2”和“5”。它由15个字节组成<br><div class="hljs"><pre><code class="hljs undefined">[<span class="hljs-number">0</span>f <span class="hljs-number">00</span> <span class="hljs-number">00</span> <span class="hljs-number">00</span>] [<span class="hljs-number">0</span>c <span class="hljs-number">00</span> <span class="hljs-number">00</span> <span class="hljs-number">00</span>] [<span class="hljs-number">02</span> <span class="hljs-number">00</span>] [<span class="hljs-number">00</span> f3] [<span class="hljs-number">02</span> f6] [ff]      |             |          |       |       |     |   zlbytes        zltail    entries   <span class="hljs-string">"2"</span>     <span class="hljs-string">"5"</span>   end</code></pre></div></p><p>Redis通过宏定义来对以上部分进行快速定位，zl为压缩列表首地址指针。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIPLIST_TAIL_OFFSET(zl) (*((uint32_t*)((zl)+sizeof(uint32_t))))</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIPLIST_LENGTH(zl)      (*((uint16_t*)((zl)+sizeof(uint32_t)*2)))</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIPLIST_HEADER_SIZE     (sizeof(uint32_t)*2+sizeof(uint16_t))</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIPLIST_END_SIZE        (sizeof(uint8_t))</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIPLIST_ENTRY_HEAD(zl)  ((zl)+ZIPLIST_HEADER_SIZE)</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIPLIST_ENTRY_TAIL(zl)  ((zl)+intrev32ifbe(ZIPLIST_TAIL_OFFSET(zl)))</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIPLIST_ENTRY_END(zl)   ((zl)+intrev32ifbe(ZIPLIST_BYTES(zl))-1)</span></code></pre></div></p><h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><p>压缩列表在获取元素长度、获取元素内容都需要经过解码运算，解码后的数据结果存储在zlentry结构中。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zlentry</span> &#123;</span>    <span class="hljs-comment">//上述的prevlen的长度</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> prevrawlensize;    <span class="hljs-comment">//上述的prevlen的内容</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> prevrawlen;    <span class="hljs-comment">//encoding字段的长度</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> lensize;    <span class="hljs-comment">//元素数据的长度</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> len;    <span class="hljs-comment">//当前元素的首部长度，prevrawlensize + lensize</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> headersize;    <span class="hljs-comment">//数据类型</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> encoding;    <span class="hljs-comment">//指向元素的首地址的指针</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *p;           &#125; zlentry;</code></pre></div></p><h1 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h1><h2 id="ziplistNew"><a href="#ziplistNew" class="headerlink" title="ziplistNew"></a>ziplistNew</h2><p>创建压缩列表，返回值为压缩列表的首地址。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIPLIST_HEADER_SIZE     (sizeof(uint32_t)*2+sizeof(uint16_t))</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZIPLIST_END_SIZE        (sizeof(uint8_t))</span><span class="hljs-function"><span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *<span class="hljs-title">ziplistNew</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span> </span>&#123;    <span class="hljs-comment">//计算分配的字节数</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> bytes = ZIPLIST_HEADER_SIZE+ZIPLIST_END_SIZE;    <span class="hljs-comment">//申请内存空间</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *zl = zmalloc(bytes);    <span class="hljs-comment">//初始化</span>    ZIPLIST_BYTES(zl) = intrev32ifbe(bytes);    ZIPLIST_TAIL_OFFSET(zl) = intrev32ifbe(ZIPLIST_HEADER_SIZE);    ZIPLIST_LENGTH(zl) = <span class="hljs-number">0</span>;    <span class="hljs-comment">//设置结尾标识符0xff</span>    zl[bytes<span class="hljs-number">-1</span>] = ZIP_END;    <span class="hljs-keyword">return</span> zl;&#125;</code></pre></div></p><h2 id="ziplistInsert"><a href="#ziplistInsert" class="headerlink" title="ziplistInsert"></a>ziplistInsert</h2><p>该接口在指定的位置p，插入的数据的地址指针为s，长度为slen。新插入的数据占据p原来的位置，p之后的数据项向后移动。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *<span class="hljs-title">ziplistInsert</span><span class="hljs-params">(<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *zl, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *p, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *s, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> slen)</span> </span>&#123;    <span class="hljs-keyword">return</span> __ziplistInsert(zl,p,s,slen);&#125;<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *__ziplistInsert(<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *zl, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *p, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *s, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> slen) &#123;    <span class="hljs-keyword">size_t</span> curlen = intrev32ifbe(ZIPLIST_BYTES(zl)), reqlen;    <span class="hljs-comment">//初始化变量</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> prevlensize, prevlen = <span class="hljs-number">0</span>;    <span class="hljs-keyword">size_t</span> offset;    <span class="hljs-keyword">int</span> nextdiff = <span class="hljs-number">0</span>;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> encoding = <span class="hljs-number">0</span>;    <span class="hljs-keyword">long</span> <span class="hljs-keyword">long</span> value = <span class="hljs-number">123456789</span>;    zlentry tail;    <span class="hljs-comment">//如果p不是尾元素</span>    <span class="hljs-keyword">if</span> (p[<span class="hljs-number">0</span>] != ZIP_END) &#123;        ZIP_DECODE_PREVLEN(p, prevlensize, prevlen);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *ptail = ZIPLIST_ENTRY_TAIL(zl);        <span class="hljs-keyword">if</span> (ptail[<span class="hljs-number">0</span>] != ZIP_END) &#123;            prevlen = zipRawEntryLength(ptail);        &#125;    &#125;    <span class="hljs-comment">//尝试将数据内容解析为整数</span>    <span class="hljs-keyword">if</span> (zipTryEncoding(s,slen,&amp;value,&amp;encoding)) &#123;        <span class="hljs-comment">//按整数类型编码进行存储</span>        reqlen = zipIntSize(encoding);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">//按字节数组类型编码进行存储</span>        reqlen = slen;    &#125;    <span class="hljs-comment">/* We need space for both the length of the previous entry and     * the length of the payload. */</span>    reqlen += zipStorePrevEntryLength(<span class="hljs-literal">NULL</span>,prevlen);    reqlen += zipStoreEntryEncoding(<span class="hljs-literal">NULL</span>,encoding,slen);    <span class="hljs-comment">//不是从尾部进行插入时，需要确保下一个entry可以存储当前entry的长度</span>    <span class="hljs-keyword">int</span> forcelarge = <span class="hljs-number">0</span>;    nextdiff = (p[<span class="hljs-number">0</span>] != ZIP_END) ? zipPrevLenByteDiff(p,reqlen) : <span class="hljs-number">0</span>;    <span class="hljs-keyword">if</span> (nextdiff == <span class="hljs-number">-4</span> &amp;&amp; reqlen &lt; <span class="hljs-number">4</span>) &#123;        nextdiff = <span class="hljs-number">0</span>;        forcelarge = <span class="hljs-number">1</span>;    &#125;    <span class="hljs-comment">//存储偏移量</span>    offset = p-zl;    <span class="hljs-comment">//调用realloc重新分配空间</span>    zl = ziplistResize(zl,curlen+reqlen+nextdiff);    <span class="hljs-comment">//重新偏移到插入位置p</span>    p = zl+offset;    <span class="hljs-comment">//数据复制</span>    <span class="hljs-keyword">if</span> (p[<span class="hljs-number">0</span>] != ZIP_END) &#123;        <span class="hljs-comment">//因为zlend恒为0xff，所以减一</span>        memmove(p+reqlen,p-nextdiff,curlen-offset<span class="hljs-number">-1</span>+nextdiff);        <span class="hljs-comment">//更新entryX + 1元素的prevlen数据</span>        <span class="hljs-keyword">if</span> (forcelarge)        <span class="hljs-comment">//该entry的prevlen仍然用5个字节存储时</span>            zipStorePrevEntryLengthLarge(p+reqlen,reqlen);        <span class="hljs-keyword">else</span>            zipStorePrevEntryLength(p+reqlen,reqlen);        <span class="hljs-comment">//更新zltail</span>        ZIPLIST_TAIL_OFFSET(zl) =            intrev32ifbe(intrev32ifbe(ZIPLIST_TAIL_OFFSET(zl))+reqlen);        <span class="hljs-comment">//</span>        zipEntry(p+reqlen, &amp;tail);        <span class="hljs-keyword">if</span> (p[reqlen+tail.headersize+tail.len] != ZIP_END) &#123;            ZIPLIST_TAIL_OFFSET(zl) =                intrev32ifbe(intrev32ifbe(ZIPLIST_TAIL_OFFSET(zl))+nextdiff);        &#125;    &#125; <span class="hljs-keyword">else</span> &#123;               ZIPLIST_TAIL_OFFSET(zl) = intrev32ifbe(p-zl);    &#125;        <span class="hljs-keyword">if</span> (nextdiff != <span class="hljs-number">0</span>) &#123;        offset = p-zl;        zl = __ziplistCascadeUpdate(zl,p+reqlen);        p = zl+offset;    &#125;    <span class="hljs-comment">//写entry</span>    p += zipStorePrevEntryLength(p,prevlen);    p += zipStoreEntryEncoding(p,encoding,slen);    <span class="hljs-keyword">if</span> (ZIP_IS_STR(encoding)) &#123;        <span class="hljs-built_in">memcpy</span>(p,s,slen);    &#125; <span class="hljs-keyword">else</span> &#123;        zipSaveInteger(p,value,encoding);    &#125;    <span class="hljs-comment">//更新zllen</span>    ZIPLIST_INCR_LENGTH(zl,<span class="hljs-number">1</span>);    <span class="hljs-keyword">return</span> zl;&#125;</code></pre></div></p><h2 id="ziplistDelete"><a href="#ziplistDelete" class="headerlink" title="ziplistDelete"></a>ziplistDelete</h2><p>zl指向压缩列表首地址，*p指向需要删除的entry的首地址，返回值为压缩列表首地址。<code>__ziplistDelete</code>可以同时删除多个连续元素，输入参数p指向的是首个待删除元素的地址，num表示待删除的元素个数。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *<span class="hljs-title">ziplistDelete</span><span class="hljs-params">(<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *zl, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> **p)</span> </span>&#123;    <span class="hljs-keyword">size_t</span> offset = *p-zl;    zl = __ziplistDelete(zl,*p,<span class="hljs-number">1</span>);        *p = zl+offset;    <span class="hljs-keyword">return</span> zl;&#125;<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *__ziplistDelete(<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *zl, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *p, <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> num) &#123;    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> i, totlen, deleted = <span class="hljs-number">0</span>;    <span class="hljs-keyword">size_t</span> offset;    <span class="hljs-keyword">int</span> nextdiff = <span class="hljs-number">0</span>;    zlentry first, tail;    <span class="hljs-comment">//解码第一个待删除的元素</span>    zipEntry(p, &amp;first);    <span class="hljs-comment">//遍历所有待删除的元素，同时指针p向后偏移</span>    <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; p[<span class="hljs-number">0</span>] != ZIP_END &amp;&amp; i &lt; num; i++) &#123;        p += zipRawEntryLength(p);        deleted++;    &#125;    <span class="hljs-comment">//该变量为待删除元素总长度</span>    totlen = p-first.p; <span class="hljs-comment">/* Bytes taken by the element(s) to delete. */</span>    <span class="hljs-keyword">if</span> (totlen &gt; <span class="hljs-number">0</span>) &#123;        <span class="hljs-keyword">if</span> (p[<span class="hljs-number">0</span>] != ZIP_END) &#123;            <span class="hljs-comment">//计算元素entryN长度的变化量</span>            nextdiff = zipPrevLenByteDiff(p,first.prevrawlen);            <span class="hljs-comment">//更新元素entryN的prevlen数据</span>            p -= nextdiff;            zipStorePrevEntryLength(p,first.prevrawlen);            <span class="hljs-comment">//更新zltail</span>            ZIPLIST_TAIL_OFFSET(zl) =                intrev32ifbe(intrev32ifbe(ZIPLIST_TAIL_OFFSET(zl))-totlen);            zipEntry(p, &amp;tail);            <span class="hljs-keyword">if</span> (p[tail.headersize+tail.len] != ZIP_END) &#123;                ZIPLIST_TAIL_OFFSET(zl) =                   intrev32ifbe(intrev32ifbe(ZIPLIST_TAIL_OFFSET(zl))+nextdiff);            &#125;            <span class="hljs-comment">//数据复制</span>            memmove(first.p,p,                intrev32ifbe(ZIPLIST_BYTES(zl))-(p-zl)<span class="hljs-number">-1</span>);        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">/* The entire tail was deleted. No need to move memory. */</span>            ZIPLIST_TAIL_OFFSET(zl) =                intrev32ifbe((first.p-zl)-first.prevrawlen);        &#125;        <span class="hljs-comment">//重新分配空间，与添加逻辑相似</span>        offset = first.p-zl;        zl = ziplistResize(zl, intrev32ifbe(ZIPLIST_BYTES(zl))-totlen+nextdiff);        ZIPLIST_INCR_LENGTH(zl,-deleted);        p = zl+offset;        <span class="hljs-keyword">if</span> (nextdiff != <span class="hljs-number">0</span>)            zl = __ziplistCascadeUpdate(zl,p);    &#125;    <span class="hljs-keyword">return</span> zl;&#125;</code></pre></div></p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文主要对压缩列表的基本概念与在Redis中的具体实现做了简要分析，同时对压缩列表在Redis中的添加删除接口做了简要的源代码分析。使用压缩列表能有效地节省内存，在Redis的Hash结构中，当field比较少时，采用压缩列表进行存储，当达到对应阈值，转为dict进行存储，与JDK1.8中的HashMap的策略有异曲同工之妙。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;p&gt;压缩列表（ziplist）的本质是一个字节数组，主要是Redis为了节省内存而设计的数据结构。在Redis的有序集合和散列表都使用了zipl
      
    
    </summary>
    
      <category term="Redis" scheme="https://l1n.wang/categories/Redis/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/categories/Redis/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/tags/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
      <category term="Redis" scheme="https://l1n.wang/tags/Redis/"/>
    
  </entry>
  
  <entry>
    <title>Redis底层数据结构-SkipList源码分析</title>
    <link href="https://l1n.wang/2020/05/redis-zskiplist/"/>
    <id>https://l1n.wang/2020/05/redis-zskiplist/</id>
    <published>2020-05-25T13:31:00.000Z</published>
    <updated>2020-05-29T10:31:44.939Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>跳表(SkipList)通过对有序链表添加多级索引，从而实现类似于二分查找效果的有序链表，它的插入/删除/搜索的时间复杂度为<code>O(log n)</code>，该数据结构可以用来代替平衡树以提高效率。其基本结构如 下图所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/redis/skiplist/skiplist.jpg" srcset="/img/loading.gif" alt="跳表基本结构"></p><p>如果此时查找51的节点，步骤基本如下：</p><ol><li>从第二层开始查找，1比51小，向后比较</li><li>21比51小，21后面为NULL，下降到第一层的21先后比较</li><li>第一层中21的next节点为41，41比51小，41的next节点61比51大，下降到第0层比较</li><li>41的next节点为51，查找完成。</li></ol><h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><p>zskiplistNode：</p><ul><li>ele：存储SDS类型的数据</li><li>score：排序用的分值</li><li>backward：后退指针，指向当前节点最底层的前驱节点，第一个指向NULL</li><li>level：数组，它的长度在生成时随机生成一个1 ~ 64的值，值越大出现概率越低<ul><li>forward：指向本层的下一个节点，尾节点指向NULL</li><li>span：指向的节点与本节点之间的元素个数</li></ul></li></ul><p>zskiplist：</p><ul><li>header：指向跳表的头节点</li><li>tail：指向跳表的尾节点</li><li>length：跳表的长度</li><li>level：跳表的高度</li></ul><div class="hljs"><pre><code class="hljs C"><span class="hljs-comment">/* ZSETs use a specialized version of Skiplists */</span><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplistNode</span> &#123;</span>    sds ele;    <span class="hljs-keyword">double</span> score;    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplistNode</span> *<span class="hljs-title">backward</span>;</span>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplistLevel</span> &#123;</span>        <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplistNode</span> *<span class="hljs-title">forward</span>;</span>        <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> span;    &#125; level[];&#125; zskiplistNode;<span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplist</span> &#123;</span>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zskiplistNode</span> *<span class="hljs-title">header</span>, *<span class="hljs-title">tail</span>;</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> length;    <span class="hljs-keyword">int</span> level;&#125; zskiplist;<span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">zset</span> &#123;</span>    dict *dict;    zskiplist *zsl;&#125; zset;</code></pre></div><h1 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h1><h2 id="zslCreate"><a href="#zslCreate" class="headerlink" title="zslCreate"></a>zslCreate</h2><h3 id="zslCreateNode"><a href="#zslCreateNode" class="headerlink" title="zslCreateNode"></a>zslCreateNode</h3><div class="hljs"><pre><code class="hljs C"><span class="hljs-comment">/* Create a skiplist node with the specified number of levels. * The SDS string 'ele' is referenced by the node after the call. */</span><span class="hljs-function">zskiplistNode *<span class="hljs-title">zslCreateNode</span><span class="hljs-params">(<span class="hljs-keyword">int</span> level, <span class="hljs-keyword">double</span> score, sds ele)</span> </span>&#123;    <span class="hljs-comment">//申请内存空间</span>    zskiplistNode *zn =        zmalloc(<span class="hljs-keyword">sizeof</span>(*zn)+level*<span class="hljs-keyword">sizeof</span>(struct zskiplistLevel));    <span class="hljs-comment">//初始化</span>    zn-&gt;score = score;    zn-&gt;ele = ele;    <span class="hljs-keyword">return</span> zn;&#125;</code></pre></div><h3 id="zslCreate-1"><a href="#zslCreate-1" class="headerlink" title="zslCreate"></a>zslCreate</h3><div class="hljs"><pre><code class="hljs C"><span class="hljs-comment">/* Create a new skiplist. */</span><span class="hljs-function">zskiplist *<span class="hljs-title">zslCreate</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span> </span>&#123;    <span class="hljs-keyword">int</span> j;    <span class="hljs-comment">//指向跳表的指针</span>    zskiplist *zsl;<span class="hljs-comment">//申请内存空间</span>    zsl = zmalloc(<span class="hljs-keyword">sizeof</span>(*zsl));    <span class="hljs-comment">//设置默认值</span>    zsl-&gt;level = <span class="hljs-number">1</span>;    zsl-&gt;length = <span class="hljs-number">0</span>;    <span class="hljs-comment">//创建头节点</span>    zsl-&gt;header = zslCreateNode(ZSKIPLIST_MAXLEVEL,<span class="hljs-number">0</span>,<span class="hljs-literal">NULL</span>);    <span class="hljs-comment">//将头节点的level数组的forward设置为NULL，span设置为0</span>    <span class="hljs-keyword">for</span> (j = <span class="hljs-number">0</span>; j &lt; ZSKIPLIST_MAXLEVEL; j++) &#123;        zsl-&gt;header-&gt;level[j].forward = <span class="hljs-literal">NULL</span>;        zsl-&gt;header-&gt;level[j].span = <span class="hljs-number">0</span>;    &#125;    <span class="hljs-comment">//设置头尾节点</span>    zsl-&gt;header-&gt;backward = <span class="hljs-literal">NULL</span>;    zsl-&gt;tail = <span class="hljs-literal">NULL</span>;    <span class="hljs-keyword">return</span> zsl;&#125;</code></pre></div><h2 id="zslRandomLevel"><a href="#zslRandomLevel" class="headerlink" title="zslRandomLevel"></a>zslRandomLevel</h2><p>level最小值为1，最大值为64，该方法随机生成1 ~ 64的值。</p><div class="hljs"><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZSKIPLIST_MAXLEVEL 64</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> ZSKIPLIST_P 0.25</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">zslRandomLevel</span><span class="hljs-params">(<span class="hljs-keyword">void</span>)</span> </span>&#123;    <span class="hljs-keyword">int</span> level = <span class="hljs-number">1</span>;    <span class="hljs-comment">//生成随机值，取低16位为x，当x &lt; 0.25 * 0xFFFF时，level自增1</span>    <span class="hljs-keyword">while</span> ((<span class="hljs-built_in">random</span>()&amp;<span class="hljs-number">0xFFFF</span>) &lt; (ZSKIPLIST_P * <span class="hljs-number">0xFFFF</span>))        level += <span class="hljs-number">1</span>;    <span class="hljs-keyword">return</span> (level&lt;ZSKIPLIST_MAXLEVEL) ? level : ZSKIPLIST_MAXLEVEL;&#125;</code></pre></div><h2 id="zslInsert"><a href="#zslInsert" class="headerlink" title="zslInsert"></a>zslInsert</h2><p>插入逻辑主要如下：</p><ul><li>查找要插入的位置</li><li>调整跳表高度</li><li>插入节点</li><li>调整backward</li></ul><div class="hljs"><pre><code class="hljs C"><span class="hljs-function">zskiplistNode *<span class="hljs-title">zslInsert</span><span class="hljs-params">(zskiplist *zsl, <span class="hljs-keyword">double</span> score, sds ele)</span> </span>&#123;    <span class="hljs-comment">//保存每一层需要更新的节点</span>    zskiplistNode *update[ZSKIPLIST_MAXLEVEL], *x;    <span class="hljs-comment">//保存从header到update[i]节点的步长</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">int</span> rank[ZSKIPLIST_MAXLEVEL];    <span class="hljs-keyword">int</span> i, level;    serverAssert(!isnan(score));    <span class="hljs-comment">//查找要插入的位置</span>    x = zsl-&gt;header;    <span class="hljs-keyword">for</span> (i = zsl-&gt;level<span class="hljs-number">-1</span>; i &gt;= <span class="hljs-number">0</span>; i--) &#123;        <span class="hljs-comment">/* store rank that is crossed to reach the insert position */</span>        rank[i] = i == (zsl-&gt;level<span class="hljs-number">-1</span>) ? <span class="hljs-number">0</span> : rank[i+<span class="hljs-number">1</span>];        <span class="hljs-keyword">while</span> (x-&gt;level[i].forward &amp;&amp;                (x-&gt;level[i].forward-&gt;score &lt; score ||                    (x-&gt;level[i].forward-&gt;score == score &amp;&amp;                    sdscmp(x-&gt;level[i].forward-&gt;ele,ele) &lt; <span class="hljs-number">0</span>)))        &#123;            rank[i] += x-&gt;level[i].span;            x = x-&gt;level[i].forward;        &#125;        update[i] = x;    &#125;    <span class="hljs-comment">//获取随机层数</span>    level = zslRandomLevel();    <span class="hljs-comment">//如果插入的层数大于最高层，设置rank和update数组</span>    <span class="hljs-keyword">if</span> (level &gt; zsl-&gt;level) &#123;        <span class="hljs-keyword">for</span> (i = zsl-&gt;level; i &lt; level; i++) &#123;            rank[i] = <span class="hljs-number">0</span>;            update[i] = zsl-&gt;header;            update[i]-&gt;level[i].span = zsl-&gt;length;        &#125;        zsl-&gt;level = level;    &#125;    <span class="hljs-comment">//创建节点</span>    x = zslCreateNode(level,score,ele);    <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; level; i++) &#123;        <span class="hljs-comment">//更新指向</span>        x-&gt;level[i].forward = update[i]-&gt;level[i].forward;        update[i]-&gt;level[i].forward = x;        <span class="hljs-comment">//更新span</span>        x-&gt;level[i].span = update[i]-&gt;level[i].span - (rank[<span class="hljs-number">0</span>] - rank[i]);        update[i]-&gt;level[i].span = (rank[<span class="hljs-number">0</span>] - rank[i]) + <span class="hljs-number">1</span>;    &#125;    <span class="hljs-comment">/* increment span for untouched levels */</span>    <span class="hljs-keyword">for</span> (i = level; i &lt; zsl-&gt;level; i++) &#123;        update[i]-&gt;level[i].span++;    &#125;<span class="hljs-comment">//调整backward指针</span>    x-&gt;backward = (update[<span class="hljs-number">0</span>] == zsl-&gt;header) ? <span class="hljs-literal">NULL</span> : update[<span class="hljs-number">0</span>];    <span class="hljs-keyword">if</span> (x-&gt;level[<span class="hljs-number">0</span>].forward)        x-&gt;level[<span class="hljs-number">0</span>].forward-&gt;backward = x;    <span class="hljs-keyword">else</span>        zsl-&gt;tail = x;    zsl-&gt;length++;    <span class="hljs-keyword">return</span> x;&#125;</code></pre></div><h2 id="zslDelete"><a href="#zslDelete" class="headerlink" title="zslDelete"></a>zslDelete</h2><p>删除逻辑主要如下：</p><ul><li>查找需要删除的节点</li><li>设置span和forward</li></ul><div class="hljs"><pre><code class="hljs C"><span class="hljs-comment">// 删除指定的节点，节点是否删除取决于 node是否为 NULL，如果为 NULL的话就删除，否则将值存到 *node中</span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">zslDelete</span><span class="hljs-params">(zskiplist *zsl, <span class="hljs-keyword">double</span> score, sds ele, zskiplistNode **node)</span> </span>&#123;    zskiplistNode *update[ZSKIPLIST_MAXLEVEL], *x;    <span class="hljs-keyword">int</span> i;    x = zsl-&gt;header;    <span class="hljs-comment">// 定位到每一层需要删除的位置</span>    <span class="hljs-keyword">for</span> (i = zsl-&gt;level<span class="hljs-number">-1</span>; i &gt;= <span class="hljs-number">0</span>; i--) &#123;        <span class="hljs-keyword">while</span> (x-&gt;level[i].forward &amp;&amp;                (x-&gt;level[i].forward-&gt;score &lt; score ||                    (x-&gt;level[i].forward-&gt;score == score &amp;&amp;                     sdscmp(x-&gt;level[i].forward-&gt;ele,ele) &lt; <span class="hljs-number">0</span>)))        &#123;            x = x-&gt;level[i].forward;        &#125;        update[i] = x;    &#125;       <span class="hljs-comment">// 在第一层(也就是完整的链表)中找到要删除的节点位置</span>    x = x-&gt;level[<span class="hljs-number">0</span>].forward;    <span class="hljs-comment">// 完全相同才进行删除</span>    <span class="hljs-keyword">if</span> (x &amp;&amp; score == x-&gt;score &amp;&amp; sdscmp(x-&gt;ele,ele) == <span class="hljs-number">0</span>) &#123;        zslDeleteNode(zsl, x, update);        <span class="hljs-keyword">if</span> (!node)            zslFreeNode(x);        <span class="hljs-keyword">else</span>            *node = x;        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;    &#125;    <span class="hljs-comment">// 遍历结束也没找到</span>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;<span class="hljs-comment">// 删除节点</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">zslDeleteNode</span><span class="hljs-params">(zskiplist *zsl, zskiplistNode *x, zskiplistNode **update)</span> </span>&#123;    <span class="hljs-keyword">int</span> i;    <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; zsl-&gt;level; i++) &#123;        <span class="hljs-comment">// 更新 span和前驱指针</span>        <span class="hljs-keyword">if</span> (update[i]-&gt;level[i].forward == x) &#123;            update[i]-&gt;level[i].span += x-&gt;level[i].span - <span class="hljs-number">1</span>;            update[i]-&gt;level[i].forward = x-&gt;level[i].forward;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-comment">// 不相等说明这些节点都在要删除的节点之前，跨度 span应该减 1</span>            update[i]-&gt;level[i].span -= <span class="hljs-number">1</span>;        &#125;    &#125;    <span class="hljs-keyword">if</span> (x-&gt;level[<span class="hljs-number">0</span>].forward) &#123;        <span class="hljs-comment">// 更新前驱指针，相当于更新双向链表的 prev指向</span>        x-&gt;level[<span class="hljs-number">0</span>].forward-&gt;backward = x-&gt;backward;    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">// 说明要删除的节点是最后一个节点，更新尾节点</span>        zsl-&gt;tail = x-&gt;backward;    &#125;    <span class="hljs-comment">// 对于最上面都是空的层，应该排除，从第一个有数据的层开始算作有效</span>    <span class="hljs-keyword">while</span>(zsl-&gt;level &gt; <span class="hljs-number">1</span> &amp;&amp; zsl-&gt;header-&gt;level[zsl-&gt;level<span class="hljs-number">-1</span>].forward == <span class="hljs-literal">NULL</span>)        zsl-&gt;level--;    zsl-&gt;length--;&#125;</code></pre></div><h2 id="zslFree"><a href="#zslFree" class="headerlink" title="zslFree"></a>zslFree</h2><p>删除跳表逻辑：</p><ul><li>释放头节点</li><li>从头节点的第0层开始，通过forward向后遍历，逐个释放节点的内存空间</li><li>最后释放跳表指针空间</li></ul><div class="hljs"><pre><code class="hljs C"><span class="hljs-comment">/* Free a whole skiplist. */</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">zslFree</span><span class="hljs-params">(zskiplist *zsl)</span> </span>&#123;    zskiplistNode *node = zsl-&gt;header-&gt;level[<span class="hljs-number">0</span>].forward, *next;    zfree(zsl-&gt;header);    <span class="hljs-keyword">while</span>(node) &#123;        next = node-&gt;level[<span class="hljs-number">0</span>].forward;        zslFreeNode(node);        node = next;    &#125;    zfree(zsl);&#125;<span class="hljs-comment">//释放节点，首先释放SDS的空间，再释放节点空间</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">zslFreeNode</span><span class="hljs-params">(zskiplistNode *node)</span> </span>&#123;    sdsfree(node-&gt;ele);    zfree(node);&#125;</code></pre></div><h2 id="zslUpdateScore"><a href="#zslUpdateScore" class="headerlink" title="zslUpdateScore"></a>zslUpdateScore</h2><p>更新节点的排序分数基本逻辑如下：</p><ul><li>找到对应需要更新的节点</li></ul><div class="hljs"><pre><code class="hljs C"><span class="hljs-function">zskiplistNode *<span class="hljs-title">zslUpdateScore</span><span class="hljs-params">(zskiplist *zsl, <span class="hljs-keyword">double</span> curscore, sds ele, <span class="hljs-keyword">double</span> newscore)</span> </span>&#123;    zskiplistNode *update[ZSKIPLIST_MAXLEVEL], *x;    <span class="hljs-keyword">int</span> i;    <span class="hljs-comment">//找到对应需要更新的节点</span>    x = zsl-&gt;header;    <span class="hljs-keyword">for</span> (i = zsl-&gt;level<span class="hljs-number">-1</span>; i &gt;= <span class="hljs-number">0</span>; i--) &#123;        <span class="hljs-keyword">while</span> (x-&gt;level[i].forward &amp;&amp;                (x-&gt;level[i].forward-&gt;score &lt; curscore ||                    (x-&gt;level[i].forward-&gt;score == curscore &amp;&amp;                     sdscmp(x-&gt;level[i].forward-&gt;ele,ele) &lt; <span class="hljs-number">0</span>)))        &#123;            x = x-&gt;level[i].forward;        &#125;        update[i] = x;    &#125;    <span class="hljs-comment">/* Jump to our element: note that this function assumes that the     * element with the matching score exists. */</span>    x = x-&gt;level[<span class="hljs-number">0</span>].forward;    serverAssert(x &amp;&amp; curscore == x-&gt;score &amp;&amp; sdscmp(x-&gt;ele,ele) == <span class="hljs-number">0</span>);<span class="hljs-comment">//直接更新    </span>    <span class="hljs-keyword">if</span> ((x-&gt;backward == <span class="hljs-literal">NULL</span> || x-&gt;backward-&gt;score &lt; newscore) &amp;&amp;        (x-&gt;level[<span class="hljs-number">0</span>].forward == <span class="hljs-literal">NULL</span> || x-&gt;level[<span class="hljs-number">0</span>].forward-&gt;score &gt; newscore))    &#123;        x-&gt;score = newscore;        <span class="hljs-keyword">return</span> x;    &#125;    <span class="hljs-comment">//无法重用旧节点时，删除后重新添加</span>    zslDeleteNode(zsl, x, update);    zskiplistNode *newnode = zslInsert(zsl,newscore,x-&gt;ele);    <span class="hljs-comment">/* We reused the old node x-&gt;ele SDS string, free the node now     * since zslInsert created a new one. */</span>    x-&gt;ele = <span class="hljs-literal">NULL</span>;    zslFreeNode(x);    <span class="hljs-keyword">return</span> newnode;&#125;</code></pre></div><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文主要对跳表的概念和基本查询思路做了简要分析，对Redis中的跳表的数据结构和基本增删改查接口做了简要的源代码分析。在Redis中的有序集合<code>zset</code>就是通过<code>skiplist</code>和<code>dict</code>组合实现的。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;p&gt;跳表(SkipList)通过对有序链表添加多级索引，从而实现类似于二分查找效果的有序链表，它的插入/删除/搜索的时间复杂度为&lt;code&gt;O(
      
    
    </summary>
    
      <category term="Redis" scheme="https://l1n.wang/categories/Redis/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/categories/Redis/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/tags/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
      <category term="Redis" scheme="https://l1n.wang/tags/Redis/"/>
    
  </entry>
  
  <entry>
    <title>Redis底层数据结构-SDS源码分析</title>
    <link href="https://l1n.wang/2020/05/redis-sds/"/>
    <id>https://l1n.wang/2020/05/redis-sds/</id>
    <published>2020-05-24T10:59:27.000Z</published>
    <updated>2020-05-29T10:31:34.940Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>简单动态字符串(Simple Dynamic Strings)是Redis的基本数据结构之一，主要用于存储字符串和整型数据。SDS兼容C语言标准字符串处理函数，同时保证了二进制安全。</p><h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><h2 id="原始版本"><a href="#原始版本" class="headerlink" title="原始版本"></a>原始版本</h2><p>在Redis 3.2之前，SDS基本结构如下：<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-class"><span class="hljs-keyword">struct</span> &#123;</span>    <span class="hljs-comment">//buf中已使用字节数</span>    <span class="hljs-keyword">int</span> len;    <span class="hljs-comment">//buf中剩余字节数</span>    <span class="hljs-keyword">int</span> <span class="hljs-built_in">free</span>;    <span class="hljs-comment">//数据</span>    <span class="hljs-keyword">char</span> buf[];&#125;</code></pre></div></p><p>该结构有如下几个优点：</p><ul><li>有单独的变量存储字符串长度，由于有长度，不会依赖于<code>\0</code>终止符，保证二进制安全。</li><li>字符串存储在buf数组中，兼容C处理字符串的函数。</li></ul><h2 id="改进"><a href="#改进" class="headerlink" title="改进"></a>改进</h2><p>Redis 3.2之后，采用如下结构进行存储。</p><div class="hljs"><pre><code class="hljs C"><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr5</span> &#123;</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> flags; <span class="hljs-comment">/* 3 lsb of type, and 5 msb of string length */</span>    <span class="hljs-keyword">char</span> buf[];&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr8</span> &#123;</span>    <span class="hljs-keyword">uint8_t</span> len; <span class="hljs-comment">/* used */</span>    <span class="hljs-keyword">uint8_t</span> alloc; <span class="hljs-comment">/* excluding the header and null terminator */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> flags; <span class="hljs-comment">/* 3 lsb of type, 5 unused bits */</span>    <span class="hljs-keyword">char</span> buf[];&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr16</span> &#123;</span>    <span class="hljs-keyword">uint16_t</span> len; <span class="hljs-comment">/* used */</span>    <span class="hljs-keyword">uint16_t</span> alloc; <span class="hljs-comment">/* excluding the header and null terminator */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> flags; <span class="hljs-comment">/* 3 lsb of type, 5 unused bits */</span>    <span class="hljs-keyword">char</span> buf[];&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr32</span> &#123;</span>    <span class="hljs-keyword">uint32_t</span> len; <span class="hljs-comment">/* used */</span>    <span class="hljs-keyword">uint32_t</span> alloc; <span class="hljs-comment">/* excluding the header and null terminator */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> flags; <span class="hljs-comment">/* 3 lsb of type, 5 unused bits */</span>    <span class="hljs-keyword">char</span> buf[];&#125;;<span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr64</span> &#123;</span>    <span class="hljs-keyword">uint64_t</span> len; <span class="hljs-comment">/* used */</span>    <span class="hljs-keyword">uint64_t</span> alloc; <span class="hljs-comment">/* excluding the header and null terminator */</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> flags; <span class="hljs-comment">/* 3 lsb of type, 5 unused bits */</span>    <span class="hljs-keyword">char</span> buf[];&#125;;</code></pre></div><p>sdshdr5中采用位来存储相关信息，其中flags占1字节，其中低3位用来表示type，高5位表示长度，所以长度区间为（0 ~ 31），所以长度大于31的字符串需要采用sdshdr8及以上存储。sdshdr8中flags低3位存储类型，剩余5位闲置。以下是字符串类型的宏定义：<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SDS_TYPE_5  0</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SDS_TYPE_8  1</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SDS_TYPE_16 2</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SDS_TYPE_32 3</span><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SDS_TYPE_64 4</span></code></pre></div></p><h3 id="对齐"><a href="#对齐" class="headerlink" title="对齐"></a>对齐</h3><p>GCC支持用<code>__attribute__</code>为变量、类型、函数、标签指定特殊属性。这些不是编程语言标准里的内容，而属于编译器对语言的扩展。在声明SDS结构时，采用了<code>__attribute__ ((__packed__))</code>，它告诉编译器结构体使用1字节对齐。使用<code>packed</code>属性可以节省内存，同时统一多个结构的指针访问。</p><h1 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h1><h2 id="sdsnewlen"><a href="#sdsnewlen" class="headerlink" title="sdsnewlen"></a>sdsnewlen</h2><div class="hljs"><pre><code class="hljs C"><span class="hljs-function">sds <span class="hljs-title">sdsnewlen</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *init, <span class="hljs-keyword">size_t</span> initlen)</span> </span>&#123;    <span class="hljs-keyword">void</span> *sh;    sds s;    <span class="hljs-comment">//根据字符串长度获取对应的类型</span>    <span class="hljs-keyword">char</span> type = sdsReqType(initlen);    <span class="hljs-comment">/* Empty strings are usually created in order to append. Use type 8     * since type 5 is not good at this. */</span>     <span class="hljs-comment">//将SDS_TYPE_5转成SDS_TYPE_5</span>    <span class="hljs-keyword">if</span> (type == SDS_TYPE_5 &amp;&amp; initlen == <span class="hljs-number">0</span>) type = SDS_TYPE_8;    <span class="hljs-comment">//计算对应结构体头部所需字节数</span>    <span class="hljs-keyword">int</span> hdrlen = sdsHdrSize(type);    <span class="hljs-comment">//指向flags的指针</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span> *fp; <span class="hljs-comment">/* flags pointer. */</span>    <span class="hljs-comment">//分配内存空间</span>    sh = s_malloc(hdrlen+initlen+<span class="hljs-number">1</span>);    <span class="hljs-comment">//判断是否为"SDS_NOINIT"</span>    <span class="hljs-keyword">if</span> (init==SDS_NOINIT)        init = <span class="hljs-literal">NULL</span>;    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!init)        <span class="hljs-comment">//初始化结构体空间数据</span>        <span class="hljs-built_in">memset</span>(sh, <span class="hljs-number">0</span>, hdrlen+initlen+<span class="hljs-number">1</span>);    <span class="hljs-keyword">if</span> (sh == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;    <span class="hljs-comment">//s是指向buf的指针</span>    s = (<span class="hljs-keyword">char</span>*)sh+hdrlen;    <span class="hljs-comment">//指向flags</span>    fp = ((<span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">char</span>*)s)<span class="hljs-number">-1</span>;    <span class="hljs-comment">//针对不同结构体类型进行初始化操作</span>    <span class="hljs-keyword">switch</span>(type) &#123;        <span class="hljs-keyword">case</span> SDS_TYPE_5: &#123;            *fp = type | (initlen &lt;&lt; SDS_TYPE_BITS);            <span class="hljs-keyword">break</span>;        &#125;        <span class="hljs-keyword">case</span> SDS_TYPE_8: &#123;            SDS_HDR_VAR(<span class="hljs-number">8</span>,s);            sh-&gt;len = initlen;            sh-&gt;alloc = initlen;            *fp = type;            <span class="hljs-keyword">break</span>;        &#125;        <span class="hljs-keyword">case</span> SDS_TYPE_16: &#123;            SDS_HDR_VAR(<span class="hljs-number">16</span>,s);            sh-&gt;len = initlen;            sh-&gt;alloc = initlen;            *fp = type;            <span class="hljs-keyword">break</span>;        &#125;        <span class="hljs-keyword">case</span> SDS_TYPE_32: &#123;            SDS_HDR_VAR(<span class="hljs-number">32</span>,s);            sh-&gt;len = initlen;            sh-&gt;alloc = initlen;            *fp = type;            <span class="hljs-keyword">break</span>;        &#125;        <span class="hljs-keyword">case</span> SDS_TYPE_64: &#123;            SDS_HDR_VAR(<span class="hljs-number">64</span>,s);            sh-&gt;len = initlen;            sh-&gt;alloc = initlen;            *fp = type;            <span class="hljs-keyword">break</span>;        &#125;    &#125;    <span class="hljs-comment">//如果两个参数不为0，将字符串数据复制到buf数组</span>    <span class="hljs-keyword">if</span> (initlen &amp;&amp; init)        <span class="hljs-built_in">memcpy</span>(s, init, initlen);    s[initlen] = <span class="hljs-string">'\0'</span>;    <span class="hljs-keyword">return</span> s;&#125;</code></pre></div><h2 id="sdsfree"><a href="#sdsfree" class="headerlink" title="sdsfree"></a>sdsfree</h2><p>通过定位SDS的头部，调用<code>s_free</code>释放内存。为了减少申请内存的开销，SDS可以提供<code>sdsclear</code>进行重置达到清空的目的。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-comment">/* Free an sds string. No operation is performed if 's' is NULL. */</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">sdsfree</span><span class="hljs-params">(sds s)</span> </span>&#123;    <span class="hljs-keyword">if</span> (s == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span>;    s_free((<span class="hljs-keyword">char</span>*)s-sdsHdrSize(s[<span class="hljs-number">-1</span>]));</code></pre></div></p><h2 id="sdsclear"><a href="#sdsclear" class="headerlink" title="sdsclear"></a>sdsclear</h2><p>该方法将字符串长度设置为0，同时将数组第一个元素置为终止符。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">sdsclear</span><span class="hljs-params">(sds s)</span> </span>&#123;    sdssetlen(s, <span class="hljs-number">0</span>);    s[<span class="hljs-number">0</span>] = <span class="hljs-string">'\0'</span>;&#125;</code></pre></div></p><h2 id="sdscatsds"><a href="#sdscatsds" class="headerlink" title="sdscatsds"></a>sdscatsds</h2><p>通过调用<code>sdscatlen</code>实现拼接逻辑。<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-function">sds <span class="hljs-title">sdscatsds</span><span class="hljs-params">(sds s, <span class="hljs-keyword">const</span> sds t)</span> </span>&#123;    <span class="hljs-keyword">return</span> sdscatlen(s, t, sdslen(t));&#125;<span class="hljs-function">sds <span class="hljs-title">sdscatlen</span><span class="hljs-params">(sds s, <span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> *t, <span class="hljs-keyword">size_t</span> len)</span> </span>&#123;    <span class="hljs-keyword">size_t</span> curlen = sdslen(s);    <span class="hljs-comment">//对buf进行扩容，会进行扩容检测</span>    s = sdsMakeRoomFor(s,len);    <span class="hljs-keyword">if</span> (s == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;    <span class="hljs-comment">//调用memcpy实现拼接</span>    <span class="hljs-built_in">memcpy</span>(s+curlen, t, len);    <span class="hljs-comment">//更新长度</span>    sdssetlen(s, curlen+len);    <span class="hljs-comment">//设置终止符</span>    s[curlen+len] = <span class="hljs-string">'\0'</span>;    <span class="hljs-keyword">return</span> s;&#125;</code></pre></div></p><h2 id="sdsMakeRoomFor"><a href="#sdsMakeRoomFor" class="headerlink" title="sdsMakeRoomFor"></a>sdsMakeRoomFor</h2><p>SDS扩容操作：<br><div class="hljs"><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> SDS_MAX_PREALLOC (1024*1024)</span><span class="hljs-function">sds <span class="hljs-title">sdsMakeRoomFor</span><span class="hljs-params">(sds s, <span class="hljs-keyword">size_t</span> addlen)</span> </span>&#123;    <span class="hljs-keyword">void</span> *sh, *newsh;    <span class="hljs-comment">//剩余有效空间</span>    <span class="hljs-keyword">size_t</span> avail = sdsavail(s);    <span class="hljs-keyword">size_t</span> len, newlen;    <span class="hljs-keyword">char</span> type, oldtype = s[<span class="hljs-number">-1</span>] &amp; SDS_TYPE_MASK;    <span class="hljs-keyword">int</span> hdrlen;    <span class="hljs-comment">//判断有效空间是否大于需要增加的空间大小</span>    <span class="hljs-keyword">if</span> (avail &gt;= addlen) <span class="hljs-keyword">return</span> s;    <span class="hljs-comment">//sds已使用长度</span>    len = sdslen(s);    sh = (<span class="hljs-keyword">char</span>*)s-sdsHdrSize(oldtype);    <span class="hljs-comment">//新长度</span>    newlen = (len+addlen);    <span class="hljs-comment">//define SDS_MAX_PREALLOC (1024*1024)</span>    <span class="hljs-comment">//如果新长度 &lt; 1MB，扩大两倍</span>    <span class="hljs-keyword">if</span> (newlen &lt; SDS_MAX_PREALLOC)        newlen *= <span class="hljs-number">2</span>;    <span class="hljs-keyword">else</span>        <span class="hljs-comment">//否则扩大1MB</span>        newlen += SDS_MAX_PREALLOC;    <span class="hljs-comment">//根据新长度获取对应type</span>    type = sdsReqType(newlen);    <span class="hljs-comment">/* Don't use type 5: the user is appending to the string and type 5 is     * not able to remember empty space, so sdsMakeRoomFor() must be called     * at every appending operation. */</span>    <span class="hljs-keyword">if</span> (type == SDS_TYPE_5) type = SDS_TYPE_8;    <span class="hljs-comment">//</span>    hdrlen = sdsHdrSize(type);    <span class="hljs-keyword">if</span> (oldtype==type) &#123;        <span class="hljs-comment">//无需更改type，通过s_realloc扩大buf数组即可</span>        newsh = s_realloc(sh, hdrlen+newlen+<span class="hljs-number">1</span>);        <span class="hljs-keyword">if</span> (newsh == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;        s = (<span class="hljs-keyword">char</span>*)newsh+hdrlen;    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-comment">/* Since the header size changes, need to move the string forward,         * and can't use realloc */</span>         <span class="hljs-comment">//按新的数组长度申请内存</span>        newsh = s_malloc(hdrlen+newlen+<span class="hljs-number">1</span>);        <span class="hljs-keyword">if</span> (newsh == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;        <span class="hljs-comment">//将buf移动到新位置</span>        <span class="hljs-built_in">memcpy</span>((<span class="hljs-keyword">char</span>*)newsh+hdrlen, s, len+<span class="hljs-number">1</span>);        <span class="hljs-comment">//释放原来的指针</span>        s_free(sh);        <span class="hljs-comment">//s是指向buf起始位置的指针</span>        s = (<span class="hljs-keyword">char</span>*)newsh+hdrlen;        <span class="hljs-comment">//赋值flags</span>        s[<span class="hljs-number">-1</span>] = type;        <span class="hljs-comment">//设置新的字符串长度</span>        sdssetlen(s, len);    &#125;    <span class="hljs-comment">//设置新的数组长度</span>    sdssetalloc(s, newlen);    <span class="hljs-keyword">return</span> s;&#125;</code></pre></div></p><h2 id="sdsRemoveFreeSpace"><a href="#sdsRemoveFreeSpace" class="headerlink" title="sdsRemoveFreeSpace"></a>sdsRemoveFreeSpace</h2><p>缩容操作：</p><div class="hljs"><pre><code class="hljs C"><span class="hljs-function">sds <span class="hljs-title">sdsRemoveFreeSpace</span><span class="hljs-params">(sds s)</span> </span>&#123;    <span class="hljs-keyword">void</span> *sh, *newsh;    <span class="hljs-keyword">char</span> type, oldtype = s[<span class="hljs-number">-1</span>] &amp; SDS_TYPE_MASK;    <span class="hljs-keyword">int</span> hdrlen, oldhdrlen = sdsHdrSize(oldtype);    <span class="hljs-comment">//字符串长度</span>    <span class="hljs-keyword">size_t</span> len = sdslen(s);    <span class="hljs-comment">//剩余有效长度</span>    <span class="hljs-keyword">size_t</span> avail = sdsavail(s);    sh = (<span class="hljs-keyword">char</span>*)s-oldhdrlen;    如果不需要缩容直接返回    <span class="hljs-keyword">if</span> (avail == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> s;    <span class="hljs-comment">//根据长度获取类型</span>    type = sdsReqType(len);    <span class="hljs-comment">//获取头部长度</span>    hdrlen = sdsHdrSize(type);    <span class="hljs-comment">/* If the type is the same, or at least a large enough type is still     * required, we just realloc(), letting the allocator to do the copy     * only if really needed. Otherwise if the change is huge, we manually     * reallocate the string to use the different header type. */</span>     <span class="hljs-comment">//如果原始类型和更新类型相似，或者type &gt; SDS_TYPE_8</span>    <span class="hljs-keyword">if</span> (oldtype==type || type &gt; SDS_TYPE_8) &#123;    <span class="hljs-comment">//重分配</span>        newsh = s_realloc(sh, oldhdrlen+len+<span class="hljs-number">1</span>);        <span class="hljs-keyword">if</span> (newsh == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;        s = (<span class="hljs-keyword">char</span>*)newsh+oldhdrlen;    &#125; <span class="hljs-keyword">else</span> &#123;    <span class="hljs-comment">//申请内存空间</span>        newsh = s_malloc(hdrlen+len+<span class="hljs-number">1</span>);        <span class="hljs-keyword">if</span> (newsh == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;        <span class="hljs-comment">//复制</span>        <span class="hljs-built_in">memcpy</span>((<span class="hljs-keyword">char</span>*)newsh+hdrlen, s, len+<span class="hljs-number">1</span>);        <span class="hljs-comment">//释放指针</span>        s_free(sh);        s = (<span class="hljs-keyword">char</span>*)newsh+hdrlen;        <span class="hljs-comment">//设置类型</span>        s[<span class="hljs-number">-1</span>] = type;        sdssetlen(s, len);    &#125;    sdssetalloc(s, len);    <span class="hljs-keyword">return</span> s;&#125;</code></pre></div><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文主要对Redis中的简单动态字符串的数据结构与基本API实现做了简要分析，基本了解了SDS如何保证二进制安全与SDS的缩容扩容策略的实现。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;p&gt;简单动态字符串(Simple Dynamic Strings)是Redis的基本数据结构之一，主要用于存储字符串和整型数据。SDS兼容C语言
      
    
    </summary>
    
      <category term="Redis" scheme="https://l1n.wang/categories/Redis/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/categories/Redis/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
    
      <category term="数据结构" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/tags/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
      <category term="Redis" scheme="https://l1n.wang/tags/Redis/"/>
    
  </entry>
  
  <entry>
    <title>设计模式之行为型模式（四）</title>
    <link href="https://l1n.wang/2020/05/design-pattern-behavior/"/>
    <id>https://l1n.wang/2020/05/design-pattern-behavior/</id>
    <published>2020-05-23T11:12:51.000Z</published>
    <updated>2020-05-23T11:13:58.993Z</updated>
    
    <content type="html"><![CDATA[<h1 id="模板方法模式"><a href="#模板方法模式" class="headerlink" title="模板方法模式"></a>模板方法模式</h1><blockquote><p>定义一个操作中的算法的框架，而将一些步骤延迟到子类中。使得子类可以不改变一个算法的结构即可重定义该算法的某些特定步骤。</p></blockquote><p>模板方法模式涉及两个角色：</p><ul><li>抽象模板角色（Abstract Template）：该角色定义多个抽象操作，以便让子类实现。</li><li>具体模板角色（Concrete Template）：该角色实现抽象模板中的抽象方法</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AbstractClass</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">operation</span><span class="hljs-params">()</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">templateMethod</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">this</span>.operation();    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteClass</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractClass</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title">operation</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">//业务</span>    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Client</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        AbstractClass ac = <span class="hljs-keyword">new</span> ConcreteClass();        ac.templateMethod();    &#125;&#125;</code></pre></div><p>优点：</p><ul><li>封装不变的部分，扩展可变部分。不变的部分封装到父类去实现，可变的通过继承进行扩展。</li><li>提取公共代码，便于维护，将公共部分的代码抽取出来放在父类中，维护时只需要修改父类中的代码。</li><li>行为由父类控制，子类实现。</li></ul><h1 id="命令模式"><a href="#命令模式" class="headerlink" title="命令模式"></a>命令模式</h1><blockquote><p>将一个请求封装成一个对象，从而让你使用不同的请求把客户端参数化，对请求排队或者记录请求日志，可以提供命令的撤销和恢复功能。</p></blockquote><p>该模式有四个角色：</p><ul><li>命令角色：该角色声明抽象接口</li><li>具体命令角色：定义一个接收者和行为之间的弱耦合，实现命令方法，并调用接收者的相应操作。</li><li>调用者：负责调用命令对象执行请求。</li><li>接收者：该角色负责具体实施和执行一个请求。</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Command</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">execute</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteCommand</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Command</span> </span>&#123;    <span class="hljs-keyword">private</span> Receiver receiver;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ConcreteCommand</span><span class="hljs-params">(Receiver receiver)</span> </span>&#123;        <span class="hljs-keyword">this</span>.receiver = receiver;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">execute</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">this</span>.receiver.action();    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Receiver</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">action</span><span class="hljs-params">()</span></span>&#123;        System.out.println(<span class="hljs-string">"执行动作"</span>);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Invoker</span> </span>&#123;    <span class="hljs-keyword">private</span> Command command;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setCommand</span><span class="hljs-params">(Command command)</span> </span>&#123;        <span class="hljs-keyword">this</span>.command = command;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">action</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">this</span>.command.execute();    &#125;&#125;</code></pre></div><p>优点：</p><ul><li>类间解耦</li><li>可扩展性</li></ul><h1 id="责任链模式"><a href="#责任链模式" class="headerlink" title="责任链模式"></a>责任链模式</h1><blockquote><p>使多个对象都有机会处理请求，从而避免了请求的发送者和请求者之间耦合关系。将这些对象连成一条链，并沿着这条链传递该请求，直到有对象处理它为止。</p></blockquote><p>该模式有两个角色：</p><ul><li>抽象处理者角色：该角色对请求进行抽象，并定义一个方法以设定和返回对下一个处理者的引用。</li><li>具体处理者角色：该角色接到请求后，可以将请求处理掉，或者将请求传给下一个处理者。</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Handler</span> </span>&#123;    <span class="hljs-keyword">private</span> Handler successor;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">handleRequest</span><span class="hljs-params">()</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> Handler <span class="hljs-title">getSuccessor</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> successor;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setSuccessor</span><span class="hljs-params">(Handler successor)</span> </span>&#123;        <span class="hljs-keyword">this</span>.successor = successor;    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteHandler</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Handler</span></span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">handleRequest</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">if</span> (getSuccessor() != <span class="hljs-keyword">null</span>)&#123;            System.out.println(<span class="hljs-string">"请求传递给："</span>+ getSuccessor());            getSuccessor().handleRequest();        &#125;<span class="hljs-keyword">else</span> &#123;            System.out.println(<span class="hljs-string">"请求处理"</span>);        &#125;    &#125;&#125;</code></pre></div><p>优点：</p><ul><li>将请求和处理分开</li><li>提高系统的灵活性</li></ul><p>缺点：</p><ul><li>链过长将降低程序的性能</li><li>不易调试</li></ul><h1 id="策略模式"><a href="#策略模式" class="headerlink" title="策略模式"></a>策略模式</h1><blockquote><p>定义一组算法，将每个算法都封装起来，并且使它们之间可以互换。</p></blockquote><p>该模式涉及三个角色：</p><ul><li>环境角色：屏蔽高层模块对策略、算法的直接访问</li><li>抽象策略角色：对策略、算法进行抽象</li><li>具体策略角色：实现抽象策略中的具体操作</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Strategy</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">strategyInterface</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteStrategy</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Strategy</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">strategyInterface</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">//具体逻辑</span>    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Context</span> </span>&#123;    <span class="hljs-keyword">private</span> Strategy strategy;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Context</span><span class="hljs-params">(Strategy strategy)</span> </span>&#123;        <span class="hljs-keyword">this</span>.strategy = strategy;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">contextInterface</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">this</span>.strategy.strategyInterface();    &#125;&#125;</code></pre></div><p>优点：</p><ul><li>提供了管理算法族的方法</li><li>可以替换继承关系的办法</li><li>可以避免使用多重条转移语句</li></ul><p>缺点：</p><ul><li>客户端必须直到所有的策略类</li><li>策略模式会造成很多策略类</li></ul><h1 id="迭代器模式"><a href="#迭代器模式" class="headerlink" title="迭代器模式"></a>迭代器模式</h1><blockquote><p>提供一种方法访问容器对象中各个元素，而又不暴露该对象的内部细节。</p></blockquote><p>该模式有四个角色：</p><ul><li>抽象迭代器角色：定义迭代接口</li><li>具体迭代器角色：负责实现迭代接口</li><li>抽象聚集角色：提供创建迭代器角色的接口</li><li>具体聚集角色：实现抽象聚集接口</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Iterator</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">next</span><span class="hljs-params">()</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasNext</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteIterator</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Iterator</span> </span>&#123;    <span class="hljs-keyword">private</span> ConcreteAggregate aggregate;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> index = <span class="hljs-number">0</span>;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> size = <span class="hljs-number">0</span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ConcreteIterator</span><span class="hljs-params">(ConcreteAggregate aggregate)</span> </span>&#123;        <span class="hljs-keyword">this</span>.aggregate = aggregate;        <span class="hljs-keyword">this</span>.index = <span class="hljs-number">0</span>;        <span class="hljs-keyword">this</span>.size = aggregate.size();    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">next</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">if</span> (hasNext()) &#123;            <span class="hljs-keyword">return</span> aggregate.getElement(index++);        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">hasNext</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> index &lt; size;    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Aggregate</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(Object obj)</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> Iterator <span class="hljs-title">createIterator</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteAggregate</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Aggregate</span> </span>&#123;    <span class="hljs-keyword">private</span> Vector vector = <span class="hljs-keyword">new</span> Vector();    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(Object obj)</span> </span>&#123;        <span class="hljs-keyword">this</span>.vector.add(obj);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Iterator <span class="hljs-title">createIterator</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ConcreteIterator(<span class="hljs-keyword">this</span>);    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">size</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">return</span> vector.size();    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getElement</span><span class="hljs-params">(<span class="hljs-keyword">int</span> index)</span> </span>&#123;        <span class="hljs-keyword">if</span> (index &lt; vector.size())&#123;            <span class="hljs-keyword">return</span> vector.get(index);        &#125;<span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;    &#125;&#125;</code></pre></div><p>优点：</p><ul><li>简化访问容器元素的操作，具备一个同意的遍历接口</li><li>封装了遍历算法</li></ul><h1 id="中介者模式"><a href="#中介者模式" class="headerlink" title="中介者模式"></a>中介者模式</h1><blockquote><p>用一个中介对象封装一系列对象的交互，中介者使各对象不需要显式地相互作用，从而使其耦合松散，而且可以独立地改变它们之间地交互。</p></blockquote><p>该模式主要有四个角色：</p><ul><li>抽象中介者角色：定义出对象到中介者的接口</li><li>具体中介者角色：实现抽象中介者</li><li>抽象对象角色：定义出中介者到对象的接口</li><li>具体对象角色：实现抽象对象</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Colleague</span> </span>&#123;    <span class="hljs-keyword">private</span> Mediator mediator;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Colleague</span><span class="hljs-params">(Mediator mediator)</span> </span>&#123;        <span class="hljs-keyword">this</span>.mediator = mediator;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> Mediator <span class="hljs-title">getMediator</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> mediator;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setMediator</span><span class="hljs-params">(Mediator mediator)</span> </span>&#123;        <span class="hljs-keyword">this</span>.mediator = mediator;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">action</span><span class="hljs-params">()</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">change</span><span class="hljs-params">()</span></span>&#123;        mediator.colleagueChanged();    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteColleague</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Colleague</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ConcreteColleague</span><span class="hljs-params">(Mediator mediator)</span> </span>&#123;        <span class="hljs-keyword">super</span>(mediator);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">action</span><span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">"action"</span>);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Mediator</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">colleagueChanged</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteMediator</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Mediator</span> </span>&#123;    <span class="hljs-keyword">private</span> ConcreteColleague concreteColleague;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">createConcreteMediator</span><span class="hljs-params">()</span></span>&#123;        concreteColleague = <span class="hljs-keyword">new</span> ConcreteColleague(<span class="hljs-keyword">this</span>);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">colleagueChanged</span><span class="hljs-params">()</span> </span>&#123;        concreteColleague.action();    &#125;&#125;</code></pre></div><p>优点：</p><ul><li>减少类间的依赖，将一对多的依赖变成一对一的依赖。</li><li>避免同事对象之间过度耦合</li><li>中介者模式将对象地行为和协作抽象化</li></ul><h1 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h1><blockquote><p>定义对象间一种一对多的依赖关系，使得每当一个对象改变状态，则所有依赖它的对象会得到通知并被自动更新。</p></blockquote><p>该模式主要有四个角色：</p><ul><li>抽象主题角色：被观察者，可以添加删除观察者角色</li><li>抽象观察者角色：为观察者定义接口</li><li>具体主题角色：将有关状态存入具体观察者对象，在内部状态改变时，通知观察者。</li><li>具体观察者角色：实现抽象接口。</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Subject</span> </span>&#123;    <span class="hljs-comment">/**     * 添加观察者     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">attach</span><span class="hljs-params">(Observer observer)</span></span>;    <span class="hljs-comment">/**     * 删除观察者     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">detach</span><span class="hljs-params">(Observer observer)</span></span>;    <span class="hljs-comment">/**     * 通知观察者     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">notifyObserver</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteSubject</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Subject</span> </span>&#123;    <span class="hljs-keyword">private</span> Vector&lt;Observer&gt; observers = <span class="hljs-keyword">new</span> Vector&lt;&gt;();    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">attach</span><span class="hljs-params">(Observer observer)</span> </span>&#123;        observers.add(observer);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">detach</span><span class="hljs-params">(Observer observer)</span> </span>&#123;        observers.remove(observer);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">notifyObserver</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">for</span> (Observer observer : observers)&#123;            observer.update();        &#125;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> Enumeration&lt;Observer&gt; <span class="hljs-title">observers</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">return</span> observers.elements();    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">change</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">this</span>.notifyObserver();    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Observer</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">update</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteObserver</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Observer</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">update</span><span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">"receive notice"</span>);    &#125;&#125;</code></pre></div><p>优点：</p><ul><li>两者之间是抽象耦合</li><li>支持向所有的观察者发出通知</li></ul><p>缺点：</p><ul><li>如果对观察者的通知是通过另外的线程进行异步投递，需要保证投递的顺序性。</li></ul><h1 id="备忘录模式"><a href="#备忘录模式" class="headerlink" title="备忘录模式"></a>备忘录模式</h1><blockquote><p>在不破坏封装性的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态，这样，以后就可以将该对象恢复到原先保存的状态。</p></blockquote><p>通俗的说，备忘录模式就是将一个对象进行备份的方法。</p><p>该模式有三个角色：</p><ul><li>发起人角色：记录当前时刻的内部状态，负责定义哪种属于备份范围的状态，负责创建和恢复数据</li><li>备忘录角色：该角色存储发起人的内部状态</li><li>负责人角色：对备忘录角色进行管理、保存和提供备忘录。</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Originator</span> </span>&#123;    <span class="hljs-keyword">private</span> String state = <span class="hljs-string">""</span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getState</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> state;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setState</span><span class="hljs-params">(String state)</span> </span>&#123;        <span class="hljs-keyword">this</span>.state = state;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> Memento <span class="hljs-title">createMemento</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> Memento(<span class="hljs-keyword">this</span>.state);    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">restoreMemento</span><span class="hljs-params">(Memento memento)</span></span>&#123;        <span class="hljs-keyword">this</span>.setState(memento.getState());    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Memento</span> </span>&#123;    <span class="hljs-keyword">private</span> String state;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Memento</span><span class="hljs-params">(String state)</span> </span>&#123;        <span class="hljs-keyword">this</span>.state = state;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getState</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> state;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setState</span><span class="hljs-params">(String state)</span> </span>&#123;        <span class="hljs-keyword">this</span>.state = state;    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Caretaker</span> </span>&#123;    <span class="hljs-keyword">private</span> Memento memento;    <span class="hljs-function"><span class="hljs-keyword">public</span> Memento <span class="hljs-title">getMemento</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">return</span> memento;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setMemento</span><span class="hljs-params">(Memento memento)</span> </span>&#123;        <span class="hljs-keyword">this</span>.memento = memento;    &#125;&#125;</code></pre></div><h1 id="访问者模式"><a href="#访问者模式" class="headerlink" title="访问者模式"></a>访问者模式</h1><blockquote><p>封装一些作用于某种数据结构中各元素的操作，它可以在不改变数据结构的前提下定义作用于这些元素的新操作。</p></blockquote><p>该模式涉及五个角色：</p><ul><li>抽象访问者角色：该角色声明一个或多个访问操作，定义访问者可以访问哪些元素</li><li>具体访问者：实现抽象访问者的接口</li><li>抽象元素角色：声明一个接受操作，接受一个访问者对象</li><li>具体元素角色：实现抽象元素中的接受接口</li><li>结构对象：提供遍历操作</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Element</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">accept</span><span class="hljs-params">(Visitor visitor)</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteElement</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Element</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">accept</span><span class="hljs-params">(Visitor visitor)</span> </span>&#123;        visitor.visit(<span class="hljs-keyword">this</span>);    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">operation</span><span class="hljs-params">()</span></span>&#123;        System.out.println(<span class="hljs-string">"访问元素"</span>);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Visitor</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">visit</span><span class="hljs-params">(ConcreteElement concreteElement)</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteVisitor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Visitor</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">visit</span><span class="hljs-params">(ConcreteElement concreteElement)</span> </span>&#123;        concreteElement.operation();    &#125;&#125;</code></pre></div><p>优点：</p><ul><li>增加新的操作很容易，只需新增新的访问者类</li><li>访问者模式将有关的行为集中到访问者对象中</li></ul><p>缺点：</p><ul><li>增加新的元素困难，每增加一个都意味着要在抽象访问者类中新增操作</li><li>破坏封装</li><li>违背依赖倒置原则</li></ul><h1 id="状态模式"><a href="#状态模式" class="headerlink" title="状态模式"></a>状态模式</h1><blockquote><p>当一个对象内在状态改变时允许改变行为，这个对象看起来像改变了其类型。</p></blockquote><p>核心是封装，状态的变更引起行为的变动，外部看好像该对象对应的类发生改变。</p><p>该模式有三个角色：</p><ul><li>抽象状态角色：该角色用以封装环境对象的一个状态所对应的行为。</li><li>具体状态角色：该角色实现抽象行为。</li><li>环境角色：定义客户端需要的接口，并负责具体状态的切换</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">State</span> </span>&#123;    <span class="hljs-keyword">private</span> Context context;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setContext</span><span class="hljs-params">(Context context)</span> </span>&#123;        <span class="hljs-keyword">this</span>.context = context;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">handle</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteState</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">State</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">handle</span><span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">"逻辑处理"</span>);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Context</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> State STATE = <span class="hljs-keyword">new</span> ConcreteState();    <span class="hljs-keyword">private</span> State currentState;    <span class="hljs-function"><span class="hljs-keyword">public</span> State <span class="hljs-title">getCurrentState</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> currentState;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setCurrentState</span><span class="hljs-params">(State currentState)</span> </span>&#123;        <span class="hljs-keyword">this</span>.currentState = currentState;        currentState.setContext(<span class="hljs-keyword">this</span>);    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">handle1</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">this</span>.setCurrentState(STATE);        <span class="hljs-keyword">this</span>.currentState.handle();    &#125;&#125;</code></pre></div><p>优点：</p><ul><li>结构清晰</li><li>遵循设计原则</li><li>封装性好</li></ul><h1 id="解释器模式"><a href="#解释器模式" class="headerlink" title="解释器模式"></a>解释器模式</h1><blockquote><p>给定一门语言，定义它的文法的一种表示，并定义一个解释器，该解释器使用该表示来解释语言中的句子。</p></blockquote><p>该模式有五个角色：</p><ul><li>抽象表达式（Abstract Expression）角色：该角色声明一个所有的具体表达式角色都需要实现的抽象接口，该接口主要是一个解释操作interpret()方法。</li><li>终结符表达式（Terminal Expression）角色：该角色实现了抽象表达式角色所要求的接口，文法中的每一个终结符都有一个具体终结表达式与之对应。</li><li>非终结符表达式（Nonterminal Expression）角色：该角色是一个具体角色，文法中的每一条规则都对应一个非终结符表达式类。</li><li>环境（Context）角色：该角色提供解释器之外的一些全局信息。</li><li>客户端（Client）角色：该角色创建一个抽象语法树，调用解释操作。</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AbstractExpression</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> Object <span class="hljs-title">interpreter</span><span class="hljs-params">(Context ctx)</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">NonterminalExpression</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractExpression</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">interpreter</span><span class="hljs-params">(Context ctx)</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TerminalExpression</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractExpression</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">interpreter</span><span class="hljs-params">(Context ctx)</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Context</span> </span>&#123;    <span class="hljs-keyword">private</span> HashMap map = <span class="hljs-keyword">new</span> HashMap();&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Client</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        Context context = <span class="hljs-keyword">new</span> Context();        <span class="hljs-comment">//todo</span>    &#125;&#125;</code></pre></div>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;模板方法模式&quot;&gt;&lt;a href=&quot;#模板方法模式&quot; class=&quot;headerlink&quot; title=&quot;模板方法模式&quot;&gt;&lt;/a&gt;模板方法模式&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;定义一个操作中的算法的框架，而将一些步骤延迟到子类中。使得子类可以不改变一个算法的
      
    
    </summary>
    
      <category term="设计模式" scheme="https://l1n.wang/categories/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
    
      <category term="设计模式" scheme="https://l1n.wang/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>设计模式之结构型模式（三）</title>
    <link href="https://l1n.wang/2020/05/design-pattern-structure/"/>
    <id>https://l1n.wang/2020/05/design-pattern-structure/</id>
    <published>2020-05-21T09:51:48.000Z</published>
    <updated>2020-05-21T09:52:30.298Z</updated>
    
    <content type="html"><![CDATA[<h1 id="适配器模式"><a href="#适配器模式" class="headerlink" title="适配器模式"></a>适配器模式</h1><blockquote><p>适配器模式将某个类的接口转换成客户端期望的另一个接口表示，主要目的是兼容性，让原本因接口不匹配不能在一起工作的两个类可以协同工作，别名为包装器（Wrapper）。</p></blockquote><h2 id="类适配器"><a href="#类适配器" class="headerlink" title="类适配器"></a>类适配器</h2><p>注意：</p><ul><li>类适配器需要继承src类，这要求dst必须是接口，有一定的局限性</li><li>src类的方法在Adapter中会被暴露出来</li><li>Adapter需要重写src的方法</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">/** * <span class="hljs-doctag">@author</span> ：L1nker4 * <span class="hljs-doctag">@date</span> ： 创建于  2020/5/19 11:40 * <span class="hljs-doctag">@description</span>： 被适配的类 */</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Voltage220V</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">output220V</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">int</span> src = <span class="hljs-number">220</span>;        System.out.println(<span class="hljs-string">"电压："</span>+ src + <span class="hljs-string">"伏"</span>);        <span class="hljs-keyword">return</span> src;    &#125;&#125;<span class="hljs-comment">/** * 适配接口 */</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">IVoltage5V</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">output5V</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-comment">/** * <span class="hljs-doctag">@author</span> ：L1nker4 * <span class="hljs-doctag">@date</span> ： 创建于  2020/5/19 11:42 * <span class="hljs-doctag">@description</span>： 适配器类 */</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">VoltageAdapter</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Voltage220V</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">IVoltage5V</span></span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">output5V</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">int</span> src = output220V();        <span class="hljs-keyword">return</span> src / <span class="hljs-number">44</span>;    &#125;&#125;<span class="hljs-comment">/** * <span class="hljs-doctag">@author</span> ：L1nker4 * <span class="hljs-doctag">@date</span> ： 创建于  2020/5/19 11:44 * <span class="hljs-doctag">@description</span>： 手机类 */</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Phone</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">charging</span><span class="hljs-params">(IVoltage5V iVoltage5V)</span></span>&#123;        <span class="hljs-keyword">if</span> (iVoltage5V.output5V() == <span class="hljs-number">5</span>)&#123;            System.out.println(<span class="hljs-string">"电压为：5V，可以充电"</span>);        &#125;<span class="hljs-keyword">else</span> &#123;            System.out.println(<span class="hljs-string">"电压不正常，无法充电"</span>);        &#125;    &#125;&#125;</code></pre></div><h2 id="对象适配器"><a href="#对象适配器" class="headerlink" title="对象适配器"></a>对象适配器</h2><ul><li>基本思路和类适配器模式相同，只是将Adapter类作修改，不是继承src类，而是持有src类的实例，以解决兼容性的问题，即：持有src类，实现dst接口，完成src-&gt;dst的适配。</li><li>根据合成复用原则，在系统中使用关联关系替代继承关系。<div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">/** * <span class="hljs-doctag">@author</span> ：L1nker4 * <span class="hljs-doctag">@date</span> ： 创建于  2020/5/19 11:42 * <span class="hljs-doctag">@description</span>： 适配器类 */</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">VoltageAdapter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">IVoltage5V</span> </span>&#123;    <span class="hljs-keyword">private</span> Voltage220V voltage220V;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">VoltageAdapter</span><span class="hljs-params">(Voltage220V voltage220V)</span> </span>&#123;        <span class="hljs-keyword">this</span>.voltage220V = voltage220V;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">output5V</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">if</span>(<span class="hljs-keyword">null</span> != voltage220V)&#123;            <span class="hljs-keyword">int</span> src = voltage220V.output220V();            System.out.println(<span class="hljs-string">"适配完成"</span>);            <span class="hljs-keyword">return</span> src / <span class="hljs-number">44</span>;        &#125;        System.out.println(<span class="hljs-string">"适配失败"</span>);        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125;&#125;</code></pre></div></li></ul><h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><p>Spring MVC - HandlerAdapter<br><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">HandlerAdapter</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">supports</span><span class="hljs-params">(Object var1)</span></span>;    <span class="hljs-meta">@Nullable</span>    <span class="hljs-function">ModelAndView <span class="hljs-title">handle</span><span class="hljs-params">(HttpServletRequest var1, HttpServletResponse var2, Object var3)</span> <span class="hljs-keyword">throws</span> Exception</span>;    <span class="hljs-function"><span class="hljs-keyword">long</span> <span class="hljs-title">getLastModified</span><span class="hljs-params">(HttpServletRequest var1, Object var2)</span></span>;&#125;</code></pre></div></p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;            processedRequest = checkMultipart(request);            multipartRequestParsed = (processedRequest != request);            <span class="hljs-comment">// Determine handler for the current request.</span>            mappedHandler = getHandler(processedRequest);            <span class="hljs-keyword">if</span> (mappedHandler == <span class="hljs-keyword">null</span>) &#123;                noHandlerFound(processedRequest, response);                <span class="hljs-keyword">return</span>;            &#125;            <span class="hljs-comment">// Determine handler adapter for the current request.</span>            HandlerAdapter ha = getHandlerAdapter(mappedHandler.getHandler());            <span class="hljs-comment">// Process last-modified header, if supported by the handler.</span>            String method = request.getMethod();            <span class="hljs-keyword">boolean</span> isGet = <span class="hljs-string">"GET"</span>.equals(method);            <span class="hljs-keyword">if</span> (isGet || <span class="hljs-string">"HEAD"</span>.equals(method)) &#123;                <span class="hljs-keyword">long</span> lastModified = ha.getLastModified(request, mappedHandler.getHandler());                <span class="hljs-keyword">if</span> (<span class="hljs-keyword">new</span> ServletWebRequest(request, response).checkNotModified(lastModified) &amp;&amp; isGet) &#123;                    <span class="hljs-keyword">return</span>;                &#125;            &#125;            <span class="hljs-keyword">if</span> (!mappedHandler.applyPreHandle(processedRequest, response)) &#123;                <span class="hljs-keyword">return</span>;            &#125;            <span class="hljs-comment">// Actually invoke the handler.</span>            mv = ha.handle(processedRequest, response, mappedHandler.getHandler());            <span class="hljs-keyword">if</span> (asyncManager.isConcurrentHandlingStarted()) &#123;                <span class="hljs-keyword">return</span>;            &#125;            applyDefaultViewName(processedRequest, mv);            mappedHandler.applyPostHandle(processedRequest, response, mv);        &#125;</code></pre></div><h1 id="桥接模式"><a href="#桥接模式" class="headerlink" title="桥接模式"></a>桥接模式</h1><blockquote><p>桥接是用于把抽象与实现解耦，使得两者可以独立变化。</p></blockquote><p>该模式主要解决在多种可能会变化的情况下，用继承会造成类爆炸问题，扩展起来不灵活。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">/** * <span class="hljs-doctag">@author</span> ：L1nker4 * <span class="hljs-doctag">@date</span> ： 创建于  2020/5/20 12:48 * <span class="hljs-doctag">@description</span>： 桥接 接口 */</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">DrawAPI</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">drawCircle</span><span class="hljs-params">(<span class="hljs-keyword">int</span> radius, <span class="hljs-keyword">int</span> x, <span class="hljs-keyword">int</span> y)</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">RedCircle</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">DrawAPI</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">drawCircle</span><span class="hljs-params">(<span class="hljs-keyword">int</span> radius, <span class="hljs-keyword">int</span> x, <span class="hljs-keyword">int</span> y)</span> </span>&#123;        System.out.println(<span class="hljs-string">"Drawing Circle[ color: red, radius: "</span>                + radius + <span class="hljs-string">", x: "</span> + x + <span class="hljs-string">", "</span> + y + <span class="hljs-string">"]"</span>);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">GreenCircle</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">DrawAPI</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">drawCircle</span><span class="hljs-params">(<span class="hljs-keyword">int</span> radius, <span class="hljs-keyword">int</span> x, <span class="hljs-keyword">int</span> y)</span> </span>&#123;        System.out.println(<span class="hljs-string">"Drawing Circle[ color: green, radius: "</span>                + radius + <span class="hljs-string">", x: "</span> + x + <span class="hljs-string">", "</span> + y + <span class="hljs-string">"]"</span>);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Shape</span> </span>&#123;    <span class="hljs-keyword">protected</span> DrawAPI drawAPI;    <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-title">Shape</span><span class="hljs-params">(DrawAPI drawAPI)</span></span>&#123;        <span class="hljs-keyword">this</span>.drawAPI = drawAPI;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Circle</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Shape</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> x, y, radius;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Circle</span><span class="hljs-params">(<span class="hljs-keyword">int</span> x, <span class="hljs-keyword">int</span> y, <span class="hljs-keyword">int</span> radius, DrawAPI drawAPI)</span> </span>&#123;        <span class="hljs-keyword">super</span>(drawAPI);        <span class="hljs-keyword">this</span>.x = x;        <span class="hljs-keyword">this</span>.y = y;        <span class="hljs-keyword">this</span>.radius = radius;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span> </span>&#123;        drawAPI.drawCircle(radius, x, y);    &#125;&#125;</code></pre></div><h2 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h2><ul><li>抽象和实现分离</li><li>对客户透明，客户端不用关心细节的实现</li></ul><h1 id="装饰模式"><a href="#装饰模式" class="headerlink" title="装饰模式"></a>装饰模式</h1><blockquote><p>动态地给一个对象添加一些额外的职责，就增加功能来说，装饰模式比生成子类更为灵活。</p></blockquote><p>主要有以下四个角色：</p><ul><li>Component：用于规范需要装饰的对象</li><li>Concrete Component：实现Component角色，定义一个需要装饰的原始类</li><li>Decorator：持有一个构建对象的实例，并定义一个与Component接口一致的接口</li><li>Concrete Decorator：负责对构建对象进行装饰</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Component</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">operation</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteComponent</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Component</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">operation</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">//业务</span>    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Decorator</span> </span>&#123;    <span class="hljs-keyword">private</span> Component component;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Decorator</span><span class="hljs-params">(Component component)</span> </span>&#123;        <span class="hljs-keyword">this</span>.component = component;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">operation</span><span class="hljs-params">()</span></span>&#123;        component.operation();    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteDecorator</span>  <span class="hljs-keyword">extends</span> <span class="hljs-title">Decorator</span></span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ConcreteDecorator</span><span class="hljs-params">(Component component)</span> </span>&#123;        <span class="hljs-keyword">super</span>(component);    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">method</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-comment">//自己的方法</span>    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">operation</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">this</span>.method();        <span class="hljs-keyword">super</span>.operation();    &#125;&#125;</code></pre></div><h2 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h2><ul><li>装饰类和被装饰类可以独立发展。</li><li>装饰模式是继承的一个替代方案。</li><li>多层装饰较为复杂。</li></ul><h1 id="组合模式"><a href="#组合模式" class="headerlink" title="组合模式"></a>组合模式</h1><blockquote><p>将对象组合成树形结构以表示部分一整体的层次结构，使得用户对单个对象和组合对象的使用具有一致性。</p></blockquote><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Employee</span> </span>&#123;   <span class="hljs-keyword">private</span> String name;   <span class="hljs-keyword">private</span> String dept;   <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> salary;   <span class="hljs-keyword">private</span> List&lt;Employee&gt; subordinates;    <span class="hljs-comment">//构造函数</span>   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Employee</span><span class="hljs-params">(String name,String dept, <span class="hljs-keyword">int</span> sal)</span> </span>&#123;      <span class="hljs-keyword">this</span>.name = name;      <span class="hljs-keyword">this</span>.dept = dept;      <span class="hljs-keyword">this</span>.salary = sal;      subordinates = <span class="hljs-keyword">new</span> ArrayList&lt;Employee&gt;();   &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(Employee e)</span> </span>&#123;      subordinates.add(e);   &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">remove</span><span class="hljs-params">(Employee e)</span> </span>&#123;      subordinates.remove(e);   &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;Employee&gt; <span class="hljs-title">getSubordinates</span><span class="hljs-params">()</span></span>&#123;     <span class="hljs-keyword">return</span> subordinates;   &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">toString</span><span class="hljs-params">()</span></span>&#123;      <span class="hljs-keyword">return</span> (<span class="hljs-string">"Employee :[ Name : "</span>+ name       +<span class="hljs-string">", dept : "</span>+ dept + <span class="hljs-string">", salary :"</span>      + salary+<span class="hljs-string">" ]"</span>);   &#125;   &#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Employee</span> </span>&#123;    <span class="hljs-keyword">private</span> String name;    <span class="hljs-keyword">private</span> String dept;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> salary;    <span class="hljs-keyword">private</span> List&lt;Employee&gt; subordinates;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Employee</span><span class="hljs-params">(String name, String dept, <span class="hljs-keyword">int</span> sal)</span> </span>&#123;        <span class="hljs-keyword">this</span>.name = name;        <span class="hljs-keyword">this</span>.dept = dept;        <span class="hljs-keyword">this</span>.salary = sal;        subordinates = <span class="hljs-keyword">new</span> ArrayList&lt;Employee&gt;();    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">add</span><span class="hljs-params">(Employee e)</span> </span>&#123;        subordinates.add(e);    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">remove</span><span class="hljs-params">(Employee e)</span> </span>&#123;        subordinates.remove(e);    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;Employee&gt; <span class="hljs-title">getSubordinates</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> subordinates;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">toString</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> (<span class="hljs-string">"Employee :[ Name : "</span> + name                + <span class="hljs-string">", dept : "</span> + dept + <span class="hljs-string">", salary :"</span>                + salary + <span class="hljs-string">" ]"</span>);    &#125;&#125;</code></pre></div><h1 id="外观模式"><a href="#外观模式" class="headerlink" title="外观模式"></a>外观模式</h1><blockquote><p>要求一个子系统的外部与其内部的通信必须通过一个统一的对象进行。外观模式提供一个高层次的接口，使得子系统更易使用。</p></blockquote><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Shape</span> </span>&#123;   <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Rectangle</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Shape</span> </span>&#123;    <span class="hljs-meta">@Override</span>   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span> </span>&#123;      System.out.println(<span class="hljs-string">"Rectangle::draw()"</span>);   &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Square</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Shape</span> </span>&#123;    <span class="hljs-meta">@Override</span>   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span> </span>&#123;      System.out.println(<span class="hljs-string">"Square::draw()"</span>);   &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Circle</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Shape</span> </span>&#123;    <span class="hljs-meta">@Override</span>   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span> </span>&#123;      System.out.println(<span class="hljs-string">"Circle::draw()"</span>);   &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ShapeMaker</span> </span>&#123;   <span class="hljs-keyword">private</span> Shape circle;   <span class="hljs-keyword">private</span> Shape rectangle;   <span class="hljs-keyword">private</span> Shape square;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ShapeMaker</span><span class="hljs-params">()</span> </span>&#123;      circle = <span class="hljs-keyword">new</span> Circle();      rectangle = <span class="hljs-keyword">new</span> Rectangle();      square = <span class="hljs-keyword">new</span> Square();   &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">drawCircle</span><span class="hljs-params">()</span></span>&#123;      circle.draw();   &#125;   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">drawRectangle</span><span class="hljs-params">()</span></span>&#123;      rectangle.draw();   &#125;   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">drawSquare</span><span class="hljs-params">()</span></span>&#123;      square.draw();   &#125;&#125;</code></pre></div><h1 id="享元模式"><a href="#享元模式" class="headerlink" title="享元模式"></a>享元模式</h1><blockquote><p>运用共享技术来有効地支持大量细粒度对象的复用。</p></blockquote><ul><li>常见于系统底层开发，能够解决重复对象的内存浪费问题。</li><li>经典应用场景：池技术（String常量池、数据库连接池、缓冲池）</li></ul><p>享元模式能做到共享的关键是区分内部状态和外部状态。</p><ul><li>内部状态是存储在享元对象内部的，可以共享的信息，不会随环境而变化。</li><li>外部状态是随环境而改变且不可以共享的状态。</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Flyweight</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">operation</span><span class="hljs-params">(String extrinsicState)</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteFlyWeight</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Flyweight</span> </span>&#123;    <span class="hljs-comment">//内部状态</span>    <span class="hljs-keyword">private</span> String intrinsicState;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ConcreteFlyWeight</span><span class="hljs-params">(String intrinsicState)</span> </span>&#123;        <span class="hljs-keyword">this</span>.intrinsicState = intrinsicState;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">operation</span><span class="hljs-params">(String extrinsicState)</span> </span>&#123;        System.out.println(<span class="hljs-string">"内部状态："</span> + intrinsicState);        System.out.println(<span class="hljs-string">"外部状态："</span> + extrinsicState);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FlyweightFactory</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Map&lt;String, Flyweight&gt; pool = <span class="hljs-keyword">new</span> HashMap&lt;&gt;();    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">FlyweightFactory</span><span class="hljs-params">()</span> </span>&#123;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Flyweight <span class="hljs-title">getFlyweight</span><span class="hljs-params">(String intrinsicState)</span></span>&#123;        Flyweight flyweight = pool.get(intrinsicState);        <span class="hljs-keyword">if</span>(flyweight == <span class="hljs-keyword">null</span>)&#123;            flyweight = <span class="hljs-keyword">new</span> ConcreteFlyWeight(intrinsicState);            pool.put(intrinsicState, flyweight);        &#125;        <span class="hljs-keyword">return</span> flyweight;    &#125;&#125;</code></pre></div><h1 id="代理模式"><a href="#代理模式" class="headerlink" title="代理模式"></a>代理模式</h1><blockquote><p>为其他对象提供一种代理以控制这个对象的访问。</p></blockquote><p>该模式主要有三个角色：</p><ul><li>Subject（抽象主题角色）：该角色是真实主题和迪阿尼主题共同接口，以便在任何可以使用真实主题的地方都可以使用代理主题。</li><li>Proxy Subject（代理主题角色）：也叫做代理类，该角色负责控制对真实主题的引用，负责在需要的时候创建或删除真实主题对象，并且在真实主题角色处理完毕钱后做预处理工作。</li><li>Real Project（真实主题角色）：该角色被称为被代理角色，是业务逻辑的具体执行者。</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Subject</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">request</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">RealSubject</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Subject</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">request</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">//业务代码</span>    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ProxySubject</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Subject</span> </span>&#123;    <span class="hljs-keyword">private</span> Subject subject;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ProxySubject</span><span class="hljs-params">(Subject subject)</span> </span>&#123;        <span class="hljs-keyword">this</span>.subject = subject;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">request</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">this</span>.beforeRequest();        subject.request();        <span class="hljs-keyword">this</span>.afterRequest();    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">beforeRequest</span><span class="hljs-params">()</span></span>&#123;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">afterRequest</span><span class="hljs-params">()</span></span>&#123;    &#125;&#125;</code></pre></div>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;适配器模式&quot;&gt;&lt;a href=&quot;#适配器模式&quot; class=&quot;headerlink&quot; title=&quot;适配器模式&quot;&gt;&lt;/a&gt;适配器模式&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;适配器模式将某个类的接口转换成客户端期望的另一个接口表示，主要目的是兼容性，让原本因接口不
      
    
    </summary>
    
      <category term="设计模式" scheme="https://l1n.wang/categories/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
    
      <category term="设计模式" scheme="https://l1n.wang/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>设计模式之创建型模式（二）</title>
    <link href="https://l1n.wang/2020/05/design-pattern-creational/"/>
    <id>https://l1n.wang/2020/05/design-pattern-creational/</id>
    <published>2020-05-17T08:39:24.000Z</published>
    <updated>2020-05-17T08:41:07.534Z</updated>
    
    <content type="html"><![CDATA[<h1 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h1><p>采取一定的方法保证在整个软件系统中，对某个类只能存在一个对象实例，并且该类只提供一个取得其对象实例的方法（静态方法）。</p><p>比如Hibernate的SessionFactory，它充当数据存储源的代理，并负责创建Session对象。</p><h2 id="饿汉式"><a href="#饿汉式" class="headerlink" title="饿汉式"></a>饿汉式</h2><h3 id="静态常量方法"><a href="#静态常量方法" class="headerlink" title="静态常量方法"></a>静态常量方法</h3><p>demo：<br><div class="hljs"><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span></span>&#123;    <span class="hljs-comment">/**     * 构造器私有化，外部不能new     */</span>    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span><span class="hljs-params">()</span></span>&#123;    &#125;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">static</span> Singleton INSTANCE = <span class="hljs-keyword">new</span> Singleton();    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title">getInstance</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">return</span> INSTANCE;    &#125;&#125;</code></pre></div></p><ul><li>优点<ul><li>写法简单，在类装载的时候就完成实例化，避免了线程安全问题。</li></ul></li><li>缺点<ul><li>没有达到<code>lazy loading</code>的效果，如果一直没有使用，就会造成内存浪费。</li></ul></li></ul><h3 id="静态代码块"><a href="#静态代码块" class="headerlink" title="静态代码块"></a>静态代码块</h3><p>demo：<br><div class="hljs"><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span></span>&#123;    <span class="hljs-comment">/**     * 构造器私有化，外部不能new     */</span>    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span><span class="hljs-params">()</span></span>&#123;        INSTANCE = <span class="hljs-keyword">new</span> Singleton();    &#125;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Singleton INSTANCE ;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title">getInstance</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">return</span> INSTANCE;    &#125;&#125;</code></pre></div></p><h2 id="懒汉式"><a href="#懒汉式" class="headerlink" title="懒汉式"></a>懒汉式</h2><h3 id="线程不安全"><a href="#线程不安全" class="headerlink" title="线程不安全"></a>线程不安全</h3><p>demo：</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span></span>&#123;    <span class="hljs-comment">/**     * 构造器私有化，外部不能new     */</span>    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span><span class="hljs-params">()</span></span>&#123;    &#125;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Singleton INSTANCE ;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title">getInstance</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">if</span> (INSTANCE == <span class="hljs-keyword">null</span>)&#123;            INSTANCE = <span class="hljs-keyword">new</span> Singleton();        &#125;        <span class="hljs-keyword">return</span> INSTANCE;    &#125;&#125;</code></pre></div><ul><li>优点<ul><li>起到了<code>lazy loading</code>的效果，当时只能在单线程下使用</li></ul></li><li>缺点<ul><li>多线程下会出现线程安全问题。 </li></ul></li></ul><h3 id="线程安全"><a href="#线程安全" class="headerlink" title="线程安全"></a>线程安全</h3><p>加上<code>synchronized</code>关键字。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span></span>&#123;    <span class="hljs-comment">/**     * 构造器私有化，外部不能new     */</span>    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span><span class="hljs-params">()</span></span>&#123;    &#125;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Singleton INSTANCE ;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">synchronized</span> Singleton <span class="hljs-title">getInstance</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">if</span> (INSTANCE == <span class="hljs-keyword">null</span>)&#123;            INSTANCE = <span class="hljs-keyword">new</span> Singleton();        &#125;        <span class="hljs-keyword">return</span> INSTANCE;    &#125;&#125;</code></pre></div><ul><li>缺点<ul><li>效率低</li></ul></li></ul><h3 id="静态内部类"><a href="#静态内部类" class="headerlink" title="静态内部类"></a>静态内部类</h3><p>demo：<br><div class="hljs"><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Singleton</span></span>&#123;    <span class="hljs-comment">/**     * 构造器私有化，外部不能new     */</span>    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">Singleton</span><span class="hljs-params">()</span></span>&#123;    &#125;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SingletonInstance</span></span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Singleton INSTANCE = <span class="hljs-keyword">new</span> Singleton();    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title">getInstance</span><span class="hljs-params">()</span></span>&#123;        <span class="hljs-keyword">return</span> SingletonInstance.INSTANCE;    &#125;&#125;</code></pre></div></p><ul><li>优点<ul><li>避免线程不安全，利用静态内部类特点实现延迟加载，效率高。 </li></ul></li></ul><h2 id="单例模式的优点"><a href="#单例模式的优点" class="headerlink" title="单例模式的优点"></a>单例模式的优点</h2><ul><li>单例模式在内存中只有一个实例，减少了内存开支，特别是一个对象频繁的创建，销毁时。</li><li>由于单例模式只生成一个实例，所以减少了系统的性能开销。</li><li>单例模式可以避免对资源的多重占用，例如写文件操作，避免对同一个资源文件同时操作。</li><li>单例模式可以在系统设置全局的访问点，优化和资源访问，例如可以设计一个单例类，负责所有数据表的映射处理。</li></ul><h2 id="单例模式的缺点"><a href="#单例模式的缺点" class="headerlink" title="单例模式的缺点"></a>单例模式的缺点</h2><ul><li>单例模式一般没有接口，扩展困难</li><li>单例模式对测试是不利的，没有接口不能使用mock的方式虚拟一个对象</li><li>单例模式与单一职责原则有冲突，一个类只实现一个逻辑</li></ul><h2 id="单例模式的使用场景"><a href="#单例模式的使用场景" class="headerlink" title="单例模式的使用场景"></a>单例模式的使用场景</h2><ul><li>要求生成唯一序列号的环境</li><li>在整个项目需要一个共享访问点或共享数据，例如一个Web页面上的计数器，使用单例模式保持计数器的值，并确保是线程安全的。</li><li>创建一个对象需要消耗的资源过多，如要访问IO和数据库等资源</li><li>需要定义大量的静态变量和静态方法的环境</li></ul><h1 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h1><h2 id="简单工厂模式-静态工厂模式"><a href="#简单工厂模式-静态工厂模式" class="headerlink" title="简单工厂模式(静态工厂模式)"></a>简单工厂模式(静态工厂模式)</h2><p>简单工厂模式是有一个工厂对象决定创建出哪一种产品类的实例。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Product</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">method</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">//业务逻辑</span>    &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">method2</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Product1</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Product</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">method2</span><span class="hljs-params">()</span> </span>&#123;    &#125;&#125;<span class="hljs-comment">/** * <span class="hljs-doctag">@author</span> ：L1nker4 * <span class="hljs-doctag">@date</span> ： 创建于  2020/5/15 17:25 * <span class="hljs-doctag">@description</span>： 简单工厂模式 */</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SimpleFactory</span> </span>&#123;    <span class="hljs-keyword">public</span>  &lt;T extends Product&gt; <span class="hljs-function">T <span class="hljs-title">createProduct</span><span class="hljs-params">(Class&lt;T&gt; c)</span> </span>&#123;        Product product = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">try</span> &#123;            product = (Product) Class.forName(c.getName()).newInstance();        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;            e.printStackTrace();        &#125;        <span class="hljs-keyword">return</span> (T) product;    &#125;&#125;</code></pre></div><h2 id="工厂方法模式"><a href="#工厂方法模式" class="headerlink" title="工厂方法模式"></a>工厂方法模式</h2><blockquote><p>定义一个用于创建对象的接口，让子类决定实例化哪一个类，工厂方法使一个类的实例化延迟到其子类。</p></blockquote><p>在工厂方法模式中，抽象产品类Product负责定义产品的共性，实现对事物最抽象的定义，Creator为抽象创建类，也就是抽象工厂，具体如何创建产品类是由具体的实现共产ConcreteCreator完成的。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Creator</span> </span>&#123;        <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> &lt;T extends Product&gt; <span class="hljs-function">T <span class="hljs-title">createProduct</span><span class="hljs-params">(Class&lt;T&gt; c)</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConcreteCreator</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Creator</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-keyword">public</span> &lt;T extends Product&gt; <span class="hljs-function">T <span class="hljs-title">createProduct</span><span class="hljs-params">(Class&lt;T&gt; c)</span> </span>&#123;        Product product = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">try</span> &#123;            product = (Product) Class.forName(c.getName()).newInstance();        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;            e.printStackTrace();        &#125;        <span class="hljs-keyword">return</span> (T) product;    &#125;&#125;</code></pre></div><h2 id="抽象工厂模式"><a href="#抽象工厂模式" class="headerlink" title="抽象工厂模式"></a>抽象工厂模式</h2><blockquote><p>为创建一组相关或相互依赖的对象提供一个接口，而且无需指定他们的具体类。</p></blockquote><ul><li>抽象工厂模式可以将简单工厂模式和工厂方法模式进行整合。</li><li>从设计层面看，抽象工厂模式就是对简单工厂模式的改进。</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AbstractProductA</span> </span>&#123;        <span class="hljs-comment">//每个产品共有的方法</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">shareMethod</span><span class="hljs-params">()</span> </span>&#123;            &#125;    <span class="hljs-comment">//每个产品相同方法，不同实现</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doSomething</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ProductA</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractProductA</span></span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doSomething</span><span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">"产品A1的实现方法"</span>);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AbstractCreator</span> </span>&#123;        <span class="hljs-comment">//创建A产品</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> AbstractProductA <span class="hljs-title">createProductA</span><span class="hljs-params">()</span></span>;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Creator</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractCreator</span></span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> AbstractProductA <span class="hljs-title">createProductA</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ProductA();    &#125;&#125;</code></pre></div><h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><p>java.util.Calendar</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Calendar <span class="hljs-title">createCalendar</span><span class="hljs-params">(TimeZone zone,                                           Locale aLocale)</span>    </span>&#123;        CalendarProvider provider =            LocaleProviderAdapter.getAdapter(CalendarProvider<span class="hljs-class">.<span class="hljs-keyword">class</span>, <span class="hljs-title">aLocale</span>)                                 .<span class="hljs-title">getCalendarProvider</span>()</span>;        <span class="hljs-keyword">if</span> (provider != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-keyword">return</span> provider.getInstance(zone, aLocale);            &#125; <span class="hljs-keyword">catch</span> (IllegalArgumentException iae) &#123;                <span class="hljs-comment">// fall back to the default instantiation</span>            &#125;        &#125;        Calendar cal = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">if</span> (aLocale.hasExtensions()) &#123;            String caltype = aLocale.getUnicodeLocaleType(<span class="hljs-string">"ca"</span>);            <span class="hljs-keyword">if</span> (caltype != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-keyword">switch</span> (caltype) &#123;                <span class="hljs-keyword">case</span> <span class="hljs-string">"buddhist"</span>:                cal = <span class="hljs-keyword">new</span> BuddhistCalendar(zone, aLocale);                    <span class="hljs-keyword">break</span>;                <span class="hljs-keyword">case</span> <span class="hljs-string">"japanese"</span>:                    cal = <span class="hljs-keyword">new</span> JapaneseImperialCalendar(zone, aLocale);                    <span class="hljs-keyword">break</span>;                <span class="hljs-keyword">case</span> <span class="hljs-string">"gregory"</span>:                    cal = <span class="hljs-keyword">new</span> GregorianCalendar(zone, aLocale);                    <span class="hljs-keyword">break</span>;                &#125;            &#125;        &#125;        <span class="hljs-keyword">if</span> (cal == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-comment">// If no known calendar type is explicitly specified,</span>            <span class="hljs-comment">// perform the traditional way to create a Calendar:</span>            <span class="hljs-comment">// create a BuddhistCalendar for th_TH locale,</span>            <span class="hljs-comment">// a JapaneseImperialCalendar for ja_JP_JP locale, or</span>            <span class="hljs-comment">// a GregorianCalendar for any other locales.</span>            <span class="hljs-comment">// <span class="hljs-doctag">NOTE:</span> The language, country and variant strings are interned.</span>            <span class="hljs-keyword">if</span> (aLocale.getLanguage() == <span class="hljs-string">"th"</span> &amp;&amp; aLocale.getCountry() == <span class="hljs-string">"TH"</span>) &#123;                cal = <span class="hljs-keyword">new</span> BuddhistCalendar(zone, aLocale);            &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (aLocale.getVariant() == <span class="hljs-string">"JP"</span> &amp;&amp; aLocale.getLanguage() == <span class="hljs-string">"ja"</span>                       &amp;&amp; aLocale.getCountry() == <span class="hljs-string">"JP"</span>) &#123;                cal = <span class="hljs-keyword">new</span> JapaneseImperialCalendar(zone, aLocale);            &#125; <span class="hljs-keyword">else</span> &#123;                cal = <span class="hljs-keyword">new</span> GregorianCalendar(zone, aLocale);            &#125;        &#125;        <span class="hljs-keyword">return</span> cal;    &#125;</code></pre></div><h1 id="原型模式"><a href="#原型模式" class="headerlink" title="原型模式"></a>原型模式</h1><p>使用原型实例指定创建对象的种类，并且通过拷贝这些原型，创建新的对象。</p><ul><li>创建新的对象比较复杂时，可以利用原型模式简化对象的创建过程，同时提高效率。</li><li>不用重新床=初始化对象，而是动态地获得对象运行时的状态。</li><li>需要注意深拷贝和浅拷贝。</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Sheep</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Cloneable</span> </span>&#123;    <span class="hljs-keyword">private</span> String name;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> age;    <span class="hljs-keyword">private</span> String color;        <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">clone</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> CloneNotSupportedException </span>&#123;        Sheep sheep = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">try</span> &#123;            sheep = (Sheep) <span class="hljs-keyword">super</span>.clone();        &#125;<span class="hljs-keyword">catch</span> (Exception e)&#123;            System.out.println(e.getMessage());        &#125;        <span class="hljs-keyword">return</span> sheep;    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Main</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> CloneNotSupportedException </span>&#123;        Sheep sheep = <span class="hljs-keyword">new</span> Sheep(<span class="hljs-string">"tom"</span>, <span class="hljs-number">1</span>, <span class="hljs-string">"white"</span>);        Sheep sheep1 = (Sheep) sheep.clone();    &#125;&#125;</code></pre></div><h2 id="应用-1"><a href="#应用-1" class="headerlink" title="应用"></a>应用</h2><p>Spring—AbstractBeanFactory</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (mbd.isPrototype()) &#123;<span class="hljs-comment">// It's a prototype -&gt; create a new instance.</span>Object prototypeInstance = <span class="hljs-keyword">null</span>;<span class="hljs-keyword">try</span> &#123;beforePrototypeCreation(beanName);prototypeInstance = createBean(beanName, mbd, args);&#125;<span class="hljs-keyword">finally</span> &#123;afterPrototypeCreation(beanName);&#125;bean = getObjectForBeanInstance(prototypeInstance, name, beanName, mbd);&#125;</code></pre></div><h1 id="建造者模式"><a href="#建造者模式" class="headerlink" title="建造者模式"></a>建造者模式</h1><p>可以将复杂对象的构造过程抽象出来，使这个抽象过程的不同实现方法可以构造出不同表现的对象。</p><p>四个角色：</p><ul><li>Product：具体的产品对象</li><li>Builder：创建一个Product的各个部件的接口</li><li>ConcreteBuilder：实现接口，构建和装配各个部件</li><li>Director：构建一个使用Builder接口的对象，主要用于创建一个复杂的对象，主要两个作用：<ul><li>隔离了客户与对象的生产过程</li><li>负责控制产品对象的生产过程</li></ul></li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">House</span> </span>&#123;    <span class="hljs-keyword">private</span> String baise;    <span class="hljs-keyword">private</span> String wall;    <span class="hljs-keyword">private</span> String roofed;&#125;<span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HouseBuilder</span> </span>&#123;    <span class="hljs-keyword">protected</span> House house = <span class="hljs-keyword">new</span> House();    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">buildBasic</span><span class="hljs-params">()</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">buildWall</span><span class="hljs-params">()</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">roofed</span><span class="hljs-params">()</span></span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> House <span class="hljs-title">buildHouse</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> house;    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CommonHouse</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">HouseBuilder</span></span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">buildBasic</span><span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">"地基"</span>);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">buildWall</span><span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">"砌墙"</span>);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">roofed</span><span class="hljs-params">()</span> </span>&#123;        System.out.println(<span class="hljs-string">"封顶"</span>);    &#125;&#125;<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HouseDirector</span> </span>&#123;    HouseBuilder houseBuilder = <span class="hljs-keyword">null</span>;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">HouseDirector</span><span class="hljs-params">(HouseBuilder houseBuilder)</span></span>&#123;        <span class="hljs-keyword">this</span>.houseBuilder = houseBuilder;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setHouseBuilder</span><span class="hljs-params">(HouseBuilder houseBuilder)</span> </span>&#123;        <span class="hljs-keyword">this</span>.houseBuilder = houseBuilder;    &#125;    <span class="hljs-comment">//如何处理建房子的过程，交给指挥者</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> House <span class="hljs-title">constructHouse</span><span class="hljs-params">()</span></span>&#123;        houseBuilder.buildBasic();        houseBuilder.buildWall();        houseBuilder.roofed();        <span class="hljs-keyword">return</span> houseBuilder.buildHouse();    &#125;&#125;</code></pre></div><h2 id="应用-2"><a href="#应用-2" class="headerlink" title="应用"></a>应用</h2><p>StringBuilder：</p><div class="hljs"><pre><code class="hljs undefined">Appendable接口定义了多个<span class="hljs-built_in">append</span>方法（抽象方法）是抽象建造者。AbstractStringBuilder实现了Appendable接口方法，该类已经是建造者，只是不能实例化StringBuilder既充当了指挥者角色，同时充当了具体的建造者。</code></pre></div>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;单例模式&quot;&gt;&lt;a href=&quot;#单例模式&quot; class=&quot;headerlink&quot; title=&quot;单例模式&quot;&gt;&lt;/a&gt;单例模式&lt;/h1&gt;&lt;p&gt;采取一定的方法保证在整个软件系统中，对某个类只能存在一个对象实例，并且该类只提供一个取得其对象实例的方法（静态方法）。&lt;/p
      
    
    </summary>
    
      <category term="设计模式" scheme="https://l1n.wang/categories/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
    
      <category term="设计模式" scheme="https://l1n.wang/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>设计模式之设计原则（一）</title>
    <link href="https://l1n.wang/2020/05/design-principle/"/>
    <id>https://l1n.wang/2020/05/design-principle/</id>
    <published>2020-05-03T10:55:28.000Z</published>
    <updated>2020-05-03T11:04:44.109Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><h2 id="什么是设计模式？"><a href="#什么是设计模式？" class="headerlink" title="什么是设计模式？"></a>什么是设计模式？</h2><blockquote><p>每一个模式描述了一个在我们周围不断重复发生的问题，以及该问题的解决方案的核心，这样，你就能不必一次又一次地使用该方案而不必重复劳动。</p></blockquote><h2 id="深入理解面向对象"><a href="#深入理解面向对象" class="headerlink" title="深入理解面向对象"></a>深入理解面向对象</h2><p>向下：三大面向对象机制</p><ul><li>封装：隐藏内部实现</li><li>继承：复用现有代码</li><li>多态：改写对象行为</li></ul><p>现上：深刻把握面向对象机制带来的抽象意义，理解如何使用这些机制来表达现实世界，掌握什么是“好的面向对象设计”</p><h2 id="软件设计复杂的根本原因"><a href="#软件设计复杂的根本原因" class="headerlink" title="软件设计复杂的根本原因"></a>软件设计复杂的根本原因</h2><p>变化：</p><ul><li>客户需求的变化</li><li>技术平台的变化</li><li>开发团队的变化</li><li>市场环境的变化</li></ul><h2 id="如何解决复杂性？"><a href="#如何解决复杂性？" class="headerlink" title="如何解决复杂性？"></a>如何解决复杂性？</h2><p>分解</p><ul><li>人们面对复杂性有一个常见的作法：分而治之，将大问题分解为多个小问题，将复杂问题分解为多个简单问题。</li></ul><p>抽象</p><ul><li>更高层次来讲，人们处理复杂性有一个通用的计数，即抽象。由于不能掌握全部的复杂对象，我们选择忽视它的非本质细节，而去处理泛化和理想化了的对象模型。</li></ul><h1 id="设计原则"><a href="#设计原则" class="headerlink" title="设计原则"></a>设计原则</h1><h2 id="单一职责原则（SRP）"><a href="#单一职责原则（SRP）" class="headerlink" title="单一职责原则（SRP）"></a>单一职责原则（SRP）</h2><blockquote><p>一个接口仅负责一个职责，一个类只能由一个原因引起变化。</p></blockquote><p>优点：</p><ul><li>类的复杂性降低，实现什么职责都有清晰明确的定义</li><li>可读性提高</li><li>可维护性提高</li><li>变更引起的风险降低，变更时必不可少的，如果接口的单一职责做好，一个接口的修改只对相对应的实现类有影响。</li></ul><h2 id="里氏替换原则（LSP）"><a href="#里氏替换原则（LSP）" class="headerlink" title="里氏替换原则（LSP）"></a>里氏替换原则（LSP）</h2><blockquote><p>所有引用基类的地方必须能透明的使用其子类的对象。</p></blockquote><p>只要父类能出现的地方子类就可以出现，而且替换为子类也不会产生任何错误或异常。</p><ul><li>子类必须完全实现父类的方法</li><li>子类可以有自己的方法和属性</li><li>覆盖或实现父类的方法时输入参数可以被放大</li><li>覆写或实现父类的方法时输出结果可以被缩小</li></ul><h2 id="依赖倒置原则（DIP）"><a href="#依赖倒置原则（DIP）" class="headerlink" title="依赖倒置原则（DIP）"></a>依赖倒置原则（DIP）</h2><blockquote><ul><li>高层模块不应该依赖低层模块，两者都应该依赖其抽象</li><li>抽象不应该依赖细节</li><li>细节应该依赖抽象</li></ul></blockquote><p>在Java语言中的表现：</p><ul><li>模块间的依赖通过抽象发生，实现类之间不发生直接的依赖关系，其依赖关系是通过接口或抽象类产生的</li><li>接口或抽象类不依赖于实现类</li><li>实现类依赖接口或抽象类</li></ul><p>对象的依赖关系有三种方式来传递：</p><ul><li>构造函数传递依赖对象</li><li>Setter方法传递依赖对象</li><li>接口声明依赖对象</li></ul><p>如何实践？</p><ul><li>每个类尽量都有接口或抽象类，或者抽象类与接口两者都具备</li><li>变量的表面类型尽量是接口或者抽象类</li><li>任何类都不应该从具体类派生</li><li>尽量不要覆写基类的方法</li><li>结合里氏替换原则使用：接口负责定义public属性和方法，并且声明与其他对象的依赖关系，抽象类负责公共内部构造部分的实现，实现类准确的实现业务逻辑，统统实在适当的时候对父类进行细化。</li></ul><h2 id="接口隔离原则（ISP）"><a href="#接口隔离原则（ISP）" class="headerlink" title="接口隔离原则（ISP）"></a>接口隔离原则（ISP）</h2><blockquote><ul><li>客户端不应该依赖它不需要的接口</li><li>类间的依赖关系应该建立在最小的接口上</li></ul></blockquote><p>接口分为两种：</p><ul><li>实例接口：在Java中声明一个类，然后用new关键字产生一个实例，它就是对一个类型的事物的描述，这是一种接口</li><li>类接口：interface关键字定义的接口</li></ul><p>接口隔离原则对接口进行规范约束，包含以下四个含义：</p><ul><li>接口要尽量小</li><li>接口要高内聚，减少对外的交互（尽量少公布public方法）</li><li>定制服务（只提供访问者需要的方法）</li><li>接口设计是有限度的（粒度越小，系统越灵活，结构越复杂，开发难度增加，可维护性降低）</li></ul><p>如何实践？</p><ul><li>一个接口只服务于一个子模块或业务逻辑</li><li>通过业务逻辑压缩接口中的public方法</li><li>已经被污染的方法，尽量去修改，若变更风险较大，则采用适配器模式进行处理</li><li>了解环境，深入理解业务逻辑，结合实际。</li></ul><h2 id="迪米特法则（LoD）"><a href="#迪米特法则（LoD）" class="headerlink" title="迪米特法则（LoD）"></a>迪米特法则（LoD）</h2><blockquote><p>一个对象应该对其他对象有最少的了解。</p></blockquote><p>类和类关系越密切，耦合度越大。</p><p>也称为最少知识原则（LKP），通俗的讲：一个对象应该对自己需要耦合或者调用的类知道的最少，你的内部如何复杂和我都没有关系，我就知道你提供这么多public方法，我就调用这么多，其他我一概不关心。</p><p>迪米特法则对类的低耦合提出了明确的要求，其包含以下四层含义：</p><ul><li>只和朋友交流（朋友类：出现在成员变量，方法输入输出参数的类）。</li><li>朋友之间有距离的。</li><li>是自己的就是自己的（一个方法放在本类种，既不增加类间关系，也对本类不产生负面影响，那就放在本类之中）。</li><li>谨慎使用Serializable。</li></ul><p>迪米特法则的核心观念就是类间解耦，弱耦合，只有弱耦合之后，类的复用率才可以提高，要求的结果就是产生了大量中转或跳转类，导致系统复杂性提高，同时也为维护带来了难度。</p><h2 id="开闭原则（OCP）"><a href="#开闭原则（OCP）" class="headerlink" title="开闭原则（OCP）"></a>开闭原则（OCP）</h2><blockquote><p>一个软件实体如类，模块和函数应该对扩展开放，对修改关闭。</p></blockquote><ul><li>对扩展开放（提供方），对修改关闭（使用方）</li><li>用抽象构建框架，用实现扩展细节</li><li>一个软件实体应该通过扩展来实现变化，而不是通过修改已有的代码来实现变化。</li><li>遵循的所有原则，以及设计模式的目的就是遵循开闭原则。</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;h2 id=&quot;什么是设计模式？&quot;&gt;&lt;a href=&quot;#什么是设计模式？&quot; class=&quot;headerlink&quot; title=&quot;什么是设计模式？&quot;
      
    
    </summary>
    
      <category term="设计模式" scheme="https://l1n.wang/categories/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
    
      <category term="设计模式" scheme="https://l1n.wang/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>深入浅出MySQL索引</title>
    <link href="https://l1n.wang/2020/05/mysql-index/"/>
    <id>https://l1n.wang/2020/05/mysql-index/</id>
    <published>2020-05-01T03:23:33.000Z</published>
    <updated>2020-07-25T15:08:49.264Z</updated>
    
    <content type="html"><![CDATA[<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>维基百科对索引的定义：数据库索引是一种数据结构，它以额外的写入和存储空间为代价来提高数据库表上数据索引操作的速度。</p><p>MySQL官方对索引的定义是用于快速查找记录的一种数据结构。</p><ul><li>索引是物理数据页，数据页大小决定了一个页可以存储多少个索引行，以及需要多少页来存储指定大小的索引。</li><li>索引可以加快检索速度，也可以降低索引列插入、删除、更新的速度，索引维护需要代价。</li></ul><p>有两种基本的索引类型：</p><ul><li>顺序索引：基于值的顺序排序</li><li>散列索引：基于将值平均分布到若干bucket中，一个值所属的bucket是由一个散列函数决定。</li></ul><h2 id="InnoDB的索引模型"><a href="#InnoDB的索引模型" class="headerlink" title="InnoDB的索引模型"></a>InnoDB的索引模型</h2><p>MySQL中，索引再存储引擎层实现，所以不同的存储引擎层支持的索引类型可以不同，InnoDB和MyISAM的索引都是使用B+ Tree实现的。B+ Tree结构如下图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/index/B%2B%20tree.png" srcset="/img/loading.gif" alt="B+ Tree"></p><p>上图中页面号为20的页面是根页面，根页面存储了<key + pageno>，pageno存储指向叶子节点的页面号。叶子节点存放的是<key + data>，真正存放哪些数据还是取决于B+ Tree是聚簇索引还是辅助索引。</key></key></p><h3 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h3><p>在 MySQL InnoDB 中索引通常可以分为两大类：主键索引（即聚簇索引）和辅助索引（非聚簇索引） 。</p><ul><li>聚簇索引：表示表中的数据按照主键顺序存储，是索引组织表，在InnoDB中就是按照主键顺序构建B+ Tree，叶子节点就是行记录，数据行和主键值存储在一起。只能创建一个。</li><li>辅助索引（二级索引）：根据索引列构建B+ Tree，B+ Tree每一个叶子节点存索引列，加速回表操作。</li></ul><p>聚簇索引占用的空间就是整个表数据量的大小，而二级索引只是存储主键值，主要提高查询效率。</p><p>对于没有指定主键的表，InnoDB会自己选择合适的字段作为主键，选择顺序如下：</p><ol><li>显式主键</li><li>第一个唯一索引</li><li>内置的6字节ROWID</li></ol><p>根据索引列个数和功能描述不同索引也可以分为：联合索引和覆盖索引。</p><ul><li>联合索引是指在多个字段联合组建的。</li><li>当通过索引即可查询到所有记录，不需要回表到聚簇索引时，这类索引称为覆盖索引。</li><li>主键查询是天然的覆盖索引，联合索引也可以是覆盖索引。</li></ul><h3 id="覆盖索引"><a href="#覆盖索引" class="headerlink" title="覆盖索引"></a>覆盖索引</h3><p>如果执行的语句为<code>select ID from T where k between 3 and 5</code>，这时只需要查ID的值，而ID的值已经在k索引树上了，因此不需要回表，也就是，索引k已经覆盖了我们的我们的查询请求，这被称为<strong>覆盖索引</strong>。</p><p><strong>由于覆盖索引可以减少树的搜索次数，显著提升查询性能，所以使用覆盖索引是一个常用的性能优化手段。</strong></p><h2 id="索引使用技巧"><a href="#索引使用技巧" class="headerlink" title="索引使用技巧"></a>索引使用技巧</h2><h3 id="创建索引规范"><a href="#创建索引规范" class="headerlink" title="创建索引规范"></a>创建索引规范</h3><ul><li>命名规范</li><li>单张表索引数量不超过5个，单个索引的字段不超过5个</li><li>禁止冗余索引，重复索引，索引维护需要成本，新增索引时优先考虑基于现有索引进行rebuild</li><li>联表查询，JOIN列的数据类型必须相同，并且要建立索引</li><li>不在低基数列创建索引（性别列）</li><li>选择区分度大的列（最好唯一性）建立索引</li><li>对过长的VARCHAR段建立索引，优先考虑前缀索引</li><li>频繁作为WHERE查询条件的字段</li><li>经常GROUP BY和ORDER BY的列</li><li>UPDATE、DELETE 的 WHERE 条件列，一般也需要创建索引</li><li>DISTINCT 字段需要创建索引</li></ul><p>索引失效的情况：</p><ul><li>索引进行表达式计算</li><li>索引字段使用函数</li><li>在WHERE子句中，如果在OR前面的条件列进行了索引，后面的没有进行索引，那么索引会失效</li><li>当我们使用 LIKE 进行模糊查询的时候，前面不能是 %</li><li>索引列与 NULL 或者 NOT NULL 进行判断的时候也会失效。</li></ul><h3 id="索引维护"><a href="#索引维护" class="headerlink" title="索引维护"></a>索引维护</h3><p>B+树在插入新值的时候必须做必要的维护，如果添加新值的时候，所在数据页已经满了，这时需要申请一个新的数据页，然后挪动部分数据过去，这个过程称为<strong>页分裂</strong>。如果删除数据，可能导致页的合并。</p><h3 id="最左前缀原则"><a href="#最左前缀原则" class="headerlink" title="最左前缀原则"></a>最左前缀原则</h3><p>B+树这种索引结构，可以利用索引的最左前缀，来定位记录。</p><p>如果你创建了一个三列的联合索引包含(col1, col2, col3), 你的索引会生效于(col1), (col1, col2), 以及(col1, col2, col3)。</p><p>问题：在建立联合索引的时候，如何安排索引内的字段顺序？</p><ul><li>第一原则是，如果通过调整顺序，可以少维护一个索引，那么这个顺序往往是需要优先考虑采用的。</li></ul><p>如果有一个联合索引(a,b)，查询条件里面如果只有b的语句，是无法使用这个联合索引的。</p><p>这时候需要考虑的原则就是空间。</p><h3 id="索引下推"><a href="#索引下推" class="headerlink" title="索引下推"></a>索引下推</h3><p>以联合索引(name, age)为例，如果查找”名字第一个字是张，年龄是10岁的所有男孩”，SQL语句如下：</p><div class="hljs"><pre><code class="hljs sql"><span class="hljs-keyword">select</span> * <span class="hljs-keyword">from</span> <span class="hljs-keyword">user</span> <span class="hljs-keyword">where</span> <span class="hljs-keyword">name</span> <span class="hljs-keyword">like</span> <span class="hljs-string">'张%'</span> <span class="hljs-keyword">and</span> age = <span class="hljs-number">10</span> <span class="hljs-keyword">and</span> ismale = <span class="hljs-number">1</span>;</code></pre></div><p>根据最左前缀原则，找到第一个满足第一个字是张的记录之后，判断其他条件是否满足。</p><p>在MySQL 5.6之前，只能从该记录开始一个个回表，到主键索引树上找到指定数据行，进行字段值比对。</p><p>在MySQL5.6引入<strong>索引下推优化</strong>，可以在索引遍历的过程中，对索引中包含的字段先进行判断，直接过滤掉不满足条件的记录，减少回表次数。</p><h2 id="索引原理"><a href="#索引原理" class="headerlink" title="索引原理"></a>索引原理</h2><h3 id="B-Tree"><a href="#B-Tree" class="headerlink" title="B Tree"></a>B Tree</h3><p>查询的时间主要依赖于磁盘I/O的次数，每次节点访问需要进行一次磁盘IO操作。<br>B Tree取代平衡二叉树主要是降低了树的高度，减少了磁盘IO的次数。其基本结构如下：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/index/B%20Tree.jpg" srcset="/img/loading.gif" alt="B Tree"></p><p>B Tree别称平衡的多路搜索树，每个节点最多包括M个子节点，M称为B树的阶。</p><p>M阶的B树（M &gt; 2）有以下的特性：</p><ul><li>根节点的儿子数的范围是 [2,M]。</li><li>每个中间节点包含 k-1 个关键字和 k 个孩子，孩子的数量 = 关键字的数量 +1，k 的取值范围为 [ceil(M/2), M]。</li><li>叶子节点包括 k-1 个关键字（叶子节点没有孩子），k 的取值范围为 [ceil(M/2), M]。</li><li>假设中间节点节点的关键字为：Key[1], Key[2], …, Key[k-1]，且关键字按照升序排序，即 Key[i]&lt;Key[i+1]。此时 k-1 个关键字相当于划分了 k 个范围，也就是对应着 k 个指针，即为：P[1], P[2], …, P[k]，其中 P[1] 指向关键字小于 Key[1] 的子树，P[i] 指向关键字属于 (Key[i-1], Key[i]) 的子树，P[k] 指向关键字大于 Key[k-1] 的子树。</li><li>所有叶子节点位于同一层。</li></ul><p>B树的查找方式类似于平衡二叉树，在此不多赘述。</p><h3 id="B-Tree-1"><a href="#B-Tree-1" class="headerlink" title="B+ Tree"></a>B+ Tree</h3><p>B+ Tree与B Tree的差异主要有以下几点：</p><ol><li>有 k 个孩子的节点就有 k 个关键字。也就是孩子数量 = 关键字数，而 B 树中，孩子数量 = 关键字数 +1。</li><li>非叶子节点的关键字也会同时存在在子节点中，并且是在子节点中所有关键字的最大（或最小）。</li><li>非叶子节点仅用于索引，不保存数据记录，跟记录有关的信息都放在叶子节点中。而 B 树中，非叶子节点既保存索引，也保存数据记录。</li><li>所有关键字都在叶子节点出现，叶子节点构成一个有序链表，而且叶子节点本身按照关键字的大小从小到大顺序链接。</li></ol><p>其基本结构如下图所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/index/B%2BTree.jpg" srcset="/img/loading.gif" alt="B+Tree"></p><p>B+ Tree与B Tree的根本的差异在于B+ Tree的中间节点不直接存储数据。</p><ol><li>B+ Tree更矮胖，IO次数更少，同样的磁盘页大小， B+ Tree可以存储更多的节点关键字。</li><li>B+ Tree查询效率更稳定，每次只有访问到叶子节点才能获取到对应的数据。</li><li>范围查询的效率高于B Tree，因为所有的关键字都出现在B+ Tree的叶子节点中，并通过有序链表进行连接。</li></ol><h3 id="Hash索引"><a href="#Hash索引" class="headerlink" title="Hash索引"></a>Hash索引</h3><p>与B+ Tree的区别：</p><ul><li>Hash索引不能进行范围查询，因为Hash索引指向的数据是无序的。</li><li>Hash索引不支持联合索引的最左侧原则，因为Hash索引计算Hash的时候是将索引键合并后再一起计算。</li><li>Hash索引不支持ORDER BY排序，因为Hash索引指向的数据是无序的，因此无法做到排序优化的作用</li><li>无法进行模糊查询。</li><li>等值查询效率高</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;基本概念&quot;&gt;&lt;a href=&quot;#基本概念&quot; class=&quot;headerlink&quot; title=&quot;基本概念&quot;&gt;&lt;/a&gt;基本概念&lt;/h2&gt;&lt;p&gt;维基百科对索引的定义：数据库索引是一种数据结构，它以额外的写入和存储空间为代价来提高数据库表上数据索引操作的速度。&lt;/p&gt;

      
    
    </summary>
    
      <category term="MySQL" scheme="https://l1n.wang/categories/MySQL/"/>
    
    
      <category term="MySQL" scheme="https://l1n.wang/tags/MySQL/"/>
    
  </entry>
  
  <entry>
    <title>探索MySQL的事务与锁机制</title>
    <link href="https://l1n.wang/2020/04/mysql-transaction/"/>
    <id>https://l1n.wang/2020/04/mysql-transaction/</id>
    <published>2020-04-29T01:47:16.000Z</published>
    <updated>2020-07-26T13:47:59.467Z</updated>
    
    <content type="html"><![CDATA[<h1 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h1><p>简单来说，事务就是要保证一组数据库操作，要么全部成功，要么全部失败。MySQL中事务支持是在引擎层实现的。事务拥有四个重要的特性：原子性、一致性、隔离性、持久性，简称为ACID特性，下文将逐一解释。</p><h2 id="ACID特性"><a href="#ACID特性" class="headerlink" title="ACID特性"></a>ACID特性</h2><ul><li>原子性（Atomicity）<ul><li>事务开始后所有操作，要么全部完成，要么全部不做，不存在只执行一部分的情况。</li></ul></li><li>一致性（Consistency）<ul><li>事务开始之前和事务结束之后，数据库的完整性限制未被破坏。</li></ul></li><li>隔离性（Isolation）<ul><li>在一个事务未执行完毕时，其它事务无法读取该事务的数据。</li><li>MySQL通过锁机制来保证事务的隔离性。</li></ul></li><li>持久性（Durability）<ul><li>事务一旦提交，数据将被保存下来，即使发生宕机等故障，数据库也能将数据恢复。</li><li>MySQL使用<code>redo log</code>来保证事务的持久性。</li></ul></li></ul><p>ACID关系如下图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/structure/%E4%BA%8B%E5%8A%A1%E7%89%B9%E6%80%A7.png" srcset="/img/loading.gif" alt="ACID关系"></p><ul><li>一致性分为约束一致性和数据一致性：<ul><li>约束一致性：创建表结构时指定的外键，Check，唯一索引等约束，MySQL不支持Check</li><li>数据一致性：由原子性、持久性、隔离性共同保证的结果</li></ul></li><li>原子性：通过<code>Write Ahead Log</code>（WAL）技术实现。</li><li>持久性：一旦事务提交，通过原子性，即便是遇到宕机，也可以从逻辑上将数据找回来后再次写入物理存储空间，这样就从逻辑和物理两个方面保证了数据不会丢失</li><li>隔离性：一个事务执行不能被其它事务干扰，锁和多版本控制符合隔离性。</li></ul><h2 id="并发事务控制"><a href="#并发事务控制" class="headerlink" title="并发事务控制"></a>并发事务控制</h2><h3 id="单版本控制-锁"><a href="#单版本控制-锁" class="headerlink" title="单版本控制-锁"></a>单版本控制-锁</h3><p>锁用独占的方式保证只有一个版本的情况下事务相互隔离。</p><h2 id="多版本控制-MVCC"><a href="#多版本控制-MVCC" class="headerlink" title="多版本控制-MVCC"></a>多版本控制-MVCC</h2><p>MVCC（Multi-Version Concurrency Control）即多版本并发控制。</p><p>MVCC 是通过数据行的多个版本管理来实现数据库的并发控制，简单来说它的思想就是保存数据的历史版本。这样我们就可以通过比较版本号决定数据是否显示出来。读取数据的时候不需要加锁也可以保证事务的隔离效果。</p><p>每次对数据库的修改，都会在Undo日志中记录当前修改记录的事务版本号以及修改前数据状态的存储地址，以便在必要的时候可以回滚到老的数据版本。</p><p>MVCC的核心是Undo Log + Read View。</p><p>MVCC主要解决以下几个问题：</p><ul><li>读写之间阻塞的问题，通过MVCC可以让读写互相不阻塞，提高并发处理能力。</li><li>降低了死锁的概率，MVCC采用了乐观锁的方式，读取数据时不需要加锁，对于写操作，只锁定必要的行。</li><li>解决一致性读的问题。一致性读也被称为快照读，当我们查询数据库在某个时间点的快照时，只能看到这个时间点之前事务提交更新的结果，而不能看到这个时间点之后事务提交的更新结果。</li></ul><p>MVCC最大的好处是读不加锁，读写不冲突，在MVCC中，读操作可分为快照读（Snapshot Read）和当前读（Current Read）。</p><ul><li>快照读：读取的是记录的可见版本，不用加锁</li><li>当前读：读取的是记录的最新版本，并且当前读返回的记录，都会加锁，保证其它事务不会并发修改这条记录。</li></ul><div class="hljs"><pre><code class="hljs SQL"><span class="hljs-comment"># 不加锁的SELECT都是快照读</span><span class="hljs-keyword">SELECT</span> * <span class="hljs-keyword">FROM</span> player <span class="hljs-keyword">WHERE</span> ...<span class="hljs-comment"># 加锁的SELECT和增删改都是用当前读</span><span class="hljs-keyword">SELECT</span> * <span class="hljs-keyword">FROM</span> player <span class="hljs-keyword">LOCK</span> <span class="hljs-keyword">IN</span> <span class="hljs-keyword">SHARE</span> <span class="hljs-keyword">MODE</span>;<span class="hljs-keyword">INSERT</span> <span class="hljs-keyword">INTO</span> player <span class="hljs-keyword">values</span> ...</code></pre></div><p>MVCC只在<code>Read Commited</code>和<code>Repeatable Read</code>两种级别下工作。核心是Undo Log + Read View。</p><h3 id="Read-VIew"><a href="#Read-VIew" class="headerlink" title="Read VIew"></a>Read VIew</h3><p>在 MVCC 机制中，多个事务对同一个行记录进行更新会产生多个历史快照，这些历史快照保存在 Undo Log 里。而Read View保存了不应该让这个事务看到的其他的事务ID列表。在<code>Read Commited</code>级别下，每一次SELECT查询都会获取一次Read View。如果两次读取的Read View不同，就会产生不可重复读和幻读的情况。</p><h2 id="并发事务问题"><a href="#并发事务问题" class="headerlink" title="并发事务问题"></a>并发事务问题</h2><h3 id="丢失更新"><a href="#丢失更新" class="headerlink" title="丢失更新"></a>丢失更新</h3><p><strong>丢失更新</strong>就是两个事务在并发下同时进行更新，后一个事务的更新覆盖了前一个事务更新的情况。基本情景如下表：</p><table><thead><tr><th style="text-align:center">时间</th><th style="text-align:center">事务A</th><th style="text-align:center">事务B</th></tr></thead><tbody><tr><td style="text-align:center">1</td><td style="text-align:center">开启事务</td><td style="text-align:center">开启事务</td></tr><tr><td style="text-align:center">2</td><td style="text-align:center">a = 100</td><td style="text-align:center">a = 100</td></tr><tr><td style="text-align:center">3</td><td style="text-align:center">/</td><td style="text-align:center">a -= 10</td></tr><tr><td style="text-align:center">4</td><td style="text-align:center">/</td><td style="text-align:center">commit</td></tr><tr><td style="text-align:center">5</td><td style="text-align:center">/</td><td style="text-align:center">/</td></tr><tr><td style="text-align:center">6</td><td style="text-align:center">a += 20</td><td style="text-align:center">/</td></tr><tr><td style="text-align:center">7</td><td style="text-align:center">commit</td><td style="text-align:center">/</td></tr></tbody></table><h3 id="脏读"><a href="#脏读" class="headerlink" title="脏读"></a>脏读</h3><p>一个事务读取了另一个未提交的事务写的数据，被称为脏读。基本情景如下：</p><table><thead><tr><th style="text-align:center">时间</th><th style="text-align:center">事务A</th><th style="text-align:center">事务B</th></tr></thead><tbody><tr><td style="text-align:center">1</td><td style="text-align:center">开启事务</td><td style="text-align:center">开启事务</td></tr><tr><td style="text-align:center">2</td><td style="text-align:center">a = 100</td><td style="text-align:center">a = 100</td></tr><tr><td style="text-align:center">3</td><td style="text-align:center">/</td><td style="text-align:center">a = 110</td></tr><tr><td style="text-align:center">4</td><td style="text-align:center">a = 110</td><td style="text-align:center">/</td></tr><tr><td style="text-align:center">5</td><td style="text-align:center">/</td><td style="text-align:center">rollback</td></tr></tbody></table><h3 id="不可重复读"><a href="#不可重复读" class="headerlink" title="不可重复读"></a>不可重复读</h3><p>一个事务读取到另一个事务已经提交的修改数据。基本情景如下：</p><table><thead><tr><th style="text-align:center">时间</th><th style="text-align:center">事务A</th><th style="text-align:center">事务B</th></tr></thead><tbody><tr><td style="text-align:center">1</td><td style="text-align:center">开启事务</td><td style="text-align:center">开启事务</td></tr><tr><td style="text-align:center"></td><td style="text-align:center">a = 100</td><td style="text-align:center">a = 100</td></tr><tr><td style="text-align:center">3</td><td style="text-align:center">/</td><td style="text-align:center">a = 110</td></tr><tr><td style="text-align:center">4</td><td style="text-align:center">a = 110</td><td style="text-align:center">commit</td></tr></tbody></table><h3 id="幻读"><a href="#幻读" class="headerlink" title="幻读"></a>幻读</h3><p>一个事务按查询条件检索数据，发现其它事务插入了满足条件的数据，这被称为幻读。基本情景如下：</p><table><thead><tr><th style="text-align:center">时间</th><th style="text-align:center">事务A</th><th style="text-align:center">事务B</th></tr></thead><tbody><tr><td style="text-align:center">1</td><td style="text-align:center">开启事务</td><td style="text-align:center">开启事务</td></tr><tr><td style="text-align:center">2</td><td style="text-align:center">select * from table where id = 1(记录为0条)</td><td style="text-align:center">/</td></tr><tr><td style="text-align:center">3</td><td style="text-align:center">/</td><td style="text-align:center">insert into table (id) value(1)</td></tr><tr><td style="text-align:center">4</td><td style="text-align:center">/</td><td style="text-align:center">commit</td></tr><tr><td style="text-align:center">5</td><td style="text-align:center">insert into table (id) value(1)</td><td style="text-align:center">/</td></tr><tr><td style="text-align:center">6</td><td style="text-align:center">commit(报错，主键冲突)</td><td style="text-align:center">/</td></tr></tbody></table><h2 id="隔离级别"><a href="#隔离级别" class="headerlink" title="隔离级别"></a>隔离级别</h2><p>由于数据库在并发事务中带来一些问题，数据库提供了事务隔离机制来解决相对应的问题。数据库的锁也是为了构建这些隔离级别而存在的。</p><table><thead><tr><th>隔离级别</th><th>脏读（Dirty Read）</th><th>不可重复读（NonRepeatable Read）</th><th>幻读（Phantom Read）</th></tr></thead><tbody><tr><td>未提交读(Read uncommited)</td><td>可能</td><td>可能</td><td>可能</td></tr><tr><td>已提交读(Read commited)</td><td>不可能</td><td>可能</td><td>可能</td></tr><tr><td>可重复读(Repeatable read)</td><td>不可能</td><td>不可能</td><td>可能</td></tr><tr><td>可串行化(Serializable)</td><td>不可能</td><td>不可能</td><td>不可能</td></tr></tbody></table><ul><li>未提交读(Read Uncommitted)：一个事务还没提交时，它的修改能被别的事务看到。</li><li>提交读(Read Committed)：一个事务提交后，它的修改才能被其它事务看到。</li><li>可重复读(Repeated Read)：一个事务执行过程中看到的数据，总是跟这个事务开始时刻是一致的。</li><li>可串行化(Serializable)：对于同一行记录，写会加写锁，读会加读锁，当出现读写冲突的时候，后面的事务必须等待前一个事务执行完成才能继续执行。</li></ul><p>查询与修改隔离级别的SQL语句：</p><div class="hljs"><pre><code class="hljs undefined"><span class="hljs-comment">-- 查看系统隔离级别：</span><span class="hljs-keyword">select</span> @@global.tx_isolation;<span class="hljs-comment">-- 查看当前会话隔离级别</span><span class="hljs-keyword">select</span> @@tx_isolation;<span class="hljs-comment">-- 设置当前会话隔离级别</span><span class="hljs-keyword">SET</span> <span class="hljs-keyword">session</span> <span class="hljs-keyword">TRANSACTION</span> <span class="hljs-keyword">ISOLATION</span> <span class="hljs-keyword">LEVEL</span> <span class="hljs-keyword">serializable</span>;<span class="hljs-comment">-- 设置全局系统隔离级别</span><span class="hljs-keyword">SET</span> <span class="hljs-keyword">GLOBAL</span> <span class="hljs-keyword">TRANSACTION</span> <span class="hljs-keyword">ISOLATION</span> <span class="hljs-keyword">LEVEL</span> <span class="hljs-keyword">READ</span> UNCOMMITTED;</code></pre></div><h2 id="ACID的实现"><a href="#ACID的实现" class="headerlink" title="ACID的实现"></a>ACID的实现</h2><h3 id="原子性的实现"><a href="#原子性的实现" class="headerlink" title="原子性的实现"></a>原子性的实现</h3><p>每一个写事务，都会修改Buffer Pool，并产生对应的Redo日志，Redo日志以Write Ahead Log方式写，如果不写日志，数据库宕机恢复后，事务无法回滚，无法保证原子性。</p><h3 id="持久性的实现"><a href="#持久性的实现" class="headerlink" title="持久性的实现"></a>持久性的实现</h3><p>通过WAL可以保证逻辑上的持久性，物理上的持久性通过存储引擎的数据刷盘实现。</p><h3 id="隔离性的实现"><a href="#隔离性的实现" class="headerlink" title="隔离性的实现"></a>隔离性的实现</h3><p>通常用Read View表示一个事务的可见性，读提交状态每一条读操作语句都会获得一次Read View，每次更新都会获取最新事务的提交状态，即每条语句执行都会更新其可见性视图。可重复读的隔离级别下，可见性视图只有在自己当前事务提交之后，才会更新，所以与其他事务没有关系。</p><p><strong>在MySQL中，实际上每条记录在更新的时候都会同时记录一条回滚操作。记录上的最新值，通过回滚操作，可以得到之前一个状态的值</strong>。假设一个值从1被按照顺序改成了2，3，4，在回滚日志中会有类似下面的记录。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/transaction/1.jpg" srcset="/img/loading.gif" alt="记录"></p><p>当前值是4，但是在查询这条记录的时候，不同时刻启动的事务会有不同的<code>read-view</code>，在视图A、B、C中，记录的值分别为1、2、4。同一条记录在系统中可以存在多个版本，就是数据库的多版本并发控制（MVCC ）。对于<code>read-view A</code>要得到1，就必须将当前值依次执行图中所有的回滚操作得到。</p><p>即使现在有另外一个事务正在将 4 改成 5，这个事务跟 <code>read-view A、B、C</code>对应的事务是不会冲突的。</p><h3 id="一致性的实现"><a href="#一致性的实现" class="headerlink" title="一致性的实现"></a>一致性的实现</h3><p>一致性是通过其它三个特性来保证的。而其它三个特性由Redo、Undo来保证的。</p><h1 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h1><p>在MySQL中有三种级别的锁：页级锁，表级锁，行级锁。其中表锁包括读锁和写锁。</p><ul><li>表级锁：开销小，加锁快，不会出现死锁，锁定粒度大，发生锁冲突的概率最高，并发度最低，会发生在：MyISAM、memory、InnoDB、BDB 等存储引擎中。</li><li>行级锁：开销大，加锁慢，会出现死锁，锁定粒度最小，发生锁冲突的咖概率最低，并发度最高，会发生在：InnoDB存储引擎</li><li>页级锁：开销和加锁时间介于表锁和行锁之间，会出现死锁，锁定粒度介于表锁和行锁之间，并发度一般，会发生在BDB存储引擎中。</li></ul><h2 id="InnoDB的锁"><a href="#InnoDB的锁" class="headerlink" title="InnoDB的锁"></a>InnoDB的锁</h2><p>InnoDB中，锁分为行锁和表锁，行锁包括两种锁。</p><ul><li>共享锁（S）：共享锁锁定的资源可以被其它用户读取，但不能修改，在进行SELECT的时候，会将对象进行共享锁锁定，数据读取完毕，释放共享锁，保证在读取的过程中不被修改。</li><li>排他锁（X）：锁定的数据只允许进行锁定操作的事务使用，其它事务无法对已锁定的数据进行查询和修改</li></ul><div class="hljs"><pre><code class="hljs SQL"><span class="hljs-comment"># 给product_comment加上共享锁</span><span class="hljs-keyword">LOCK</span> <span class="hljs-keyword">TABLE</span> product_comment <span class="hljs-keyword">READ</span>;<span class="hljs-comment"># 解锁</span><span class="hljs-keyword">UNLOCK</span> <span class="hljs-keyword">TABLE</span>;<span class="hljs-comment"># 对 user_id=912178 的数据行加上共享锁</span><span class="hljs-keyword">SELECT</span> comment_id, product_id, comment_text, user_id <span class="hljs-keyword">FROM</span> product_comment <span class="hljs-keyword">WHERE</span> user_id = <span class="hljs-number">912178</span> <span class="hljs-keyword">LOCK</span> <span class="hljs-keyword">IN</span> <span class="hljs-keyword">SHARE</span> <span class="hljs-keyword">MODE</span><span class="hljs-comment"># 给 product_comment 数据表添加排它锁</span><span class="hljs-keyword">LOCK</span> <span class="hljs-keyword">TABLE</span> product_comment WRITE;<span class="hljs-comment"># 解锁</span><span class="hljs-keyword">UNLOCK</span> <span class="hljs-keyword">TABLE</span>;<span class="hljs-comment"># 对 user_id=912178 的数据行加上排他锁</span><span class="hljs-keyword">SELECT</span> comment_id, product_id, comment_text, user_id <span class="hljs-keyword">FROM</span> product_comment <span class="hljs-keyword">WHERE</span> user_id = <span class="hljs-number">912178</span> <span class="hljs-keyword">FOR</span> <span class="hljs-keyword">UPDATE</span>;</code></pre></div><p>InnoDB为了允许行锁和表锁共存，实现多粒度锁机制，InnoDB还有两种内部使用的意向锁，也都是表锁，表锁分为三种：</p><ul><li>意向共享锁（IS）：事务计划给数据行加行共享锁，事务在给一个数据行加共享锁前必须先取得该表的IS锁。</li><li>意向排他锁（IX）：事务打算给数据行加行排他锁，事务在给一个数据行加排他锁前必须先取得该表的IX锁。</li><li>自增锁（AUTO-INC Locks）：特殊表锁，自增长计数器通过该“锁”来获得子增长计数器最大的计数值。自增主键会涉及自增锁，在INSERT结束后立即释放。</li></ul><p>意向锁，简单来说就是给更大一级别的空间示意里面是否已经上过锁。举个例子，你可以给整个房子设置一个标识，告诉它里面有人，即使你只是获取了房子中某一个房间的锁。这样其他人如果想要获取整个房子的控制权，只需要看这个房子的标识即可，不需要再对房子中的每个房间进行查找。</p><p>如果事务想要获得数据表中某些记录的共享锁，就需要在数据表上添加意向共享锁。同理，事务想要获得数据表中某些记录的排他锁，就需要在数据表上添加意向排他锁。这时，意向锁会告诉其他事务已经有人锁定了表中的某些记录，不能对整个表进行全表扫描。</p><p>在加行锁之前必须先获得表级意向锁，否则等待 innodb_lock_wait_timeout 超时后根据innodb_rollback_on_timeout 决定是否回滚事务。</p><p>锁关系矩阵如下图所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/transaction/%E9%94%81%E5%85%BC%E5%AE%B9%E7%9F%A9%E9%98%B5.png" srcset="/img/loading.gif" alt="InnoDB锁关系矩阵"></p><p>InnoDB行锁是通过对索引数据页上的记录加锁实现的，主要有三种算法：</p><ul><li>Record锁：单个行记录的锁（锁数据，不锁Gap）</li><li>Gap锁：间隙锁，锁定一个范围，不包括记录本身（不锁数据，锁数据前面的Gap）</li><li>Next-key锁：锁数据并且锁Gap，可以解决幻读问题。</li></ul><h3 id="InnoDB死锁"><a href="#InnoDB死锁" class="headerlink" title="InnoDB死锁"></a>InnoDB死锁</h3><p>由于InnoDB是逐行加锁的，极容易产生死锁，产生死锁的四个条件：</p><ul><li>互斥条件：一个资源每次只能被一个进程使用。</li><li>请求与保持条件：一个进程因请求资源而阻塞时，对已获得的资源保持不放。</li><li>不剥夺条件：进程以获得的资源，在没有使用完之前，不能强行剥夺。</li><li>循环等待条件：多个进程之前形成的一种互相循环等待资源的关系。</li></ul><p>在发生死锁时，InnoDB 存储引擎会自动检测，并且会自动回滚代价较小的事务来解决死锁问题。但很多时候一旦发生死锁，InnoDB 存储引擎的处理的效率是很低下的或者有时候根本解决不了问题，需要人为手动去解决。</p><p>避免死锁产生的建议：</p><ul><li>更新SQL的where条件尽量用索引</li><li>加锁索引准确，缩小锁定范围</li><li>减少范围更新，尤其非主键/非唯一索引的范围更新。</li><li>控制事务大小，减少锁定数据量和锁定时间长度</li><li>加锁顺序一致，尽可能一次性锁定所有所需的数据行。</li></ul><h3 id="InnoDB如何解决幻读的"><a href="#InnoDB如何解决幻读的" class="headerlink" title="InnoDB如何解决幻读的"></a>InnoDB如何解决幻读的</h3><p>在可重复读的情况下，InnoDB可以通过<code>Next-key Lock</code>和<code>MVCC</code>解决幻读问题。</p><p>在读已提交的情况下，即使使用MVCC方式也会出现幻读。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;事务&quot;&gt;&lt;a href=&quot;#事务&quot; class=&quot;headerlink&quot; title=&quot;事务&quot;&gt;&lt;/a&gt;事务&lt;/h1&gt;&lt;p&gt;简单来说，事务就是要保证一组数据库操作，要么全部成功，要么全部失败。MySQL中事务支持是在引擎层实现的。事务拥有四个重要的特性：原子性、一
      
    
    </summary>
    
      <category term="MySQL" scheme="https://l1n.wang/categories/MySQL/"/>
    
    
      <category term="MySQL" scheme="https://l1n.wang/tags/MySQL/"/>
    
  </entry>
  
  <entry>
    <title>ThreadLocalRondom原理剖析</title>
    <link href="https://l1n.wang/2020/04/threadlocal-rondom/"/>
    <id>https://l1n.wang/2020/04/threadlocal-rondom/</id>
    <published>2020-04-28T09:31:07.000Z</published>
    <updated>2020-05-09T09:16:32.992Z</updated>
    
    <content type="html"><![CDATA[<p>ThreadLocalRondom是JDK 7在并发包中新增的随机数生成器，该类弥补了Random类在并发环境下的缺陷。</p><a id="more"></a><h2 id="Random的局限性"><a href="#Random的局限性" class="headerlink" title="Random的局限性"></a>Random的局限性</h2><p>Random生成随机数的方法如下：</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">nextInt</span><span class="hljs-params">(<span class="hljs-keyword">int</span> bound)</span> </span>&#123;    <span class="hljs-comment">//参数校验</span>        <span class="hljs-keyword">if</span> (bound &lt;= <span class="hljs-number">0</span>)            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(BadBound);<span class="hljs-comment">//根据老的种子生成新的种子</span>        <span class="hljs-keyword">int</span> r = next(<span class="hljs-number">31</span>);        <span class="hljs-keyword">int</span> m = bound - <span class="hljs-number">1</span>;    <span class="hljs-comment">//根据新种子计算随机数</span>        <span class="hljs-keyword">if</span> ((bound &amp; m) == <span class="hljs-number">0</span>)            r = (<span class="hljs-keyword">int</span>)((bound * (<span class="hljs-keyword">long</span>)r) &gt;&gt; <span class="hljs-number">31</span>);        <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> u = r;                 u - (r = u % bound) + m &lt; <span class="hljs-number">0</span>;                 u = next(<span class="hljs-number">31</span>))                ;        &#125;        <span class="hljs-keyword">return</span> r;    &#125;</code></pre></div><p>next方法通过计算生成新的种子。用原子变量来存放种子，多线程的情况下，CAS操作会保证只有一个线程可以更新老的种子为新种子，更新失败的线程进行自旋，这降低了并发性能，所以产生了ThreadLocalRandom。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">int</span> <span class="hljs-title">next</span><span class="hljs-params">(<span class="hljs-keyword">int</span> bits)</span> </span>&#123;        <span class="hljs-keyword">long</span> oldseed, nextseed;        AtomicLong seed = <span class="hljs-keyword">this</span>.seed;        <span class="hljs-keyword">do</span> &#123;            oldseed = seed.get();            <span class="hljs-comment">//seed计算公式，通过CAS操作进行更新</span>            nextseed = (oldseed * multiplier + addend) &amp; mask;        &#125; <span class="hljs-keyword">while</span> (!seed.compareAndSet(oldseed, nextseed));    <span class="hljs-comment">//将得到的值进行逻辑右移</span>        <span class="hljs-keyword">return</span> (<span class="hljs-keyword">int</span>)(nextseed &gt;&gt;&gt; (<span class="hljs-number">48</span> - bits));    &#125;</code></pre></div><h2 id="ThreadLocalRandom简介"><a href="#ThreadLocalRandom简介" class="headerlink" title="ThreadLocalRandom简介"></a>ThreadLocalRandom简介</h2><p>ThreadLocalRandom和ThreadLocal的原理相似，ThreadLocalRandom使得每个线程都维护自己独有的种子变量，这样就不存在竞争问题，大大提高并发性能。</p><h2 id="current方法"><a href="#current方法" class="headerlink" title="current方法"></a>current方法</h2><p>在current方法中，获得ThreadLocalRandom实例并初始化。seed不再是一个AtomicLong变量，在Thread类中有三个变量。</p><ul><li>threadLocalRandomSeed：使用它来控制随机数种子。</li><li>threadLocalRandomProbe：使用它来控制初始化。</li><li>threadLocalRandomSecondarySeed：二级种子。</li></ul><p>这三个变量都加了<code>sun.misc.Contended</code>注解，用来避免伪共享问题。</p><div class="hljs"><pre><code class="hljs java">   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> ThreadLocalRandom <span class="hljs-title">current</span><span class="hljs-params">()</span> </span>&#123;       <span class="hljs-comment">//判断是否初始化</span>       <span class="hljs-keyword">if</span> (UNSAFE.getInt(Thread.currentThread(), PROBE) == <span class="hljs-number">0</span>)           <span class="hljs-comment">//进行初始化</span>           localInit();       <span class="hljs-keyword">return</span> instance;   &#125;<span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">localInit</span><span class="hljs-params">()</span> </span>&#123;       <span class="hljs-keyword">int</span> p = probeGenerator.addAndGet(PROBE_INCREMENT);       <span class="hljs-keyword">int</span> probe = (p == <span class="hljs-number">0</span>) ? <span class="hljs-number">1</span> : p; <span class="hljs-comment">// skip 0</span>       <span class="hljs-keyword">long</span> seed = mix64(seeder.getAndAdd(SEEDER_INCREMENT));       Thread t = Thread.currentThread();       UNSAFE.putLong(t, SEED, seed);       UNSAFE.putInt(t, PROBE, probe);   &#125;<span class="hljs-comment">/** The current seed for a ThreadLocalRandom */</span>   <span class="hljs-meta">@sun</span>.misc.Contended(<span class="hljs-string">"tlr"</span>)   <span class="hljs-keyword">long</span> threadLocalRandomSeed;   <span class="hljs-comment">/** Probe hash value; nonzero if threadLocalRandomSeed initialized */</span>   <span class="hljs-meta">@sun</span>.misc.Contended(<span class="hljs-string">"tlr"</span>)   <span class="hljs-keyword">int</span> threadLocalRandomProbe;   <span class="hljs-comment">/** Secondary seed isolated from public ThreadLocalRandom sequence */</span>   <span class="hljs-meta">@sun</span>.misc.Contended(<span class="hljs-string">"tlr"</span>)   <span class="hljs-keyword">int</span> threadLocalRandomSecondarySeed;</code></pre></div><h2 id="Unsafe机制"><a href="#Unsafe机制" class="headerlink" title="Unsafe机制"></a>Unsafe机制</h2><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> sun.misc.Unsafe UNSAFE;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> SEED;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> PROBE;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> SECONDARY;<span class="hljs-keyword">static</span> &#123;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">//获取Unsafe实例</span>        UNSAFE = sun.misc.Unsafe.getUnsafe();        Class&lt;?&gt; tk = Thread<span class="hljs-class">.<span class="hljs-keyword">class</span></span>;        <span class="hljs-comment">//获取Thread类里面threadLocalRandomSeed变量在Thread实例的偏移量</span>        SEED = UNSAFE.objectFieldOffset            (tk.getDeclaredField(<span class="hljs-string">"threadLocalRandomSeed"</span>));        <span class="hljs-comment">//获取Thread类里面threadLocalRandomProbe变量在Thread实例的偏移量</span>        PROBE = UNSAFE.objectFieldOffset            (tk.getDeclaredField(<span class="hljs-string">"threadLocalRandomProbe"</span>));        <span class="hljs-comment">//获取Thread类里面threadLocalRandomSecondarySeed变量在Thread实例的偏移量</span>        SECONDARY = UNSAFE.objectFieldOffset            (tk.getDeclaredField(<span class="hljs-string">"threadLocalRandomSecondarySeed"</span>));    &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(e);    &#125;&#125;</code></pre></div><h2 id="nextInt方法"><a href="#nextInt方法" class="headerlink" title="nextInt方法"></a>nextInt方法</h2><p>nextInt方法用于获取下一个随机数。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">nextInt</span><span class="hljs-params">(<span class="hljs-keyword">int</span> bound)</span> </span>&#123;       <span class="hljs-comment">//参数校验</span>       <span class="hljs-keyword">if</span> (bound &lt;= <span class="hljs-number">0</span>)           <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(BadBound);       <span class="hljs-comment">//根据当前线程中的种子计算新种子</span>       <span class="hljs-keyword">int</span> r = mix32(nextSeed());       <span class="hljs-comment">//根据新种子计算随机数</span>       <span class="hljs-keyword">int</span> m = bound - <span class="hljs-number">1</span>;       <span class="hljs-keyword">if</span> ((bound &amp; m) == <span class="hljs-number">0</span>) <span class="hljs-comment">// power of two</span>           r &amp;= m;       <span class="hljs-keyword">else</span> &#123;           <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> u = r &gt;&gt;&gt; <span class="hljs-number">1</span>;                u + m - (r = u % bound) &lt; <span class="hljs-number">0</span>;                u = mix32(nextSeed()) &gt;&gt;&gt; <span class="hljs-number">1</span>)               ;       &#125;       <span class="hljs-keyword">return</span> r;   &#125;</code></pre></div><h2 id="nextSeed方法"><a href="#nextSeed方法" class="headerlink" title="nextSeed方法"></a>nextSeed方法</h2><p>获取当前线程的threadLocalRandomSeed变量值，然后加上GAMMA值作为新种子。可参照上文Unsafe机制。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> <span class="hljs-title">nextSeed</span><span class="hljs-params">()</span> </span>&#123;    Thread t; <span class="hljs-keyword">long</span> r;    UNSAFE.putLong(t = Thread.currentThread(), SEED,                   r = UNSAFE.getLong(t, SEED) + GAMMA);    <span class="hljs-keyword">return</span> r;&#125;</code></pre></div><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p>《Java并发编程之美》</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;ThreadLocalRondom是JDK 7在并发包中新增的随机数生成器，该类弥补了Random类在并发环境下的缺陷。&lt;/p&gt;
    
    </summary>
    
      <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
      <category term="源码分析" scheme="https://l1n.wang/categories/Java/%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    
    
      <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
      <category term="并发" scheme="https://l1n.wang/tags/%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>MySQL表</title>
    <link href="https://l1n.wang/2020/04/mysql-table/"/>
    <id>https://l1n.wang/2020/04/mysql-table/</id>
    <published>2020-04-17T05:25:04.000Z</published>
    <updated>2020-04-29T01:52:32.401Z</updated>
    
    <content type="html"><![CDATA[<h2 id="索引组织表"><a href="#索引组织表" class="headerlink" title="索引组织表"></a>索引组织表</h2><p>在InnoDB存储引擎中，表是根据主键顺序组织存放的，这种存储方式的表称为索引组织表，每张表都有一个主键，如果创建时没有显式定义主键，InnoDB存储引擎会按照如下方式进行创建主键：</p><ul><li>判断表中是否有非空的唯一索引，如果有，则该列为主键。</li><li>如果不符合上列条件，InnoDB存储引擎会自动创建一个6字节大小的指针。</li></ul><p>当表中有多个非空唯一索引，InnoDB会选择第一个定义的非空唯一索引作为主键。</p><h2 id="InnoDB逻辑存储结构"><a href="#InnoDB逻辑存储结构" class="headerlink" title="InnoDB逻辑存储结构"></a>InnoDB逻辑存储结构</h2><p>所有的数据被逻辑存放在表空间，表空间又由段，区，页（块）组成。存储结构如图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/table/IMG_0064.PNG" srcset="/img/loading.gif" alt="InnoDB逻辑存储结构"></p><h3 id="表空间"><a href="#表空间" class="headerlink" title="表空间"></a>表空间</h3><p>表空间是InnoDB存储引擎逻辑结构的最高层，所有的数据都存放在表空间中。如果开启了<code>innodb_file_per_table</code>，每张表的数据可以单独放到一个表空间中。但是每张表的表空间存放的只是数据、索引和插入缓冲Bitmap页。其他类的数据，例如回滚信息，插入缓冲索引页，系统事务信息，二次写缓冲等还是存放在原来的共享表空间中。</p><h3 id="段"><a href="#段" class="headerlink" title="段"></a>段</h3><p>表空间由各个段构成，常见的段有：数据段、索引段、回滚段等。InnoDB存储引擎表是索引组织的，因此数据即索引，索引即数据，数据段即为B+树的叶子节点，索引段即为B+树的非索引节点。在InnoDB存储引擎中，对段的管理都是由引擎自身完成，DBA不能也没有必要对其进行控制。</p><h3 id="区"><a href="#区" class="headerlink" title="区"></a>区</h3><p>区是由连续页组成的空间，在任何情况下每个区的大小都为1MB，为了保证区的连续性，InnoDB一次从磁盘申请4~5个区，在默认情况下，页的大小为16KB，即一个区中共有64个连续页。</p><h3 id="页"><a href="#页" class="headerlink" title="页"></a>页</h3><p>页是InnoDB磁盘管理的最小单位，默认每个页大小为16KB，可以通过参数<code>innodb_page_size</code>将页的大小设置为4K，8K、16K。</p><p>常见的页类型有：</p><ul><li>数据页</li><li>undo页</li><li>系统页</li><li>事务数据页</li><li>插入缓冲位图页</li><li>插入缓冲空闲列表页</li><li>未压缩的二进制大对象页</li><li>压缩的二进制大对象页</li></ul><h3 id="行"><a href="#行" class="headerlink" title="行"></a>行</h3><p>InnoDB存储引擎是面向行（row-oriented）的，每个页最多存放16K/2~200行的记录（7992行）。</p><h2 id="InnoDB行记录格式"><a href="#InnoDB行记录格式" class="headerlink" title="InnoDB行记录格式"></a>InnoDB行记录格式</h2><h3 id="Compact行记录格式"><a href="#Compact行记录格式" class="headerlink" title="Compact行记录格式"></a>Compact行记录格式</h3><p>该格式在MySQL5.0中引入，其设计目的是高效地存储数据。简单说，一个页存放的行数据越多，其性能就越高。它的存储方式如图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/table/IMG_0065%2820200416-174412%29.PNG" srcset="/img/loading.gif" alt="Compact行记录格式"></p><p>Compact行记录格式的首部是一个非NULL变长字段长度列表，并且是逆序放置的，其长度：</p><ul><li>若列的长度小于255字节，用1字节表示</li><li>若大于255字节，用2字节表示</li></ul><p>NULL标志位表示该行数据是否有NULL值，有则用1表示，占用1字节。头信息固定5字节（40位），每位的含义如下：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/table/IMG_0066.PNG" srcset="/img/loading.gif" alt="Compact记录头信息"></p><p>最后的部分就是实际存储每个列的数据，NULL不占用任何空间，除了之前的标志位，每行数据除了用户定义的列之外，还有两个隐藏列，事务ID列（6字节）和回滚指针列（7字节）。如果没有定义主键，没行还会增加一个rowid列（6字节）。</p><h3 id="行溢出数据"><a href="#行溢出数据" class="headerlink" title="行溢出数据"></a>行溢出数据</h3><p>InnoDB存储引擎会将一些数据存储在数据页面之外。BLOB可以不存放在溢出页面，VARCHAR类型有可能被存放位行溢出数据。</p><h2 id="InnoDB数据页结构"><a href="#InnoDB数据页结构" class="headerlink" title="InnoDB数据页结构"></a>InnoDB数据页结构</h2><p>InnoDB数据页由以下七个部分构成，如图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/table/IMG_0067.PNG" srcset="/img/loading.gif" alt="数据页结构"></p><p>其中<code>File Header、Page Header、File Trailer</code>的大小是固定的，这些空间是用来标记该页的一些信息，如Checksum，数据页所在的B+树索引的层数。</p><h3 id="File-Header"><a href="#File-Header" class="headerlink" title="File Header"></a>File Header</h3><p>该部分用来记录一些头信息，共由八个部分组成，占用38字节。<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/table/IMG_0068.PNG" srcset="/img/loading.gif" alt="File Header组成部分"></p><h3 id="Page-Header"><a href="#Page-Header" class="headerlink" title="Page Header"></a>Page Header</h3><p>该部分用来记录数据页的状态信息，由14个部分组成，占用56字节，如图所示：<br><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/mysql/table/IMG_0069.PNG" srcset="/img/loading.gif" alt="Page Header组成部分"></p><h3 id="Infimun和Supremum-Records"><a href="#Infimun和Supremum-Records" class="headerlink" title="Infimun和Supremum Records"></a>Infimun和Supremum Records</h3><p>Infimun用来记录是比该页中任何主键值都要小的值，Supremum Records指比任何值都大的值。这两个值在页创建时被建立。</p><h3 id="User-Record和Free-Space"><a href="#User-Record和Free-Space" class="headerlink" title="User Record和Free Space"></a>User Record和Free Space</h3><p>User Record是实际存储行记录的内容。Free Space指的是空闲空间，是一个链表数据结构，在一条记录被删除后，该空间会被加入到空闲链表中。</p><h3 id="Page-Directory"><a href="#Page-Directory" class="headerlink" title="Page Directory"></a>Page Directory</h3><p>该部分存放了记录的相对位置，有时这些记录指针称为Slots或Directory Slots，一个槽可能包含多个记录。B+树索引本身并不能找到具体的一条记录，只能找到该记录所在的页，数据库把页载入内存，通过Page Directory进行二叉查找。</p><h3 id="File-Trailer"><a href="#File-Trailer" class="headerlink" title="File Trailer"></a>File Trailer</h3><p>该部分是为了检测页是否已经完整地写入磁盘，File Trailer只有一个<code>FIL_PAGE_END_LSN</code>部分，占用8字节，前四个字节代表该页地checksum值，最后四个字节和File Header中的<code>FIL_PAGE_LSN</code>相同。将这两个值与File Header中的<code>FIL_PAGE_SPACE_OR_CHKSUM</code>和<code>FIL_PAGE_LSN</code>的值进行比较，看是否一致，以此来保证页的完整性。</p><h2 id="约束"><a href="#约束" class="headerlink" title="约束"></a>约束</h2><p>约束机制用来保证数据库中数据的完整性。<br>完整性有以下三种形式：</p><ol><li>实体完整性保证表中有一个主键。</li><li>域完整性保证数据每列的值满足特定的条件，域完整性通过以下几种方式来保证：<ul><li>选择合适的数据类型确保一个数据值满足特定条i教案</li><li>外键约束</li><li>编写触发器</li></ul></li><li>参照完整性保证两张表之间的关系。</li></ol><p>对于InnoDB存储引擎，提供了以下几种约束：</p><ul><li>Primary Key</li><li>Unique Key</li><li>Foreign Key</li><li>Default</li><li>NOT NULL</li></ul><p>约束和索引的区别：约束是一个逻辑上的概念，用来保证数据的完整性，而索引是一个数据结构，既有逻辑上的概念，在数据库上还代表着物理存储的方式。</p><h2 id="分区表"><a href="#分区表" class="headerlink" title="分区表"></a>分区表</h2><p>分区功能并不是在存储引擎层完成的，不是只有InnoDB存储引擎支持分区，MySQL5.1添加了对分区的支持，分区的国恒就是将一个表或索引分解为多个更小的、更可管理的部分。从逻辑上讲，只有一个表或一个索引，但是物理上这个表或索引可能由数十个物理分区组成，每个分区都是独立的对象。</p><p>MySQL支持的分区类型为水平分（将同一表中不同行的记录分配到不同的物理文件中），不支持垂直分（将同一表中不同列的记录分配到不同的物理文件中）。MySQL的分区是局部分区索引，一个分区既存放了数据又存放了索引，而全局分区是指，数据存放在各个分区中，数据的索引放在一个对象中。</p><p>MySQL支持以下几种类型的分区：</p><ul><li>RANGE分区：行数据基于属于一个给定连续区间的列值被放入分区。</li><li>LIST分区：面向离散值的分区。</li><li>HASH分区：根据用户自定义的表达式的返回值来进行分区，返回值不能为负数。</li><li>KEY分区：根据MySQL数据库提供的Hash函数进行分区。</li></ul><p>无论哪种分区方式，如果表中存在主键或唯一索引时，分区列必须是唯一索引的一个组成部分。</p><h3 id="子分区"><a href="#子分区" class="headerlink" title="子分区"></a>子分区</h3><p>子分区是在分区的基础上再进行分区，也成为复合分区，MySQL允许在RANGE和LIST分区上再进行HASH和KEY的子分区。<br>子分区需要注意以下几个问题：</p><ul><li>每个子分区的数量必须相同。</li><li>要在一个分区表的任何分区上使用SUBPARTITION来明确定义任何子分区，就必须定义所有的子分区。</li><li>每个SUBPARTITION子句必须包括子分区的一个名字。</li><li>子分区的名字必须是唯一的。</li></ul><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p>《MySQL技术内幕：InnoDB存储引擎(第二版)》</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;索引组织表&quot;&gt;&lt;a href=&quot;#索引组织表&quot; class=&quot;headerlink&quot; title=&quot;索引组织表&quot;&gt;&lt;/a&gt;索引组织表&lt;/h2&gt;&lt;p&gt;在InnoDB存储引擎中，表是根据主键顺序组织存放的，这种存储方式的表称为索引组织表，每张表都有一个主键，如果创建时
      
    
    </summary>
    
      <category term="MySQL" scheme="https://l1n.wang/categories/MySQL/"/>
    
    
      <category term="MySQL" scheme="https://l1n.wang/tags/MySQL/"/>
    
  </entry>
  
</feed>
