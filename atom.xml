<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>l1nker4&#39;s Blog</title>
  
  <subtitle>没有退路，唯有一往无前</subtitle>
  <link href="https://l1n.wang/atom.xml" rel="self"/>
  
  <link href="https://l1n.wang/"/>
  <updated>2022-01-20T12:18:17.878Z</updated>
  <id>https://l1n.wang/</id>
  
  <author>
    <name>L1nker4</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Bitcask存储模型</title>
    <link href="https://l1n.wang/2022/01/bitcask/"/>
    <id>https://l1n.wang/2022/01/bitcask/</id>
    <published>2022-01-13T09:45:04.000Z</published>
    <updated>2022-01-20T12:18:17.878Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>Bitcask是Riak分布式数据库使用的日志型存储模型，主要有以下几点特性：</p><ul><li>读写低延迟</li><li>高吞吐量，尤其是随机写</li><li>能处理大量的数据集而不降低性能</li><li>故障时快速恢复且不丢失数据</li><li>易于备份和恢复</li><li>易于理解的代码结构和数据格式</li></ul><h2 id="结构"><a href="#结构" class="headerlink" title="结构"></a>结构</h2><h3 id="硬盘结构"><a href="#硬盘结构" class="headerlink" title="硬盘结构"></a>硬盘结构</h3><p>在给定的一段时间内，只有一个<code>active data file</code>能提供<code>append</code>（写入）功能，当该文件大小达到指定阈值时，则会创建一个新的<code>active data file</code>。每一个写入到file的entry结构如下：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/bitcask/entry.jpg" alt="Entry结构"></p><p>包括以下内容：</p><ul><li>crc：校验值</li><li>tstamp：记录写入该数据的时间</li><li>ksz：key_size</li><li>value_sz：value_size</li><li>key：key</li><li>value：value</li></ul><h3 id="内存结构"><a href="#内存结构" class="headerlink" title="内存结构"></a>内存结构</h3><p>在写入之后，一个被称为<code>keydir</code>（hash table）的内存结构将被更新，每一个键映射到固定大小的结构中，包括了以下内容：</p><ul><li>file_id：去哪个文件查询</li><li>value_size：读取value多长的字节</li><li>value_position：value在文件中的位置</li></ul><p>每一次写操作发生，<code>keydir</code>都会进行原子性的更新。</p><h2 id="操作"><a href="#操作" class="headerlink" title="操作"></a>操作</h2><h3 id="read"><a href="#read" class="headerlink" title="read"></a>read</h3><p>读取value的过程：</p><ol><li>查找keydir中的key，并读取<code>file_id</code>，<code>position</code>，<code>size</code>。</li><li>通过<code>file_id</code>找到对应的<code>data file</code>，再通过<code>position</code>和<code>size</code>字段找到entry中的value。</li></ol><h3 id="merge"><a href="#merge" class="headerlink" title="merge"></a>merge</h3><p>由于删除操作并不会真正删除掉entry，只是将删除操作封装成entry写入文件，因此需要<code>merge</code>操作将所有的非<code>active</code>文件中entry遍历，并重组为一个新文件。同时生成一个<code>hint file</code>，用于存储<code>data file</code>的位置和大小。</p><blockquote><p>Q：hint file的作用是什么？</p></blockquote><p>A：每次进程重启时需要重建<code>keydir</code>，需要扫描所有的数据文件，因此使用<code>hint file</code>加速构建<code>keydir</code>的速度。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;p&gt;Bitcask是Riak分布式数据库使用的日志型存储模型，主要有以下几点特性：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;读写低延迟&lt;/li&gt;
&lt;li&gt;高吞</summary>
      
    
    
    
    <category term="分布式系统" scheme="https://l1n.wang/categories/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    
    
    <category term="存储模型" scheme="https://l1n.wang/tags/%E5%AD%98%E5%82%A8%E6%A8%A1%E5%9E%8B/"/>
    
    <category term="数据库" scheme="https://l1n.wang/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
  </entry>
  
  <entry>
    <title>2021年6月</title>
    <link href="https://l1n.wang/2021/07/2021-6-summary/"/>
    <id>https://l1n.wang/2021/07/2021-6-summary/</id>
    <published>2021-07-10T04:22:09.000Z</published>
    <updated>2021-07-10T04:26:18.946Z</updated>
    
    <content type="html"><![CDATA[<div id="hexo-blog-encrypt" data-wpm="Oh, this is an invalid password. Check and try again, please." data-whm="OOPS, these decrypted content may changed, but you can still have a look.">  <div class="hbe-input-container">  <input type="password" id="hbePass" placeholder="" />    <label for="hbePass">Hey, password is required here.</label>    <div class="bottom-line"></div>  </div>  <script id="hbeData" type="hbeData" data-hmacdigest="68eacffd52ca667a91418c3f38bbd9e724612f4168918cc25676579ceaa4a543"></script></div><script src="/lib/blog-encrypt.js"></script><link href="/css/blog-encrypt.css" rel="stylesheet" type="text/css">]]></content>
    
    
    <summary type="html">Here&#39;s something encrypted, password is required to continue reading.</summary>
    
    
    
    <category term="随笔" scheme="https://l1n.wang/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
  </entry>
  
  <entry>
    <title>2021年5月</title>
    <link href="https://l1n.wang/2021/06/2021-5-summary/"/>
    <id>https://l1n.wang/2021/06/2021-5-summary/</id>
    <published>2021-06-01T04:37:55.000Z</published>
    <updated>2021-06-01T04:49:28.018Z</updated>
    
    <content type="html"><![CDATA[<div id="hexo-blog-encrypt" data-wpm="Oh, this is an invalid password. Check and try again, please." data-whm="OOPS, these decrypted content may changed, but you can still have a look.">  <div class="hbe-input-container">  <input type="password" id="hbePass" placeholder="" />    <label for="hbePass">Hey, password is required here.</label>    <div class="bottom-line"></div>  </div>  <script id="hbeData" type="hbeData" data-hmacdigest="9f7e8e5d5e58a2a51cf8baf105378710400fcf2222e6fceb877fd87957c1a7b2"></script></div><script src="/lib/blog-encrypt.js"></script><link href="/css/blog-encrypt.css" rel="stylesheet" type="text/css">]]></content>
    
    
    <summary type="html">Here&#39;s something encrypted, password is required to continue reading.</summary>
    
    
    
    <category term="随笔" scheme="https://l1n.wang/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
  </entry>
  
  <entry>
    <title>2021年4月</title>
    <link href="https://l1n.wang/2021/05/2021-4-summary/"/>
    <id>https://l1n.wang/2021/05/2021-4-summary/</id>
    <published>2021-05-04T02:43:40.000Z</published>
    <updated>2021-06-01T04:38:37.640Z</updated>
    
    <content type="html"><![CDATA[<div id="hexo-blog-encrypt" data-wpm="Oh, this is an invalid password. Check and try again, please." data-whm="OOPS, these decrypted content may changed, but you can still have a look.">  <div class="hbe-input-container">  <input type="password" id="hbePass" placeholder="" />    <label for="hbePass">Hey, password is required here.</label>    <div class="bottom-line"></div>  </div>  <script id="hbeData" type="hbeData" data-hmacdigest="3e34fc691fea7d69aa2f449176358809a6d2c355f69afebd06947dec50e51c90"></script></div><script src="/lib/blog-encrypt.js"></script><link href="/css/blog-encrypt.css" rel="stylesheet" type="text/css">]]></content>
    
    
    <summary type="html">Here&#39;s something encrypted, password is required to continue reading.</summary>
    
    
    
    <category term="随笔" scheme="https://l1n.wang/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
  </entry>
  
  <entry>
    <title>Leetcode-字符串刷题记录</title>
    <link href="https://l1n.wang/2021/02/leetcode-string/"/>
    <id>https://l1n.wang/2021/02/leetcode-string/</id>
    <published>2021-02-18T09:24:19.000Z</published>
    <updated>2021-02-18T09:43:13.128Z</updated>
    
    <content type="html"><![CDATA[<h3 id="520-检测大写字母"><a href="#520-检测大写字母" class="headerlink" title="520. 检测大写字母"></a>520. 检测大写字母</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">detectCapitalUse</span><span class="hljs-params">(String word)</span> </span>&#123;        <span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>;        <span class="hljs-keyword">char</span>[] arr = word.toCharArray();        <span class="hljs-keyword">int</span> up = <span class="hljs-number">0</span>, low = <span class="hljs-number">0</span>;        <span class="hljs-keyword">while</span>(i &lt; arr.length)&#123;            <span class="hljs-keyword">if</span>(arr[i] &gt;= <span class="hljs-string">&#x27;a&#x27;</span>)&#123;                low++;            &#125;<span class="hljs-keyword">else</span>&#123;                up++;            &#125;            i++;        &#125;        <span class="hljs-keyword">if</span>(up == arr.length) <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        <span class="hljs-keyword">if</span>(low == arr.length) <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        <span class="hljs-keyword">if</span>(up == <span class="hljs-number">1</span> &amp;&amp; arr[<span class="hljs-number">0</span>] &lt; <span class="hljs-string">&#x27;a&#x27;</span>) <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;</code></pre><h3 id="125-验证回文串"><a href="#125-验证回文串" class="headerlink" title="125. 验证回文串"></a>125. 验证回文串</h3><p>经典问题，需要过滤非字符非数字。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isPalindrome</span><span class="hljs-params">(String s)</span> </span>&#123;        <span class="hljs-keyword">if</span>(s == <span class="hljs-keyword">null</span>) <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        s = s.toLowerCase();        StringBuilder str = <span class="hljs-keyword">new</span> StringBuilder(s.length());        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">char</span> c : s.toCharArray()) &#123;            <span class="hljs-keyword">if</span> ((c &gt;= <span class="hljs-string">&#x27;0&#x27;</span> &amp;&amp; c &lt;= <span class="hljs-string">&#x27;9&#x27;</span>) || (c &gt;= <span class="hljs-string">&#x27;a&#x27;</span> &amp;&amp; c &lt;= <span class="hljs-string">&#x27;z&#x27;</span>)) &#123;                str.append(c);            &#125;        &#125;        <span class="hljs-keyword">return</span> str.toString().equals(str.reverse().toString());    &#125;</code></pre><h3 id="14-最长公共前缀"><a href="#14-最长公共前缀" class="headerlink" title="14. 最长公共前缀"></a>14. 最长公共前缀</h3><ul><li>首先计算出最短字符串的长度，这个长度就是外层循环的遍历次数。</li><li>内层逐个比较字符串的指定位置的值。</li><li>通过StringBuilder拼接公共前缀。</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">longestCommonPrefix</span><span class="hljs-params">(String[] strs)</span> </span>&#123;        <span class="hljs-keyword">if</span>(strs.length == <span class="hljs-number">0</span>)             <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;&quot;</span>;        StringBuilder sb = <span class="hljs-keyword">new</span> StringBuilder();        <span class="hljs-keyword">int</span> minLength = <span class="hljs-number">1000</span>;        <span class="hljs-comment">//计算最短字符串长度</span>        <span class="hljs-keyword">for</span>(String str : strs)&#123;            <span class="hljs-keyword">if</span>(str.length() &lt; minLength)&#123;                minLength = str.length();            &#125;        &#125;        String demo = strs[<span class="hljs-number">0</span>];        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i&lt; minLength; i++)&#123;            <span class="hljs-keyword">boolean</span> flag = <span class="hljs-keyword">true</span>;            <span class="hljs-keyword">char</span> c = demo.charAt(i);            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j = <span class="hljs-number">1</span>; j &lt; strs.length; j++)&#123;                <span class="hljs-keyword">if</span>(strs[j].charAt(i) != c)&#123;                    <span class="hljs-comment">//不等于</span>                    flag = <span class="hljs-keyword">false</span>;                    <span class="hljs-keyword">break</span>;                &#125;            &#125;            <span class="hljs-keyword">if</span>(flag)&#123;                sb.append(c);                flag = <span class="hljs-keyword">true</span>;            &#125;<span class="hljs-keyword">else</span>&#123;                <span class="hljs-keyword">break</span>;            &#125;        &#125;        <span class="hljs-keyword">return</span> sb.toString();    &#125;</code></pre><h3 id="434-字符串中的单词数"><a href="#434-字符串中的单词数" class="headerlink" title="434. 字符串中的单词数"></a>434. 字符串中的单词数</h3><p>一轮遍历即可，判断依据是：<strong>当前值是空格，前一位非空格</strong></p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">countSegments</span><span class="hljs-params">(String s)</span> </span>&#123;        <span class="hljs-keyword">if</span> (s == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;        &#125;        s = s.trim();        <span class="hljs-keyword">if</span> (s.length() == <span class="hljs-number">0</span>) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;        &#125;        <span class="hljs-keyword">int</span> count = <span class="hljs-number">1</span>;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">1</span>; i &lt; s.length(); i++) &#123;            <span class="hljs-comment">// 当前值是空格，前一位非空格</span>            <span class="hljs-keyword">if</span> (s.charAt(i) == <span class="hljs-number">32</span> &amp;&amp; s.charAt(i - <span class="hljs-number">1</span>) != <span class="hljs-number">32</span>) &#123;                count++;            &#125;        &#125;        <span class="hljs-keyword">return</span> count;    &#125;</code></pre><h3 id="58-最后一个单词的长度"><a href="#58-最后一个单词的长度" class="headerlink" title="58. 最后一个单词的长度"></a>58. 最后一个单词的长度</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">lengthOfLastWord</span><span class="hljs-params">(String s)</span> </span>&#123;        String[] s1 = s.split(<span class="hljs-string">&quot; &quot;</span>);        <span class="hljs-keyword">if</span>(s1.length == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;        <span class="hljs-keyword">return</span> s1[s1.length -<span class="hljs-number">1</span>].length() == <span class="hljs-number">0</span> ? <span class="hljs-number">0</span> : s1[s1.length -<span class="hljs-number">1</span>].length();    &#125;</code></pre><h3 id="344-反转字符串"><a href="#344-反转字符串" class="headerlink" title="344. 反转字符串"></a>344. 反转字符串</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">reverseString</span><span class="hljs-params">(<span class="hljs-keyword">char</span>[] s)</span> </span>&#123;        <span class="hljs-keyword">int</span> left = <span class="hljs-number">0</span>, right = s.length -<span class="hljs-number">1</span>;        <span class="hljs-keyword">while</span>(left &lt; right)&#123;            swap(s , left, right);            left++;            right--;        &#125;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">swap</span><span class="hljs-params">(<span class="hljs-keyword">char</span>[] s, <span class="hljs-keyword">int</span> i, <span class="hljs-keyword">int</span> j)</span></span>&#123;        <span class="hljs-keyword">char</span> tmp = s[i];        s[i] = s[j];        s[j] = tmp;    &#125;</code></pre><h3 id="541-反转字符串Ⅱ"><a href="#541-反转字符串Ⅱ" class="headerlink" title="541 反转字符串Ⅱ"></a>541 反转字符串Ⅱ</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">reverseStr</span><span class="hljs-params">(String s, <span class="hljs-keyword">int</span> k)</span> </span>&#123;        <span class="hljs-keyword">char</span>[] arr = s.toCharArray();        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; arr.length; i+= <span class="hljs-number">2</span>*k)&#123;            <span class="hljs-comment">//每隔2k反转一次</span>            <span class="hljs-comment">//判断剩余字符</span>            <span class="hljs-keyword">int</span> rest = arr.length - i;            <span class="hljs-keyword">if</span>(rest &lt; k)&#123;               reverse(arr,i, arr.length - <span class="hljs-number">1</span>);             &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(rest &lt; <span class="hljs-number">2</span> * k &amp;&amp; rest &gt;= k)&#123;                reverse(arr,i, i + k - <span class="hljs-number">1</span>);            &#125;<span class="hljs-keyword">else</span>&#123;                reverse(arr,i, i+k - <span class="hljs-number">1</span>);            &#125;        &#125;        <span class="hljs-keyword">return</span> String.valueOf(arr);    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">reverse</span><span class="hljs-params">(<span class="hljs-keyword">char</span>[] s,<span class="hljs-keyword">int</span> i, <span class="hljs-keyword">int</span> j)</span> </span>&#123;        <span class="hljs-keyword">int</span> left = i, right = j;        <span class="hljs-keyword">while</span>(left &lt; right)&#123;            swap(s , left, right);            left++;            right--;        &#125;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">swap</span><span class="hljs-params">(<span class="hljs-keyword">char</span>[] s, <span class="hljs-keyword">int</span> i, <span class="hljs-keyword">int</span> j)</span></span>&#123;        <span class="hljs-keyword">char</span> tmp = s[i];        s[i] = s[j];        s[j] = tmp;    &#125;</code></pre><h3 id="557-反转字符串中的单词Ⅲ"><a href="#557-反转字符串中的单词Ⅲ" class="headerlink" title="557. 反转字符串中的单词Ⅲ"></a>557. 反转字符串中的单词Ⅲ</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">reverseWords</span><span class="hljs-params">(String s)</span> </span>&#123;        String[] strs = s.split(<span class="hljs-string">&quot; &quot;</span>);        String[] reverseString = <span class="hljs-keyword">new</span> String [strs.length];        <span class="hljs-keyword">int</span> index = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span>(String str : strs)&#123;            reverseString[index++] = <span class="hljs-keyword">new</span> StringBuffer(str).reverse().toString();        &#125;        StringBuilder sb = <span class="hljs-keyword">new</span> StringBuilder();        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i =<span class="hljs-number">0</span>; i &lt; strs.length; i++)&#123;            sb.append(reverseString[i]);            <span class="hljs-keyword">if</span>(i != reverseString.length - <span class="hljs-number">1</span>) sb.append(<span class="hljs-string">&quot; &quot;</span>);        &#125;        <span class="hljs-keyword">return</span> sb.toString();    &#125;</code></pre><h3 id="151-反转字符串中的单词"><a href="#151-反转字符串中的单词" class="headerlink" title="151. 反转字符串中的单词"></a>151. 反转字符串中的单词</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">reverseWords</span><span class="hljs-params">(String s)</span> </span>&#123;        s = s.trim();        List&lt;String&gt; wordList = Arrays.asList(s.split(<span class="hljs-string">&quot;\\s+&quot;</span>));        Collections.reverse(wordList);        <span class="hljs-keyword">return</span> String.join(<span class="hljs-string">&quot; &quot;</span>, wordList);    &#125;</code></pre><h3 id="387-字符串中的第一个唯一字符"><a href="#387-字符串中的第一个唯一字符" class="headerlink" title="387. 字符串中的第一个唯一字符"></a>387. 字符串中的第一个唯一字符</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">firstUniqChar</span><span class="hljs-params">(String s)</span> </span>&#123;        <span class="hljs-keyword">int</span>[] letter=<span class="hljs-keyword">new</span> <span class="hljs-keyword">int</span>[<span class="hljs-number">26</span>];        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">char</span> c:s.toCharArray()) &#123;            letter[c-<span class="hljs-string">&#x27;a&#x27;</span>]++;        &#125;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt;s.length() ; i++) &#123;            <span class="hljs-keyword">if</span>(letter[s.charAt(i)-<span class="hljs-string">&#x27;a&#x27;</span>]==<span class="hljs-number">1</span>) &#123;                <span class="hljs-keyword">return</span> i;            &#125;        &#125;        <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;    &#125;</code></pre><h3 id="389-找不同"><a href="#389-找不同" class="headerlink" title="389. 找不同"></a>389. 找不同</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-keyword">char</span> <span class="hljs-title">findTheDifference</span><span class="hljs-params">(<span class="hljs-built_in">string</span> s, <span class="hljs-built_in">string</span> t)</span> </span>&#123;        <span class="hljs-function"><span class="hljs-built_in">vector</span>&lt;<span class="hljs-keyword">int</span>&gt; <span class="hljs-title">count</span><span class="hljs-params">(<span class="hljs-number">26</span>,<span class="hljs-number">0</span>)</span></span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">char</span> ch : s)&#123;            count[ch - <span class="hljs-string">&#x27;a&#x27;</span>]++;        &#125;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">char</span> ch : t)&#123;            count[ch - <span class="hljs-string">&#x27;a&#x27;</span>]--;            <span class="hljs-keyword">if</span>(count[ch - <span class="hljs-string">&#x27;a&#x27;</span>] &lt; <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> ch;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-string">&#x27; &#x27;</span>;    &#125;</code></pre><h3 id="383-赎金信"><a href="#383-赎金信" class="headerlink" title="383. 赎金信"></a>383. 赎金信</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">canConstruct</span><span class="hljs-params">(<span class="hljs-built_in">string</span> ransomNote, <span class="hljs-built_in">string</span> magazine)</span> </span>&#123;        <span class="hljs-function"><span class="hljs-built_in">vector</span>&lt;<span class="hljs-keyword">int</span>&gt; <span class="hljs-title">count</span><span class="hljs-params">(<span class="hljs-number">26</span>, <span class="hljs-number">0</span>)</span></span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">char</span> ch : magazine)&#123;            count[ch - <span class="hljs-string">&#x27;a&#x27;</span>]++;        &#125;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">char</span> ch : ransomNote)&#123;            count[ch - <span class="hljs-string">&#x27;a&#x27;</span>]--;            <span class="hljs-keyword">if</span>(count[ch - <span class="hljs-string">&#x27;a&#x27;</span>] &lt; <span class="hljs-number">0</span>)&#123;                <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;            &#125;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;    &#125;</code></pre><h3 id="242-有效的字母异位词"><a href="#242-有效的字母异位词" class="headerlink" title="242. 有效的字母异位词"></a>242. 有效的字母异位词</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">isAnagram</span><span class="hljs-params">(<span class="hljs-built_in">string</span> s, <span class="hljs-built_in">string</span> t)</span> </span>&#123;        <span class="hljs-function"><span class="hljs-built_in">vector</span>&lt;<span class="hljs-keyword">int</span>&gt; <span class="hljs-title">cnt</span><span class="hljs-params">(<span class="hljs-number">26</span>,<span class="hljs-number">0</span>)</span></span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">char</span> ch : s) cnt[ch - <span class="hljs-string">&#x27;a&#x27;</span>]++;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">char</span> ch : t) cnt[ch - <span class="hljs-string">&#x27;a&#x27;</span>]--;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> count : cnt) <span class="hljs-keyword">if</span>(count != <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;    &#125;</code></pre><h3 id="49-字母异位词分组"><a href="#49-字母异位词分组" class="headerlink" title="49. 字母异位词分组"></a>49. 字母异位词分组</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">string</span>&gt;&gt; <span class="hljs-title">groupAnagrams</span><span class="hljs-params">(<span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">string</span>&gt;&amp; strs)</span> </span>&#123;        <span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">string</span>&gt;&gt; res;        <span class="hljs-built_in">map</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">string</span>&gt;&gt; <span class="hljs-built_in">map</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i =<span class="hljs-number">0</span>; i &lt; strs.size(); i++)&#123;            <span class="hljs-built_in">string</span> key = strs[i];            sort(key.begin(), key.end());            <span class="hljs-built_in">map</span>[key].push_back(strs[i]);        &#125;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">auto</span> ite = <span class="hljs-built_in">map</span>.begin(); ite != <span class="hljs-built_in">map</span>.end(); ite++) res.push_back(ite-&gt;second);        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="451-根据字符出现频率排序"><a href="#451-根据字符出现频率排序" class="headerlink" title="451. 根据字符出现频率排序"></a>451. 根据字符出现频率排序</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-built_in">string</span> <span class="hljs-title">frequencySort</span><span class="hljs-params">(<span class="hljs-built_in">string</span> s)</span> </span>&#123;        <span class="hljs-built_in">map</span>&lt;<span class="hljs-keyword">char</span>,<span class="hljs-keyword">int</span>&gt; <span class="hljs-built_in">map</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">char</span> c : s) <span class="hljs-built_in">map</span>[c]++;        <span class="hljs-comment">//sort</span>        vector&lt;pair&lt;char, int&gt;&gt; vec(map.begin(), map.end());        sort(vec.begin(), vec.end(), [](<span class="hljs-keyword">auto</span> &amp; a, <span class="hljs-keyword">auto</span> &amp; b)&#123;            <span class="hljs-keyword">return</span> a.second &gt; b.second;        &#125;);        <span class="hljs-built_in">string</span> res;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">auto</span> v : vec) res += <span class="hljs-built_in">string</span>(v.second,v.first);        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="657-机器人能否返回原点"><a href="#657-机器人能否返回原点" class="headerlink" title="657. 机器人能否返回原点"></a>657. 机器人能否返回原点</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">judgeCircle</span><span class="hljs-params">(<span class="hljs-built_in">string</span> moves)</span> </span>&#123;        <span class="hljs-keyword">int</span> index = <span class="hljs-number">0</span>;        <span class="hljs-keyword">int</span> updown = <span class="hljs-number">0</span>, rightleft = <span class="hljs-number">0</span>;        <span class="hljs-keyword">while</span>(index &lt;= moves.length())&#123;            <span class="hljs-keyword">switch</span>(moves[index])&#123;                <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;R&#x27;</span>:                    rightleft++;                    <span class="hljs-keyword">break</span>;                <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;L&#x27;</span>:                    rightleft--;                    <span class="hljs-keyword">break</span>;                <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;U&#x27;</span>:                    updown++;                    <span class="hljs-keyword">break</span>;                <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;D&#x27;</span>:                    updown--;                    <span class="hljs-keyword">break</span>;            &#125;            index++;        &#125;        <span class="hljs-keyword">if</span>(updown == <span class="hljs-number">0</span> &amp;&amp; rightleft == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;        <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;    &#125;</code></pre><h3 id="551-学生出勤记录Ⅰ"><a href="#551-学生出勤记录Ⅰ" class="headerlink" title="551. 学生出勤记录Ⅰ"></a>551. 学生出勤记录Ⅰ</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-keyword">bool</span> <span class="hljs-title">checkRecord</span><span class="hljs-params">(<span class="hljs-built_in">string</span> s)</span> </span>&#123;        <span class="hljs-keyword">int</span> late = <span class="hljs-number">0</span>, absent = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; s.length(); i++)&#123;            <span class="hljs-keyword">if</span>(s[i] == <span class="hljs-string">&#x27;A&#x27;</span>)&#123;                <span class="hljs-comment">//absent</span>                absent++;                late = <span class="hljs-number">0</span>;                <span class="hljs-keyword">if</span>(absent &gt; <span class="hljs-number">1</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;            &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(s[i] == <span class="hljs-string">&#x27;L&#x27;</span>)&#123;                late++;                <span class="hljs-keyword">if</span>(late &gt; <span class="hljs-number">2</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;            &#125;<span class="hljs-keyword">else</span> &#123;                <span class="hljs-comment">//P</span>                late = <span class="hljs-number">0</span>;            &#125;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;    &#125;</code></pre><h3 id="28-实现strStr"><a href="#28-实现strStr" class="headerlink" title="28. 实现strStr()"></a>28. 实现strStr()</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">strStr</span><span class="hljs-params">(<span class="hljs-built_in">string</span> haystack, <span class="hljs-built_in">string</span> needle)</span> </span>&#123;        <span class="hljs-keyword">if</span> ( needle == <span class="hljs-string">&quot;&quot;</span> )            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;        <span class="hljs-keyword">int</span> hlen = haystack.length();        <span class="hljs-keyword">int</span> nlen = needle.length();        <span class="hljs-keyword">int</span> i,j;        <span class="hljs-keyword">for</span>(i=<span class="hljs-number">0</span>;i&lt;hlen;i++)&#123;            <span class="hljs-keyword">for</span>(j=<span class="hljs-number">0</span>;j&lt;nlen;j++)                <span class="hljs-keyword">if</span>(haystack[i+j]!=needle[j])                    <span class="hljs-keyword">break</span>;      <span class="hljs-comment">//不符合就结束本轮匹配</span>            <span class="hljs-keyword">if</span>(j==nlen)                <span class="hljs-keyword">return</span> i;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;    &#125;</code></pre><h3 id="8-字符串转换整数"><a href="#8-字符串转换整数" class="headerlink" title="8. 字符串转换整数"></a>8. 字符串转换整数</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">myAtoi</span><span class="hljs-params">(<span class="hljs-built_in">string</span> s)</span> </span>&#123;        <span class="hljs-keyword">int</span> i=<span class="hljs-number">0</span>; <span class="hljs-comment">// 遍历的游标</span>        <span class="hljs-keyword">while</span>(i&lt;s.size()&amp;&amp;s[i]==<span class="hljs-string">&#x27; &#x27;</span>) i++; <span class="hljs-comment">// 去掉空白字符</span>        <span class="hljs-keyword">if</span>(i==s.size()) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>; <span class="hljs-comment">// 如果s为全空</span>                <span class="hljs-comment">// 处理第一个非空字符</span>        <span class="hljs-keyword">int</span> sign=<span class="hljs-number">1</span>;        <span class="hljs-keyword">if</span>(s[i]==<span class="hljs-string">&#x27;-&#x27;</span>)&#123;            sign=<span class="hljs-number">-1</span>;            i++;        &#125;        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(s[i]==<span class="hljs-string">&#x27;+&#x27;</span>)            i++;        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(!<span class="hljs-built_in">isdigit</span>(s[i]))            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;        <span class="hljs-comment">// 处理第一个数字字符</span>        <span class="hljs-keyword">int</span> n=<span class="hljs-number">0</span>;        <span class="hljs-keyword">while</span>(<span class="hljs-built_in">isdigit</span>(s[i])&amp;&amp;i&lt;s.size())&#123;            <span class="hljs-keyword">if</span>((INT_MAX-(s[i]-<span class="hljs-string">&#x27;0&#x27;</span>))/<span class="hljs-number">10.0</span>&lt;n) <span class="hljs-keyword">return</span> sign==<span class="hljs-number">-1</span>?sign*INT_MAX<span class="hljs-number">-1</span>:INT_MAX; <span class="hljs-comment">// 如果溢出</span>            n=<span class="hljs-number">10</span>*n+(s[i]-<span class="hljs-string">&#x27;0&#x27;</span>);            i++;        &#125;        <span class="hljs-keyword">return</span> sign*n;    &#125;</code></pre><h3 id="67-二进制求和"><a href="#67-二进制求和" class="headerlink" title="67. 二进制求和"></a>67. 二进制求和</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-built_in">string</span> <span class="hljs-title">addBinary</span><span class="hljs-params">(<span class="hljs-built_in">string</span> a, <span class="hljs-built_in">string</span> b)</span> </span>&#123;        <span class="hljs-built_in">string</span> result;        <span class="hljs-keyword">const</span> <span class="hljs-keyword">int</span> n = a.size() &gt; b.size() ? a.size() : b.size();        reverse(a.begin(), a.end());        reverse(b.begin(), b.end());        <span class="hljs-keyword">int</span> carry = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)&#123;            <span class="hljs-keyword">int</span> ai = i &lt; a.size() ? a[i] - <span class="hljs-string">&#x27;0&#x27;</span> : <span class="hljs-number">0</span>;            <span class="hljs-keyword">int</span> bi = i &lt; b.size() ? b[i] - <span class="hljs-string">&#x27;0&#x27;</span> : <span class="hljs-number">0</span>;            <span class="hljs-keyword">int</span> val = (ai + bi + carry) % <span class="hljs-number">2</span>;            carry = (ai + bi + carry) / <span class="hljs-number">2</span>;            result.insert(result.begin(), val + <span class="hljs-string">&#x27;0&#x27;</span>);        &#125;        <span class="hljs-keyword">if</span>(carry == <span class="hljs-number">1</span>) result.insert(result.begin(), <span class="hljs-string">&#x27;1&#x27;</span>);        <span class="hljs-keyword">return</span> result;    &#125;</code></pre><h3 id="412-Fizz-Buzz"><a href="#412-Fizz-Buzz" class="headerlink" title="412. Fizz Buzz"></a>412. Fizz Buzz</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">string</span>&gt; <span class="hljs-title">fizzBuzz</span><span class="hljs-params">(<span class="hljs-keyword">int</span> n)</span> </span>&#123;        <span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">string</span>&gt; res;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)&#123;            <span class="hljs-built_in">string</span> tmp;            <span class="hljs-keyword">if</span>(i % <span class="hljs-number">15</span> == <span class="hljs-number">0</span>)&#123;                res.push_back(<span class="hljs-string">&quot;FizzBuzz&quot;</span>);            &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(i % <span class="hljs-number">5</span> == <span class="hljs-number">0</span>)&#123;                res.push_back(<span class="hljs-string">&quot;Buzz&quot;</span>);            &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(i % <span class="hljs-number">3</span> == <span class="hljs-number">0</span>)&#123;                res.push_back(<span class="hljs-string">&quot;Fizz&quot;</span>);            &#125;<span class="hljs-keyword">else</span>&#123;                res.push_back(to_string(i));            &#125;                    &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="299-猜数字游戏"><a href="#299-猜数字游戏" class="headerlink" title="299. 猜数字游戏"></a>299. 猜数字游戏</h3><p>需要注意数字出现的次数。</p><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-built_in">string</span> <span class="hljs-title">getHint</span><span class="hljs-params">(<span class="hljs-built_in">string</span> secret, <span class="hljs-built_in">string</span> guess)</span> </span>&#123;        <span class="hljs-keyword">int</span> bull = <span class="hljs-number">0</span>, cow = <span class="hljs-number">0</span>;        <span class="hljs-function"><span class="hljs-built_in">vector</span>&lt;<span class="hljs-keyword">int</span>&gt; <span class="hljs-title">num</span><span class="hljs-params">(<span class="hljs-number">10</span>,<span class="hljs-number">0</span>)</span></span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; guess.size(); i++)&#123;            <span class="hljs-keyword">if</span>(guess[i] == secret[i])&#123;                bull++;            &#125;<span class="hljs-keyword">else</span> &#123;                <span class="hljs-comment">//当前位置不一致，需要判断当前字符是否出现在secret里面</span>                <span class="hljs-comment">//通过vector存储数字出现次数</span>                <span class="hljs-keyword">if</span>(num[secret[i] - <span class="hljs-string">&#x27;0&#x27;</span>]++ &lt; <span class="hljs-number">0</span>) cow++;                <span class="hljs-keyword">if</span>(num[guess[i] - <span class="hljs-string">&#x27;0&#x27;</span>]-- &gt; <span class="hljs-number">0</span>) cow++;            &#125;        &#125;        <span class="hljs-keyword">return</span> to_string(bull) + <span class="hljs-string">&#x27;A&#x27;</span> + to_string(cow) + <span class="hljs-string">&#x27;B&#x27;</span>;    &#125;</code></pre><h3 id="506-相对名次"><a href="#506-相对名次" class="headerlink" title="506. 相对名次"></a>506. 相对名次</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">string</span>&gt; <span class="hljs-title">findRelativeRanks</span><span class="hljs-params">(<span class="hljs-built_in">vector</span>&lt;<span class="hljs-keyword">int</span>&gt;&amp; score)</span> </span>&#123;        <span class="hljs-built_in">vector</span>&lt;<span class="hljs-keyword">int</span>&gt; org = score;        sort(score.rbegin(), score.rend());        <span class="hljs-built_in">unordered_map</span>&lt;<span class="hljs-keyword">int</span>, <span class="hljs-built_in">string</span>&gt; order;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; score.size(); i++)&#123;            <span class="hljs-keyword">if</span>(i &gt;= <span class="hljs-number">3</span>) order[score[i]] = to_string(i+ <span class="hljs-number">1</span>);            <span class="hljs-keyword">if</span>(i == <span class="hljs-number">0</span>) order[score[i]] = <span class="hljs-string">&quot;Gold Medal&quot;</span>;            <span class="hljs-keyword">if</span> (i == <span class="hljs-number">1</span>) order[score[i]] = <span class="hljs-string">&quot;Silver Medal&quot;</span>;            <span class="hljs-keyword">if</span> (i == <span class="hljs-number">2</span>) order[score[i]] = <span class="hljs-string">&quot;Bronze Medal&quot;</span>;        &#125;        <span class="hljs-function"><span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">string</span>&gt; <span class="hljs-title">res</span><span class="hljs-params">(score.size(), <span class="hljs-string">&quot;&quot;</span>)</span></span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; res.size(); i++)&#123;            res[i] = order[org[i]];        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="539-最小时间差"><a href="#539-最小时间差" class="headerlink" title="539. 最小时间差"></a>539. 最小时间差</h3><ul><li>将时间转换成分钟。</li><li>注意钟表是模运算。</li></ul><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">findMinDifference</span><span class="hljs-params">(<span class="hljs-built_in">vector</span>&lt;<span class="hljs-built_in">string</span>&gt;&amp; timePoints)</span> </span>&#123;        <span class="hljs-built_in">vector</span>&lt;<span class="hljs-keyword">int</span>&gt; q;        <span class="hljs-keyword">int</span> res = INT_MAX;        <span class="hljs-keyword">for</span>(<span class="hljs-built_in">string</span> t : timePoints)&#123;            <span class="hljs-keyword">int</span> hour = <span class="hljs-number">10</span> * (t[<span class="hljs-number">0</span>] - <span class="hljs-string">&#x27;0&#x27;</span>) + t[<span class="hljs-number">1</span>] - <span class="hljs-string">&#x27;0&#x27;</span>;            <span class="hljs-keyword">int</span> min = <span class="hljs-number">10</span> * (t[<span class="hljs-number">3</span>] - <span class="hljs-string">&#x27;0&#x27;</span>) + t[<span class="hljs-number">4</span>] - <span class="hljs-string">&#x27;0&#x27;</span>;            q.push_back(<span class="hljs-number">60</span> * hour + min);        &#125;        sort(q.begin(), q.end());        q.push_back(q[<span class="hljs-number">0</span>] + <span class="hljs-number">24</span> * <span class="hljs-number">60</span>);        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">1</span>; i &lt; q.size(); i++)&#123;            res = min(res, q[i] - q[i - <span class="hljs-number">1</span>]);        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="553-最优除法"><a href="#553-最优除法" class="headerlink" title="553. 最优除法"></a>553. 最优除法</h3><p>题解：<strong>被除数不变，除数最小</strong>，如果能理解这个数学知识，那么就是一道easy难度的题目。</p><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-built_in">string</span> <span class="hljs-title">optimalDivision</span><span class="hljs-params">(<span class="hljs-built_in">vector</span>&lt;<span class="hljs-keyword">int</span>&gt;&amp; nums)</span> </span>&#123;        <span class="hljs-keyword">if</span>(nums.size() == <span class="hljs-number">1</span>)&#123;            <span class="hljs-keyword">return</span> to_string(nums[<span class="hljs-number">0</span>]);        &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(nums.size() == <span class="hljs-number">2</span>)&#123;            <span class="hljs-keyword">return</span> to_string(nums[<span class="hljs-number">0</span>]) + <span class="hljs-string">&quot;/&quot;</span> + to_string(nums[<span class="hljs-number">1</span>]);        &#125;        <span class="hljs-function"><span class="hljs-built_in">string</span> <span class="hljs-title">res</span><span class="hljs-params">(to_string(nums[<span class="hljs-number">0</span>]) + <span class="hljs-string">&quot;/(&quot;</span>)</span></span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">1</span>; i &lt; nums.size() - <span class="hljs-number">1</span>; i++)&#123;            res += to_string(nums[i]) + <span class="hljs-string">&quot;/&quot;</span>;        &#125;        res += to_string(nums.back()) + <span class="hljs-string">&quot;)&quot;</span>;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="38-外观数列"><a href="#38-外观数列" class="headerlink" title="38. 外观数列"></a>38. 外观数列</h3><pre><code class="hljs C++"><span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">countAndSay</span><span class="hljs-params">(<span class="hljs-keyword">int</span> n)</span> </span>&#123;        String res = <span class="hljs-string">&quot;1&quot;</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">2</span>; i &lt;= n; i++)&#123;            StringBuilder temp = <span class="hljs-keyword">new</span> StringBuilder();            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j = <span class="hljs-number">0</span>; j &lt; res.length(); j++)&#123;                <span class="hljs-keyword">int</span> count = <span class="hljs-number">1</span>;                <span class="hljs-keyword">while</span>(j + <span class="hljs-number">1</span> &lt; res.length() &amp;&amp; res.charAt(j) == res.charAt(j + <span class="hljs-number">1</span>))&#123;                    j++;                    count++;                &#125;                temp.append(count).append(res.charAt(j));            &#125;            res = temp.toString();        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre>]]></content>
    
    
      
      
    <summary type="html">&lt;h3 id=&quot;520-检测大写字母&quot;&gt;&lt;a href=&quot;#520-检测大写字母&quot; class=&quot;headerlink&quot; title=&quot;520. 检测大写字母&quot;&gt;&lt;/a&gt;520. 检测大写字母&lt;/h3&gt;&lt;pre&gt;&lt;code class=&quot;hljs java&quot;&gt;&lt;span clas</summary>
      
    
    
    
    <category term="计算机基础" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/"/>
    
    <category term="算法" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/%E7%AE%97%E6%B3%95/"/>
    
    
    <category term="算法" scheme="https://l1n.wang/tags/%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>分布式系统学习笔记</title>
    <link href="https://l1n.wang/2021/01/distributed-system-note/"/>
    <id>https://l1n.wang/2021/01/distributed-system-note/</id>
    <published>2021-01-23T06:12:00.000Z</published>
    <updated>2021-02-14T07:31:17.948Z</updated>
    
    <content type="html"><![CDATA[<h1 id="理论"><a href="#理论" class="headerlink" title="理论"></a>理论</h1><h2 id="拜占庭将军问题"><a href="#拜占庭将军问题" class="headerlink" title="拜占庭将军问题"></a>拜占庭将军问题</h2><p>存在恶意节点行为的场景中（数字货币的区块链技术中）：必须使用拜占庭容错算法（BFT）。常用的有PBFT算法、PoW算法。</p><p>计算机分布式系统中，最常用的是非拜占庭容错算法，即故障容错算法（CFT）。<strong>CFT解决的是分布式系统中存在故障，但不存在恶意节点的场景下的共识问题</strong>。这个场景可能会丢失信息、消息重复问题，但是不存在错误消息，或者伪造消息的情况。常见算法有Paxos算法、Raft算法、ZAB协议。</p><h2 id="CAP理论"><a href="#CAP理论" class="headerlink" title="CAP理论"></a>CAP理论</h2><blockquote><p>Q：如何设计分区容错一致性模型？</p></blockquote><h3 id="CAP三指标"><a href="#CAP三指标" class="headerlink" title="CAP三指标"></a>CAP三指标</h3><ul><li><p>一致性（Consistency）</p><ul><li>强调的是数据在集群中的一致性。</li><li>客户端每次的读操作，无论访问哪个节点，要么读到同一份数据，要么读取失败。</li></ul></li><li><p>可用性（Availability）</p><ul><li>强调的是服务可用，但不保证数据的一致。</li><li>无论客户端访问哪个节点，都能得到响应数据，但是不保证数据是最新的。</li></ul></li><li><p>分区容错性（Partition Tolerance）</p><ul><li><p>强调的是集群对分区故障的容错能力。</p></li><li><p>节点出现任意数量的消息丢失或高延迟，系统仍然可以继续提供服务。</p></li></ul></li></ul><h3 id="CAP不可能三角"><a href="#CAP不可能三角" class="headerlink" title="CAP不可能三角"></a>CAP不可能三角</h3><p>对于一个分布式系统而言，一致性、可用性、分区容错性三个指标不可兼得，只能进行三选二。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/CAP/CAP-triangle.jpg" alt="CAP不可能三角"></p><h3 id="如何使用CAP理论"><a href="#如何使用CAP理论" class="headerlink" title="如何使用CAP理论"></a>如何使用CAP理论</h3><p>节点之间的分区故障是必然发生的，因此分区容错性（P）是必须要保证的。那么只剩下两种选择：</p><ul><li>CP：选择一致性（C）的时候，如果因为消息丢失、延迟过高发生了网络分区，部分节点无法保持一致，这时集群节点拒绝客户端的写操作。</li><li>AP：选择可用性（A）的时候，系统将始终处理客户端的查询，返回特定信息，如果发生了网络分区，一些节点无法返回最新特定信息。</li></ul><h2 id="ACID"><a href="#ACID" class="headerlink" title="ACID"></a>ACID</h2><p>单机上实现ACID可以通过锁、时间序列等机制保障操作的顺序执行。分布式系统的ACID实现需要掌握分布式事务协议，比如二阶段提交协议、TCC（Try-Confirm-Cancel）。</p><p>可以将ACID特性理解为CAP中一致性的边界（最强的一致性），但是大部分场景对一致性的要求不是特别高，<strong>如果不是必须，尽量不要实现事务，可以考虑采用强一致性或最终一致性。</strong></p><h3 id="二阶段提交协议"><a href="#二阶段提交协议" class="headerlink" title="二阶段提交协议"></a>二阶段提交协议</h3><ul><li>提交请求阶段（投票阶段）<ul><li>每一个参与者投票表决事务是放弃还是提交，一旦参与者投票要求提交事务，那么不允许中途放弃事务，这个特性需要代码实现时保障的。</li></ul></li><li>提交执行阶段（完成阶段）<ul><li>事务的每个参与者执行最终统一的决定，提交事务或者放弃事务，这个约定是为了实现ACID中的原子性。</li></ul></li></ul><p>存在的问题：</p><ul><li>提交请求阶段，需要预留资源，在资源预留时期，其他人不能操作。</li><li>数据库是独立的系统。无法动态调整锁的粒度，并发性能下降。</li></ul><h3 id="TCC（Try-Confirm-Cancel）"><a href="#TCC（Try-Confirm-Cancel）" class="headerlink" title="TCC（Try-Confirm-Cancel）"></a>TCC（Try-Confirm-Cancel）</h3><ul><li>Try（预留）：预留相关资源</li><li>Confirm（确认）：确认操作，完成分布式事务。</li><li>Cancel（撤销）：如果无法操作，则进行撤销操作。</li></ul><p>TCC本质上是补偿事务，核心思想是针对每个操作都要注册一个与之对应的确认操作和撤销操作。它是一个业务层面的协议。在业务中实现分布式事务。对业务代码的侵入性较高。实现的复杂度更高。</p><h2 id="BASE理论"><a href="#BASE理论" class="headerlink" title="BASE理论"></a>BASE理论</h2><p>BASE理论是CAP理论中的AP的延伸，是互联网大规模分布式系统的实践总结，强调可用性。BASE理论的核心就是：</p><ul><li>基本可用（Basically Available）</li><li>最终一致性（Eventually Consistent）</li></ul><h3 id="基本可用"><a href="#基本可用" class="headerlink" title="基本可用"></a>基本可用</h3><p>当分布式系统出现不可预知的故障时，允许损失部分功能的可用性，保证核心功能的可用性。例如：</p><ul><li><strong>延迟响应</strong>：12306购票系统出现突发流量时，会将购票请求放入队列中进行排队等待处理，通过牺牲响应时间的可用性，保障核心功能的运行。</li><li><strong>流量削峰</strong>：在不同的时间，出售不同的票，将请求错开，削弱请求峰值。</li><li><strong>体验降级</strong>：大流量情况，通过降低图片的清晰度和大小，提升系统的处理能力。</li><li><strong>过载保护</strong>：请求排队时，如果请求等待时间超时，这时直接拒绝超时请求。或者队列满了之后，清除队列中一定数量的排队请求，保证系统不过载，实现系统的基本可用。</li></ul><h3 id="最终一致性"><a href="#最终一致性" class="headerlink" title="最终一致性"></a>最终一致性</h3><p>​        系统中所有的数据副本在经过一段时间的同步后，最终能达到一个一致的状态。这存在一个短暂的延迟。几乎所有的互联网系统采用的都是最终一致性，只有实在无法使用最终一致性，才使用强一致性或分布式事务。对于金融系统，需要考虑分布式事务。</p><p>​        如何实现最终一致性？</p><ul><li>以最新写入的数据为准，比如AP模型的KV存储采用的是这种方式。</li><li>以第一次写入的数据为准，如果你不希望存储的数据被更改，可以以它为准。</li></ul><p>​        如何实现最终一致性？</p><ul><li>读时修复：在读取数据时，检测数据的不一致，进行修复。</li><li>写时修复：在写入数据，检测数据的不一致，进行修复。<ul><li>写时修复不需要做数据一致性对比，性能消耗较低，对系统运行影响不大，推荐使用。</li></ul></li><li>异步修复：最常用的方式，通过定时对账检测副本数据的一致性，并修复。</li></ul><h1 id="算法"><a href="#算法" class="headerlink" title="算法"></a>算法</h1><h2 id="Paxos算法"><a href="#Paxos算法" class="headerlink" title="Paxos算法"></a>Paxos算法</h2><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>当前最常用的一批共识算法是基于该算法改进的。例如Fast Paxos算法、Cheap Paxos算法、Raft算法、ZAB协议等。</p><p>兰伯特提出的Paxos算法包含两个部分：</p><ul><li>Basic Paxos：描述的是多节点之间如何就某个Value达成共识。</li><li>Multi-Paxos：描述的是执行多个Basic Paxos实例，就一系列值达成共识。</li></ul><p>Basic Paxos是Multi-Paxos思想的核心，Multi-Paxos是多执行几次Basic Paxos。</p><h3 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h3><p>我们要实现一个分布式集群，集群有节点A、B、C构成，提供只读KV存储服务。一个节点创建只读变量后就无法修改它。所有节点必须对只读变量的值达成共识，然后所有节点一起创建这个只读变量。</p><p>那么当多个客户端试图创建同一个只读变量，例如client 1执行<code>set x = 3</code>，client 1执行<code>set x = 7</code>，那么分布式集群如何达成共识？</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/paxos/question.jpg" alt=""></p><h3 id="Basic-Paxos中的三种角色"><a href="#Basic-Paxos中的三种角色" class="headerlink" title="Basic Paxos中的三种角色"></a>Basic Paxos中的三种角色</h3><ul><li>提议者（Proposer）：提议一个值，用于投票表决。一般是集群中接收到客户端请求的节点。</li><li>接收者（Acceptor）：对每个提议的值进行投票，并存储接受的值。一般来说，集群中所有节点都是扮演接受者的角色，参与共识协商，并接受和存储数据。</li><li>学习者（Learner）：被告知投票的结果，接受达成共识的值，存储保存，不参与投票的过程。一般来说，学习者是数据备份节点，容灾备份。</li></ul><p>三种角色代表三种功能：</p><ul><li>提议者代表的是接入和协调功能，收到客户端请求后，发起二阶段提交，进行共识协商。</li><li>接受者代表投票协商和存储数据，对提议的值进行投票，并接受达成共识的值，进行存储保存。</li><li>学习者代表存储数据，不参与共识协商，只接受达成共识的值，存储保存。</li></ul><h3 id="如何达成共识？"><a href="#如何达成共识？" class="headerlink" title="如何达成共识？"></a>如何达成共识？</h3><p>在Basic Paxos中，用提案代表一个提议，除了提案编号，还包括提议值。使用<code>[n, v]</code>代表一个提案，其中n为提案编号，v为提议值。</p><h3 id="准备阶段"><a href="#准备阶段" class="headerlink" title="准备阶段"></a>准备阶段</h3><p>client 1、2作为提议者，分别向所有接受者发送包含提案编号的请求。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/paxos/prepare.jpg" alt=""></p><p>当节点A、B接收到提案编号为1的准备请求，C收到提案编号为5的准备请求，将这样处理：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/paxos/prepare-2.jpg" alt=""></p><ul><li>由于之前未通过任何提案，节点A、B将返回一个<strong>尚无提案</strong>的响应，这是告诉提议者，之前没有通过任何提案，并承诺以后不再响应提案编号小于等于1的准备请求，不会通过小于5的提案。</li><li>节点C也是入磁，返回一个<strong>尚无提案</strong>的响应，并承诺不响应提案编号小于等于5的请求，不会通过编号小于5的提案。</li></ul><p>当节点A、B接收到提案编号为5的准备请求，节点C收到提案编号为1的准备请求时，将这样处理：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/paxos/prepare-3.jpg" alt=""></p><ul><li>A、B接收到编号为5的提案时，因为编号5大于之前的编号1，并且两个节点都没有通过任何提案，所以返回一个<strong>尚无提案</strong>的响应，并承诺以后响应提案编号小于等于5的准备请求，不会通过编号小于5的提案。</li><li>C收到编号为1的准备请求时，由于小于编号5，所以丢弃该请求。</li></ul><h3 id="接受阶段"><a href="#接受阶段" class="headerlink" title="接受阶段"></a>接受阶段</h3><p>client1、2收到节点的响应后，会发送接受请求。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/paxos/accept.jpg" alt=""></p><ul><li>客户端1根据响应中提案编号最大的提案的值，设置接受请求中的值，由于响应是暂无提案，就把自己的提议值设置为提案的值，发送接受请求<code>[1,3]</code></li><li>客户端2收到准备响应后，根据响应中提案最大的值，设置请求中的值，由于A、B、C都是响应暂无提案，则将自己的提议值7作为提案值，发送接受请求<code>[5,7]</code>。</li></ul><p>当三个节点接收到客户端的接受请求后，会这样处理：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/paxos/accept-2.jpg" alt=""></p><ul><li>当节点A、B、C收到接受请求<code>[1,3]</code>的时候，由于提案编号1小于三个节点中能通过的提案的最小提案编号5，所以<code>[1,3]</code>将被拒绝。</li><li>当A、B、C收到接受请求<code>[5,7]</code>时，由于提案编号不小于三个节点承诺的能通过的提案的最小编号5，则通过提案<code>[5, 7]</code>。</li></ul><p>当提案被通过后，会通知给所有的学习者，当学习者发现大多数接受者都通过某个提案，那么它将接受该提案的值。</p><ul><li>Basic Paxos的容错能力，源自于大多数的约定。</li><li>Basic Paxos是通过二阶段提交的方式来达成共识的。</li><li>Basic Paxos还实现了容错，在少于一半节点出现故障时，集群也能工作。</li><li>本质上：提案编号的大小代表着<strong>优先级</strong>。</li><li>Basic Paxos只能就单个值达成共识。</li></ul><h3 id="Multi-Paxos"><a href="#Multi-Paxos" class="headerlink" title="Multi Paxos"></a>Multi Paxos</h3><p>由于Multi Paxos并没有提供实现细节，导致每个人实现的不一样，例如Chubby的Multi-Paxos实现、Raft算法、ZAB协议等。</p><p><strong>Multi Paxos是一种思想，它指的是基于Multi Paxos思想，通过多个Basic Paxos实例实现一系列值的共识算法。</strong></p><p>多次执行Basic Paxos实例，实现共识的问题：</p><ul><li>如果多个提议者同时提交提案，可能出现提案冲突，在准备阶段没有提议者接收到大多数准备响应，需要重新协商。</li><li>两轮RPC通信往返消息多、延迟大，需要考虑优化。</li></ul><h3 id="领导者"><a href="#领导者" class="headerlink" title="领导者"></a>领导者</h3><p>可以引入领导者节点，它作为唯一提议者，这样就不存在提案冲突的现象。</p><p><strong>在论文中，作者并没有说如何选举领导者，需要我们实现Multi Paxos算法时自己实现。</strong></p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/paxos/leader.jpg" alt=""></p><h3 id="优化Basic-Paxos执行"><a href="#优化Basic-Paxos执行" class="headerlink" title="优化Basic Paxos执行"></a>优化Basic Paxos执行</h3><p><strong>当leader处于稳定状态，省略掉准备阶段，直接进入接受阶段。</strong>leader节点中，序列中的命令都是最新的。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/paxos/leader-1.jpg" alt=""></p><h3 id="Chubby的Multi-Paxos实现"><a href="#Chubby的Multi-Paxos实现" class="headerlink" title="Chubby的Multi Paxos实现"></a>Chubby的Multi Paxos实现</h3><p>在Chubby中，主节点通过执行Basic Paxos算法，进行投票选举产生的，并且在运行过程中，主节点会通过不断续租的方式来延长租期。如果主节点出现故障，其他节点又会投票选举出新的主节点。</p><p>Chubby也实现了上述的优化机制。实现了成员变更（Group membership），以此保证节点变更的时候集群的平稳运行。</p><p>在Chubby中，为了实现强一致性，操作只能在主节点上执行。</p><ul><li>所有的读写请求都在主节点处理，当主节点接收到写请求时，作为提议者，执行Basic Paxos实例，将数据发送给所有的节点，并且大多数节点接受了这个写请求之后，再响应给客户端成功。</li><li>当主节点接收到读请求，只需要主节点查询本地数据。</li></ul><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/paxos/chubby.jpg" alt=""></p><h2 id="Raft算法"><a href="#Raft算法" class="headerlink" title="Raft算法"></a>Raft算法</h2><p>Raft算法属于Multi-Paxos算法，做了简化和限制。Raft算法是现在分布式系统开发首选的共识算法。本质上说：<strong>Raft算法是通过一切以领导者为准的方式，实现一系列值的共识和各节点日志的一致。</strong></p><blockquote><p>Q：如何保证同一个时间，集群中只有一个领导者？</p></blockquote><h3 id="节点身份"><a href="#节点身份" class="headerlink" title="节点身份"></a>节点身份</h3><ul><li>Leader：一切以领导者为准，主要工作为三部分：<ol><li>处理写请求</li><li>管理日志复制</li><li>发送心跳信息，告知其他节点自己还活着。</li></ol></li><li>Follower：默默接收和处理来自领导者的消息，当等待领导者心跳信息超时的时候，会主动站出来，推荐自己当候选人。</li><li>Candidate：候选人向其他节点发送请求投票RPC消息，通知其他节点来投票，如果赢得了投票，就晋升为领导者。</li></ul><h3 id="领导者选举"><a href="#领导者选举" class="headerlink" title="领导者选举"></a>领导者选举</h3><p>初始状态如下：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/raft/status.jpg" alt=""></p><p>Raft算法实现了<strong>随机超时时间</strong>（每个节点等待leader节点心跳信息的超时时间间隔是随机的）。</p><p>当节点超过自己的随机超时时间，节点会增加自己的任期编号，并推举自己为候选人，先给自己投一张选票，然后向其它节点发送请求投票的RPC消息。请他们选举自己为leader。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/raft/vote.jpg" alt=""></p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/raft/vote2.jpg" alt=""></p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/raft/success.jpg" alt=""></p><h4 id="节点之间如何通信？"><a href="#节点之间如何通信？" class="headerlink" title="节点之间如何通信？"></a>节点之间如何通信？</h4><p>通信采用RPC，在领导者选举过程中。通常用到两类RPC：</p><ul><li>请求投票RPC，由候选人在选举期间发起，通知各节点进行投票。</li><li>日志复制RPC：由领导者发起，用来复制日志和提供心跳信息。</li></ul><h4 id="任期"><a href="#任期" class="headerlink" title="任期"></a>任期</h4><p>Raft算法中的领导者有任期，每个任期由单调递增的数字标识（任期编号）.</p><ul><li>Follower在等待leader心跳信息超时后，推举自己为candidate时，会增加自己的任期号，比如节点A的当前任期编号为0，那么推举自己为candidate时，会将自己的任期编号增加为1.</li><li>如果一个节点发现自己的任期编号比其他节点小，那么它会更新自己的编号到较大的编号值，比如B的任期编号为0，当收到A的请求投票RPC消息时，因为消息中包含了A的任期编号，并且编号为1，那么B将自己的任期编号更改为1.</li></ul><p>Raft算法中的任期不只是时间段，会影响领导者选举和请求的处理。</p><ol><li>如果一个候选人或者领导者，发现自己的任期编号比其他节点小，那么它会立即恢复成跟随着状态，比如分区错误恢复后，任期编号为3的领导者节点B，收到来自新领导者的任期编号为4的心跳信息，节点B会立即恢复成跟随者。</li><li>如果一个节点接收到包含任期较小的请求，那么它会直接拒绝这个请求。</li></ol><h4 id="选举规则"><a href="#选举规则" class="headerlink" title="选举规则"></a>选举规则</h4><p>主要有以下几点规则：</p><ol><li>领导者周期性发送心跳信息，阻止跟随者发送新的选举。</li><li>指定时间内，跟随着没有接收到领导者信息，它会认为当前没有领导者，并推举自己为候选人，发起领导者选举。</li><li>在选举中，赢得大多数投票的候选人，晋升为领导者。</li><li>在一个任期内，除非领导者心跳消息未接收到，其他节点才能发出新一轮的选举。</li><li>一次选举中，一个节点最多会对一个任期编号投出一张选票，按照先来先服务的规则进行投票。</li><li>任期编号相同时，日志完整性高的跟随者（最后一条日志项对应的任期编号更大）拒绝投票给日志完整性低的候选者。（<strong>换而言之：日志最完整的节点才能当选领导者</strong>）</li></ol><h4 id="随机超时时间"><a href="#随机超时时间" class="headerlink" title="随机超时时间"></a>随机超时时间</h4><p>Raft算法如何处理选举无效的问题？（未达到半数票，需要重新选举）</p><p>Raft算法巧妙使用随机选举超时时间的方法，把超时时间分散开来，在大多数情况下只有一个服务器节点先发起选举，这样能减少因为同时选举而导致选票被瓜分导致选举失败的情况。</p><h3 id="日志复制"><a href="#日志复制" class="headerlink" title="日志复制"></a>日志复制</h3><p>Raft算法中，副本数据是以日志的形式存在的，领导者接收到来自客户端写请求后，处理写请求的过程就是一个复制和提交日志项的过程。</p><h4 id="日志"><a href="#日志" class="headerlink" title="日志"></a>日志</h4><p>日志由日志项构成，日志项是一种数据格式，主要包含用户指令，还包括一些附加信息，例如日志索引、任期编号。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/raft/log-entry.jpg" alt=""></p><h4 id="如何复制日志"><a href="#如何复制日志" class="headerlink" title="如何复制日志"></a>如何复制日志</h4><ul><li><p>接收到客户端请求，领导者创建一个日志项，并附加到本地日志中。</p></li><li><p>领导者通过日志复制RPC消息，将日志项复制到集群其他节点上。</p></li><li>在复制到其他节点上时，领导者会将这条日志项提交到它的状态机中。</li><li><p>领导者将执行的结果返回给客户端。</p></li><li><p>当跟随者接收到心跳消息，或者新的日志复制RPC消息后，如果跟随着发现领导者已经提交某条日志项，而它还没提交，那么跟随着就将这条日志项提交到本地的状态机中。</p></li></ul><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/raft/log.jpg" alt=""></p><h4 id="如何实现日志的一致"><a href="#如何实现日志的一致" class="headerlink" title="如何实现日志的一致"></a>如何实现日志的一致</h4><p>Raft算法中，以领导者的日志为准，来实现各节点日志的一致性。主要有以下两个步骤：</p><ul><li>首先，领导者通过日志复制RPC的一致性检查，找到跟随者节点上，与自己相同日志项的最大索引值，这个索引值之后的日志是不一致的。</li><li>然后领导者强制跟随者更新覆盖不一致的日志项。</li></ul><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/raft/process.jpg" alt=""></p><p>上图中PrevLogEntry表示<strong>当前要复制的日志项，前一条日志项的索引值</strong>，PrevLogTerm表示<strong>当前要复制的日志项，前一条日志项的任期编号。</strong></p><ol><li>领导者通过日志复制RPC消息，发送当前最新日志项到跟随者，消息的PrevLogEntry为7，PrevLogTerm为4</li><li>如果跟随者在它的日志中，找不到PrevLogEntry为7，PrevLogTerm为4的日志项，那么跟随者就会拒收新的日志项，并返回失败消息给领导者。</li><li>这时，领导者会递减要复制的日志项的索引值，发送新日志项给跟随者，这个消息的PrevLogEntry为6，PrevLogTerm为3。</li><li>如果跟随者在日志中，发现了[6,3]这个日志项，那么返回成功消息，这样领导者知道跟随者在这个位置与自己日志相同。</li><li>领导者通过日志复制RPC，复制并更新覆盖索引值之后的日志，最终实现各节点的日志一致。</li></ol><h3 id="成员变更"><a href="#成员变更" class="headerlink" title="成员变更"></a>成员变更</h3><p>当集群成员变更时，可能会出现多个领导者，Raft算法通过<strong>单节点变更</strong>来解决。</p><h4 id="单节点变更"><a href="#单节点变更" class="headerlink" title="单节点变更"></a>单节点变更</h4><p>通过一次变更一个节点实现成员变更，如果需要变更多个节点，那么则需要执行多次单节点变更。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/raft/single-node.jpg" alt=""></p><p>但是如果并发执行单节点变更，坑里出现一次单节点变更尚未完成，新的单节点变更又在执行，导致集群出现两个领导者的情况。</p><h3 id="一致性哈希算法"><a href="#一致性哈希算法" class="headerlink" title="一致性哈希算法"></a>一致性哈希算法</h3><p>Q：Raft算法实现了分布式存储，但是集群性能约等于单机性能，，如果通过分集群来提升性能，那么会存在数据迁移的过程，一致性哈希主要解决数据迁移成本高的问题。</p><p>一致性哈希算法具有较好的容错性和可扩展性。</p><h4 id="哈希寻址"><a href="#哈希寻址" class="headerlink" title="哈希寻址"></a>哈希寻址</h4><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/hashing/hashing-1.jpg" alt=""></p><p>当需要对指定key的值进行读写的时候，通过下面两步进行寻址：</p><ul><li>首先：将key作为参数执行<code>c - hash()</code>计算哈希值，并确定此key在环上的位置。</li><li>然后，从这个位置沿着哈希环顺时针走，遇到第一个节点就是key对应的节点。</li></ul><p>如果现在一个节点存在故障，如上图，假设C出现故障，那么key-03的寻址将被重定位到A。</p><p>如果在B、C之前扩容一个D节点，那么受影响的仅仅是key-03会被重定位到D。</p><p>在哈希寻址中，如果节点过少，可能会导致节点分布不均匀，造成数据访问的冷热不均，如下图：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/hashing/pic.jpg" alt=""></p><p>该问题主要通过<strong>虚拟节点</strong>来解决。</p><p>对每个节点计算多个哈希值，在每个计算结果位置上，都放置一个虚拟节点，并将虚拟节点映射到实际节点。如下图：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/hashing/pic1.jpg" alt=""></p><ul><li>一致性哈希是一种特殊的哈希算法，节点增减变化时只影响到部分数据的路由寻址，因此只需要迁移部分数据。</li><li>节点数量较少时，可能出现节点在哈希环上分布不均匀的情况，导致节点的访问冷热不均。</li><li>一致性哈希算法本质是一种路由寻址算法，适合简单的路由寻址场景。</li></ul><h2 id="Gossip协议"><a href="#Gossip协议" class="headerlink" title="Gossip协议"></a>Gossip协议</h2><p>用于实现最终一致性。利用一种随机、带有传染性的方式，将消息传播到整个网络中，并在一定时间内，使得系统内的所有节点数据一致。</p><p>三个主要功能：</p><ul><li>直接邮寄（Direct Mail）：直接发送更新数据，当数据发送失败时，将数据缓存下来，然后重传。<ul><li>可能因为缓存队列满了而丢失数据。无法实现最终一致性。</li></ul></li><li>反熵（Anti-entropy）：本质上是通过异步修复实现最终一致性。<ul><li>本意是指集群中的节点，每隔一段时间就随机选择某个其它节点，然后通过交换自己所有数据来消除两者之间的差异，实现数据的最终一致性。</li><li>推、拉、推拉三种方式。</li><li>反熵需要节点两两交换数据，成本较高。可以通过引入校验和等机制，降低需要对比的数据量和通讯消息。</li><li>但是如果在节点数量动态变化的分布式环境下，反熵不适用。</li></ul></li><li>谣言传播（Rumor mongering）：一个节点有了新数据后，这个节点变成活跃状态，并周期性地联系其它节点向其发送新数据，直到所有的节点都存储了该数据。</li></ul><h2 id="Quorum-NWR算法"><a href="#Quorum-NWR算法" class="headerlink" title="Quorum NWR算法"></a>Quorum NWR算法</h2><p>Quorum NWR算法可以通过组合NWR三个元素来实现自定义一致性级别的。</p><h3 id="三要素"><a href="#三要素" class="headerlink" title="三要素"></a>三要素</h3><ul><li>N：代表集群中同一份数据有多少个副本。</li><li>W：写一致性级别，表示成功完成W个副本的更新，才完成写操作。</li><li>R：读一致性级别，表示读取一个数据对象时需要读R个副本。换而言之，读取R副本时，返回R个副本中最新的那份数据。</li></ul><p>NWR三者的不同组合，会产生不同的一致性效果：</p><ol><li>W + R &gt; N时，对于客户端来讲，一定能保证强一致性。</li><li>W + R &lt; N时，对于客户端来讲，能保证最终一致性。</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;理论&quot;&gt;&lt;a href=&quot;#理论&quot; class=&quot;headerlink&quot; title=&quot;理论&quot;&gt;&lt;/a&gt;理论&lt;/h1&gt;&lt;h2 id=&quot;拜占庭将军问题&quot;&gt;&lt;a href=&quot;#拜占庭将军问题&quot; class=&quot;headerlink&quot; title=&quot;拜占庭将军问题&quot;&gt;&lt;/</summary>
      
    
    
    
    <category term="分布式系统" scheme="https://l1n.wang/categories/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    
    
    <category term="分布式系统" scheme="https://l1n.wang/tags/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    
  </entry>
  
  <entry>
    <title>Leetcode-数组刷题记录</title>
    <link href="https://l1n.wang/2021/01/leetcode-array/"/>
    <id>https://l1n.wang/2021/01/leetcode-array/</id>
    <published>2021-01-08T13:58:44.000Z</published>
    <updated>2021-02-18T09:43:27.335Z</updated>
    
    <content type="html"><![CDATA[<h2 id="数组的遍历"><a href="#数组的遍历" class="headerlink" title="数组的遍历"></a>数组的遍历</h2><h3 id="485-最大连续1的个数"><a href="#485-最大连续1的个数" class="headerlink" title="485. 最大连续1的个数"></a>485. 最大连续1的个数</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">findMaxConsecutiveOnes</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        <span class="hljs-comment">//res存储最长的子数组</span>        <span class="hljs-keyword">int</span> res = <span class="hljs-number">0</span>;        <span class="hljs-comment">//count记录的是每个被0隔断的子数组</span>        <span class="hljs-keyword">int</span> count = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++)&#123;            <span class="hljs-comment">//如果当前元素是1，更新当前子数组的长度</span>            <span class="hljs-keyword">if</span>(nums[i] == <span class="hljs-number">1</span>)&#123;                count++;            &#125;<span class="hljs-keyword">else</span> &#123;                <span class="hljs-comment">//遇到0，比较res, count的大小，并将count重置为0</span>                res = Math.max(res, count);                count = <span class="hljs-number">0</span>;            &#125;        &#125;        <span class="hljs-comment">//避免最后一个子数组是最大的情况，例如：1,0,1,1,0,1,1,1,1</span>        <span class="hljs-keyword">return</span> Math.max(res, count);    &#125;</code></pre><h3 id="495-提莫攻击"><a href="#495-提莫攻击" class="headerlink" title="495. 提莫攻击"></a>495. 提莫攻击</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">findPoisonedDuration</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] timeSeries, <span class="hljs-keyword">int</span> duration)</span> </span>&#123;        <span class="hljs-keyword">int</span> res = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; timeSeries.length; i ++)&#123;            <span class="hljs-comment">//当前能持续的时间</span>            <span class="hljs-keyword">int</span> time = timeSeries[i] + duration -<span class="hljs-number">1</span>;            <span class="hljs-comment">//超过下一次攻击的时间</span>            <span class="hljs-keyword">if</span>(i != timeSeries.length -<span class="hljs-number">1</span> &amp;&amp; time &gt;= timeSeries[i + <span class="hljs-number">1</span>])&#123;                res += (timeSeries[i + <span class="hljs-number">1</span>] - timeSeries[i]);            &#125;<span class="hljs-keyword">else</span>&#123;                res += duration;            &#125;        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="第三大的数"><a href="#第三大的数" class="headerlink" title="第三大的数"></a>第三大的数</h3><p>时间复杂度要求$O(n)$，也就是只能通过一趟遍历完成。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">thirdMax</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        <span class="hljs-keyword">if</span>(nums.length == <span class="hljs-number">1</span>)&#123;            <span class="hljs-keyword">return</span> nums[<span class="hljs-number">0</span>];        &#125;        <span class="hljs-keyword">if</span>(nums.length == <span class="hljs-number">2</span>)&#123;            <span class="hljs-keyword">return</span> nums[<span class="hljs-number">0</span>] &gt;= nums[<span class="hljs-number">1</span>] ? nums[<span class="hljs-number">0</span>] : nums[<span class="hljs-number">1</span>];        &#125;        <span class="hljs-keyword">long</span> firstMax = Long.MIN_VALUE;        <span class="hljs-keyword">long</span> secondMax = Long.MIN_VALUE;        <span class="hljs-keyword">long</span> thirdMax = Long.MIN_VALUE;    <span class="hljs-comment">//判断相等情况是避免相同数字,例如：[2, 2, 3, 1]</span>        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> num : nums)&#123;            <span class="hljs-keyword">if</span>(num &gt; firstMax) &#123;                thirdMax = secondMax;                secondMax = firstMax;                firstMax = num;            &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(num == firstMax) &#123;                <span class="hljs-keyword">continue</span>;            &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(num &gt; secondMax)&#123;                thirdMax = secondMax;                secondMax = num;            &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(num == secondMax) &#123;                <span class="hljs-keyword">continue</span>;            &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(num &gt; thirdMax)&#123;                thirdMax = num;            &#125;        &#125;        <span class="hljs-keyword">if</span>(thirdMax == Long.MIN_VALUE)&#123;            <span class="hljs-keyword">return</span> (<span class="hljs-keyword">int</span>)firstMax;        &#125;<span class="hljs-keyword">else</span>&#123;            <span class="hljs-keyword">return</span> (<span class="hljs-keyword">int</span>) thirdMax;        &#125;    &#125;</code></pre><h3 id="628-三个数的最大乘积"><a href="#628-三个数的最大乘积" class="headerlink" title="628. 三个数的最大乘积"></a>628. 三个数的最大乘积</h3><p>偷🐔解法，需要注意考虑负数的情况。</p><p>时间复杂度为$O(nlogN)$。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">maximumProduct</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        Arrays.sort(nums);        <span class="hljs-keyword">int</span> len = nums.length;                <span class="hljs-keyword">return</span> Math.max(nums[<span class="hljs-number">0</span>] * nums[<span class="hljs-number">1</span>] * nums[nums.length - <span class="hljs-number">1</span>],                                 nums[nums.length - <span class="hljs-number">1</span>] * nums[nums.length - <span class="hljs-number">2</span>] * nums[nums.length - <span class="hljs-number">3</span>]);    &#125;</code></pre><p>如果使用一趟遍历的方法，那么需要使用五个变量，分别保存最大的三个数，和最小的两个数。</p><h2 id="统计数组中的元素"><a href="#统计数组中的元素" class="headerlink" title="统计数组中的元素"></a>统计数组中的元素</h2><h3 id="645-错误的集合"><a href="#645-错误的集合" class="headerlink" title="645. 错误的集合"></a>645. 错误的集合</h3><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span>[] findErrorNums(<span class="hljs-keyword">int</span>[] nums) &#123;        <span class="hljs-keyword">int</span>[] res = <span class="hljs-keyword">new</span> <span class="hljs-keyword">int</span>[<span class="hljs-number">2</span>];        <span class="hljs-keyword">int</span>[] count = <span class="hljs-keyword">new</span> <span class="hljs-keyword">int</span>[nums.length];        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> num : nums)&#123;            count[num - <span class="hljs-number">1</span>]++;        &#125;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i&lt; count.length;i++)&#123;            <span class="hljs-keyword">if</span>(count[i] == <span class="hljs-number">2</span>)&#123;                res[<span class="hljs-number">0</span>] = i + <span class="hljs-number">1</span>;            &#125;<span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(count[i] == <span class="hljs-number">0</span>)&#123;                res[<span class="hljs-number">1</span>] = i + <span class="hljs-number">1</span>;            &#125;        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="697-数组的度"><a href="#697-数组的度" class="headerlink" title="697. 数组的度"></a>697. 数组的度</h3><p>三个<strong>HashMap</strong>的作用分别如下：</p><ul><li>left：数字第一次出现的索引</li><li>right：数字最后一次出现的索引</li><li>count：数字出现的次数</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">findShortestSubArray</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        Map&lt;Integer, Integer&gt; left = <span class="hljs-keyword">new</span> HashMap(),            right = <span class="hljs-keyword">new</span> HashMap(), count = <span class="hljs-keyword">new</span> HashMap();        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++) &#123;            <span class="hljs-keyword">int</span> x = nums[i];            <span class="hljs-keyword">if</span> (left.get(x) == <span class="hljs-keyword">null</span>) left.put(x, i);            right.put(x, i);            count.put(x, count.getOrDefault(x, <span class="hljs-number">0</span>) + <span class="hljs-number">1</span>);        &#125;        <span class="hljs-keyword">int</span> ans = nums.length;        <span class="hljs-keyword">int</span> degree = Collections.max(count.values());        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> x: count.keySet()) &#123;            <span class="hljs-keyword">if</span> (count.get(x) == degree) &#123;                ans = Math.min(ans, right.get(x) - left.get(x) + <span class="hljs-number">1</span>);            &#125;        &#125;        <span class="hljs-keyword">return</span> ans;    &#125;</code></pre><h3 id="448-找到所有数组中消失的数字"><a href="#448-找到所有数组中消失的数字" class="headerlink" title="448. 找到所有数组中消失的数字"></a>448. 找到所有数组中消失的数字</h3><p><strong>HashMap</strong>做记录，再通过一轮遍历找到不存在的数字。</p><p>时间复杂度$O(n)$，空间复杂度$O(n)$。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;Integer&gt; <span class="hljs-title">findDisappearedNumbers</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        HashMap&lt;Integer, Boolean&gt; map = <span class="hljs-keyword">new</span> HashMap&lt;Integer, Boolean&gt;();        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++) &#123;            map.put(nums[i], <span class="hljs-keyword">true</span>);        &#125;        List&lt;Integer&gt; res = <span class="hljs-keyword">new</span> LinkedList&lt;Integer&gt;();        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">1</span>; i &lt;= nums.length; i++) &#123;            <span class="hljs-keyword">if</span> (!map.containsKey(i)) &#123;                res.add(i);            &#125;        &#125;          <span class="hljs-keyword">return</span> res;    &#125;</code></pre><p>如果使用时间复杂度$O(n)$，并不用额外的空间，可以通过添加标记来解决。</p><p>一轮遍历添加标记：能标识该数字出现过。</p><p>第二轮遍历查找没有做标记的数字。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;Integer&gt; <span class="hljs-title">findDisappearedNumbers</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++) &#123;            <span class="hljs-keyword">int</span> index = (nums[i] - <span class="hljs-number">1</span>) % nums.length;            nums[index] += nums.length;        &#125;        List&lt;Integer&gt; res = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++)&#123;            <span class="hljs-keyword">if</span>(nums[i] &lt;= nums.length)&#123;                res.add(i + <span class="hljs-number">1</span>);            &#125;        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="442-数组中重复的数据"><a href="#442-数组中重复的数据" class="headerlink" title="442. 数组中重复的数据"></a>442. 数组中重复的数据</h3><p>与上面一题异曲同工。使用标记法。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;Integer&gt; <span class="hljs-title">findDuplicates</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++) &#123;            <span class="hljs-keyword">int</span> index = (nums[i] - <span class="hljs-number">1</span>) % nums.length;            nums[index] += nums.length;        &#125;        List&lt;Integer&gt; res = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++)&#123;            <span class="hljs-keyword">if</span>(nums[i] &gt; nums.length * <span class="hljs-number">2</span>)&#123;                res.add(i + <span class="hljs-number">1</span>);            &#125;        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="41-缺失的第一个正数"><a href="#41-缺失的第一个正数" class="headerlink" title="41. 缺失的第一个正数"></a>41. 缺失的第一个正数</h3><p>通过索引交换元素。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">firstMissingPositive</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++) &#123;            <span class="hljs-keyword">while</span>((nums[i] &gt;= <span class="hljs-number">1</span> &amp;&amp; nums[i] &lt;= nums.length) &amp;&amp; (nums[i] != nums[nums[i] - <span class="hljs-number">1</span>]))&#123;                <span class="hljs-keyword">int</span> temp = nums[i];                nums[i] = nums[temp -<span class="hljs-number">1</span>];                nums[temp -<span class="hljs-number">1</span>] = temp;            &#125;        &#125;        <span class="hljs-keyword">int</span> res = nums.length + <span class="hljs-number">1</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++)&#123;            <span class="hljs-keyword">if</span> (nums[i] != (i + <span class="hljs-number">1</span>)) &#123;                res = i + <span class="hljs-number">1</span>;                <span class="hljs-keyword">break</span>;            &#125;        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="274-H指数"><a href="#274-H指数" class="headerlink" title="274. H指数"></a>274. H指数</h3><p>偷🐔了一下，直接sort解决。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">hIndex</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] citations)</span> </span>&#123;        <span class="hljs-keyword">int</span> len = citations.length;        Arrays.sort(citations);        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; len; i++)&#123;            <span class="hljs-keyword">int</span> count = len - i;            <span class="hljs-keyword">if</span>(citations[i] &gt;= count)&#123;                <span class="hljs-keyword">return</span> count;            &#125;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125;</code></pre><h2 id="数组的改变、移动"><a href="#数组的改变、移动" class="headerlink" title="数组的改变、移动"></a>数组的改变、移动</h2><h3 id="453-最小操作次数使数组元素相等"><a href="#453-最小操作次数使数组元素相等" class="headerlink" title="453. 最小操作次数使数组元素相等"></a>453. 最小操作次数使数组元素相等</h3><p>每次操作会使$n-1$个元素增加1  == &gt;  每次操作让一个元素减去1</p><p>因此需要计算的次数就是：<strong>数组中所有元素（除去最小值）与最小值之间的差值。</strong></p><p>（Python的API用的舒服）</p><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">minMoves</span>(<span class="hljs-params">self, nums</span>):</span>        <span class="hljs-built_in">sum</span> = <span class="hljs-number">0</span>        minnum = <span class="hljs-built_in">min</span>(nums)        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> nums:            <span class="hljs-built_in">sum</span> += i - minnum        <span class="hljs-keyword">return</span> <span class="hljs-built_in">sum</span></code></pre><h3 id="665-非递减数列"><a href="#665-非递减数列" class="headerlink" title="665. 非递减数列"></a>665. 非递减数列</h3><p>拐点不能出现两次，修复会有两种情况：</p><ul><li>nums[i - 2] &gt; nums[i]：将nums[i]改为nums[i - 1]</li><li>nums[i - 2] &lt;= nums[i]：将nums[i - 1]改为nums[i]</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">checkPossibility</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        <span class="hljs-keyword">int</span> count = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">1</span> ; i&lt; nums.length; i++)&#123;            <span class="hljs-keyword">if</span>(nums[i - <span class="hljs-number">1</span>] &gt; nums[i])&#123;                count++;                <span class="hljs-keyword">if</span>(count &gt;= <span class="hljs-number">2</span>) <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;                <span class="hljs-keyword">if</span>(i - <span class="hljs-number">2</span> &gt;= <span class="hljs-number">0</span> &amp;&amp; nums[i - <span class="hljs-number">2</span>] &gt; nums[i])&#123;                    nums[i] = nums[i - <span class="hljs-number">1</span>];                &#125;<span class="hljs-keyword">else</span>&#123;                    nums[i - <span class="hljs-number">1</span>] = nums[i];                &#125;            &#125;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;&#125;</code></pre><h3 id="283-移动零"><a href="#283-移动零" class="headerlink" title="283. 移动零"></a>283. 移动零</h3><p>通过<code>index</code>存储有效位。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">moveZeroes</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums)</span> </span>&#123;        <span class="hljs-keyword">if</span> (nums == <span class="hljs-keyword">null</span> || nums.length == <span class="hljs-number">0</span>) &#123;            <span class="hljs-keyword">return</span>;        &#125;        <span class="hljs-keyword">int</span> index = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++) &#123;            <span class="hljs-keyword">if</span> (nums[i] != <span class="hljs-number">0</span>) &#123;                nums[index++] = nums[i];            &#125;        &#125;        <span class="hljs-keyword">while</span> (index &lt; nums.length) &#123;            nums[index++] = <span class="hljs-number">0</span>;        &#125;    &#125;</code></pre><h2 id="二维数组及滚动数组"><a href="#二维数组及滚动数组" class="headerlink" title="二维数组及滚动数组"></a>二维数组及滚动数组</h2><h3 id="118-杨辉三角"><a href="#118-杨辉三角" class="headerlink" title="118. 杨辉三角"></a>118. 杨辉三角</h3><pre><code class="hljs java"><span class="hljs-keyword">public</span> List&lt;List&lt;Integer&gt;&gt; generate(<span class="hljs-keyword">int</span> numRows) &#123;       List&lt;List&lt;Integer&gt;&gt; ans = <span class="hljs-keyword">new</span> ArrayList&lt;List&lt;Integer&gt;&gt;();        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; numRows; i++)&#123;            List&lt;Integer&gt; list = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();            list.add(<span class="hljs-number">1</span>);            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j = <span class="hljs-number">1</span>; j &lt; i; j++)&#123;                List&lt;Integer&gt; front = ans.get(i - <span class="hljs-number">1</span>);                list.add(front.get(j) + front.get(j -<span class="hljs-number">1</span>));            &#125;            <span class="hljs-keyword">if</span>(i &gt; <span class="hljs-number">0</span>)&#123;                list.add(<span class="hljs-number">1</span>);            &#125;            ans.add(list);        &#125;        <span class="hljs-keyword">return</span> ans;    &#125;</code></pre><h3 id="119-杨辉三角Ⅱ"><a href="#119-杨辉三角Ⅱ" class="headerlink" title="119. 杨辉三角Ⅱ"></a>119. 杨辉三角Ⅱ</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;Integer&gt; <span class="hljs-title">getRow</span><span class="hljs-params">(<span class="hljs-keyword">int</span> rowIndex)</span> </span>&#123;        List&lt;List&lt;Integer&gt;&gt; ans = <span class="hljs-keyword">new</span> ArrayList&lt;List&lt;Integer&gt;&gt;();        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt;= rowIndex; i++)&#123;            List&lt;Integer&gt; list = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();            list.add(<span class="hljs-number">1</span>);            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j = <span class="hljs-number">1</span>; j &lt; i; j++)&#123;                List&lt;Integer&gt; front = ans.get(i - <span class="hljs-number">1</span>);                list.add(front.get(j) + front.get(j -<span class="hljs-number">1</span>));            &#125;            <span class="hljs-keyword">if</span>(i &gt; <span class="hljs-number">0</span>)&#123;                list.add(<span class="hljs-number">1</span>);            &#125;            ans.add(list);        &#125;        <span class="hljs-keyword">return</span> ans.get(rowIndex);    &#125;</code></pre><h3 id="598-范围求和Ⅱ"><a href="#598-范围求和Ⅱ" class="headerlink" title="598. 范围求和Ⅱ"></a>598. 范围求和Ⅱ</h3><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">maxCount</span><span class="hljs-params">(<span class="hljs-keyword">int</span> m, <span class="hljs-keyword">int</span> n, <span class="hljs-keyword">int</span>[][] ops)</span> </span>&#123;        <span class="hljs-keyword">int</span> minX = m , minY = n;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; ops.length; i++)&#123;            minX = Math.min(minX, ops[i][<span class="hljs-number">0</span>]);            minY = Math.min(minY, ops[i][<span class="hljs-number">1</span>]);        &#125;        <span class="hljs-keyword">return</span> minX * minY;    &#125;</code></pre><h3 id="419-甲板上的战舰"><a href="#419-甲板上的战舰" class="headerlink" title="419. 甲板上的战舰"></a>419. 甲板上的战舰</h3><p>暴力判断：如果当前点是X，判断其左边和上边是否有X，没有则不连通，增加计数。</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">countBattleships</span><span class="hljs-params">(<span class="hljs-keyword">char</span>[][] board)</span> </span>&#123;        <span class="hljs-keyword">int</span> count = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt;board.length; i++)&#123;            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> j = <span class="hljs-number">0</span>; j &lt; board[i].length; j++)&#123;                <span class="hljs-keyword">if</span>(board[i][j] == <span class="hljs-string">&#x27;X&#x27;</span>)&#123;                    <span class="hljs-keyword">if</span>(i &gt; <span class="hljs-number">0</span> &amp;&amp; board[i - <span class="hljs-number">1</span>][j] == <span class="hljs-string">&#x27;X&#x27;</span> || j &gt; <span class="hljs-number">0</span> &amp;&amp; board[i][j - <span class="hljs-number">1</span>] == <span class="hljs-string">&#x27;X&#x27;</span>) <span class="hljs-keyword">continue</span>;                    count++;                &#125;            &#125;        &#125;        <span class="hljs-keyword">return</span> count;    &#125;</code></pre><h3 id="189-数组的旋转"><a href="#189-数组的旋转" class="headerlink" title="189. 数组的旋转"></a>189. 数组的旋转</h3><p>三轮reverse。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">rotate</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums, <span class="hljs-keyword">int</span> k)</span> </span>&#123;        <span class="hljs-comment">//如果数组长度小于旋转次数，那么执行k次和执行k % length次结果一样</span>        k %= nums.length;        reverse(nums, <span class="hljs-number">0</span>, nums.length - <span class="hljs-number">1</span>);        reverse(nums, <span class="hljs-number">0</span>, k - <span class="hljs-number">1</span>);        reverse(nums, k, nums.length - <span class="hljs-number">1</span>);    &#125;    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">reverse</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] nums, <span class="hljs-keyword">int</span> start, <span class="hljs-keyword">int</span> end)</span> </span>&#123;        <span class="hljs-keyword">while</span> (start &lt; end) &#123;            <span class="hljs-keyword">int</span> tmp = nums[start];            nums[start] = nums[end];            nums[end] = tmp;            start++;            end--;        &#125;    &#125;</code></pre><h3 id="396-旋转函数"><a href="#396-旋转函数" class="headerlink" title="396. 旋转函数"></a>396. 旋转函数</h3><p>可通过数学归纳法得到$F[k]= F[k-1] + sum - n*A[n-k]$</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">maxRotateFunction</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[] A)</span> </span>&#123;        <span class="hljs-keyword">int</span> n = A.length;        <span class="hljs-keyword">int</span> max = <span class="hljs-number">0</span>;        <span class="hljs-keyword">int</span> count = <span class="hljs-number">0</span>;        <span class="hljs-comment">// 统计数组所有数的和</span>        <span class="hljs-keyword">int</span> sum = <span class="hljs-number">0</span>;        <span class="hljs-comment">// 计算 F(1) 的值</span>        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i : A) &#123;            max += count++ * i;            sum += i;        &#125;        <span class="hljs-keyword">int</span> tmp = max;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">1</span>; i &lt; n; i++) &#123;            tmp = tmp + sum - n * A[n - i];            max = Math.max(tmp, max);        &#125;        <span class="hljs-keyword">return</span> max;    &#125;</code></pre><h2 id="特定顺序遍历二维数组"><a href="#特定顺序遍历二维数组" class="headerlink" title="特定顺序遍历二维数组"></a>特定顺序遍历二维数组</h2><h3 id="54-螺旋矩阵"><a href="#54-螺旋矩阵" class="headerlink" title="54. 螺旋矩阵"></a>54. 螺旋矩阵</h3><p>抓住两个index的变化趋势，按照规律变化就行。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;Integer&gt; <span class="hljs-title">spiralOrder</span><span class="hljs-params">(<span class="hljs-keyword">int</span>[][] matrix)</span> </span>&#123;        List&lt;Integer&gt; res = <span class="hljs-keyword">new</span> ArrayList&lt;&gt;();        <span class="hljs-keyword">int</span> m = matrix.length, n = matrix[<span class="hljs-number">0</span>].length;        <span class="hljs-keyword">int</span> up = <span class="hljs-number">0</span>, down = m - <span class="hljs-number">1</span>, left = <span class="hljs-number">0</span>, right = n -<span class="hljs-number">1</span>;        <span class="hljs-keyword">while</span>(<span class="hljs-keyword">true</span>)&#123;            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = left; i &lt;= right; i++) res.add(matrix[up][i]);            <span class="hljs-keyword">if</span>(++up &gt; down) <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = up; i &lt;= down; i++) res.add(matrix[i][right]);            <span class="hljs-keyword">if</span> (--right &lt; left) <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = right; i &gt;= left; i--) res.add(matrix[down][i]);            <span class="hljs-keyword">if</span> (--down &lt; up) <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = down; i &gt;= up; i--) res.add(matrix[i][left]);            <span class="hljs-keyword">if</span>(++left &gt; right) <span class="hljs-keyword">break</span>;        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><h3 id="59-螺旋矩阵Ⅱ"><a href="#59-螺旋矩阵Ⅱ" class="headerlink" title="59. 螺旋矩阵Ⅱ"></a>59. 螺旋矩阵Ⅱ</h3><p>和上题一样。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span>[][] generateMatrix(<span class="hljs-keyword">int</span> n) &#123;        <span class="hljs-keyword">int</span>[][] matrix = <span class="hljs-keyword">new</span> <span class="hljs-keyword">int</span>[n][n];        <span class="hljs-keyword">int</span> up = <span class="hljs-number">0</span>, down = n - <span class="hljs-number">1</span>, left = <span class="hljs-number">0</span>, right = n -<span class="hljs-number">1</span>;        <span class="hljs-keyword">int</span> num = <span class="hljs-number">1</span>;        <span class="hljs-keyword">while</span>(<span class="hljs-keyword">true</span>)&#123;            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = left; i &lt;= right; i++) matrix[up][i] = num++;            <span class="hljs-keyword">if</span>(++up &gt; down) <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = up; i &lt;= down; i++) matrix[i][right] = num++;            <span class="hljs-keyword">if</span> (--right &lt; left) <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = right; i &gt;= left; i--) matrix[down][i] = num++;            <span class="hljs-keyword">if</span> (--down &lt; up) <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = down; i &gt;= up; i--) matrix[i][left] = num++;            <span class="hljs-keyword">if</span>(++left &gt; right) <span class="hljs-keyword">break</span>;        &#125;        <span class="hljs-keyword">return</span> matrix;    &#125;</code></pre><h3 id="498-对角线遍历"><a href="#498-对角线遍历" class="headerlink" title="498. 对角线遍历"></a>498. 对角线遍历</h3><h2 id="二维数组变换"><a href="#二维数组变换" class="headerlink" title="二维数组变换"></a>二维数组变换</h2><h3 id="566-重塑矩阵"><a href="#566-重塑矩阵" class="headerlink" title="566. 重塑矩阵"></a>566. 重塑矩阵</h3><p>笨方法，放入队列中，然后逐个按序取出。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span>[][] matrixReshape(<span class="hljs-keyword">int</span>[][] nums, <span class="hljs-keyword">int</span> r, <span class="hljs-keyword">int</span> c) &#123;        <span class="hljs-keyword">int</span>[][] res = <span class="hljs-keyword">new</span> <span class="hljs-keyword">int</span>[r][c];        <span class="hljs-keyword">if</span> (nums.length == <span class="hljs-number">0</span> || r * c != nums.length * nums[<span class="hljs-number">0</span>].length)            <span class="hljs-keyword">return</span> nums;        <span class="hljs-keyword">int</span> count = <span class="hljs-number">0</span>;        Queue &lt;Integer&gt; queue = <span class="hljs-keyword">new</span> LinkedList &lt; &gt; ();        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++) &#123;            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> j = <span class="hljs-number">0</span>; j &lt; nums[<span class="hljs-number">0</span>].length; j++) &#123;                queue.add(nums[i][j]);            &#125;        &#125;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; r; i++) &#123;            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> j = <span class="hljs-number">0</span>; j &lt; c; j++) &#123;                res[i][j] = queue.remove();            &#125;        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre><p>原地算法：</p><ul><li>$count$用于保存当前到第几个元素</li><li>$count / c$用于计算当前是第几行，$count % c$用于计算当前是第几个。</li></ul><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span>[][] matrixReshape(<span class="hljs-keyword">int</span>[][] nums, <span class="hljs-keyword">int</span> r, <span class="hljs-keyword">int</span> c) &#123;        <span class="hljs-keyword">int</span>[][] res = <span class="hljs-keyword">new</span> <span class="hljs-keyword">int</span>[r][c];        <span class="hljs-keyword">if</span> (nums.length == <span class="hljs-number">0</span> || r * c != nums.length * nums[<span class="hljs-number">0</span>].length)            <span class="hljs-keyword">return</span> nums;        <span class="hljs-keyword">int</span> count = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.length; i++) &#123;            <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> j = <span class="hljs-number">0</span>; j &lt; nums[<span class="hljs-number">0</span>].length; j++) &#123;                res[count / c][count % c] = nums[i][j];                count++;            &#125;        &#125;        <span class="hljs-keyword">return</span> res;    &#125;</code></pre>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;数组的遍历&quot;&gt;&lt;a href=&quot;#数组的遍历&quot; class=&quot;headerlink&quot; title=&quot;数组的遍历&quot;&gt;&lt;/a&gt;数组的遍历&lt;/h2&gt;&lt;h3 id=&quot;485-最大连续1的个数&quot;&gt;&lt;a href=&quot;#485-最大连续1的个数&quot; class=&quot;headerli</summary>
      
    
    
    
    <category term="计算机基础" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/"/>
    
    <category term="算法" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/%E7%AE%97%E6%B3%95/"/>
    
    
    <category term="算法" scheme="https://l1n.wang/tags/%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>2020总结</title>
    <link href="https://l1n.wang/2020/12/2020-summary/"/>
    <id>https://l1n.wang/2020/12/2020-summary/</id>
    <published>2020-12-31T02:49:19.000Z</published>
    <updated>2021-08-17T14:44:05.346Z</updated>
    
    <content type="html"><![CDATA[<div id="hexo-blog-encrypt" data-wpm="Oh, this is an invalid password. Check and try again, please." data-whm="OOPS, these decrypted content may changed, but you can still have a look.">  <div class="hbe-input-container">  <input type="password" id="hbePass" placeholder="" />    <label for="hbePass">Hey, password is required here.</label>    <div class="bottom-line"></div>  </div>  <script id="hbeData" type="hbeData" data-hmacdigest="e8eb7f5ed336b6bcae5677b8b5b3a84a50972bbec701580aa9752c080dc7f4a9"></script></div><script src="/lib/blog-encrypt.js"></script><link href="/css/blog-encrypt.css" rel="stylesheet" type="text/css">]]></content>
    
    
    <summary type="html">Here&#39;s something encrypted, password is required to continue reading.</summary>
    
    
    
    <category term="随笔" scheme="https://l1n.wang/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
  </entry>
  
  <entry>
    <title>MapReduce论文阅读笔记</title>
    <link href="https://l1n.wang/2020/12/MapReduce-note/"/>
    <id>https://l1n.wang/2020/12/MapReduce-note/</id>
    <published>2020-12-23T11:29:00.000Z</published>
    <updated>2020-12-28T11:08:49.599Z</updated>
    
    <content type="html"><![CDATA[<h2 id="存在的问题"><a href="#存在的问题" class="headerlink" title="存在的问题"></a>存在的问题</h2><p>Google所面临的问题：大数据处理任务庞大，如何通过分布式系统完成并行计算、分发数据、处理错误？</p><p>为了解决这个问题，需要设计一个新的抽象模型，用来表述我们需要执行的计算。不用关心底层的实现细节，包括并行计算、容错、数据分布、负载均衡等方面。</p><h2 id="编程模型"><a href="#编程模型" class="headerlink" title="编程模型"></a>编程模型</h2><p>MapReduce编程模型的原理：利用一个输入的<strong>key/value pair</strong>集合来产生一个输出的<strong>key/value pair</strong>集合。</p><p>自定义的Map函数接受一个<strong>key/value pair</strong>输入，然后产生一个中间<strong>key/value pair</strong>集合。会将相同key和对应多个value值集合在一起传递给reduce函数。</p><p>自定义的Reduce函数接受上面的集合，合并这些value值，形成一个新的value值集合。</p><p>下面是一个计算大文档集合中每个单词出现的次数的案例：</p><pre><code class="hljs php">map(<span class="hljs-keyword">String</span> key, <span class="hljs-keyword">String</span> value):<span class="hljs-comment">//key : document name</span><span class="hljs-comment">//value: document contents</span><span class="hljs-keyword">foreach</span> word w in value:emitintermediate(w, <span class="hljs-string">&quot;1&quot;</span>);reduce(<span class="hljs-keyword">String</span> key, <span class="hljs-built_in">Iterator</span> values):<span class="hljs-comment">//key : a word</span><span class="hljs-comment">//values: a list of counts</span><span class="hljs-keyword">int</span> result = <span class="hljs-number">0</span>;<span class="hljs-keyword">foreach</span> v in values:result += ParseInt(v);emit(AsString(result));</code></pre><p>Map计函数计算文档中<code>(word,count)</code>这样的<strong>key/value pair</strong>，Reduce函数把Map函数产生的计数累加起来。</p><p>Map Reduce函数可以抽象成以下形式：</p><pre><code class="hljs livescript"><span class="hljs-keyword">map</span><span class="hljs-function"><span class="hljs-params">(k1, v1)</span> -&gt;</span> <span class="hljs-keyword">list</span>(k2, v2)reduce<span class="hljs-function"><span class="hljs-params">(k2, <span class="hljs-keyword">list</span>(v2))</span> -&gt;</span> <span class="hljs-keyword">list</span>(v2)</code></pre><h3 id="其他案例"><a href="#其他案例" class="headerlink" title="其他案例"></a>其他案例</h3><ul><li>分布式的grep：Map输出匹配某个模式的一行，Reduce将中间数据处理再输出。</li><li>计算URL访问频率：Map处理日志，输出<code>(URL, 1)</code>，Reduce将中间数据累加处理，产生<code>(URL, totalCount)</code>再输出。</li></ul><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>将输入数据自动分割成M块数据片段，Map函数在多台机器上并行处理。Reduce调用也是在多台机器上并行处理。MapReduce实现的全部流程如图所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/distribute%20system/MapReduce/execution-overview.jpg" alt=""></p><h3 id="master数据结构"><a href="#master数据结构" class="headerlink" title="master数据结构"></a>master数据结构</h3><p>Master存储每个Map和Reduce任务的状态，以及Worker机器的标识。</p><p>Master像一个数据管道，存储已完成的Map任务的计算结果。并将这些信息推送给Reduce任务。</p><h3 id="容错"><a href="#容错" class="headerlink" title="容错"></a>容错</h3><ul><li>worker故障<ul><li>master会对worker进行心跳检测，如果在指定时间内没有收到返回信息，会将worker标记为失效。</li><li>所有由这个失效worker完成的Map任务需要重新分配给其他的worker。</li><li>当这个Map任务被调度到worker B执行时，会通知执行Reduce任务的worker从这台机器上读取数据。</li></ul></li><li>master故障<ul><li>让master定期的将上述的数据结构写入磁盘，如果master出现故障，可以让新的master从这个<strong>checkpoint</strong>恢复。</li><li>由于只有一个master进程，如果master失效，需要中止MapReduce运算。</li></ul></li></ul><h3 id="存储位置"><a href="#存储位置" class="headerlink" title="存储位置"></a>存储位置</h3><p>MapReduce的master在调度Map任务时会考虑输入文件的位置信息，尽量将一个Map任务调度在有数据拷贝的机器上执行。这样可以减少网络带宽的消耗。</p><h3 id="任务粒度"><a href="#任务粒度" class="headerlink" title="任务粒度"></a>任务粒度</h3><p>Map拆分成 M个片段，Reduce拆分成R个片段执行。</p><p>但是实际上对M和R的取值有一定的客观限制：</p><ul><li>master必须执行$O(M + R)$次调度</li><li>需要在内存中保存$O(M * R)$个状态</li></ul><p>R由用户指定的，实际使用时需要选择合适的M，以使得每一个独立任务都处理大约16M~64M的输入数据。R值应该设置为worker机器数量的倍数。</p><h3 id="备用任务"><a href="#备用任务" class="headerlink" title="备用任务"></a>备用任务</h3><p>如果一个worker花了很长时间才完成最后几个Map或Reduce任务，会导致MapReduce操作总的执行时间超过预期。产生以上现象的原因有很多。</p><p>因此需要使用一个通用的机制来减少这种现象，当一个MapReduce操作接近完成的时候，master调度备用任务进程来处理剩下的任务。</p><h2 id="改良的功能"><a href="#改良的功能" class="headerlink" title="改良的功能"></a>改良的功能</h2><h3 id="分区函数"><a href="#分区函数" class="headerlink" title="分区函数"></a>分区函数</h3><p>选择什么函数来进行对数据进行分区？</p><p><code>hash(key) mod R</code>能产生非常平衡的分区，但是，其它分区函数对一些key的分区效果较好，例如输入的key值是URL，我们希望每个主机的所有条目保持在同一个输出文件中。</p><h3 id="顺序保证"><a href="#顺序保证" class="headerlink" title="顺序保证"></a>顺序保证</h3><p>需要保证Reduce的生成顺序。</p><h3 id="Combiner函数"><a href="#Combiner函数" class="headerlink" title="Combiner函数"></a>Combiner函数</h3><p>某些情况下，Map函数产生的中间key值的重复数据会占很大的比重，因此，允许用户指定一个可选的combiner函数，该函数首先在本地将重复数据进行一次合并，然后再通过网络发送出去。</p><h3 id="输入和输出的类型"><a href="#输入和输出的类型" class="headerlink" title="输入和输出的类型"></a>输入和输出的类型</h3><p>可以预定义输入/输出的数据类型。</p><h3 id="跳过损坏的记录"><a href="#跳过损坏的记录" class="headerlink" title="跳过损坏的记录"></a>跳过损坏的记录</h3><p>有时，用户程序中的Bug会导致Map/Reduce函数在处理记录的时候发生crash，常用的做法是修复Bug后再执行MapReduce操作。</p><p>但是很多时候，忽略一些有问题的记录是可以接受的，因此提供了一种执行模式，MapReduce会检测哪些记录导致了crash，并且会跳过这些记录不做处理。</p><p>每个worker进程都设置了信号处理函数来捕获内存段错误和总线错误。在执行MapReduce之前，通过全局变量保存记录序号。如果用户程序触发了一个系统信号，消息处理函数会通过UDP向master发送处理的最后一条记录的序号，当master看到处理某条记录失败多次后，master标记此条记录将不会被处理。</p><h3 id="本地运行"><a href="#本地运行" class="headerlink" title="本地运行"></a>本地运行</h3><p>需要本地进行debug来测试。</p><h3 id="状态信息"><a href="#状态信息" class="headerlink" title="状态信息"></a>状态信息</h3><p>master可以使用HTTP服务器来显示状态信息，用户可以监控各类信息。包括任务完成数量、任务处理数量、执行时间、资源占用等。</p><h3 id="计数器"><a href="#计数器" class="headerlink" title="计数器"></a>计数器</h3><p>MapReduce提供计数功能，需要用户在程序中创建一个计数器对象，在Map/Reduce函数中相应的增加计数器的值。</p><p>计数器机制对于MapReduce操作的完整性检查十分有用。某些时候，用户需要确保输出的<strong>key/value pair</strong>精确等于输入的<strong>key/value pair</strong></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;存在的问题&quot;&gt;&lt;a href=&quot;#存在的问题&quot; class=&quot;headerlink&quot; title=&quot;存在的问题&quot;&gt;&lt;/a&gt;存在的问题&lt;/h2&gt;&lt;p&gt;Google所面临的问题：大数据处理任务庞大，如何通过分布式系统完成并行计算、分发数据、处理错误？&lt;/p&gt;
&lt;p&gt;为</summary>
      
    
    
    
    <category term="分布式系统" scheme="https://l1n.wang/categories/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    
    
    <category term="分布式系统" scheme="https://l1n.wang/tags/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F/"/>
    
  </entry>
  
  <entry>
    <title>OSTEP阅读笔记-Persistence（三）</title>
    <link href="https://l1n.wang/2020/12/ostep-persistence/"/>
    <id>https://l1n.wang/2020/12/ostep-persistence/</id>
    <published>2020-12-22T11:49:48.000Z</published>
    <updated>2020-12-23T02:51:08.199Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Chapter-I-O设备"><a href="#Chapter-I-O设备" class="headerlink" title="Chapter I/O设备"></a>Chapter I/O设备</h2><blockquote><p>Q：如何将I/O集成进计算机系统中？</p></blockquote><h3 id="36-1-系统架构"><a href="#36-1-系统架构" class="headerlink" title="36.1 系统架构"></a>36.1 系统架构</h3><p>典型的系统架构如图所示：CPU通过<strong>memory bus</strong>连接到系统内存。显卡或者其它高速I/O设备通过常规的IO总线（I/O bus，例如PCI）连接到系统。最后是外围总线（peripheral bus，例如SCSI、SATA、USB），他们将最慢的设备连接到系统，包括磁盘、鼠标等。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-36/36.1.jpg" alt=""></p><h3 id="36-2-标准设备"><a href="#36-2-标准设备" class="headerlink" title="36.2 标准设备"></a>36.2 标准设备</h3><p>标准设备包括两部分，分别是：</p><ul><li>向系统其他部分展现的硬件/软件接口</li><li>内部结构，包括设备相关的特定实现，负责具体实现设备展现给系统的抽象接口。</li></ul><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-36/36.2.jpg" alt=""></p><h3 id="36-3-标准协议"><a href="#36-3-标准协议" class="headerlink" title="36.3 标准协议"></a>36.3 标准协议</h3><p>一个简化的设备接口包括三个寄存器：</p><ul><li>一个状态（<strong>status</strong>）寄存器：读取并查看设备的当前状态</li><li>一个命令（<strong>command</strong>）寄存器：用于通知设备执行某个具体任务。</li><li>一个数据（<strong>data</strong>）寄存器：将数据传给设备或从设备接收数据。</li></ul><p>通过读写这些寄存器，OS可以控制设备的行为。</p><p>因此可以将操作步骤设置为如下：</p><ol><li>OS反复读取状态寄存器，等待设备进入可以接受命令的就绪状态。（轮询）</li><li>OS将数据发送到数据寄存器。</li><li>OS将命令写入命令寄存器。</li><li>OS再次通过不断轮询设备，等待并判断设备是否执行完成命令。</li></ol><blockquote><p>Q：如何减少频繁轮询，从而降低管理设备的CPU的开销？</p></blockquote><h3 id="36-4-利用中断减少CPU开销"><a href="#36-4-利用中断减少CPU开销" class="headerlink" title="36.4 利用中断减少CPU开销"></a>36.4 利用中断减少CPU开销</h3><p>设备可以抛出一个硬件中断，引发CPU跳转执行OS预先定义好的中断服务例程（ISR）或者更为简单的中断处理程序（interrupt handler）。</p><p>使用中断并非是最佳方案，假如有一个高性能的设备，在CPU第一次轮询时就可以返回结果，那么使用中断反而效率更低。如果设备的速度未知，可以考虑混合策略，先尝试轮询一小段事件，如果设备没有完成操作，此时再使用中断。</p><p>如果是网络环境中，那么不要使用中断，因为每个数据包都会发生一次中断，那么可能导致OS发生活锁（一直处理中断程序而不处理用户的请求）。</p><p>另一个基于中断的优化就是合并（coalescing），设备在抛出中断之前往往会等待一小段时间，在此期间如果有其他请求的中断，会将多次中断合并为一次中断抛出。从而降低处理中断的代价。</p><h3 id="36-5-DMA方式"><a href="#36-5-DMA方式" class="headerlink" title="36.5 DMA方式"></a>36.5 DMA方式</h3><blockquote><p>Q：如何减少Programming IO的开销？</p></blockquote><p>使用DMA（<strong>Direct Memory Access</strong>）：DMA引擎是一种特殊设备，它可以协调完成内存和设备间的数据传递，不需要CPU介入。</p><p>工作过程如下：为了能够将数据传送给设备，OS通过programming告诉DMA引擎数据在内存的位置，要拷贝的大小以及拷贝到哪个设备。在此之后，OS就可以处理其他请求了。当DMA的任务完成后，DMA控制器会抛出一个中断来告诉OS自己完成了数据传输。</p><h3 id="36-6-设备交互的方法"><a href="#36-6-设备交互的方法" class="headerlink" title="36.6 设备交互的方法"></a>36.6 设备交互的方法</h3><blockquote><p>Q：如何与设备通信？</p></blockquote><ul><li>使用明确的I/O指令。</li><li>内存映射I/O（memory-mapped I/O）<ul><li>通过这种方式，硬件将设备寄存器映射到指定的内存地址中。</li></ul></li></ul><h3 id="36-7-纳入OS：设备驱动程序"><a href="#36-7-纳入OS：设备驱动程序" class="headerlink" title="36.7 纳入OS：设备驱动程序"></a>36.7 纳入OS：设备驱动程序</h3><p>每个设备都有非常具体的接口，如何将它们纳入OS，而我们希望OS尽可能通用。</p><blockquote><p>Q：如何实现一个设备无关的OS？</p></blockquote><p>操作系统将与IO设备交互的软件称为设备驱动程序。（封装，向上层展现接口即可）</p><h3 id="36-8-IDE磁盘驱动程序"><a href="#36-8-IDE磁盘驱动程序" class="headerlink" title="36.8 IDE磁盘驱动程序"></a>36.8 IDE磁盘驱动程序</h3><p>基本逻辑如下：</p><ol><li><strong>等待驱动就绪</strong>：当驱动READY，读取状态寄存器（0x1F7）</li><li><strong>向命令寄存器写入参数</strong>：写入扇区数，待访问扇区对应的逻辑块地址（LBA），并将驱动编号（master= 0x00，slave = 0x10，因为IDE允许接入两个硬盘）写入命令寄存器（0x1F2- 0x1F6）。</li><li><strong>开启I/O</strong>：发送读写命令到命令寄存器。向命令寄存器（0x1F7）中写入<code>READ-WRITE</code>命令。</li><li><strong>数据传送</strong>（针对写请求）：等待直到驱动状态为<code>READY</code>和<code>DRQ</code>（驱动请求数据），向数据端口写入数据。</li><li><strong>中断处理</strong>：每个扇区的数据传送结束后都会触发一次中断处理程序，可以合并中断。</li><li><strong>错误处理</strong>：每次操作之后读取状态寄存器，如果ERROR位被置位。可以读取错误寄存器来获取详细信息。</li></ol><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-36/36.5.jpg" alt=""></p><p>四个主要函数：</p><ul><li><strong>ide_rw()</strong>：将一个请求加入队列，或者直接将请求发送到磁盘（通过<code>ide_start_request()</code>）</li><li><strong>ide_start_request()</strong>：将请求发送到磁盘</li><li><strong>ide_wait_ready()</strong>：确保驱动处于就绪状态</li><li><strong>ide_intr()</strong>：发生中断时，该函数会被调用，会从设备中读取数据，并且结束后唤醒等待的进程。如果队列中还有未处理的请求，会调用第二个函数接着处理下一个IO请求。</li></ul><h2 id="Chapter-37-磁盘驱动器"><a href="#Chapter-37-磁盘驱动器" class="headerlink" title="Chapter 37 磁盘驱动器"></a>Chapter 37 磁盘驱动器</h2><blockquote><p>Q：现代磁盘驱动器如何存储数据？接口是什么？数据是如何存储和访问的？磁盘调度如何提高性能？</p></blockquote><h3 id="37-1-接口"><a href="#37-1-接口" class="headerlink" title="37.1 接口"></a>37.1 接口</h3><p>现代驱动器的基本接口非常简单，驱动器由大量扇区组成，可以进行多扇区操作，但是写入操作只能保证单扇区是原子性的。无法在断电情况下保证完整写入。</p><h3 id="37-2-基本几何形状"><a href="#37-2-基本几何形状" class="headerlink" title="37.2 基本几何形状"></a>37.2 基本几何形状</h3><ul><li><strong>盘片（platter）</strong>：磁盘有一个或多个盘片，每个盘片有两面，都称为表面（surface），盘片由一些硬质材料制成，然后涂上薄薄的磁性层，从而持久存储数据位。</li><li><strong>主轴（spindle）</strong>：所有盘片围绕主轴连接在一起，主轴连接到电机，以一个恒定的速度旋转盘片，旋转速度以每分钟转数（Rotations Per Minute, RPM）来测量。</li><li><strong>扇区（sector）</strong>：将磁道划分成若干个弧段，每个弧段称为一个扇区。</li><li><strong>磁道（track）</strong>：一个表面由磁道构成，是同心圆的结构。</li><li><strong>磁头（disk head）</strong>：要从表面进行读写操作，需要一种机制，能感应磁盘上的磁性数据，读写操作由磁头完成。</li><li><strong>磁盘臂（disk arm）</strong>：每个表面都有一个磁头，磁头连接到单个磁盘臂，磁盘臂在表面移动，将磁头定位到期望的磁道上。</li></ul><h3 id="37-3-简单的磁盘驱动器"><a href="#37-3-简单的磁盘驱动器" class="headerlink" title="37.3 简单的磁盘驱动器"></a>37.3 简单的磁盘驱动器</h3><h4 id="单磁道延迟：旋转延迟"><a href="#单磁道延迟：旋转延迟" class="headerlink" title="单磁道延迟：旋转延迟"></a>单磁道延迟：旋转延迟</h4><p>旋转延迟（<strong>rotation delay</strong>）：磁头移动的时间</p><h4 id="寻道时间"><a href="#寻道时间" class="headerlink" title="寻道时间"></a>寻道时间</h4><p>寻道时间包括以下阶段：</p><ol><li>磁盘臂移动时的加速阶段</li><li>随着磁盘臂全速移动而惯性移动</li><li>随着磁盘臂减速，最后磁道在正确的磁道上停下来，停放时间通常不小，例如<code>0.5 ~ 2ms</code>。</li></ol><h4 id="其他细节"><a href="#其他细节" class="headerlink" title="其他细节"></a>其他细节</h4><ul><li><p>磁道偏斜（track skew）：确保即使在跨越磁道边界时，顺序读取也能方便地服务。</p></li><li><p>多区域（multi-zoned）：外圈磁道比内圈磁道具有更多扇区，磁盘被组织成多个区域，区域是表面上连续的一组磁道。每个区域每个磁道具有相同的扇区数量。</p></li><li>磁道缓冲区（track buffer）：少量的内存空间，通常大小位8MB或16MB。例如：从磁盘读取扇区时，驱动器可能决定读取该磁道上的所有扇区并将其缓存在其驱动器上，这样可以让驱动器快速响应所有后续的同一磁道的请求。</li></ul><p>写入操作分为以下几种：</p><ul><li>后写（write back）：将数据放入内存中，在一段时间后写入。</li><li>直写（write through）：直接写入磁盘。</li></ul><h3 id="37-4-I-O时间"><a href="#37-4-I-O时间" class="headerlink" title="37.4 I/O时间"></a>37.4 I/O时间</h3><script type="math/tex; mode=display">T_{I/O} = T_{寻道} + T_{旋转}$ + T_{传输}</script><h3 id="37-5-磁盘调度"><a href="#37-5-磁盘调度" class="headerlink" title="37.5 磁盘调度"></a>37.5 磁盘调度</h3><h4 id="SSTF：最短寻道时间优先"><a href="#SSTF：最短寻道时间优先" class="headerlink" title="SSTF：最短寻道时间优先"></a>SSTF：最短寻道时间优先</h4><p>会导致饥饿问题，距离长的始终无法调度。</p><h4 id="SCAN电梯算法"><a href="#SCAN电梯算法" class="headerlink" title="SCAN电梯算法"></a>SCAN电梯算法</h4><h4 id="SPTF：最短定位时间优先"><a href="#SPTF：最短定位时间优先" class="headerlink" title="SPTF：最短定位时间优先"></a>SPTF：最短定位时间优先</h4><h2 id="Chapter-38-RAID"><a href="#Chapter-38-RAID" class="headerlink" title="Chapter 38 RAID"></a>Chapter 38 RAID</h2><blockquote><p>Q：如何构建大型、快速、可靠的存储系统？</p></blockquote><p>RAID（Redundant Array of Inexpensive Disks）：廉价冗余磁盘阵列。</p><p>从外部看：RAID看起来像一个磁盘：一组可以读取或写入的块。</p><p>在内部：RAID是由多个磁盘、内存以及多个处理器来管理系统，更像一个计算机系统，专门管理一组磁盘。</p><p>RAID优点：</p><ul><li>性能高：并行使用多个磁盘可以大大加快I/O时间</li><li>容量大</li><li>可靠性：RAID通过redundancy，可以增加数据可靠性。</li><li>透明部署：向系统添加新功能时，不需要对其余部分进行修改</li></ul><h3 id="38-1-接口和RAID内部"><a href="#38-1-接口和RAID内部" class="headerlink" title="38.1 接口和RAID内部"></a>38.1 接口和RAID内部</h3><p>提供接口给上层调用。</p><p>RAID内部包括一个微控制器，运行固件以指导RAID的操作。某些情况下，还包括非易失性存储器，安全地缓冲写入。</p><h3 id="38-2-故障模型"><a href="#38-2-故障模型" class="headerlink" title="38.2 故障模型"></a>38.2 故障模型</h3><ul><li><strong>fail-stop</strong>：故障停止模型，在这种模式下，磁盘可以处于两种状态之一：工作状态或故障状态，使用工作状态的磁盘时，所有块都可以读写，相反，磁盘出现故障时，将不提供服务。</li></ul><h3 id="38-3-如何评估RAID"><a href="#38-3-如何评估RAID" class="headerlink" title="38.3 如何评估RAID"></a>38.3 如何评估RAID</h3><p>三个方面：</p><ol><li>容量</li><li>可靠性</li><li>性能</li></ol><h3 id="38-4-RAID-0：条带化"><a href="#38-4-RAID-0：条带化" class="headerlink" title="38.4 RAID 0：条带化"></a>38.4 RAID 0：条带化</h3><p>实际上不是RAID级别，因为没有冗余。</p><p>基本思想：以轮转方式将磁盘阵列的块分布在磁盘上。目的是对数组的连续块请求时，从阵列中获得最大的并行性。将同一行中的块称为条带。</p><p>RAID映射问题：给定一个逻辑块地址A，RAID如何确定物理磁盘和偏移量？</p><p>使用一些映射关系进行计算。</p><h4 id="大块大小"><a href="#大块大小" class="headerlink" title="大块大小"></a>大块大小</h4><p>大块大小影响针列地性能，大小较小的大块意味着许多文件将多个磁盘进行条带化，从而增加了对单个文件的读取和写入的并行性。但是，跨多个磁盘访问块的定位时间会增加。因为整个请求的定位时间由所有驱动器上请求的最大定位时间决定。</p><h3 id="38-5-RAID-1：镜像"><a href="#38-5-RAID-1：镜像" class="headerlink" title="38.5 RAID 1：镜像"></a>38.5 RAID 1：镜像</h3><p>只需要生成系统中每个块的多个副本。</p><p>一致性更新问题：可以通过预写日志解决，首先记录RAID简要执行的操作，采用这种办法，可以在发生崩溃时，通过运行恢复程序，将数据恢复到最新状态。</p><h3 id="38-6-RAID-4：通过奇偶校验节省空间"><a href="#38-6-RAID-4：通过奇偶校验节省空间" class="headerlink" title="38.6 RAID 4：通过奇偶校验节省空间"></a>38.6 RAID 4：通过奇偶校验节省空间</h3><p>对于每一条数据，都添加一个奇偶校验（parity）块，用于存储该条块的冗余信息。</p><p>为了计算奇偶性，需要使用一个数学函数，可以使用XOR。</p><p>存在小写入问题（small-write problem），导致性能下降。</p><h3 id="38-7-RAID-5：旋转奇偶校验"><a href="#38-7-RAID-5：旋转奇偶校验" class="headerlink" title="38.7 RAID 5：旋转奇偶校验"></a>38.7 RAID 5：旋转奇偶校验</h3><p>为了解决小写入问题，推出了RAID-5。RAID-5的工作原理与RAID-4几乎完全相同，只是RAID-5将奇偶校验块跨驱动器旋转。</p><h2 id="Chapter-39-文件和目录"><a href="#Chapter-39-文件和目录" class="headerlink" title="Chapter 39 文件和目录"></a>Chapter 39 文件和目录</h2><blockquote><p>Q：OS如何管理持久设备？</p></blockquote><h3 id="39-1-文件和目录"><a href="#39-1-文件和目录" class="headerlink" title="39.1 文件和目录"></a>39.1 文件和目录</h3><p>存储虚拟化形成了两个关键的抽象：</p><ul><li>文件：线性字节数组，每个文件都有一个<strong>low-level name</strong>。通常这个name被称为inode number。</li><li>目录：也存在inode number，包括一个<code>(用户可读名字,low-level name)</code>键值对列表</li></ul><h3 id="39-2-文件系统接口"><a href="#39-2-文件系统接口" class="headerlink" title="39.2 文件系统接口"></a>39.2 文件系统接口</h3><h4 id="创建文件"><a href="#创建文件" class="headerlink" title="创建文件"></a>创建文件</h4><pre><code class="hljs C"><span class="hljs-keyword">int</span> fd = open(<span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span> * pathname, <span class="hljs-keyword">int</span> flags);</code></pre><p>flags部分含义如下：</p><ul><li>O_CREAT：创建文件</li><li>O_WRONLY：以只读方式打开文件</li><li>O_WRONLY：以只写方式打开文件</li></ul><h3 id="39-4-读写文件"><a href="#39-4-读写文件" class="headerlink" title="39.4 读写文件"></a>39.4 读写文件</h3><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">ssize_t</span> <span class="hljs-title">write</span> <span class="hljs-params">(<span class="hljs-keyword">int</span> fd, <span class="hljs-keyword">const</span> <span class="hljs-keyword">void</span> * buf, <span class="hljs-keyword">size_t</span> count)</span></span>;</code></pre><p>write()会把buf所指的内存写入count个字节到参数fd所指的文件内. 当然, 文件读写位置也会随之移动。</p><p>返回值：如果顺利write()会返回实际写入的字节数. 当有错误发生时则返回-1, 错误代码存入errno中.</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">ssize_t</span> <span class="hljs-title">read</span><span class="hljs-params">(<span class="hljs-keyword">int</span> fd, <span class="hljs-keyword">void</span> * buf, <span class="hljs-keyword">size_t</span> count)</span></span>;</code></pre><p>read()会把参数fd 所指的文件传送count 个字节到buf 指针所指的内存中. 若参数count 为0, 则read()不会有作用并返回0. 返回值为实际读取到的字节数, 如果返回0, 表示已到达文件尾或是无可读取的数据,此外文件读写位置会随读取到的字节移动。</p><h3 id="39-6-fsync"><a href="#39-6-fsync" class="headerlink" title="39.6 fsync()"></a>39.6 fsync()</h3><p>当程序调用<code>write()</code>时，只是将数据写入内存缓冲区中，等过一段时间写入存储设备。可以调用<code>fsync()</code>以确保立即强制写入磁盘。</p><h3 id="39-7-文件重命名"><a href="#39-7-文件重命名" class="headerlink" title="39.7 文件重命名"></a>39.7 文件重命名</h3><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">rename</span><span class="hljs-params">(<span class="hljs-keyword">char</span> * oldname, <span class="hljs-keyword">char</span> * newname)</span></span>;</code></pre><h3 id="39-8-获取文件信息"><a href="#39-8-获取文件信息" class="headerlink" title="39.8 获取文件信息"></a>39.8 获取文件信息</h3><p>这些信息通常称为metadata，通过<code>stat()</code>或<code>fstat()</code>系统调用完成。</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">stat</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span> * file_name, struct stat *buf)</span></span>;</code></pre><p>stat结构体如下：</p><pre><code class="hljs C"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">stat</span></span><span class="hljs-class">&#123;</span>    <span class="hljs-keyword">dev_t</span> st_dev; <span class="hljs-comment">//device 文件的设备编号</span>    <span class="hljs-keyword">ino_t</span> st_ino; <span class="hljs-comment">//inode 文件的i-node</span>    <span class="hljs-keyword">mode_t</span> st_mode; <span class="hljs-comment">//protection 文件的类型和存取的权限</span>    <span class="hljs-keyword">nlink_t</span> st_nlink; <span class="hljs-comment">//number of hard links 连到该文件的硬连接数目, 刚建立的文件值为1.</span>    <span class="hljs-keyword">uid_t</span> st_uid; <span class="hljs-comment">//user ID of owner 文件所有者的用户识别码</span>    <span class="hljs-keyword">gid_t</span> st_gid; <span class="hljs-comment">//group ID of owner 文件所有者的组识别码</span>    <span class="hljs-keyword">dev_t</span> st_rdev; <span class="hljs-comment">//device type 若此文件为装置设备文件, 则为其设备编号</span>    <span class="hljs-keyword">off_t</span> st_size; <span class="hljs-comment">//total size, in bytes 文件大小, 以字节计算</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> st_blksize; <span class="hljs-comment">//blocksize for filesystem I/O 文件系统的I/O 缓冲区大小.</span>    <span class="hljs-keyword">unsigned</span> <span class="hljs-keyword">long</span> st_blocks; <span class="hljs-comment">//number of blocks allocated 占用文件区块的个数, 每一区块大小为512 个字节.</span>    <span class="hljs-keyword">time_t</span> st_atime; <span class="hljs-comment">//time of lastaccess 文件最近一次被存取或被执行的时间, 一般只有在用mknod、utime、read、write 与tructate 时改变.</span>    <span class="hljs-keyword">time_t</span> st_mtime; <span class="hljs-comment">//time of last modification 文件最后一次被修改的时间, 一般只有在用mknod、utime 和write 时才会改变</span>    <span class="hljs-keyword">time_t</span> st_ctime; <span class="hljs-comment">//time of last change i-node 最近一次被更改的时间, 此参数会在文件所有者、组、权限被更改时更新</span>&#125;;</code></pre><h3 id="39-9-删除文件"><a href="#39-9-删除文件" class="headerlink" title="39.9 删除文件"></a>39.9 删除文件</h3><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">unlink</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span> * pathname)</span></span>;</code></pre><h3 id="39-10-创建目录"><a href="#39-10-创建目录" class="headerlink" title="39.10 创建目录"></a>39.10 创建目录</h3><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">mkdir</span><span class="hljs-params">(<span class="hljs-keyword">const</span> <span class="hljs-keyword">char</span> *pathname, <span class="hljs-keyword">mode_t</span> mode)</span></span>;</code></pre><h3 id="39-13-硬链接"><a href="#39-13-硬链接" class="headerlink" title="39.13 硬链接"></a>39.13 硬链接</h3><p>link()系统调用，在创建链接的目录中创建了另一个名称，并将其指向原有的文件的inode number。</p><h3 id="39-14-符号链接"><a href="#39-14-符号链接" class="headerlink" title="39.14 符号链接"></a>39.14 符号链接</h3><p>也称为软链接，硬链接有些局限：不能创建目录的硬链接（担心会在目录树中创建一个环），不能硬链接到其他磁盘分区中的文件（inode在文件系统中唯一，不能跨文件系统）</p><p>符号链接实际上与硬链接完全不同，其本身实际上是一个不同类型的文件。符号链接指向文件的路径作为链接文件的数据。</p><h2 id="Chapter-40-文件系统实现"><a href="#Chapter-40-文件系统实现" class="headerlink" title="Chapter 40 文件系统实现"></a>Chapter 40 文件系统实现</h2><blockquote><p>Q：如何构建一个简单的文件系统？磁盘上需要什么结构？</p></blockquote><h3 id="40-1-思考方式"><a href="#40-1-思考方式" class="headerlink" title="40.1 思考方式"></a>40.1 思考方式</h3><ul><li>文件系统的结构：文件系统在磁盘上使用哪些类型的结构来组织数据和元数据。</li><li>访问方法：如何将进程发出的调用映射到它的结构上。</li></ul><h3 id="40-2-整体组织"><a href="#40-2-整体组织" class="headerlink" title="40.2 整体组织"></a>40.2 整体组织</h3><p>首先将磁盘分成一系列的块，每块大小4KB，在N个4KB的块中，分出大部分空间存储用户数据，这部分被称为<strong>data region</strong>。还有一部分磁盘空间需要存放元数据，这部分被称为<strong>inode table</strong>。</p><p>还需要一些空间记录inode或者数据块是空闲还是已分配。可以使用空闲列表（free list），或者使用位图（bitmap）。</p><p>还需要一块留给superblock，其中包含一些特定的文件系统信息，包括文件系统中有多少个inode和数据块、inode表的开始位置。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-40/disk-structure.jpg" alt=""></p><h3 id="40-3-文件组织：inode"><a href="#40-3-文件组织：inode" class="headerlink" title="40.3 文件组织：inode"></a>40.3 文件组织：inode</h3><p><code>inode</code>用于保存元数据结构，例如其长度、权限以及其组成块的位置。每个<code>inode</code>都由一个数字隐式引用。</p><p><code>inode</code>表大小为20KB，由80个<code>inode</code>组成。每个<code>inode</code>大小为<code>256 bytes</code>。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-40/inode-table.jpg" alt=""></p><p>要读取<code>inode 32</code>，文件系统首先计算inode区域的偏移量（32 * inode大小 = 8192），将其加商inode表的起始地址（inodeStartAddr = 12KB） ，从而可以得到希望的inode块的正确地址。</p><p>inode块的扇区地址<code>iaddr</code>可以计算如下：</p><pre><code class="hljs C">blk = (inumber * <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">inode_t</span>)) / blockSize;sector = ((blk * blockSize) + inodeStartAddr) / sectorSize;</code></pre><h4 id="多级索引"><a href="#多级索引" class="headerlink" title="多级索引"></a>多级索引</h4><p>为了支持更大的文件，文件系统的设计者必须在inode中引入不同的结构，一种常见的思路是用一个称为间接指针，它指向包含更多指针的块，每个指针再指向用户数据。</p><h3 id="40-4-目录组织"><a href="#40-4-目录组织" class="headerlink" title="40.4 目录组织"></a>40.4 目录组织</h3><p>目录基本上只包含一个二元组<code>(条目名称, inode号)</code>。但是线性存储不是存储这些信息的唯一方法，任何数据结构都可以，XFS使用B树存储目录。</p><h3 id="40-5-空闲空间管理"><a href="#40-5-空闲空间管理" class="headerlink" title="40.5 空闲空间管理"></a>40.5 空闲空间管理</h3><p>文件系统需要记录哪些inode和数据块是空闲的，哪些不是。当创建一个文件时，需要为该文件分配一个inode，文件系统将通过位图搜索一个空闲的内容，将其分配给文件，文件系统将inode标记为已使用。</p><h3 id="40-6-访问路径：读取和写入"><a href="#40-6-访问路径：读取和写入" class="headerlink" title="40.6 访问路径：读取和写入"></a>40.6 访问路径：读取和写入</h3><h4 id="从磁盘读取文件"><a href="#从磁盘读取文件" class="headerlink" title="从磁盘读取文件"></a>从磁盘读取文件</h4><p>发出<code>open(&quot;/foo/bar&quot;, O_RDONLY)</code>调用时，文件系统首先需要找到文件bar的inode，从而获得文件的基本信息，通过遍历路径名查找。从根目录开始遍历。找到inode后，读取包含foo的inode以及目录数据的块，最后找到bar的inode号，通过<code>open()</code>系统调用将该inode读入内存。打开后，通过<code>read()</code>系统调用，从文件中读取。</p><h4 id="写入磁盘"><a href="#写入磁盘" class="headerlink" title="写入磁盘"></a>写入磁盘</h4><p>写入文件是一个类似的过程，首先文件必须打开，然后通过<code>write()</code>系统调用以新内容更新文件，最后关闭文件。</p><p>写入文件可能会分配一个块，当写入一个新文件时，不仅需要将数据写入磁盘，还需要决定将哪个块分配给文件，从而相应地更新磁盘的其他结构。每次更新会导致五个I/O，分别是：</p><ul><li>读取数据位图，然后更新以标记新分配的块被使用</li><li>一个写入位图（将新状态存入磁盘）</li><li>两次读取，然后写入inode</li><li>最后一次写入真正的数据块本身</li></ul><blockquote><p>Q：如何降低文件系统I/O成本？</p></blockquote><h3 id="40-7-缓存和缓冲"><a href="#40-7-缓存和缓冲" class="headerlink" title="40.7 缓存和缓冲"></a>40.7 缓存和缓冲</h3><p>大多数文件系统使用DRAM来缓存重要的块。</p><p>早期文件系统引入一个固定大小的缓存来保存常用的块。然而静态的内存划分可能导致浪费。</p><p>现代系统采用动态划分方法，具体来说，许多现代操作系统将虚拟内存页面和文件系统集成到统一页面缓存中（unified page cache）通过这种方式，可以在虚拟内存和文件系统中更灵活的分配内存。</p><h2 id="Chapter-44-数据完整性和保护"><a href="#Chapter-44-数据完整性和保护" class="headerlink" title="Chapter 44 数据完整性和保护"></a>Chapter 44 数据完整性和保护</h2><blockquote><p>Q：系统如何确保数据完整性？</p></blockquote><h3 id="44-1-磁盘故障模式"><a href="#44-1-磁盘故障模式" class="headerlink" title="44.1 磁盘故障模式"></a>44.1 磁盘故障模式</h3><p>分为两种类型：</p><ul><li>潜在扇区错误（latent-sector errors , LSE）</li><li>块讹误（block corruption）</li></ul><p>当扇区以某种方式发生block corruption时，会出现LSE。例如磁头由于某种原因接触到表面（磁头碰撞，head crash），则会使得数据位不可读。</p><p>驱动器会使用<strong>Error Correcting Code</strong>来确定磁盘位是否良好，会修复错误位。</p><h3 id="44-2-处理潜在的扇区错误"><a href="#44-2-处理潜在的扇区错误" class="headerlink" title="44.2 处理潜在的扇区错误"></a>44.2 处理潜在的扇区错误</h3><blockquote><p>Q：如何处理潜在的扇区错误？</p></blockquote><p>如果存储系统尝试访问块，并且磁盘块返回错误时，需要用它的冗余机制来返回正确数据。</p><p>但是，LSE的增长影响了RAID的设计。当全盘故障核LSE接连发生时，RAID-4/5会尝试读取奇偶校验组中的所有磁盘，并重新计算缺失值，来重建磁盘。</p><h3 id="44-3-检查讹误：校验和"><a href="#44-3-检查讹误：校验和" class="headerlink" title="44.3 检查讹误：校验和"></a>44.3 检查讹误：校验和</h3><blockquote><p>Q：如何检测出现了讹误？</p></blockquote><p>现代存储系统用于保持数据完整性的主要机制称为校验和（checksum），校验和是一个函数的结果。该函数以一块数据作为输入，输出数据内容的概要（4字节或8字节），此摘要称为校验和。</p><p>通过将数据和校验和一起存储，然后再访问时确定数据是否被破坏或改变。</p><h3 id="44-5-错误的写入"><a href="#44-5-错误的写入" class="headerlink" title="44.5 错误的写入"></a>44.5 错误的写入</h3><p>错误位置的写入（misdirected write）：写入位置出错。</p><blockquote><p> Q：如何解决错误的写入？</p></blockquote><p>在每个校验和中添加更多的信息，例如：添加物理ID。</p><h3 id="44-6-丢失的写入"><a href="#44-6-丢失的写入" class="headerlink" title="44.6 丢失的写入"></a>44.6 丢失的写入</h3><p>丢失写入（lost write）：实际上没写入。</p><blockquote><p> Q：如何处理丢失写入？</p></blockquote><p>执行写入验证（write verify），或者写入后读取验证（read-after-write）。</p><h3 id="44-7-擦净"><a href="#44-7-擦净" class="headerlink" title="44.7 擦净"></a>44.7 擦净</h3><blockquote><p>校验和何时得到实际检查？</p></blockquote><p>大多数数据很少访问，因此将保持未检查状态。未检查的数据对于可靠的存储系统是一个问题，因为数据位衰减最终可能影响特定数据的副本。</p><p>为了解决这个问题，许多系统利用各种形式的磁盘擦净（disk scrubbing），定期去读取系统的每个块，并检查校验和是否有效。</p><h3 id="44-8-校验和的开销"><a href="#44-8-校验和的开销" class="headerlink" title="44.8 校验和的开销"></a>44.8 校验和的开销</h3><p>空间开销：</p><ul><li>磁盘本身需要存储校验和</li></ul><p>时间开销：</p><ul><li>CPU需要计算每个块的校验和，存储/访问时都需要计算。</li></ul><h2 id="Chapter-47-分布式系统"><a href="#Chapter-47-分布式系统" class="headerlink" title="Chapter 47 分布式系统"></a>Chapter 47 分布式系统</h2><blockquote><p>Q：如何构建在出现故障时仍能工作的系统？</p></blockquote><p>其他问题：</p><ul><li>系统性能</li><li>安全</li></ul><h3 id="47-1-通信基础"><a href="#47-1-通信基础" class="headerlink" title="47.1 通信基础"></a>47.1 通信基础</h3><p>丢包是网络的基本现象。问题：如何处理丢包？</p><h3 id="47-5-RPC"><a href="#47-5-RPC" class="headerlink" title="47.5 RPC"></a>47.5 RPC</h3><p>RPC是分布式系统中的通信方式。</p><p>RPC系统分成两部分：</p><ul><li>stub generator/protocol compiler<ul><li>通过自动化，简化将函数参数和结果打包成消息的过程。</li><li>优化代码，提高性能。</li></ul></li><li>runtime library</li></ul><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">interface</span> </span>&#123;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">func1</span><span class="hljs-params">(<span class="hljs-keyword">int</span> arg1)</span></span>;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">func2</span><span class="hljs-params">(<span class="hljs-keyword">int</span> arg1, <span class="hljs-keyword">int</span> arg2)</span></span>;&#125;</code></pre><h4 id="stub-generator"><a href="#stub-generator" class="headerlink" title="stub generator"></a>stub generator</h4><p>stub generator接收接口，并生成不同的代码片段，对于client，生成client stub，其中包含接口中的每个函数。希望使用此RPC服务的客户端程序将链接此client stub，调用它进行RPC。</p><p>在内部，client stub中每个函数都执行RPC的核心业务代码。主要分为以下步骤：</p><ol><li><strong>创建消息缓冲区</strong>。</li><li><strong>将所需信息打包到消息缓冲区中。</strong>包括函数名，参数。也称为消息的序列化。</li><li><strong>将消息发送到目标RPC服务器。</strong>，与RPC服务器的通信，由RPC的runtime library处理。</li><li><strong>等待回复。</strong></li><li><strong>解包返回代码和其他参数。</strong>也称为反序列化。</li><li><strong>返回给调用者。</strong></li></ol><p>其它问题：</p><ul><li>复杂参数如何封装</li><li>并发编程</li></ul><h4 id="runtime-library"><a href="#runtime-library" class="headerlink" title="runtime library"></a>runtime library</h4><p>首要挑战：如何找到远程服务？</p><p>建立命名系统，存储对应的RPC服务名称和主机IP地址、端口等。</p><p>下一个问题：选择TCP/UDP传输协议？</p><p>根据系统功能做抉择。</p><h4 id="其他问题"><a href="#其他问题" class="headerlink" title="其他问题"></a>其他问题</h4><ul><li>超时机制</li><li>大参数的过程调用</li><li>字节序，有些机器使用大端序，有些机器使用小端序。</li><li>异步同步问题</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;Chapter-I-O设备&quot;&gt;&lt;a href=&quot;#Chapter-I-O设备&quot; class=&quot;headerlink&quot; title=&quot;Chapter I/O设备&quot;&gt;&lt;/a&gt;Chapter I/O设备&lt;/h2&gt;&lt;blockquote&gt;
&lt;p&gt;Q：如何将I/O集成进计算</summary>
      
    
    
    
    <category term="计算机基础" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/"/>
    
    <category term="操作系统" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
    
    <category term="操作系统" scheme="https://l1n.wang/tags/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
  </entry>
  
  <entry>
    <title>OSTEP阅读笔记- Concurrency（二）</title>
    <link href="https://l1n.wang/2020/12/ostep-concurrency/"/>
    <id>https://l1n.wang/2020/12/ostep-concurrency/</id>
    <published>2020-12-17T08:57:51.000Z</published>
    <updated>2020-12-17T09:05:24.453Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Chapter-26-并发介绍"><a href="#Chapter-26-并发介绍" class="headerlink" title="Chapter 26 并发介绍"></a>Chapter 26 并发介绍</h2><p>本章介绍为单个运行进程提供的新抽象：thread。线程共享地址空间。</p><p>每个线程都有自己的PC与自己用于计算的<code>register</code>。如果有两个线程运行在同一个处理器上，发生<code>context switch</code>时，与进程不同的是，使用PCB保存进程状态，使用TCB保存线程状态。区别是：地址空间保持不变（不需要切换当前使用的页表）。</p><p>另一个主要区别在于栈，传统进程只有一个栈，如图26.1所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-26/26.1.jpg" alt=""></p><p>多线程中，每个线程拥有自己的栈，位于栈上的变量、参数、返回值是无法共享的。</p><h3 id="26-1-实例：线程创建"><a href="#26-1-实例：线程创建" class="headerlink" title="26.1 实例：线程创建"></a>26.1 实例：线程创建</h3><p>示例：使用<code>pthead_create()</code>创建了两个线程，主程序调用<code>pthread_join()</code>等待指定线程执行完成。</p><p>线程的执行顺序取决于系统调度。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-26/26.2.jpg" alt=""></p><h3 id="26-2-共享数据导致的问题"><a href="#26-2-共享数据导致的问题" class="headerlink" title="26.2 共享数据导致的问题"></a>26.2 共享数据导致的问题</h3><p>多线程对共享数据进行操作会导致非预期结果。</p><h3 id="26-3-核心问题：不可控的调度"><a href="#26-3-核心问题：不可控的调度" class="headerlink" title="26.3 核心问题：不可控的调度"></a>26.3 核心问题：不可控的调度</h3><p>竞态条件（<strong>race condition</strong>）：结果取决于代码的执行顺序。运气不好会导致错误的结果。</p><h3 id="26-4-原子性"><a href="#26-4-原子性" class="headerlink" title="26.4 原子性"></a>26.4 原子性</h3><blockquote><p>Q：如何构建有用的同步原语？需要从硬件中获取那些支持？</p></blockquote><h3 id="26-5-等待另一个线程"><a href="#26-5-等待另一个线程" class="headerlink" title="26.5 等待另一个线程"></a>26.5 等待另一个线程</h3><p>需要研究多线程中的<code>wait/signal</code>机制。</p><h2 id="Chapter-27-线程API"><a href="#Chapter-27-线程API" class="headerlink" title="Chapter 27 线程API"></a>Chapter 27 线程API</h2><blockquote><p>Q：如何创建和控制线程？</p></blockquote><h3 id="27-1-pthread-create"><a href="#27-1-pthread-create" class="headerlink" title="27.1 pthread_create"></a>27.1 pthread_create</h3><p>在POSIX中：</p><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;pthread.h&gt;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">pthread_create</span><span class="hljs-params">( <span class="hljs-keyword">pthread_t</span> * thread,</span></span><span class="hljs-function"><span class="hljs-params"><span class="hljs-keyword">const</span> <span class="hljs-keyword">pthread_attr_t</span> * attr,</span></span><span class="hljs-function"><span class="hljs-params"><span class="hljs-keyword">void</span> * (*start_routine)(<span class="hljs-keyword">void</span>*),</span></span><span class="hljs-function"><span class="hljs-params"><span class="hljs-keyword">void</span> * arg)</span></span>;</code></pre><p>参数：</p><ul><li><strong>thread</strong>：指向pthread_t结构类型的指针，利用它与线程交互。</li><li><strong>attr</strong>：用于指定该线程可能具有的任何属性，包括设置栈大小、调度优先级。属性通过调用<code>pthread_attr_init()</code>初始化。</li><li><strong>start_routine</strong>：一个函数指针，指示线程应该在哪个函数上运行。</li><li><strong>arg</strong>：传递给线程的参数</li></ul><h3 id="27-2-pthread-join"><a href="#27-2-pthread-join" class="headerlink" title="27.2 pthread_join"></a>27.2 pthread_join</h3><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;pthread.h&gt;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">pthread_join</span><span class="hljs-params">(<span class="hljs-keyword">pthread_t</span> thread, <span class="hljs-keyword">void</span> **retval)</span></span>;</code></pre><p>参数：</p><ul><li><strong>thread</strong>：指定需要等待的线程</li><li><strong>retval</strong>：指向你希望得到的返回值，定义为一个指向void的指针。</li></ul><h3 id="27-3-锁"><a href="#27-3-锁" class="headerlink" title="27.3 锁"></a>27.3 锁</h3><p>互斥进入临界区：</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">pthread_mutex_lock</span><span class="hljs-params">(<span class="hljs-keyword">pthread_mutex_t</span> *mutex)</span></span>;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">pthread_mutex_unlock</span><span class="hljs-params">(<span class="hljs-keyword">pthread_mutex_t</span> *mutex)</span></span>;</code></pre><p>使用：</p><pre><code class="hljs C"><span class="hljs-keyword">pthread_mutex_t</span> lock = PTHREAD_MUTEX_INITIALIZER;pthread_mutex_lock(&amp;lock);x = x + <span class="hljs-number">1</span>; <span class="hljs-comment">// or whatever your critical section is</span>pthread_mutex_unlock(&amp;lock);</code></pre><h3 id="27-4-条件变量"><a href="#27-4-条件变量" class="headerlink" title="27.4 条件变量"></a>27.4 条件变量</h3><p>当线程之间必须发生某种信号时，如果一个线程在等待另一个线程继续执行某些操作，条件变量就很有用。</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">pthread_cond_wait</span><span class="hljs-params">(<span class="hljs-keyword">pthread_cond_t</span> *cond, <span class="hljs-keyword">pthread_mutex_t</span> *mutex)</span></span>;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">pthread_cond_signal</span><span class="hljs-params">(<span class="hljs-keyword">pthread_cond_t</span> *cond)</span></span>;</code></pre><p>要使用条件变量，还需要一个与条件相关的锁。</p><ul><li>pthread_cond_wait：使调用线程进入休眠状态，等待其他线程发出信号。</li><li>pthread_cond_signal：唤醒线程</li></ul><h3 id="补充：线程API使用指导"><a href="#补充：线程API使用指导" class="headerlink" title="补充：线程API使用指导"></a>补充：线程API使用指导</h3><ul><li>保持简洁</li><li>让线程交互减到最少</li><li>初始化锁和条件变量</li><li>检查返回值</li><li>注意传给线程的参数和返回指</li><li>每个线程都有自己的栈</li><li>线程之间通过条件变量发送信号</li><li>多查手册</li></ul><h2 id="Chapter-28-锁"><a href="#Chapter-28-锁" class="headerlink" title="Chapter 28 锁"></a>Chapter 28 锁</h2><p>并发编程的基本问题：希望原子式执行一系列指令，但是由于处理器中断，无法做到。</p><p>锁可以保证在临界区能够保证原子性。</p><h3 id="28-1-锁的基本思想"><a href="#28-1-锁的基本思想" class="headerlink" title="28.1 锁的基本思想"></a>28.1 锁的基本思想</h3><p>锁就是一个变量，因此需要声明才能使用，锁变量保存了锁在某一时刻的状态，要么是可用的：表示没有线程持有锁，要么是被占用的：表示有线程持有锁。也可以访问其他信息：比如查询持有锁的线程，或请求获取锁的线程队列。</p><ul><li>lock：尝试获取锁，如果没有其他线程持有锁，该线程会获得锁，进入临界区，并成为锁的持有者。</li><li>unlock：持有者一旦调用<code>unlock()</code>，锁就会变成可用，如果没有其他等待线程。</li></ul><h3 id="28-4-评价锁"><a href="#28-4-评价锁" class="headerlink" title="28.4 评价锁"></a>28.4 评价锁</h3><p>如何评价一种锁实现的效果？</p><p>标准：</p><ul><li>第一：锁是否能实现互斥</li><li>第二：公平性</li><li>第三：性能</li></ul><h3 id="28-5-控制中断"><a href="#28-5-控制中断" class="headerlink" title="28.5 控制中断"></a>28.5 控制中断</h3><p>最早提供的互斥解决方案之一就是在临界区关闭中断。这个解决方案是为单处理器系统开发的。优点是简单。</p><p>缺点：</p><ul><li>不安全的操作，允许任何线程进行关中断</li><li>不支持多处理器，如果多个线程运行在不同的CPU上，每个线程都试图进入同一临界区。</li><li>关闭中断导致中断丢失，比如磁盘设备完成读取任务，但是CPU无法识别到中断</li><li>效率低</li></ul><h3 id="28-6-测试并设置指令-原子交换"><a href="#28-6-测试并设置指令-原子交换" class="headerlink" title="28.6 测试并设置指令 原子交换"></a>28.6 测试并设置指令 原子交换</h3><p>由于关中断方式无法在多处理器上运行，所以需要让硬件支持，最简单的硬件支持是测试并设置（<strong>test-and-set</strong>），也称为原子交换（<strong>atomic exchange</strong>）。</p><p>通过自旋等待，直到持有锁的线程释放资源。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-28/28.1.jpg" alt=""></p><p>上述代码有两个问题：</p><ul><li>正确性<ul><li>无法互斥</li></ul></li><li>性能<ul><li>自旋等待性能开销大</li></ul></li></ul><h3 id="28-7-实现可用的自旋锁"><a href="#28-7-实现可用的自旋锁" class="headerlink" title="28.7 实现可用的自旋锁"></a>28.7 实现可用的自旋锁</h3><p>没有硬件支持是无法实现的，因此产生了一些指令，在<code>x86</code>中，是<code>xchg</code>指令。其功能是返回旧值，更新为新值，C代码描述如下：</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">testAndSet</span><span class="hljs-params">(<span class="hljs-keyword">int</span> *old_ptr, <span class="hljs-keyword">int</span> <span class="hljs-keyword">new</span>)</span></span>&#123;    <span class="hljs-keyword">int</span> old = * old_ptr;    *old_ptr = <span class="hljs-keyword">new</span>;    <span class="hljs-keyword">return</span> old;&#125;</code></pre><p>可以利用这个指令完成一个简单的<code>spin lock</code>。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-28/28.3.jpg" alt=""></p><p>此方法与28.1中的实现区别在于：<strong>获取并更新两个操作是原子性的，并不会存在同时进入临界区的时机。</strong></p><p>这也是此方法能完成互斥功能的关键。</p><h3 id="28-8-评价自旋锁"><a href="#28-8-评价自旋锁" class="headerlink" title="28.8 评价自旋锁"></a>28.8 评价自旋锁</h3><ul><li>正确性：能够互斥</li><li>公平性：自旋锁没有公平性，会导致线程饥饿</li><li>性能<ul><li>单处理器：性能开销大</li><li>多处理器：性能不错</li></ul></li></ul><h3 id="28-9-CAS"><a href="#28-9-CAS" class="headerlink" title="28.9 CAS"></a>28.9 CAS</h3><p>比较并交换指令（compare-and-set），C代码描述如下：</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">compareAndSet</span><span class="hljs-params">(<span class="hljs-keyword">int</span> * ptr, <span class="hljs-keyword">int</span> expected, <span class="hljs-keyword">int</span> <span class="hljs-keyword">new</span>)</span></span>&#123;    <span class="hljs-keyword">int</span> actual = *ptr;    <span class="hljs-keyword">if</span>(actual == expected)        *ptr = <span class="hljs-keyword">new</span>;    <span class="hljs-keyword">return</span> actual;&#125;</code></pre><p>可以使用该指令改写<code>lock()</code>。</p><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">(<span class="hljs-keyword">lock_t</span> *lock)</span></span>&#123;    <span class="hljs-keyword">while</span>(compareAndSet(&amp;lock-&gt;flag,<span class="hljs-number">0</span>,<span class="hljs-number">1</span>) == <span class="hljs-number">1</span>)        ;<span class="hljs-comment">//spin</span>&#125;</code></pre><p>X86的CAS指令如下：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-28/CAS-X86.jpg" alt=""></p><h3 id="28-10-链接的加载和条件式存储指令"><a href="#28-10-链接的加载和条件式存储指令" class="headerlink" title="28.10 链接的加载和条件式存储指令"></a>28.10 链接的加载和条件式存储指令</h3><p>链接的加载指令：从内存中取出值。</p><p>条件式存储指令：只有上一次加载的地址在期间都没有更新时，才会成功。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-28/28.5.jpg" alt=""></p><p>使用这两个指令如何实现一个锁？</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-28/28.6.jpg" alt=""></p><h3 id="28-11-获取并增加"><a href="#28-11-获取并增加" class="headerlink" title="28.11 获取并增加"></a>28.11 获取并增加</h3><p>获取并增加：它能原子性的返回特定地址的旧值，并将值自增1，C代码如下：</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">fetchAndAdd</span><span class="hljs-params">(<span class="hljs-keyword">int</span> * ptr)</span></span>&#123;    <span class="hljs-keyword">int</span> old = *ptr;    *ptr = old + <span class="hljs-number">1</span>;    <span class="hljs-keyword">return</span> old;&#125;</code></pre><p>可以通过此指令完成一个ticket锁：</p><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">lock_t</span>&#123;</span>    <span class="hljs-keyword">int</span> ticket;    <span class="hljs-keyword">int</span> turn;&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">lock_init</span><span class="hljs-params">(<span class="hljs-keyword">lock_t</span> *lock)</span></span>&#123;    lock-&gt;ticket = <span class="hljs-number">0</span>;    lock-&gt;turn = <span class="hljs-number">0</span>;&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">(<span class="hljs-keyword">lock_t</span> *lock)</span></span>&#123;    <span class="hljs-keyword">int</span> myturn = fetchAndAdd(&amp;lock-&gt;ticket);    <span class="hljs-keyword">while</span>(lock-&gt;turn!= myturn)        ;<span class="hljs-comment">//spin</span>&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">(<span class="hljs-keyword">lock_t</span> *lock)</span></span>&#123;    fetchAndAdd(&amp;lock-&gt;turn);&#125;</code></pre><p>通过检查<code>turn</code>字段与获取的<code>ticket</code>是否相等，判断是否可以进入临界区。</p><p>不同于之前的方法，本方法能保证所有线程都能获取锁，而不会导致某一个线程饥饿。</p><h3 id="28-12-自旋过多：怎么办"><a href="#28-12-自旋过多：怎么办" class="headerlink" title="28.12 自旋过多：怎么办"></a>28.12 自旋过多：怎么办</h3><blockquote><p>Q：这样避免自旋浪费CPU时间？</p></blockquote><h3 id="28-13-简单方法：让出CPU"><a href="#28-13-简单方法：让出CPU" class="headerlink" title="28.13 简单方法：让出CPU"></a>28.13 简单方法：让出CPU</h3><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span></span>&#123;    <span class="hljs-keyword">while</span>(testAndSet(&amp;flag, <span class="hljs-number">1</span>) == <span class="hljs-number">1</span>)        yield();&#125;</code></pre><p>调用<code>yield()</code>让线程主动放弃CPU。</p><p>但是如果线程数量多，采用此方法会导致<code>context switch</code>成本提高。</p><p>同时还会有线程饥饿问题，一个线程可能一直处在让出CPU的循环，而其他线程反复进入临界区。</p><h3 id="28-14-使用队列：休眠代替自旋"><a href="#28-14-使用队列：休眠代替自旋" class="headerlink" title="28.14 使用队列：休眠代替自旋"></a>28.14 使用队列：休眠代替自旋</h3><p>如图28.9所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-28/28.9.jpg" alt=""></p><h3 id="28-15-Linux的fulex锁"><a href="#28-15-Linux的fulex锁" class="headerlink" title="28.15 Linux的fulex锁"></a>28.15 Linux的fulex锁</h3><p>利用一个整数，同时利记录锁是否被持有（整数的最高位），以及等待者的个数（整数的其它位）。</p><p>因此整数如果是负数，则处于被持有状态（有符号整数，高位为1为负数）。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-28/28.10.jpg" alt=""></p><h3 id="28-16-两阶段锁"><a href="#28-16-两阶段锁" class="headerlink" title="28.16 两阶段锁"></a>28.16 两阶段锁</h3><p>两阶段锁的第一阶段会先自旋一段时间，希望可以获得锁。但是如果自旋阶段没有获得锁，第二阶段调用者会睡眠，直到锁可用。</p><h2 id="Chapter-29-基于锁的并发数据结构"><a href="#Chapter-29-基于锁的并发数据结构" class="headerlink" title="Chapter 29 基于锁的并发数据结构"></a>Chapter 29 基于锁的并发数据结构</h2><blockquote><p>Q：如何给数据结构加锁，能够保证高性能，并实现线程安全？</p></blockquote><h3 id="29-1-并发计数器"><a href="#29-1-并发计数器" class="headerlink" title="29.1 并发计数器"></a>29.1 并发计数器</h3><p>如何实现一个线程安全的计数器？</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-29/29.2.jpg" alt=""></p><p>但是通过锁保护线程安全影响性能。</p><p>如果多线程运行下的速率和单线程下一样，就将其称为完美扩展（perfect scaling）。</p><blockquote><p>注：下文的扩展性是指，程序在并发环境下的性能优劣。</p></blockquote><p>人们提出了懒惰计数器（<strong>sloopy counter</strong>），懒惰计数器通过多个局部计数器和一个全局计数器来实现一个逻辑计数器，其中每个CPU核心都有一个局部计数器，每个计数器都有一个锁保护线程安全。</p><p>基本思想：如果一个CPU上的线程想增加计数器，增加自己的局部计数器，访问局部计数器通过对应的局部锁同步的。全局计数器的值会定期更新，通过获取全局锁，然后加上局部计数器的值，并将其清空。</p><p>更新的频率取决去阈值$S$，$S$越小，懒惰计数器越趋向于非扩展的计数器，$S$越大，扩展性越强，但是全局计数器和局部计数器的偏差较大。图29.5是懒惰计数器的基本实现：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-29/29.5.jpg" alt=""></p><h3 id="29-2-并发List"><a href="#29-2-并发List" class="headerlink" title="29.2 并发List"></a>29.2 并发List</h3><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-29/29.7.jpg" alt=""></p><p>插入时检查是否发生内存分配错误，但是异常控制流容易产生错误。如何重写插入和查找函数？</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-29/29.8.jpg" alt=""></p><p>上述的写法，在插入时，缩小了锁的粒度。但是扩展性不好。</p><p><strong>hand-over-hand locking</strong>。：每个节点都有一个锁，代替了原来整个链表共享一个锁。遍历链表时，首先抢占下一个节点的锁，然后释放当前节点的锁。</p><p>但是这种方法的性能并不比单锁快。</p><h3 id="29-3-并发Queue"><a href="#29-3-并发Queue" class="headerlink" title="29.3 并发Queue"></a>29.3 并发Queue</h3><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-29/29.9.jpg" alt=""></p><p>特点：</p><ul><li>入队只获取tail锁</li><li>出队只获取head锁</li></ul><h3 id="29-4-并发Hash"><a href="#29-4-并发Hash" class="headerlink" title="29.4 并发Hash"></a>29.4 并发Hash</h3><p>并发Hash中，每个桶都有一个锁。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-29/29.10.jpg" alt=""></p><h2 id="Chapter-30-条件变量"><a href="#Chapter-30-条件变量" class="headerlink" title="Chapter 30 条件变量"></a>Chapter 30 条件变量</h2><p>很多情况下，线程需要检查某一condition满足之后，才会继续运行。</p><blockquote><p>Q：线程应该如何等待一个条件？</p></blockquote><h3 id="30-1-定义和程序"><a href="#30-1-定义和程序" class="headerlink" title="30.1 定义和程序"></a>30.1 定义和程序</h3><p>线程可以使用<code>condition variable</code>，来等待一个条件变为真，条件变量是一个显式队列，当条件不满足时，线程把自己加入阻塞队列，进入阻塞状态，另外某个线程，当它改变了上述状态时，就可以唤醒一个或多个等待线程。</p><p>POSIX调用如图30.3所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-30/30.3.jpg" alt=""></p><p>那么done变量是否需要，下面的代码是否正确？</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-30/30.3-1.jpg" alt=""></p><p>假设子线程立即运行，并且调用<code>thr_exit()</code>，在这种情况下，子线程发送信号，但是没有在条件变量上等待的线程，父线程运行时，就会调用<code>wait()</code>进入阻塞状态，并且没有其它线程唤醒它。</p><h3 id="30-2-生产者-消费者问题"><a href="#30-2-生产者-消费者问题" class="headerlink" title="30.2 生产者/消费者问题"></a>30.2 生产者/消费者问题</h3><p>假设有一个或多个生产者线程和一个或多个消费者线程，生产者把生产的数据项放入缓冲区；消费者从缓冲区取走数据项，以某种方式进行消费。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-30/30.12.jpg" alt=""></p><h3 id="30-3-覆盖条件"><a href="#30-3-覆盖条件" class="headerlink" title="30.3 覆盖条件"></a>30.3 覆盖条件</h3><p>下面是一个多线程内存分配库的覆盖条件的例子：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-30/30.13.jpg" alt=""></p><p>当线程进入内存分配中时，它可能会因为内存不足而等待。线程释放内存后，会发出信号通知会有更多的内存空间，但是有一个问题：应该唤醒那个等待线程？</p><p>场景：线程$Ta$申请100 bytes内存，线程$Tb$申请10 bytes内存，但是当前无空闲内存可分配，$Ta$和$Tb$都等待在条件变量上。此时线程$Tc$调用了<code>free(50)</code>，但是$Tc$如果唤醒$Ta$，那么$Ta$会因为内存不够继续等待。</p><p>因此可以使用<code>pthread_cond_broadcast()</code>代替上面的<code>pthread_cond_signal()</code>唤醒所有的等待线程。当然也有缺点：影响性能，不必要地唤醒了其他等待的线程。</p><p>这种条件变量称为覆盖条件（<strong>covering condition</strong>）。</p><h2 id="Chapter-31-信号量"><a href="#Chapter-31-信号量" class="headerlink" title="Chapter 31 信号量"></a>Chapter 31 信号量</h2><blockquote><p>Q：如何使用信号量代替锁和条件变量？信号量的概念，不用锁和条件变量，如何实现信号量？</p></blockquote><h3 id="31-1-信号量的定义"><a href="#31-1-信号量的定义" class="headerlink" title="31.1 信号量的定义"></a>31.1 信号量的定义</h3><p>信号量是一个整数型的对象，可以用两个函数来操作它，在POSIX标准中，分别是<code>sem_wait()</code>和<code>sem_post()</code>。含义分别如下：</p><ul><li><strong>sem_wait</strong>：<ul><li>decrement the value of semaphore s by one</li><li>wait it value of semaphore s is negative</li></ul></li><li><strong>sem_post</strong>：<ul><li>increment the value of semphore s by one</li><li>if there are one or more threads waiting, wake one</li></ul></li></ul><p>信号量如果为负数，那么绝对值就是等待线程的个数。</p><h3 id="31-2-二值信号量"><a href="#31-2-二值信号量" class="headerlink" title="31.2 二值信号量"></a>31.2 二值信号量</h3><p>二值信号量就是锁。</p><h3 id="31-3-信号量用作条件变量"><a href="#31-3-信号量用作条件变量" class="headerlink" title="31.3 信号量用作条件变量"></a>31.3 信号量用作条件变量</h3><p>信号量也可以用在一个线程暂停执行，等待某一条件成立的场景。例如：一个线程要等待一个链表非空，才能删除一个元素。此时可以将信号量作为条件变量。</p><h3 id="31-4-生产者消费者问题"><a href="#31-4-生产者消费者问题" class="headerlink" title="31.4 生产者消费者问题"></a>31.4 生产者消费者问题</h3><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-31/31.12.jpg" alt=""></p><h3 id="31-5-读写锁"><a href="#31-5-读写锁" class="headerlink" title="31.5 读写锁"></a>31.5 读写锁</h3><p>如果某个线程需要进行更新操作，需要调用<code>rwlock_acquire_lock()</code>获得写锁，调用<code>rwlock_release_writelock()</code>释放锁，内部通过一个信号量保证只有一个写者能进入临界区。</p><p>读锁会通过<code>reader</code>变量追踪当前有多少个读者在访问该数据结构。</p><p>一旦一个读者获得了读锁，其他的读者也可以获得这个读锁。当时想要获取写锁的线程，必须等待所有的读者都结束。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-31/30.13.jpg" alt=""></p><h3 id="31-6-哲学家就餐问题"><a href="#31-6-哲学家就餐问题" class="headerlink" title="31.6 哲学家就餐问题"></a>31.6 哲学家就餐问题</h3><p>基本问题：假设又五位哲学家围着一个圆桌，每两位哲学家之间有一把叉子，一位哲学家只有同时拿到了左手边和右手边的两把叉子才能吃到东西。如何解决同步问题。</p><p>每位哲学家的基本操作：</p><pre><code class="hljs isbl"><span class="hljs-function"><span class="hljs-title">while</span>(<span class="hljs-number">1</span>) &#123;</span><span class="hljs-function"><span class="hljs-title">think</span>();</span><span class="hljs-function"><span class="hljs-title">getforks</span>();</span><span class="hljs-function"><span class="hljs-title">eat</span>();</span><span class="hljs-function"><span class="hljs-title">putforks</span>();</span><span class="hljs-function">&#125;</span></code></pre><p>如何保证没有死锁，并且不会有哲学家饿死？</p><p>需要一些辅助函数：例如哲学家P希望用左手边的叉子，他会调用left(p)，右手边的叉子调用right(p)。</p><pre><code class="hljs arduino"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">left</span><span class="hljs-params">(<span class="hljs-keyword">int</span> p)</span></span>&#123;<span class="hljs-keyword">return</span> p&#125;;<span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">right</span><span class="hljs-params">(<span class="hljs-keyword">int</span> p)</span></span>&#123;<span class="hljs-keyword">return</span> (p + <span class="hljs-number">1</span>) % <span class="hljs-number">5</span> ;&#125;</code></pre><h4 id="第一次尝试"><a href="#第一次尝试" class="headerlink" title="第一次尝试"></a>第一次尝试</h4><p>将每个信号量都设置为1</p><pre><code class="hljs reasonml">void getforks<span class="hljs-literal">()</span>&#123;sem<span class="hljs-constructor">_wait(<span class="hljs-params">forks</span>[<span class="hljs-params">left</span>(<span class="hljs-params">p</span>)</span>]);sem<span class="hljs-constructor">_wait(<span class="hljs-params">forks</span>[<span class="hljs-params">right</span>(<span class="hljs-params">p</span>)</span>]);&#125;void putforks<span class="hljs-literal">()</span> &#123;sem<span class="hljs-constructor">_post(<span class="hljs-params">forks</span>[<span class="hljs-params">left</span>(<span class="hljs-params">p</span>)</span>]);sem<span class="hljs-constructor">_post(<span class="hljs-params">forks</span>[<span class="hljs-params">right</span>(<span class="hljs-params">p</span>)</span>]);&#125;</code></pre><p>步骤是依次获取每把叉子的锁，先获取左手边的。</p><p>这个方案是有问题的，如果每个哲学家都拿到了左手边的叉子，他们会被阻塞，并一直等待另一只叉子。</p><h4 id="破除依赖"><a href="#破除依赖" class="headerlink" title="破除依赖"></a>破除依赖</h4><p>如何解决？ 修改某个哲学家的取叉子顺序。</p><pre><code class="hljs reasonml">void getforks<span class="hljs-literal">()</span> &#123;<span class="hljs-keyword">if</span>(p<span class="hljs-operator"> == </span><span class="hljs-number">4</span>)&#123;sem<span class="hljs-constructor">_wait(<span class="hljs-params">forks</span>[<span class="hljs-params">right</span>(<span class="hljs-params">p</span>)</span>]);sem<span class="hljs-constructor">_wait(<span class="hljs-params">forks</span>[<span class="hljs-params">left</span>(<span class="hljs-params">p</span>)</span>]);&#125;<span class="hljs-keyword">else</span>&#123;sem<span class="hljs-constructor">_wait(<span class="hljs-params">forks</span>[<span class="hljs-params">left</span>(<span class="hljs-params">p</span>)</span>]);sem<span class="hljs-constructor">_wait(<span class="hljs-params">forks</span>[<span class="hljs-params">right</span>(<span class="hljs-params">p</span>)</span>]);&#125;&#125;</code></pre><h3 id="31-7-如何实现信号量"><a href="#31-7-如何实现信号量" class="headerlink" title="31.7 如何实现信号量"></a>31.7 如何实现信号量</h3><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-31/31.16.jpg" alt=""></p><h2 id="Chapter-32-常见并发问题"><a href="#Chapter-32-常见并发问题" class="headerlink" title="Chapter 32 常见并发问题"></a>Chapter 32 常见并发问题</h2><blockquote><p>Q：如何处理常见的并发缺陷？</p></blockquote><h3 id="32-1-有哪些类型的缺陷"><a href="#32-1-有哪些类型的缺陷" class="headerlink" title="32.1 有哪些类型的缺陷"></a>32.1 有哪些类型的缺陷</h3><p>死锁和非死锁。</p><h3 id="32-2-非死锁缺陷"><a href="#32-2-非死锁缺陷" class="headerlink" title="32.2 非死锁缺陷"></a>32.2 非死锁缺陷</h3><ul><li>反原子性</li><li>错误顺序缺陷</li></ul><h4 id="违反原子性缺陷"><a href="#违反原子性缺陷" class="headerlink" title="违反原子性缺陷"></a>违反原子性缺陷</h4><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-32/mysql_concurrency.jpg" alt=""></p><p>这是MySQL中出现的例子，两个线程都要访问<code>thd</code>中的<code>proc_info</code>，第一个线程检查<code>proc_info</code>非空，然后打印值，第二个线程设置其为空，当第一个线程恢复执行时，会由于空指针而报错。</p><p>解决方案很简单，将共享变量的访问加锁，确保串行化访问变量。</p><h4 id="违反顺序缺陷"><a href="#违反顺序缺陷" class="headerlink" title="违反顺序缺陷"></a>违反顺序缺陷</h4><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-32/2.jpg" alt=""></p><p>线程2假定已经初始化了，然而如果线程1没有首先执行，线程2则会因为空指针报错。</p><p>如何解决？ 保证执行顺序不被打破。</p><p>可以使用条件变量解决。</p><h3 id="32-3-死锁缺陷"><a href="#32-3-死锁缺陷" class="headerlink" title="32.3 死锁缺陷"></a>32.3 死锁缺陷</h3><p>线程1持有锁L1，线程2持有锁L2，线程1请求L2，线程2请求L1。</p><blockquote><p>Q：如何应对死锁？</p></blockquote><h4 id="发生的原因"><a href="#发生的原因" class="headerlink" title="发生的原因"></a>发生的原因</h4><ul><li>大型项目中，组件之间有复杂的依赖。</li><li>封装会隐藏实现细节，会让人忽视。</li></ul><h4 id="产生死锁的条件"><a href="#产生死锁的条件" class="headerlink" title="产生死锁的条件"></a>产生死锁的条件</h4><ul><li>互斥：线程对于需要的资源进行互斥访问。</li><li>持有并等待：线程持有了资源。</li><li>非抢占：线程获得的资源，不能被抢占。</li><li>循环等待：线程之间存在一个环路，环路上的每个线程都额外持有一个资源，而这个资源又是下一个线程要申请的。</li></ul><h4 id="预防"><a href="#预防" class="headerlink" title="预防"></a>预防</h4><ul><li><p>从循环等待条件解决：让代码不会产生循环等待，最直接的方法就是获取锁时提供一个全序，偏序也是一种有用的方法，Linux中内存映射就是一个偏序锁的好例子。</p></li><li><p>从持有并等待条件解决：通过原子性地抢锁来避免。</p></li><li>从非抢占条件解决：可以通过<code>tryLock()</code>解决问题。</li><li>从互斥条件解决：方法是完全避免互斥，但是代码总会存在临界区，因此很难避免互斥。<ul><li>可以使用lock-free，通过强大地硬件指令，可以构造出不需要锁的数据结构。</li></ul></li></ul><h4 id="通过调度避免死锁"><a href="#通过调度避免死锁" class="headerlink" title="通过调度避免死锁"></a>通过调度避免死锁</h4><h4 id="检查和恢复"><a href="#检查和恢复" class="headerlink" title="检查和恢复"></a>检查和恢复</h4><p>允许死锁偶尔发生，检查到死锁时再采取行动。</p><p>很多数据库系统使用了死锁检测和恢复技术，死锁检测器会定期运行，通过构建资源图来检查循环。</p><h2 id="Chapter-33-基于事件的并发"><a href="#Chapter-33-基于事件的并发" class="headerlink" title="Chapter 33 基于事件的并发"></a>Chapter 33 基于事件的并发</h2><blockquote><p>Q：不用线程，同时保证对并发的控制，如何构建并发服务器？</p></blockquote><h3 id="33-1-基本想法：事件循环"><a href="#33-1-基本想法：事件循环" class="headerlink" title="33.1 基本想法：事件循环"></a>33.1 基本想法：事件循环</h3><p>使用的基本的方法是<strong>基于事件的并发</strong>。该方法很简单：等待某事发生。当它发生时，检查事件类型，然后做少量的相应工作。</p><p>想看一个典型的基于事件的服务器，这种应用被称为事件循环：</p><pre><code class="hljs isbl"><span class="hljs-function"><span class="hljs-title">while</span>(<span class="hljs-number">1</span>) &#123;</span><span class="hljs-function"><span class="hljs-variable">events</span> = <span class="hljs-title">getEvents</span>();</span><span class="hljs-function"><span class="hljs-title">for</span>(<span class="hljs-variable">e</span> <span class="hljs-variable"><span class="hljs-keyword">in</span></span> <span class="hljs-variable">events</span>)</span><span class="hljs-function"><span class="hljs-title">processEvent</span>(<span class="hljs-variable">e</span>);</span><span class="hljs-function">&#125;</span></code></pre><p>这也带来了问题：基于事件的服务器如何决定哪个事件发生，尤其是对于网络和磁盘IO？时间服务器如何确定是否有它的消息已经到达？</p><h3 id="33-2-重要API：select"><a href="#33-2-重要API：select" class="headerlink" title="33.2 重要API：select()"></a>33.2 重要API：select()</h3><p>大多数OS提供了基本的API决定如何接收事件。即通过<code>select()</code>或<code>poll()</code>系统调用。</p><p>这些接口对程序的支持很简单：检查是否有任何应该关注的进入I/O。假定网络程序希望检查是否有网络数据包已到达，以便为它们提供服务，</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">select</span><span class="hljs-params">(<span class="hljs-keyword">int</span> nfds,</span></span><span class="hljs-function"><span class="hljs-params">          fd_set *<span class="hljs-keyword">restrict</span> readfds,</span></span><span class="hljs-function"><span class="hljs-params">          fd_set *<span class="hljs-keyword">restrict</span> writefds,</span></span><span class="hljs-function"><span class="hljs-params">          fd_set *<span class="hljs-keyword">restrict</span> errorfds,</span></span><span class="hljs-function"><span class="hljs-params">          struct timeval *<span class="hljs-keyword">restrict</span> timeout)</span></span>;</code></pre><p><code>select()</code>检查I/O描述符集合，它们的地址通过<code>readfds</code>、<code>writefds</code>、<code>errorfds</code>传入，分别查看它们中的某些描述符是否已准备好读取，是否准备好写入，或者异常情况待处理。在每个集合中检查前nfds个描述符，返回时，<code>select()</code>用给定请求操作准备好的描述符组成的子集替换给定的描述符集合。<code>select()</code>返回所有集合中就绪描述符的总数。</p><ul><li>阻塞接口：在返回给调用者之前完成所有工作。</li><li>非阻塞接口：开始一些工作后立即返回，从而让所有需要完成的工作都在后台完成。</li></ul><h3 id="33-3-使用select"><a href="#33-3-使用select" class="headerlink" title="33.3 使用select()"></a>33.3 使用select()</h3><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-32/33.1.jpg" alt=""></p><ol><li>使用<code>FD_ZERO()</code>宏首先清除文件描述符集合。</li><li>使用<code>FD_SET()</code>将所有从<code>minFD</code>到<code>maxFD</code>的文件描述符包含到集合中。<ul><li>例如，这组描述符可能表示服务器正在关注的所有网络套接字。</li></ul></li><li>最后，服务器调用<code>select()</code>来查看哪些连接有可用的数据。</li><li>然后通过在循环中使用<code>FD_ISSET()</code>，事件服务器可以查看哪些描述符已准备好数据并处理传入的数据。</li></ol><h3 id="33-4-无需锁"><a href="#33-4-无需锁" class="headerlink" title="33.4 无需锁"></a>33.4 无需锁</h3><p>使用单个CPU和基于事件的应用程序，也就不存在并发问题。具体来说，因为一次只处理一个事件，所以不需要锁来保护指定对象。</p><h3 id="33-5-阻塞系统调用"><a href="#33-5-阻塞系统调用" class="headerlink" title="33.5 阻塞系统调用"></a>33.5 阻塞系统调用</h3><p>如果某个事件要求你发出可能会阻塞的系统调用，该怎么办？</p><p>例如：一个请求进入服务器，要求磁盘读取文件并将内容返回到客户端。为了处理这样的请求，需要发出<code>open()</code>系统调用打开文件，<code>read()</code>读取文件。</p><p>由于IO操作远慢于CPU，因此需要很长时间才能提供服务。</p><p>在基于事件的方法中，事件循环阻塞时，系统会处于闲置状态，并不会读取新事件。曹诚资源浪费。</p><p>因此，在基于事件的系统中必须：不允许阻塞调用。</p><h3 id="33-6-解决方案：异步I-O"><a href="#33-6-解决方案：异步I-O" class="headerlink" title="33.6 解决方案：异步I/O"></a>33.6 解决方案：异步I/O</h3><p>异步I/O（<strong>asynchronous I/O</strong>）：发出I/O请求后，在I/O完成之前立即将控制权返回给调用者，另外的接口让应用程序能够确定各种I/O是否已完成。</p><p>macOS中提供了AIO的接口，这些API围绕着一个基本结构：</p><pre><code class="hljs C"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">aiocb</span>&#123;</span>    <span class="hljs-keyword">int</span> aio_fildes;    <span class="hljs-keyword">off_t</span> aio_offset;    <span class="hljs-keyword">volatile</span> <span class="hljs-keyword">void</span> *aio_buf;    <span class="hljs-keyword">size_t</span> aio_nbytes;&#125;</code></pre><p>要发起AIO请求，应用程序首先用相关信息填充此结构：</p><ul><li><strong>aio_fildes</strong>：要读取文件的文件描述符</li><li><strong>aio_offset</strong>：文件内的偏移量</li><li><strong>aio_buf</strong>：复制读取结果的目标内存位置</li><li><strong>aio_nbytes</strong>：读取长度</li></ul><p>通过<code>aio_read()</code>进行异步读取。</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">aio_read</span><span class="hljs-params">(struct aiocb * aiocbp)</span></span>;</code></pre><p>如何知道IO何时完成，并且缓冲区现在有了请求的数据？</p><p><code>aio_error()</code>检查aiocbp应用的请求是否已完成。</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">aio_error</span><span class="hljs-params">(<span class="hljs-keyword">const</span> struct aiocb * aiocbp)</span></span>;</code></pre><p>因此，检查AIO是否已经完成需要轮询。为了解决这个问题，一些系统提供了基于中断的方法，此方法使用UNIX信号（signal）在异步IO完成时通知应用程序，从而消除了轮询的需要。</p><h3 id="33-7-另一个问题：状态管理"><a href="#33-7-另一个问题：状态管理" class="headerlink" title="33.7 另一个问题：状态管理"></a>33.7 另一个问题：状态管理</h3><p>基于事件的方法存在另一个问题：代码比传统的基于线程的复杂。</p><p>原因在于：需要手动的管理事件IO状态，基于线程的程序不需要，因为时线程栈自动管理。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;Chapter-26-并发介绍&quot;&gt;&lt;a href=&quot;#Chapter-26-并发介绍&quot; class=&quot;headerlink&quot; title=&quot;Chapter 26 并发介绍&quot;&gt;&lt;/a&gt;Chapter 26 并发介绍&lt;/h2&gt;&lt;p&gt;本章介绍为单个运行进程提供的新抽象：</summary>
      
    
    
    
    <category term="计算机基础" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/"/>
    
    <category term="操作系统" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
    
    <category term="操作系统" scheme="https://l1n.wang/tags/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
  </entry>
  
  <entry>
    <title>OSTEP阅读笔记-Virtuallization（一）</title>
    <link href="https://l1n.wang/2020/12/ostep-virtualization/"/>
    <id>https://l1n.wang/2020/12/ostep-virtualization/</id>
    <published>2020-12-05T06:21:23.000Z</published>
    <updated>2020-12-05T12:00:14.349Z</updated>
    
    <content type="html"><![CDATA[<h1 id="进程虚拟化"><a href="#进程虚拟化" class="headerlink" title="进程虚拟化"></a>进程虚拟化</h1><h2 id="Chapter-4-进程"><a href="#Chapter-4-进程" class="headerlink" title="Chapter 4 进程"></a>Chapter 4 进程</h2><blockquote><p>Q：如何提供有许多CPU的假象？</p></blockquote><p>OS通过<code>Visualizing</code> CPU来提供这种假象。通过让一个进程只运行一个时间片，然后切换到其它进程，这样就提供了存在多个虚拟CPU的假象。</p><ul><li>时分共享：典型的就是时间片划分，将一小段时间分配给不同的进程。</li><li>空分共享：典型的就是磁盘空间，将空间分配给不同的文件。</li></ul><p>Qustions：</p><ul><li>如何实现<code>context switch</code></li><li>OS如何执行<code>scheduling policy</code></li></ul><h3 id="4-1-抽象：进程"><a href="#4-1-抽象：进程" class="headerlink" title="4.1 抽象：进程"></a>4.1 抽象：进程</h3><blockquote><p>OS为正在运行的程序提供的抽象，就是所谓的进程。</p></blockquote><p>进程的<code>machine state</code>：</p><ul><li>进程可访问的内存（<code>address space</code>）：指令和数据存储在<code>address space</code>。</li><li>寄存器（<code>register</code>）</li></ul><h3 id="4-2-进程API"><a href="#4-2-进程API" class="headerlink" title="4.2 进程API"></a>4.2 进程API</h3><ul><li>create</li><li>destroy</li><li>wait</li><li>miscellaneous control</li><li>status</li></ul><h3 id="4-3-进程创建的细节"><a href="#4-3-进程创建的细节" class="headerlink" title="4.3 进程创建的细节"></a>4.3 进程创建的细节</h3><blockquote><p>Q: 程序如何转换为进程？换而言之，OS如何启动并运行一个程序？</p></blockquote><ol><li>OS将代码和静态数据<code>load</code>到内存中，加载到进程的<code>address space</code>。如图4.1所示：</li></ol><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-4/program-to-process.jpg" alt="从程序到进程"></p><ol><li>加载后，OS在运行前需要执行初始化操作。<ul><li>为程序的<strong>运行时栈（run-time stack）</strong>分配一些内存。C程序使用栈存放局部变量，函数参数和返回地址。</li><li>为<strong>堆（heap）</strong>分配一些内存，C程序中，堆用于显式请求的动态分配数据（<code>malloc &amp; free</code>）</li><li><code>I/O</code>相关的任务。UNIX中每个进程都会有三个打开的文件描述符（<code>file descriptor</code>）</li></ul></li></ol><h3 id="4-4-进程状态"><a href="#4-4-进程状态" class="headerlink" title="4.4 进程状态"></a>4.4 进程状态</h3><ul><li>运行</li><li>就绪</li><li>阻塞</li></ul><p>进程的状态转化图如图4.2所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-4/process-state.jpg" alt="进程：状态转换"></p><h3 id="4-5-数据结构"><a href="#4-5-数据结构" class="headerlink" title="4.5 数据结构"></a>4.5 数据结构</h3><p>对于停止的进程，上下文将保存寄存器的内容，通过上下文切换可以使OS恢复运行该进程。</p><p>下图中的<code>context</code>就是上下文的数据结构，<code>proc</code></p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-4/proc.jpg" alt="xv6的proc结构"></p><h2 id="Chapter-5-进程API"><a href="#Chapter-5-进程API" class="headerlink" title="Chapter 5 进程API"></a>Chapter 5 进程API</h2><p>本章讨论在UNIX系统中的进程创建。</p><blockquote><p>Q：如何创建并控制进程？</p></blockquote><h3 id="fork-系统调用"><a href="#fork-系统调用" class="headerlink" title="fork() 系统调用"></a>fork() 系统调用</h3><p><code>fork()</code>用于创建新进程，<code>fork</code>的子进程从<code>fork</code>处开始执行。示例代码如下：</p><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdio.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdlib.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;unistd.h&gt;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">int</span> argc, <span class="hljs-keyword">char</span> <span class="hljs-keyword">const</span> *argv[])</span> </span>&#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;hello world (pid: %d)\n&quot;</span>, (<span class="hljs-keyword">int</span>)getpid());    <span class="hljs-keyword">int</span> rc = fork();    <span class="hljs-keyword">if</span> (rc &lt; <span class="hljs-number">0</span>) &#123;        <span class="hljs-comment">// fork failed</span>        <span class="hljs-built_in">fprintf</span>(<span class="hljs-built_in">stderr</span>, <span class="hljs-string">&quot;fork failed\n&quot;</span>);        <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (rc == <span class="hljs-number">0</span>) &#123;        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;hello, I&#x27;am child (pid: %d)\n&quot;</span>, (<span class="hljs-keyword">int</span>)getpid());    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;hello, I am parent of %d (pid: %d)\n&quot;</span>, rc, (<span class="hljs-keyword">int</span>)getpid());    &#125;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;yes !!\n&quot;</span>);    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre><p>运行结果如下：</p><pre><code class="hljs apache"><span class="hljs-attribute">hello</span> world (pid: <span class="hljs-number">13360</span>)<span class="hljs-attribute">hello</span>, I am parent of <span class="hljs-number">13361</span> (pid: <span class="hljs-number">13360</span>)<span class="hljs-attribute">yes</span> !!<span class="hljs-attribute">hello</span>, I&#x27;am child (pid: <span class="hljs-number">13361</span>)<span class="hljs-attribute">yes</span> !!</code></pre><h3 id="wait-系统调用"><a href="#wait-系统调用" class="headerlink" title="wait() 系统调用"></a>wait() 系统调用</h3><p>父进程等待子进程执行完毕。示例代码如下：</p><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdio.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdlib.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;unistd.h&gt;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">int</span> argc, <span class="hljs-keyword">char</span> <span class="hljs-keyword">const</span> *argv[])</span> </span>&#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;hello world (pid: %d)\n&quot;</span>, (<span class="hljs-keyword">int</span>)getpid());    <span class="hljs-keyword">int</span> rc = fork();    <span class="hljs-keyword">if</span> (rc &lt; <span class="hljs-number">0</span>) &#123;        <span class="hljs-comment">// fork failed</span>        <span class="hljs-built_in">fprintf</span>(<span class="hljs-built_in">stderr</span>, <span class="hljs-string">&quot;fork failed\n&quot;</span>);        <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (rc == <span class="hljs-number">0</span>) &#123;        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;hello, I&#x27;am child (pid: %d)\n&quot;</span>, (<span class="hljs-keyword">int</span>)getpid());    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">int</span> wc = wait(<span class="hljs-literal">NULL</span>);        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;hello, I am parent of %d (wc: %d)(pid: %d)\n&quot;</span>, rc, wc,               (<span class="hljs-keyword">int</span>)getpid());    &#125;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;yes !!\n&quot;</span>);    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre><p>运行结果如下：</p><pre><code class="hljs apache"><span class="hljs-attribute">hello</span> world (pid: <span class="hljs-number">13787</span>)<span class="hljs-attribute">hello</span>, I&#x27;am child (pid: <span class="hljs-number">13788</span>)<span class="hljs-attribute">yes</span> !!<span class="hljs-attribute">hello</span>, I am parent of <span class="hljs-number">13788</span> (wc: <span class="hljs-number">13788</span>)(pid: <span class="hljs-number">13787</span>)<span class="hljs-attribute">yes</span> !!</code></pre><h3 id="exec-系统调用"><a href="#exec-系统调用" class="headerlink" title="exec() 系统调用"></a>exec() 系统调用</h3><p>子进程调用<code>execvp()</code>来运行字符计数程序<code>wc</code>。</p><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdio.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;stdlib.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;string.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;sys/wait.h&gt;</span></span><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">&lt;unistd.h&gt;</span></span><span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">main</span><span class="hljs-params">(<span class="hljs-keyword">int</span> argc, <span class="hljs-keyword">char</span> <span class="hljs-keyword">const</span> *argv[])</span> </span>&#123;    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;hello world (pid: %d)\n&quot;</span>, (<span class="hljs-keyword">int</span>)getpid());    <span class="hljs-keyword">int</span> rc = fork();    <span class="hljs-keyword">if</span> (rc &lt; <span class="hljs-number">0</span>) &#123;        <span class="hljs-comment">// fork failed</span>        <span class="hljs-built_in">fprintf</span>(<span class="hljs-built_in">stderr</span>, <span class="hljs-string">&quot;fork failed\n&quot;</span>);        <span class="hljs-built_in">exit</span>(<span class="hljs-number">1</span>);    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (rc == <span class="hljs-number">0</span>) &#123;        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;hello, I&#x27;am child (pid: %d)\n&quot;</span>, (<span class="hljs-keyword">int</span>)getpid());        <span class="hljs-keyword">char</span> *myargs[<span class="hljs-number">3</span>];        myargs[<span class="hljs-number">0</span>] = strdup(<span class="hljs-string">&quot;wc&quot;</span>);        myargs[<span class="hljs-number">1</span>] = strdup(<span class="hljs-string">&quot;p3.c&quot;</span>);        myargs[<span class="hljs-number">2</span>] = <span class="hljs-literal">NULL</span>;        execvp(myargs[<span class="hljs-number">0</span>], myargs);        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;this shouldn&#x27;t print out\n&quot;</span>);    &#125; <span class="hljs-keyword">else</span> &#123;        <span class="hljs-keyword">int</span> wc = wait(<span class="hljs-literal">NULL</span>);        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;hello, I am parent of %d (wc: %d)(pid: %d)\n&quot;</span>, rc, wc,               (<span class="hljs-keyword">int</span>)getpid());    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;&#125;</code></pre><p>运行结果如下：</p><pre><code class="hljs apache"><span class="hljs-attribute">hello</span> world (pid: <span class="hljs-number">14384</span>)<span class="hljs-attribute">hello</span>, I&#x27;am child (pid: <span class="hljs-number">14385</span>) <span class="hljs-attribute">36</span> <span class="hljs-number">115</span> <span class="hljs-number">906</span> p<span class="hljs-number">3</span>.c<span class="hljs-attribute">hello</span>, I am parent of <span class="hljs-number">14385</span> (wc: <span class="hljs-number">14385</span>)(pid: <span class="hljs-number">14384</span>)</code></pre><h2 id="Chapter-6-受限直接执行"><a href="#Chapter-6-受限直接执行" class="headerlink" title="Chapter 6 受限直接执行"></a>Chapter 6 受限直接执行</h2><blockquote><p> Q：如何高效、可控地虚拟化CPU？</p></blockquote><h3 id="6-1-基本技巧：受限直接执行"><a href="#6-1-基本技巧：受限直接执行" class="headerlink" title="6.1 基本技巧：受限直接执行"></a>6.1 基本技巧：受限直接执行</h3><p>直接执行：直接在CPU上运行程序。</p><p>下表展示了基本的直接执行协议：</p><div class="table-container"><table><thead><tr><th style="text-align:center">OS</th><th style="text-align:center">Program</th></tr></thead><tbody><tr><td style="text-align:center">为进程列表创建条目；为程序分配内存；将程序加载到内存；将参数设置到程序栈</td><td style="text-align:center"></td></tr><tr><td style="text-align:center">清楚寄存器；执行call main()</td><td style="text-align:center"></td></tr><tr><td style="text-align:center"></td><td style="text-align:center">执行main()；从main中执行return</td></tr><tr><td style="text-align:center">释放进程的内存；将进程从进程列表中清除</td></tr></tbody></table></div><p>但是这会产生问题：</p><ul><li>OS如何确保程序不做不安全的操作</li><li>OS如何进行进程切换</li></ul><h3 id="6-2-问题1：受限制的操作"><a href="#6-2-问题1：受限制的操作" class="headerlink" title="6.2 问题1：受限制的操作"></a>6.2 问题1：受限制的操作</h3><blockquote><p> Q：如何执行受限制的操作？</p></blockquote><p>引入用户态/内核态的概念，程序在用户态下运行的代码会受到限制。OS以内核态运行，特权指令在这个模式下运行。</p><blockquote><p>Q：用户希望执行特权操作，如何实现？</p></blockquote><p>硬件提供了用户执行系统调用的能力，允许内核向用户进程暴露一些系统调用，例如访问文件系统、创建和销毁进程、与其他进程通信以及内存分配等操作。</p><p>要执行系统调用，程序必须执行特殊的<code>trap</code>指令。该指令同时跳入内核，转为内核态进行系统调用，完成后，OS调用<code>return-from-trap</code>指令，返回到发出调用的用户程序中，同时将级别转为用户态。</p><p>为了保证<code>return-from-trap</code>正确返回，需要将进程<code>trap</code>之前的状态保存起来。</p><blockquote><p>Q：陷阱如何知道在OS内运行哪些代码？</p></blockquote><p>内核通过在启动时设置<code>trap table</code>（中断向量表）来实现。</p><h3 id="6-3-在进程之间切换"><a href="#6-3-在进程之间切换" class="headerlink" title="6.3 在进程之间切换"></a>6.3 在进程之间切换</h3><blockquote><p>Q：OS如果重新获得CPU的控制权，以便它可以在进程之间切换？</p></blockquote><h4 id="协作方式：等待系统调用"><a href="#协作方式：等待系统调用" class="headerlink" title="协作方式：等待系统调用"></a>协作方式：等待系统调用</h4><p>这是一种被动获取方式，还不能确保。</p><h4 id="非协作方式：操作系统进行控制"><a href="#非协作方式：操作系统进行控制" class="headerlink" title="非协作方式：操作系统进行控制"></a>非协作方式：操作系统进行控制</h4><p>进行时钟中断（timer interrupt），时钟设备可以设置为每隔几毫秒产生一次中断，中断时，当前正在运行的进程 停止，OS中预先准备的中断处理程序会运行，此时OS重新获得CPU的控制权。</p><h4 id="保存和恢复上下文"><a href="#保存和恢复上下文" class="headerlink" title="保存和恢复上下文"></a>保存和恢复上下文</h4><p><code>context switch</code>：为当前正在执行的进程保存一些寄存器的值，并为即将运行的进程恢复一些寄存器的值。</p><p>需要注意：<strong>上下文的切换不仅仅来自于保存和恢复少量的寄存器的操作。</strong>程序运行时，会在<code>CPU Cache</code>、<code>TLB</code>、分支预测器和其它片上硬件中建立大量的状态。</p><p>图6.1是xv6的上下文切换代码：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-4/switch.jpg" alt=""></p><h2 id="Chapter-7-进程调度"><a href="#Chapter-7-进程调度" class="headerlink" title="Chapter 7 进程调度"></a>Chapter 7 进程调度</h2><blockquote><p>Q：如何开发一个考虑调度的基本框架？</p></blockquote><h3 id="7-1-工作负载假设"><a href="#7-1-工作负载假设" class="headerlink" title="7.1 工作负载假设"></a>7.1 工作负载假设</h3><p>我们对OS中运行的进程做出如下的工作负载假设（这些假设是不现实）：</p><ul><li>每一个工作运行相同的时间。</li><li>所有的工作同时到达。</li><li>一旦开始，每个工作保持运行直到完成。</li><li>所有的工作只是用CPU。（不执行IO操作）</li><li>每个工作的运行时间是已知的。</li></ul><h3 id="7-2-调度指标"><a href="#7-2-调度指标" class="headerlink" title="7.2 调度指标"></a>7.2 调度指标</h3><p>只用一个指标：周转时间（turnaround time），定义是任务完成时间减去任务到达时间。正式定义：</p><script type="math/tex; mode=display">T周转时间 = T完成时间 - T到达时间</script><p>公平是另一个性能指标，但是性能和公平在调度系统中是矛盾的。</p><h3 id="7-3-FIFO"><a href="#7-3-FIFO" class="headerlink" title="7.3 FIFO"></a>7.3 FIFO</h3><p>先进先出调度，也称为先来先服务（FCFS），特性：简单并易于实现。</p><p>如果存在护航效应（convoy effect）：即前面的任务运行时间长，一些耗时较少的进程排在耗时较长的进程之后。</p><p>Q：如何开发以中更好的算法来处理这个情况？</p><h3 id="7-4-最短任务优先（SJF）"><a href="#7-4-最短任务优先（SJF）" class="headerlink" title="7.4 最短任务优先（SJF）"></a>7.4 最短任务优先（SJF）</h3><p>如果所有任务同时到达，，SJF是一个最优的调度算法。</p><p>但是如果不是同时到达，存在饥饿问题。</p><h3 id="7-5-最短完成时间优先（STCF）"><a href="#7-5-最短完成时间优先（STCF）" class="headerlink" title="7.5 最短完成时间优先（STCF）"></a>7.5 最短完成时间优先（STCF）</h3><p>如果对SJF添加抢占，则被称为最短完成时间优先（STCF）。每当新进程进入系统，它会确认所有的进程，谁的剩余完成时间最少，然后调度它。</p><h3 id="7-6-新度量指标：响应时间"><a href="#7-6-新度量指标：响应时间" class="headerlink" title="7.6 新度量指标：响应时间"></a>7.6 新度量指标：响应时间</h3><p>对于早期的批处理系统来说，STCF可能是一个很好的策略。但是，引入分时系统之后，用户会对系统的交互性能有所要求。因此诞生了一个新的调度程序度量标准：<strong>响应时间</strong>。</p><script type="math/tex; mode=display">T响应时间 = T首次运行时间 - T到达时间</script><p>STCF和相关方法在响应时间的表现上并不是很好。例如：如果3个进程同时到达，第三个工作必须等待前两个工作全部运行后才能运行。</p><h3 id="7-7-轮转（Round-Robin）"><a href="#7-7-轮转（Round-Robin）" class="headerlink" title="7.7 轮转（Round-Robin）"></a>7.7 轮转（Round-Robin）</h3><p>基本思想：RR在一个时间片内运行工作，然后切换到运行队列中的下一个任务，而不是运行一个任务直到结束，反复执行，直到所有任务完成。</p><p>注意点：</p><ul><li>时间片长度必须是时钟中断周期的倍数。</li><li>时间片长度对于RR至关重要，时间片越短，在响应时间上表现越好，但是会增加上下文切换的性能开销。因此需要权衡时间片的大小。</li></ul><p>两种调度程序：</p><ul><li>SJF、STCF：优化周转时间，对响应时间不利。</li><li>RR：优化响应时间，对周转时间不利。</li></ul><h3 id="7-8-结合I-O"><a href="#7-8-结合I-O" class="headerlink" title="7.8 结合I/O"></a>7.8 结合I/O</h3><p>调度程序会在工作时发起I/O请求，OS会根据具体情况来选择执行顺序，以便更好地交互使用资源。</p><h3 id="7-9-无法预知"><a href="#7-9-无法预知" class="headerlink" title="7.9 无法预知"></a>7.9 无法预知</h3><p>在通用的OS中，操作系统无法预知每个作业的长度，因此我们如何能够将已经看到的一些想法与RR调度程序结合起来，以便响应时间变得不错？</p><h2 id="Chapter-8-多级反馈队列"><a href="#Chapter-8-多级反馈队列" class="headerlink" title="Chapter 8 多级反馈队列"></a>Chapter 8 多级反馈队列</h2><p>多级反馈队列（MLFQ）应用于兼容时分共享系统。主要解决两方面问题：</p><ul><li>优化周转时间：先执行短作业来实现。</li><li>降低响应时间：如何降低？</li></ul><blockquote><p>Q： 没有工作长度的前提条件，如何设计一个能同时减少响应时间和周转时间的调度程序？</p></blockquote><h3 id="8-1-基本规则"><a href="#8-1-基本规则" class="headerlink" title="8.1 基本规则"></a>8.1 基本规则</h3><p>MLFQ中有许多独立的队列，每个队列有不同的优先级，任何时刻，一个任务只能存在于一个队列中，MLFQ总是执行优先级较高的工作。</p><p>MLFQ会根据观察到的行为调整它的优先级。例如：一个工作不断放弃CPU去等待键盘输入，MLFQ则推断它是交互式进程，并让它保留高优先级。相反，如果一个工作长时间占用CPU，MLFQ则会降低其优先级。</p><h3 id="8-2-尝试1：如何改变优先级"><a href="#8-2-尝试1：如何改变优先级" class="headerlink" title="8.2 尝试1：如何改变优先级"></a>8.2 尝试1：如何改变优先级</h3><blockquote><p> Q：MLFQ如何改变任务的优先级？</p></blockquote><p>既有很多运行时间很短、频繁放弃CPU的交互式进程，也有需要很多CPU时间、响应时间不重要的计算密集型进程。优先级调整算法：</p><ul><li>任务进入系统，放在最高优先级的队列。</li><li>任务用完整个时间片，降低其优先级，下移一个队列。</li><li>如果任务在其时间片内主动释放CPU，则优先级不变。</li></ul><h4 id="存在的问题"><a href="#存在的问题" class="headerlink" title="存在的问题"></a>存在的问题</h4><ul><li>饥饿问题：如果系统有很多交互式进程，就会不断占用CPU，导致长任务永远无法得到CPU。</li><li>如何阻止调度程序被欺骗？</li></ul><h3 id="8-3-尝试2：提升优先级"><a href="#8-3-尝试2：提升优先级" class="headerlink" title="8.3 尝试2：提升优先级"></a>8.3 尝试2：提升优先级</h3><p>上面存在的问题需要解决，一个简单的思路就是周期性的提升所有任务的优先级。规则如下：</p><ul><li>经过一段时间S，就将系统中所有任务重新加入最高优先级队列中。</li></ul><blockquote><p> Q：时间S如何设置？</p></blockquote><p>太高会导致长任务饥饿，太低会让交互式任务得不到合适的CPU时间。</p><h3 id="8-4-尝试3：更好的计时方式"><a href="#8-4-尝试3：更好的计时方式" class="headerlink" title="8.4 尝试3：更好的计时方式"></a>8.4 尝试3：更好的计时方式</h3><blockquote><p>Q：如何阻止调度程序被欺骗？</p></blockquote><p>解决：为每层队列提供更完善的CPU计时方式，调度程序应该记录一个进程在某一层中消耗的总时间。而不是在调度时重新计时。规则如下：</p><ul><li>一旦任务用完了其在某一层的时间配额，则降低其优先级。</li></ul><h3 id="8-5-MLFQ调优及其他问题"><a href="#8-5-MLFQ调优及其他问题" class="headerlink" title="8.5 MLFQ调优及其他问题"></a>8.5 MLFQ调优及其他问题</h3><p>MLFQ调度算法的参数如何设置？</p><p><code>Solaris</code>的MLFQ提供了一组表来决定进程在其生命周期中如何调整优先级，每层的时间片大小、时间S都由管理员动态调整。</p><h3 id="8-6-MLFQ-小结"><a href="#8-6-MLFQ-小结" class="headerlink" title="8.6 MLFQ 小结"></a>8.6 MLFQ 小结</h3><ul><li>规则1：如果A优先级 &gt; B优先级，运行A</li><li>规则2：如果A优先级 = B优先级，轮转运行A和B</li><li>规则3：任务进入系统时，放在最高优先级。</li><li>规则4：一旦任务用完了其在某一层的时间配额，就降低其优先级。</li><li>规则5：经过一段时间S，就将系统中所有任务重新加入最高优先级队列。</li></ul><h2 id="Chapter-9-比例份额"><a href="#Chapter-9-比例份额" class="headerlink" title="Chapter 9 比例份额"></a>Chapter 9 比例份额</h2><p>比例份额算法基于一个简单的想法：调度程序的最终目标，是确保每个工作获得一定比例的CPU时间，而不是优化周转时间和响应时间。</p><blockquote><p>Q：如何按比例分配CPU？关键机制与效率如何？</p></blockquote><h3 id="9-1-基本概念：彩票数代表份额"><a href="#9-1-基本概念：彩票数代表份额" class="headerlink" title="9.1 基本概念：彩票数代表份额"></a>9.1 基本概念：彩票数代表份额</h3><p>彩票调度（<strong>lottery scheduling</strong>）背后是一个基本概念：彩票数（<strong>ticket</strong>）代表了进程占有某个资源的份额。一个进程拥有的<code>ticket</code>占总<code>ticket</code>的百分比，就是它占有资源的份额。</p><p>例如：进程A拥有75张<code>ticket</code>，B拥有25张，则将CPU时间按<code>3:1</code>分配给A、B进程。</p><p>通过定时地抽取<code>ticket</code>，由于程序知道总<code>ticket</code>，因此每次通过随机数对进程占用时间进行分配。</p><h3 id="9-2-彩票机制"><a href="#9-2-彩票机制" class="headerlink" title="9.2 彩票机制"></a>9.2 彩票机制</h3><p>该机制提供了一些有效的方式来调度彩票。</p><ul><li>利用彩票货币（<strong>ticket currency</strong>）的概念，允许拥有一组<code>ticket</code>的用户以他们喜欢的某种货币，将<code>ticket</code>分给自己的不同工作。之后OS再自动将这种货币兑换成正确的全局<code>ticket</code>。</li><li>彩票转让（<strong>ticket transfer</strong>）：一个进程可以临时将自己拥有的彩票交给另一个进程。</li><li>彩票通胀（<strong>ticket inflation</strong>）：一个进程可以临时提升或降低自己拥有的彩票数量。</li></ul><h3 id="9-3-实现"><a href="#9-3-实现" class="headerlink" title="9.3 实现"></a>9.3 实现</h3><p>需要一个随机数生成器来选择中奖彩票和一个记录系统中所有进程的数据结构（链表），以及所有彩票的总数。</p><pre><code class="hljs C"><span class="hljs-keyword">int</span> counter = <span class="hljs-number">0</span>;<span class="hljs-keyword">int</span> winner = getrandom(<span class="hljs-number">0</span>, totalTickets);<span class="hljs-keyword">node_t</span> * current = head;<span class="hljs-keyword">while</span>(current)&#123;    counter = counter + current-&gt;tickets;    <span class="hljs-keyword">if</span> (counter &gt; winner)        <span class="hljs-keyword">break</span>;    current = current-&gt;next;&#125;</code></pre><h2 id="Chapter-10-多处理器调度"><a href="#Chapter-10-多处理器调度" class="headerlink" title="Chapter 10 多处理器调度"></a>Chapter 10 多处理器调度</h2><blockquote><p>Q：OS如何在多CPU上进行调度工作？会遇到什么新问题？</p></blockquote><h3 id="10-1-多处理器架构"><a href="#10-1-多处理器架构" class="headerlink" title="10.1 多处理器架构"></a>10.1 多处理器架构</h3><p>与单CPU之间的基本区别：对硬件缓存的使用，以及多处理器之间共享数据的方式。</p><p>如果系统有多个处理器，并共享同一个内存，会出现什么问题？</p><blockquote><p>案例：如果一个运行在CPU1上的程序从内存地址A读取数据，由于未命中缓存，系统直接访问内存，得到值D，然后修改了地址A处的值，但实际上h只是将缓存修改为新值D’。系统会在一段时间之后再将数据从缓存更新到内存。假设此时，CPU2中的程序读取地址A处的值，然而得到了旧值D。</p></blockquote><p>以上问题被称为缓存一致性（<strong>cache coherence</strong>）问题。</p><p>硬件提供了 这个问题的基本解决方案：<strong>通过监控内存访问，硬件可以保证获得正确的数据，并保证共享内存的唯一性。</strong>在基于总线的系统中，一种方式是使用<strong>bus snooping</strong>。每个缓存都通过监听链接所有缓存和内存的总线。如果发现内存访问，则对缓存中的对应数据进行更新。</p><h3 id="10-2-同步"><a href="#10-2-同步" class="headerlink" title="10.2 同步"></a>10.2 同步</h3><p>跨CPU访问共享数据时，需要使用互斥原语或其他方法（lock-free）才能保证正确性。</p><h3 id="10-3-缓存亲和度"><a href="#10-3-缓存亲和度" class="headerlink" title="10.3 缓存亲和度"></a>10.3 缓存亲和度</h3><p><strong>cache affinity</strong>：一个进程在CPU上运行时，会在该CPU的缓存中维护许多状态。下次该进程在相同CPU上运行时，由于缓存中的数据而执行的更快。相反，在不同的CPU上执行，会由于需要重新加载数据而很慢。因此多处理器调度应该考虑这种缓存亲和性，尽量将进程保持在同一个CPU上执行。</p><h3 id="10-4-单队列调度"><a href="#10-4-单队列调度" class="headerlink" title="10.4 单队列调度"></a>10.4 单队列调度</h3><blockquote><p>Q：如何设计一个多处理器系统的调度程序？</p></blockquote><p>最基本的方式是简单地复用单处理器调度的基本架构。将所有进程放到一个队列中。将此称为单队列多处理器调度（SQMS）。</p><p>缺点：</p><ul><li>缺乏可扩展性</li><li>缓存亲和性</li></ul><h3 id="10-5-多队列调度"><a href="#10-5-多队列调度" class="headerlink" title="10.5 多队列调度"></a>10.5 多队列调度</h3><p>每个CPU一个队列。将此称为多队列多处理器调度（MQMS）。每个队列使用不同的调度规则。当进程进入系统后，会根据一些启发性原则（随机或选择较空的队列）将其放入某个调度队列。</p><p>优点：</p><ul><li>可扩展性</li><li>有良好的缓存亲和性</li></ul><p>缺点：</p><ul><li>负载不均</li></ul><blockquote><p>Q：如何应对负载不均？</p></blockquote><p><strong>migration</strong>：将工作跨CPU移动。如果CPU1的队列无任务，CPU1的队列中有两个任务，OS会将CPU1队列中的一个任务migration到CPU0的队列。</p><p><strong>work stealing</strong>：较少任务的队列会从其它队列中steal任务。缺点：频繁检查会引起较高的开销。需要设置合理的检查间隔。</p><h3 id="10-6-Linux多处理器调度"><a href="#10-6-Linux多处理器调度" class="headerlink" title="10.6 Linux多处理器调度"></a>10.6 Linux多处理器调度</h3><p>Linux存在三种不同的调度程序：</p><ul><li>O(1)调度程序<ul><li>基于优先级，随着时间推移改变进程的优先级</li></ul></li><li>完全公平调度程序（CFS）<ul><li>使用多队列</li><li>确定的比例调度方法</li></ul></li><li>BF调度程序（BFS）<ul><li>使用单队列</li><li>基于比例调度</li></ul></li></ul><h1 id="内存虚拟化"><a href="#内存虚拟化" class="headerlink" title="内存虚拟化"></a>内存虚拟化</h1><h2 id="Chapter-13-地址空间"><a href="#Chapter-13-地址空间" class="headerlink" title="Chapter 13 地址空间"></a>Chapter 13 地址空间</h2><h3 id="13-1-早期系统"><a href="#13-1-早期系统" class="headerlink" title="13.1 早期系统"></a>13.1 早期系统</h3><p>早期的机器物理内存看起来如图13.1所示。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-13/os-early.jpg" alt=""></p><h3 id="13-2-多道程序和时分共享"><a href="#13-2-多道程序和时分共享" class="headerlink" title="13.2 多道程序和时分共享"></a>13.2 多道程序和时分共享</h3><p>多道程序系统时代开启后，多个进程运行时，如果一个进程等待IO操作，操作系统会切换进程，以提高CPU的有效利用率。</p><p>分时系统中交互性变得很重要，因为许多用户可能同时在使用机器，每个人都在等待任务及时响应。一种实现时分共享的方法，是让一个进程单独占用全部内存运行一小段时间。然后停止它，并将它所有的状态信息保存在磁盘上，进行其他进程的调度，这样可以实现比较粗糙的机器共享。</p><p>但是这样操作会使得进程切换变得十分缓慢。可以选择将进程数据保存在内存中。</p><h3 id="13-3-地址空间"><a href="#13-3-地址空间" class="headerlink" title="13.3 地址空间"></a>13.3 地址空间</h3><p>OS需要提供一个易用的物理内存抽象模型，这个抽象模型叫做<strong>address space</strong>。是运行的程序看到的内存。</p><p>一个进程的地址空间包括运行的程序的所有内存状态，图13.3是一个简单的例子。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-13/address-space.jpg" alt=""></p><blockquote><p>Q：OS如何在单一的物理内存上为多个运行的进程构建一个地址空间？</p></blockquote><h3 id="13-4-目标"><a href="#13-4-目标" class="headerlink" title="13.4 目标"></a>13.4 目标</h3><p>虚拟内存的目标：</p><ul><li>透明（transparency）<ul><li>OS实现虚拟内存的方式，应该让运行的程序看不见。</li></ul></li><li>效率（efficiency）<ul><li>追求虚拟化尽可能高效。</li></ul></li><li>保护（protection）<ul><li>当一个进程执行加载、存储等操作不会受其它进程影响，也不能以任何方式访问或影响其他的进程。进程之间需要隔离。</li></ul></li></ul><h2 id="Chapter-14-内存操作API"><a href="#Chapter-14-内存操作API" class="headerlink" title="Chapter 14 内存操作API"></a>Chapter 14 内存操作API</h2><blockquote><p>Q：在UNIX/C系统中，如何分配和管理内存？</p></blockquote><h3 id="14-1-内存类型"><a href="#14-1-内存类型" class="headerlink" title="14.1 内存类型"></a>14.1 内存类型</h3><p>在运行一个C程序的时候，会分配两种类型的内存：</p><ul><li>栈内存：申请和释放是编译器来隐式管理的，有时也称为自动内存。<ul><li>int x = 1;</li></ul></li><li>堆内存：申请和释放是由程序员显式地完成，长期内存<ul><li>int <em>x = (int </em>) malloc(sizeof(int))<ul><li>指针存储在栈内存，malloc申请的空间在堆内存</li></ul></li></ul></li></ul><h3 id="14-2-malloc调用"><a href="#14-2-malloc调用" class="headerlink" title="14.2 malloc调用"></a>14.2 malloc调用</h3><blockquote><p>void malloc(size_t size)</p></blockquote><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-meta-keyword">include</span><span class="hljs-meta-string">&lt;stdio.h&gt;</span></span><span class="hljs-keyword">double</span> *d = (<span class="hljs-keyword">double</span> *) <span class="hljs-built_in">malloc</span>(<span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">double</span>));</code></pre><h3 id="14-3-free调用"><a href="#14-3-free调用" class="headerlink" title="14.3 free调用"></a>14.3 free调用</h3><p>释放内存通过调用<code>free()</code>来实现。</p><pre><code class="hljs C"><span class="hljs-keyword">int</span> *x = <span class="hljs-built_in">malloc</span>(<span class="hljs-number">10</span> * <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">int</span>))<span class="hljs-built_in">free</span>(x);</code></pre><h3 id="14-4-常见错误"><a href="#14-4-常见错误" class="headerlink" title="14.4 常见错误"></a>14.4 常见错误</h3><ul><li>忘记分配内存<ul><li>没有<code>malloc</code>就使用指针</li></ul></li><li>没有分配足够的内存</li><li>忘记初始化分配的内存<ul><li>遇到未初始化的读取，会读取到一些未知值得数据。</li></ul></li><li>忘记释放内存<ul><li>内存泄露（<strong>memory leak</strong>）</li></ul></li><li>在用完之前释放内存<ul><li>悬挂指针（<strong>dangling pointer</strong>）</li></ul></li><li>反复释放内存</li><li>错误调用free()<ul><li>并没有传入指针，错误了传入其他值。</li></ul></li></ul><h2 id="Chapter-15-地址转换"><a href="#Chapter-15-地址转换" class="headerlink" title="Chapter 15 地址转换"></a>Chapter 15 地址转换</h2><blockquote><p>Q：如何实现高效的内存虚拟化？如何提供应用程序所需的灵活性？如何保持控制程序可访问的内存位置，从而确保应用程序的内存访问收到合理的限制？</p></blockquote><p>通过基于硬件的地址转换，它可以看成是受限直接执行这种一般方式的补充。</p><p>通过地址转换，硬件对每次对内存的访问进行处理，将指令中的虚拟地址转换为数据实际存储的物理地址。</p><h3 id="15-1-假设"><a href="#15-1-假设" class="headerlink" title="15.1 假设"></a>15.1 假设</h3><p>我们先假设用户的地址空间必须连续地放在物理内存中。同时，为了简单，我们假设地址空间不是很大，小于物理内存，假设每个地址空间的大小完全一样。</p><h3 id="15-2-一个例子"><a href="#15-2-一个例子" class="headerlink" title="15.2 一个例子"></a>15.2 一个例子</h3><p>C程序如下：</p><pre><code class="hljs C"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">func</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">int</span> x;    x = x + <span class="hljs-number">3</span>;&#125;</code></pre><p>程序编译成汇编代码后：</p><pre><code class="hljs assembly">128: movl 0x0(%ebx), %eax132: addl $0x03, %eax135: movl %eax, 0x0(%ebx)</code></pre><p>以上的程序在进行内存访问时，OS会根据内部机制，将对应的虚拟地址转换成物理地址再进行内存访问。</p><h3 id="15-3-动态重定位"><a href="#15-3-动态重定位" class="headerlink" title="15.3 动态重定位"></a>15.3 动态重定位</h3><p>第一次应用：基址加界限机制（也称为动态重定位 <strong>dynamic relocation</strong>）。</p><p>含义：每个CPU需要两个硬件寄存器，基址寄存器和界限寄存器。</p><p>采用这种方式，在编写和编译程序时假设地址空间从零开始，但是当程序真正执行时，OS会决定其在物理内存中的实际加载地址，并将起始地址记录在基址寄存器上。</p><pre><code class="hljs excel">physical <span class="hljs-built_in">address</span> = virtual <span class="hljs-built_in">address</span> + <span class="hljs-built_in">base</span></code></pre><p>如果虚拟地址超过界限寄存器，CPU将触发异常。</p><p>通常将CPU中负责地址转换的部分统称为内存管理单元（<strong>MMU</strong>）。</p><h3 id="15-4-硬件支持"><a href="#15-4-硬件支持" class="headerlink" title="15.4 硬件支持"></a>15.4 硬件支持</h3><p>动态重定位的硬件要求：</p><div class="table-container"><table><thead><tr><th style="text-align:center">硬件要求</th><th style="text-align:center">解释</th></tr></thead><tbody><tr><td style="text-align:center">特权模式</td><td style="text-align:center">需要，以访用户模式的进程执行特权操作</td></tr><tr><td style="text-align:center">基址/界限寄存器</td><td style="text-align:center">每个CPU需要一对寄存器来支持地址转换和界限检查</td></tr><tr><td style="text-align:center">能够转换虚拟地址并检查是否越界</td><td style="text-align:center">电路来完成转换和检查界限</td></tr><tr><td style="text-align:center">修改基址/界限寄存器的特权指令</td><td style="text-align:center">在用户程序运行前，OS必须能设置这些值</td></tr><tr><td style="text-align:center">注册异常处理程序的特权指令</td><td style="text-align:center">OS必须能告诉硬件，异常发生需要执行哪些代码</td></tr><tr><td style="text-align:center">能够触发异常</td><td style="text-align:center">如果进程试图使用特权指令或越界的内存</td></tr></tbody></table></div><h2 id="Chapter-16-分段"><a href="#Chapter-16-分段" class="headerlink" title="Chapter 16 分段"></a>Chapter 16 分段</h2><p>简单的通过基址寄存器和界限寄存器实现的虚拟内存很浪费，那么就存在了问题。</p><blockquote><p>Q：怎样支持大地址空间？堆栈之间有大量空闲空间的问题如何解决？</p></blockquote><h3 id="16-1-分段：泛化的基址-界限"><a href="#16-1-分段：泛化的基址-界限" class="headerlink" title="16.1 分段：泛化的基址/界限"></a>16.1 分段：泛化的基址/界限</h3><p>为了解决上述问题，引入分段概念。一个段是地址空间里的一个连续定长的区域。一般有三个逻辑段：代码、栈和堆。分段机制使得OS能够将不同的段放到不同的物理内存区域。从而避免了虚拟地址空间中的未使用部分占用物理内存。</p><h3 id="16-2-引用哪个段"><a href="#16-2-引用哪个段" class="headerlink" title="16.2 引用哪个段"></a>16.2 引用哪个段</h3><p>硬件在地址转换时如何知道段内偏移量？以及地址引用了哪个段？</p><p>显式方式：用虚拟地址的前几位标识不同的段。如图所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-16/segment1.jpg" alt=""></p><p>硬件获取物理地址的操作：</p><pre><code class="hljs nix"><span class="hljs-attr">segment</span> = (VirtualAddress &amp; SEG_MASK) &gt;&gt; SEG_SHIFT<span class="hljs-attr">Offset</span> = VirtualAddress &amp; OFFSET_MASK<span class="hljs-keyword">if</span>(Offset &gt;= Bounds[Segment])Exception(FAULT)<span class="hljs-keyword">else</span><span class="hljs-attr">PhysAddr</span> = Base[Segment] + Offset<span class="hljs-attr">Register</span> = AccessMemory(PhysAddr)</code></pre><p>硬件还有其它方法来确定特定地址在哪个段，在隐式方式中，硬件通过地址产生的方式来确定段。例如，如果地址由PC产生，那么地址在代码段，如果基于栈或基址指针，它一定在栈段，其他地址在堆段。</p><h3 id="16-3-栈怎么办"><a href="#16-3-栈怎么办" class="headerlink" title="16.3 栈怎么办"></a>16.3 栈怎么办</h3><p>由于栈模型在内存中是反向增长的，例如：在物理内存中，开始于28KB，增长到26KB，相应虚拟地址从16KB到14KB，地址转换与正向增长略微不同。</p><p>首先需要硬件支持，硬件需要知道段的增长方向。</p><h3 id="16-4-支持共享"><a href="#16-4-支持共享" class="headerlink" title="16.4 支持共享"></a>16.4 支持共享</h3><p>随着分段机制的不断改进，人们意识到通过再多一点的硬件支持，就能实现新的效率提升。为了节省内存，在地址空间里共享某些内存段是有必要的。</p><p>为了支持共享，需要增加一些额外的硬件支持，通过保护位（<strong>protection bit</strong>），标识程序是否能读写该段，</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-16/register1.jpg" alt=""></p><h3 id="16-5-细粒度与细粒度的分段"><a href="#16-5-细粒度与细粒度的分段" class="headerlink" title="16.5 细粒度与细粒度的分段"></a>16.5 细粒度与细粒度的分段</h3><p>上述方式的分段是粗粒度的，它将地址空间分成较大的、粗粒度的块。一些早期系统更灵活，允许将地址空间划分为大量较小的段。</p><p>段的数量增加需要进一步的硬件支持，并在内存中保存<strong>segment table</strong>。</p><h3 id="16-6-操作系统支持"><a href="#16-6-操作系统支持" class="headerlink" title="16.6 操作系统支持"></a>16.6 操作系统支持</h3><p>分段带来了新的问题：</p><ul><li>OS在context switch时应该做什么？<ul><li>各个段寄存器中内容的保存和恢复</li></ul></li><li>管理物理内存的空闲空间</li><li>内存碎片<ul><li>紧凑（<strong>compact</strong>）物理内存，重新安排原有的段<ul><li>OS先终止运行进程，将它们的数据重新移动到新的段</li><li>成本高，拷贝段是内存密集型，会占用大量的处理器时间</li></ul></li><li>分配算法可以适当解决该问题</li></ul></li></ul><h2 id="Chapter-17-空闲空间管理"><a href="#Chapter-17-空闲空间管理" class="headerlink" title="Chapter 17 空闲空间管理"></a>Chapter 17 空闲空间管理</h2><blockquote><p>Q：如何管理空闲空间？什么策略可以让碎片最小化？不同方法的时空开销如何？</p></blockquote><h3 id="17-1-假设"><a href="#17-1-假设" class="headerlink" title="17.1 假设"></a>17.1 假设</h3><p>我们假设基本的接口像<code>malloc()</code>和<code>free()</code>那样，在堆上管理空闲空间。</p><p>进一步假设，我们主要关心的是外部碎片（<strong>external fragmentation</strong>），分配程序也可能出现内部碎片问题。如果分配程序给出的内存块超出请求的大小，在这种情况下会被称为内部碎片。</p><p>还假设：内存一旦分配给用户，就不可以被重定位到其他位置。也就是不能进行compaction操作，但是OS在实现分段时，可以通过紧凑来减少碎片。</p><p>最后假设：分配程序所管理的是连续的一块字节区域。在一些情况下，分配程序可以要求这块区域增长，例如：用户级的内存分配库在空间即将用完时，可以向内核申请增加堆空间。</p><h3 id="17-2-底层机制"><a href="#17-2-底层机制" class="headerlink" title="17.2 底层机制"></a>17.2 底层机制</h3><p>分割与合并：</p><ul><li>如果请求的空间大小小于某块空闲块，分配程序通常会进行分割</li><li>如果回收一块空闲内存时，如果回收的内存块地址与其它空闲块地址邻近，就将它们合并为一个较大的空闲块。</li></ul><blockquote><p> Q：如何追踪已分配空间的大小？</p></blockquote><p>用户请求N字节的内存，库分配的是<code>N+header</code>大小的空闲块。</p><p>header中保存一些额外的信息：</p><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">header_t</span> &#123;</span>    <span class="hljs-keyword">int</span> size;    <span class="hljs-keyword">int</span> magic;&#125;<span class="hljs-keyword">header_t</span>;</code></pre><p>用户在调用<code>free(ptr)</code>时，库会通过简单的指针运算得到header的位置：</p><pre><code class="hljs c"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">free</span><span class="hljs-params">(<span class="hljs-keyword">void</span> *ptr)</span> </span>&#123;    <span class="hljs-keyword">header_t</span> *hptr = (<span class="hljs-keyword">void</span> *) ptr - <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">header_t</span>);&#125;</code></pre><p>获得header的指针后，库可以很容易的确定幻数是否符合预期值，简单计算释放的空间大小（header大小 + 分配给用户的空间的大小）。</p><blockquote><p>Q：如何在空闲内存内部建立一个列表？</p></blockquote><pre><code class="hljs C"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">node_t</span> &#123;</span>    <span class="hljs-keyword">int</span> size;    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">node_t</span> *<span class="hljs-title">next</span>;</span>&#125;<span class="hljs-keyword">node_t</span>;</code></pre><p>如何初始化列表？</p><pre><code class="hljs C"><span class="hljs-keyword">node_t</span> *head = mmap(<span class="hljs-literal">NULL</span>, <span class="hljs-number">4096</span>, PROT_READ|PROT_WRITE,                   MAP_ANON|MAP_PRIVATE, <span class="hljs-number">-1</span>, <span class="hljs-number">0</span>);head-&gt;size = <span class="hljs-number">4096</span> - <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">node_t</span>);head-&gt;next = <span class="hljs-literal">NULL</span>;</code></pre><p>假设此时有一个100字节的内存请求，，堆中空间如图17.4所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-16/heap1.jpg" alt=""></p><h3 id="17-3-基本策略"><a href="#17-3-基本策略" class="headerlink" title="17.3 基本策略"></a>17.3 基本策略</h3><h4 id="最优匹配-best-fit"><a href="#最优匹配-best-fit" class="headerlink" title="最优匹配 best fit"></a>最优匹配 best fit</h4><p>遍历整个空闲链表，找到和请求大小一样或更大的空闲块，然后返回当中符合要求最小的一块。</p><p>能减少空间浪费，但是要付出较高的性能代价。</p><h4 id="最差匹配-worst-fit"><a href="#最差匹配-worst-fit" class="headerlink" title="最差匹配 worst fit"></a>最差匹配 worst fit</h4><p>遍历整个空闲链表，找到满足要求最大的空闲块。</p><p>碎片不仅多，还有很好的性能代价。</p><h4 id="首次匹配-first-fit"><a href="#首次匹配-first-fit" class="headerlink" title="首次匹配 first fit"></a>首次匹配 first fit</h4><p>找到第一个足够大的块，将请求的空间返回给用户。</p><p>有速度优势，但是会让空闲链表开头部分有很多小块。可以基于地址排序，通过合并操作减少内存碎片。</p><h4 id="下次匹配-next-fit"><a href="#下次匹配-next-fit" class="headerlink" title="下次匹配 next fit"></a>下次匹配 next fit</h4><p>维护一个指针，指向上一次查找结束的位置。从该指针开始查找合适的节点。</p><p>避免了对链表开头的频繁操作。</p><h3 id="17-4-其他方式"><a href="#17-4-其他方式" class="headerlink" title="17.4 其他方式"></a>17.4 其他方式</h3><h4 id="分离空闲链表"><a href="#分离空闲链表" class="headerlink" title="分离空闲链表"></a>分离空闲链表</h4><p>如果应用程序经常申请一种或几种大小的内存空间，那就用一个独立的链表，管理这些大小的对象，其它大小的请求都交给更通用的内存分配程序。</p><p>例如：在内核启动时，它为可能频繁请求的内核对象创建一些对象缓存，比如锁和文件系统inode等。</p><h4 id="伙伴系统"><a href="#伙伴系统" class="headerlink" title="伙伴系统"></a>伙伴系统</h4><p>binary buddy allocator：空闲空间首先从概念上被看成为$2^N$大小，当有一个内存分配请求时，空闲空间被递归地一分为二，直至刚好满足请求的大小。</p><p>这种分配策略只允许分配2的整数次幂大小的空闲块。</p><p>如果回收内存时，会检查对应伙伴（与其相等大小的另一半）是否空闲，如果空闲则进行合并，直至整个内存区域。</p><h4 id="其它想法"><a href="#其它想法" class="headerlink" title="其它想法"></a>其它想法</h4><p>查询链表的时间复杂度较高，可以采用查询性能更为高效的数据结构来优化开销哦啊，例如平衡二叉树、伸展树等。</p><h2 id="Chapter-18-分页介绍"><a href="#Chapter-18-分页介绍" class="headerlink" title="Chapter 18 分页介绍"></a>Chapter 18 分页介绍</h2><p>分段技术随着时间推移，分配内存会变得比较困难。</p><p>还可以考虑第二种方法：<strong>将空间分割成固定长度的分片。</strong>这种思想成为分页。分页是将一个进程的地址空间分割成几个不同长度的逻辑段（代码、堆、段），而是分割成固定大小的党员，每个单元成为一页。相应地，我们把物理内存看成是定长槽块的阵列，叫做页帧（<strong>page frame</strong>）。每个这样的页帧包含一个虚拟内存页。</p><blockquote><p>Q：如何通过页来实现虚拟内存？从而避免分段产生的问题？基本技术是什么？</p></blockquote><h3 id="18-1-一个简单例子"><a href="#18-1-一个简单例子" class="headerlink" title="18.1 一个简单例子"></a>18.1 一个简单例子</h3><p>如图所示，物理内存由一组固定大小的槽块组成。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-18/page-model.jpg" alt=""></p><p>为了记录地址空间的每个虚拟页放在物理内存中的位置，操作系统通常为每个进程保存一个数据结构，称为页表（<strong>page table</strong>），主要作用是为地址空间的每个虚拟页面保存地址转换（<strong>address translation</strong>），从而让我们知道每一页在物理内存中的位置。</p><p>分页如何进行地址转换？</p><p>虚拟地址的格式如下：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-18/page-address.jpg" alt=""></p><p>分成：</p><ul><li>虚拟页面号（virtual page number VPN）</li><li>页内的偏移量（offset）</li></ul><h3 id="18-2-页表存在哪里"><a href="#18-2-页表存在哪里" class="headerlink" title="18.2 页表存在哪里"></a>18.2 页表存在哪里</h3><p>页面会变得非常大，例如：典型的32位地址空间，带有4KB的页，这个虚拟地址分成20位VPN和VPN的12位偏移量。20位的VPN意味着操作系统需要管理$2^{20}$个地址转换，假设每个页表条目（Page Table Entry）需要4个字节，那么每个页表则需要4MB内存。</p><p>图18.4是OS内存中的页表：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-18/page-table.jpg" alt=""></p><h3 id="18-3-Page-Table-Entry"><a href="#18-3-Page-Table-Entry" class="headerlink" title="18.3 Page Table Entry"></a>18.3 Page Table Entry</h3><p>PTE中有什么？</p><ul><li>有效位（<strong>valid bit</strong>）：通常用于指示特定地址转换是否有效<ul><li>例如：一个程序开始运行时，它的代码和堆在其地址空间的一端，栈在另一端。所有未使用的中间空间都会被标记为无效，如果进程访问这部分，则会<code>trap</code>，导致进程终止。</li><li>有效位对于支持稀疏地址空间至关重要，通过简单地将地址空间中所有未使用的页面标记为无效。我们不需要再为这些页面分配物理帧，从而节省大量内存。</li></ul></li><li>保护位（<strong>protection bit</strong>）：表明页是否可以读取、写入或执行。<ul><li>以不允许的方式来访问页，会<code>trap</code>。</li></ul></li><li>存在位（<strong>present bit</strong>）：表明该页是在内存中还是在磁盘上（是否被换出）</li><li>脏位（<strong>dirty bit</strong>）：表明页面被带入内存后是否被修改过</li><li>参考位（<strong>reference bit</strong>）：表明用于追踪页是否被访问，也用于确定哪些页很受欢迎。<ul><li>在<strong>page replacement</strong>时十分重要。</li></ul></li></ul><p>X86架构的示例PTE如图18.5所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-18/x86-pte.jpg" alt=""></p><p>主要包含：</p><ul><li>存在位P</li><li>是否允许读写R/W</li><li>确定用户是否可以访问该页面的用户/超级用户位U/S</li><li>确定硬件缓存如何为这些页面工作PWT、PCD、PAT、G</li><li>访问位A</li><li>脏位D</li><li>页帧号PFN</li></ul><h3 id="18-3-分页：也很慢"><a href="#18-3-分页：也很慢" class="headerlink" title="18.3 分页：也很慢"></a>18.3 分页：也很慢</h3><p>分页模式中进行地址转换，硬件必须知道正在运行的进程的页表的位置，现在假设一个页表基址寄存器（page-table base register）包含了页表的起始位置的物理地址，为了找到想要的PTE位置，硬件将执行以下功能：</p><pre><code class="hljs ini"><span class="hljs-attr">VPN_MASK</span> = <span class="hljs-number">0</span>x30 //<span class="hljs-number">110000</span><span class="hljs-attr">SHIFT</span> = <span class="hljs-number">4</span>  //偏移量的位数<span class="hljs-attr">VPN</span> = (VirtualAddress &amp; VPN_MASK) &gt;&gt; SHIFT<span class="hljs-attr">PTEAddr</span> = PageTableBaseRegister + (VPN * sizeof(PTE))</code></pre><p>主要是：</p><ul><li>将VPN向右移动以形成正确的整数虚拟页码。<ul><li>例如21（010101）掩码将此值转换为010000，移位将它变为01，或虚拟页1</li></ul></li><li>使用此值作为页表基址寄存器指向的PTE数组的索引</li></ul><p>一旦知道了PTE的物理地址，硬件就可以从内存中获取PTE，提取PFN，将其与虚拟地址的偏移量连接起来，形成所需的物理地址。</p><p>其流程如图18.6所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-18/page-access-memory.jpg" alt=""></p><p>最后，硬件可以从内存中获取所需的数据放入寄存器中。</p><h2 id="Chapter-19-快速地址转换（TLB）"><a href="#Chapter-19-快速地址转换（TLB）" class="headerlink" title="Chapter 19 快速地址转换（TLB）"></a>Chapter 19 快速地址转换（TLB）</h2><p>使用分页作为核心机制来实现虚拟内存，可能会带来较高的性能开销，因为页表存储在物理内存，需要额外多处一次内存访问。</p><blockquote><p>Q：如何加速地址转换，尽量避免额外的内存访问？需要什么样的硬件支持？操作系统如何支持？</p></blockquote><p>地址转换旁路缓冲寄存器（translation-lookaside buffer TLB）：它是频繁发生的虚拟到物理地址转换的硬件cache。对于每次的内存访问，硬件先检查TLB，看其中是否有期望的转换映射。如果有，就完成转换，不用访问页表。</p><h3 id="19-1-TLB的基本算法"><a href="#19-1-TLB的基本算法" class="headerlink" title="19.1 TLB的基本算法"></a>19.1 TLB的基本算法</h3><p>TLB的基本算法如图所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-19/TLB-algo.jpg" alt=""></p><p>主要步骤如下：</p><ol><li>首先容虚拟地址中提取页号（VPN）</li><li>然后检查TLB中是否有该VPN的转换映射</li><li>如果TLB命中，这意味着TLB有该页的转换映射。那么去除对应的页帧号（PFN），与原来的虚拟地址中的偏移量组合形成期望的物理地址，并访问内存。</li><li>如果CPU没有在TLB中找到转换映射，那么访问页表来寻找转换映射，并用该转换映射更新TLB。</li></ol><h3 id="19-2-示例：访问数组"><a href="#19-2-示例：访问数组" class="headerlink" title="19.2 示例：访问数组"></a>19.2 示例：访问数组</h3><p>为了弄清TLB的操作，我们来看一个简单的虚拟地址追踪。本案例中，假设有一个由10个4字节整型组成的数组，起始虚地址是100，进一步假设，有一个8位的小虚地址空间，页大小为16B，那么我们可以将虚地址划分为4位的VPN（那么总共有16个虚拟内存页）和4位的偏移量（每个页中有16个字节）</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-19/example.jpg" alt=""></p><p>图19.2展示了数组的布局。</p><p>那么考虑一个简单的程序：访问数组的每个元素</p><pre><code class="hljs C"><span class="hljs-keyword">int</span> sum = <span class="hljs-number">0</span>;<span class="hljs-keyword">for</span>(<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++)&#123;    sum += a[i];&#125;</code></pre><p>那么程序的执行步骤如下：</p><ul><li>首先访问<code>a[0]</code>，CPU会看到虚拟地址100，硬件从中提取VPN = 06，然后检查TLB，此时TLB未命中</li><li>访问<code>a[1]</code>，TLB命中，因为和第一个元素位于同一页</li><li><code>a[2]</code>命中，同理</li><li><code>a[3]</code>未命中，和上述同理</li><li>……</li></ul><p>通过TLB的表现来看，命中率为70%。得益于空间局部性。同时，页大小也会影响本例的结果。</p><p>如果这次循环不久，再次访问，TLB的命中率会大大提高，这是因为时间局部性，所以TLB的命中率会提高。</p><h3 id="19-3-谁来处理TLB未命中"><a href="#19-3-谁来处理TLB未命中" class="headerlink" title="19.3 谁来处理TLB未命中"></a>19.3 谁来处理TLB未命中</h3><p>两个答案：硬件或OS</p><p>以前硬件的复杂指令集（CISC）全权处理TLB未命中，为了做到这一点，硬件必须知道页表在内存中的确切位置（通过页表基址寄存器）以及页表的确切格式，发生未命中时，硬件会遍历页表，找到正确的entry，取出想要的地址映射。用它更新TLB。</p><p>RISC有所谓的软件管理TLB，发生TLB未命中时，硬件会抛出一个异常，这时会暂停当前的指令流，将特权级提升至内核模式，跳转至陷阱处理程序（trap handler），这段代码用于处理TLB未命中的情况。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-19/tlb-miss-os-handle.jpg" alt=""></p><ul><li>先查找页表中的转换映射</li><li>然后用特权指令更新TLB，并从陷阱返回</li><li>此时硬件会重试该指令</li></ul><p>几个细节：</p><ul><li>从陷阱返回指令于不用于系统调用从陷阱返回<ul><li>系统调用返回是，会继续执行此调用之后的语句</li><li>TLB未命中时，陷阱返回后，硬件必须从导致陷阱的指令继续执行。那么此次会命中TLB。</li></ul></li><li>运行TLB未命中的处理代码时，OS需要避免引起TLB未命中的无限递归<ul><li>可以将TLB未命中的陷阱处理程序直接放入物理内存中。</li><li>或者在TLB中保留一些项，记录永久有效的地址转换，并将其中一些永久地址转换槽块留给处理代码本身。</li></ul></li></ul><h3 id="19-4-TLB的内容"><a href="#19-4-TLB的内容" class="headerlink" title="19.4 TLB的内容"></a>19.4 TLB的内容</h3><p>典型的TLB有32项、64项或128项，并且是全相联的（<strong>fully associative</strong>）。基本上这意味着一条地址映射可能存在TLB中的任意位置。一条TLB的entry内容可能如下：</p><pre><code class="hljs gherkin">VPN |<span class="hljs-string"> FPN </span>|<span class="hljs-string"> 其它位</span></code></pre><p>注意：TLB的有效位 != 页表的有效位</p><ul><li>页表中，如果PTE被标为无效，意味着该页不能被进程申请使用。</li><li>TLB有效位只是指出TLB的entry不是有效的地址映射。</li></ul><h3 id="19-5-上下文切换时对TLB的处理"><a href="#19-5-上下文切换时对TLB的处理" class="headerlink" title="19.5 上下文切换时对TLB的处理"></a>19.5 上下文切换时对TLB的处理</h3><p>TLB中包含的地址映射只是对当前线程有效，因此发生进程切换时，OS或硬件需要确保即将运行的进程不要错误读取了之前进程的地址映射。</p><blockquote><p>Q：进程切换时如何管理TLB的内容？</p></blockquote><p>可能的解决方案：上下文切换时，简单地flush TLB。但是此方案有一定开销。</p><p>因此一些系统中增加了硬件支持，实现跨上下文切换的TLB共享。比如有的系统在TLB中添加一个地址空间标识符（Address Space Identifier ASID），可以将ASID看成是进程标识符（PID）。该位标识该地址映射是哪个进程所持有的。如图19.3所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-19/context-switch-with-tlb-content.jpg" alt=""></p><h3 id="19-6-TLB替换策略"><a href="#19-6-TLB替换策略" class="headerlink" title="19.6 TLB替换策略"></a>19.6 TLB替换策略</h3><blockquote><p>Q：如何设计TLB替换策略？</p></blockquote><ul><li>LRU</li><li>random</li></ul><h3 id="19-7-实际系统的TLB表项"><a href="#19-7-实际系统的TLB表项" class="headerlink" title="19.7 实际系统的TLB表项"></a>19.7 实际系统的TLB表项</h3><p>MIPS R4000采用软件管理TLB，图19.4展示了简化的MIPS TLB表项：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-19/tlb.jpg" alt=""></p><p>支持32位的地址空间，页大小为4KB，因此有20位的VPN和12位偏移量。</p><p>MIPS TLB还有一些标识位：</p><ul><li>全局位（Global G）：指示这个页是不是所有进程共享的，如果是，则忽略ASID</li><li>一致性位（Coherence C）：决定硬件如何缓存该页</li><li>脏位（Dirty D）：表示该页是否被写入新数据</li><li>有效位（valid）：告诉硬件该项的地址映射是否有效</li></ul><h2 id="Chapter-20-分页：较小的表"><a href="#Chapter-20-分页：较小的表" class="headerlink" title="Chapter 20 分页：较小的表"></a>Chapter 20 分页：较小的表</h2><p>上述方案会有一个问题：页表太大，消耗太多内存，那么问题来了：</p><blockquote><p>Q： 如何让页表更小？</p></blockquote><h3 id="20-1-简单的解决方案：更大的页"><a href="#20-1-简单的解决方案：更大的页" class="headerlink" title="20.1 简单的解决方案：更大的页"></a>20.1 简单的解决方案：更大的页</h3><p>如果32位的地址空间为例：如每页大小为16KB，则会有18位的VPN，14位的偏移量。假设页表项大小为4字节，那么线性页表中有218项，则每个页表的总大小为1MB。</p><p>但是大内存页面会导致每页内部的浪费（内部碎片），因此大部分OS通常使用较小的页：4KB。</p><h3 id="20-2-混合方法：分页和分段"><a href="#20-2-混合方法：分页和分段" class="headerlink" title="20.2 混合方法：分页和分段"></a>20.2 混合方法：分页和分段</h3><p>将分页和分段结合，减少页表的内存开销。</p><p>分段中，有一个基址寄存器，告诉我们每个段在物理内存中的位置，还有一个界限寄存器，告诉我们段的大小。</p><p>但是分页分段结合的模式下，使用基址寄存器保存该段的页表的物理地址，界限寄存器用于指示一共有多少页。</p><p>虚拟地址示例如下：</p><pre><code class="hljs coq">Seg  | <span class="hljs-type">VPN</span> | <span class="hljs-type">Offset</span>x bit | <span class="hljs-type">y</span> bit | <span class="hljs-type">z</span> bit</code></pre><p>TLB未命中时，硬件使用分段位（SN）来确定要用哪个基址和界限对，然后硬件将其中的物理地址与VPN结合起来，形成页表项的地址。</p><p>使用分段会存在问题：</p><ul><li>例如：如果有一个大而稀疏的堆，仍然可能导致大量的页表浪费。</li></ul><h3 id="20-3-多级页表"><a href="#20-3-多级页表" class="headerlink" title="20.3 多级页表"></a>20.3 多级页表</h3><p>如何去掉页表中无效区域？而不是将它们全部保存在内存中。</p><p>使用多级页表，将线性页表变成了类似树的东西。</p><p>在一个简单的二级页表中，页目录中每个页目录项（Page Directory Entries PDE）包括：有效位和页帧号（page frame number PFN），类似于PTE。</p><p>多级页表的基本思想很简单，将页表分成页大小的单元，如果页目录项无效，就不分配该页的页表。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-20/20.3.jpg" alt=""></p><p>问题：</p><ul><li>多级页表是有成本的，在TLB未命中时，需要从内存加载两次，才能从页表中获得正确的地址转换信息。（一次用于页目录，另一次用于PTE本身）</li><li>复杂性</li></ul><h4 id="案例"><a href="#案例" class="headerlink" title="案例"></a>案例</h4><p>假设有一个大小为16KB的地址空间，每个页大小为64byte，因此需要一个14位的虚拟地址，其中VPN8位，偏移量6位。</p><p>那么即使只有一小部分地址空间正在使用，线性页表也会有$2 ^ 8$个项。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-20/20.4.jpg" alt=""></p><p>要为这个地址空间构建一个二级页表，我们从完整的线性页表开始，将他分解为页大小的单元。</p><p>假设PTE大小为4 bytes，那么页表大小为1KB（256 * 4 bytes）,页表可以被分成16个 64byte大小的页，每个页有16个PTE。</p><p>那么如何获取VPN并且用它索引到页目录中，然后索引到页表的页中。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-20/address.jpg" alt=""></p><p>首先通过4位VPN来索引Page Directory，那么可以通过简单的计算得到PDE的地址。</p><script type="math/tex; mode=display">PDEAddr = PageDirBase + (PDIndex * sizeof(PDE))</script><p>如果获取的页目录项标记为无效，则会引发异常，有效则通过页目录项指向的页表中获得PTE。</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-20/page-table-index.jpg" alt=""></p><p>通过page table index可以得到PTE地址。</p><script type="math/tex; mode=display">PTEAddr = (PDE.PFN) << SHIFT + (PTIndex * sizeof(PTE))</script><p>最后根据PTE中的映射地址加上偏移量得到物理地址。</p><h3 id="20-4-反向页表"><a href="#20-4-反向页表" class="headerlink" title="20.4 反向页表"></a>20.4 反向页表</h3><p>只保留一个页表，其中的项代表系统的每个物理页，而不是每个进程一个页表，页表项告诉我们哪个进程正在使用该页，以及该进程的哪个虚拟页映射到此物理页。</p><p>如果进行线性扫描，性能开销较高，因此通常使用散列表来加速查找。</p><h3 id="20-5-将页表交换到磁盘"><a href="#20-5-将页表交换到磁盘" class="headerlink" title="20.5 将页表交换到磁盘"></a>20.5 将页表交换到磁盘</h3><p>一些系统在内存压力较大时，允许将页表swap到磁盘中。</p><h2 id="Chapter-21-超越物理内存：机制"><a href="#Chapter-21-超越物理内存：机制" class="headerlink" title="Chapter 21 超越物理内存：机制"></a>Chapter 21 超越物理内存：机制</h2><blockquote><p>Q：如何利用大而慢的设备，透明地提供巨大虚拟地址空间的假象？</p></blockquote><p>为了支持更大的地址空间，就需要更大的内存存储页表，一般可以通过swap，将暂时没用的页表swap到磁盘中。</p><h3 id="21-1-交换空间"><a href="#21-1-交换空间" class="headerlink" title="21.1 交换空间"></a>21.1 交换空间</h3><p>在OS中，一般将硬盘中存储换入换出的空间称为交换空间。</p><h3 id="21-2-存在位"><a href="#21-2-存在位" class="headerlink" title="21.2 存在位"></a>21.2 存在位</h3><p>如果页表项的存在位为1，则表示该页存在于物理内存中，如果为0，则在硬盘上，访问为0的页会有page fault。</p><h3 id="21-3-page-fault"><a href="#21-3-page-fault" class="headerlink" title="21.3 page fault"></a>21.3 page fault</h3><p>如果一个页不存在，它已被交换到硬盘，那么在处理该错误的时候，OS需要将该页交换到内存中。</p><blockquote><p>Q：OS如何知道需要的页在哪？</p></blockquote><p>可以用PTE存储硬盘地址。</p><p>当硬盘I/O完成时，操作系统会更新页表，将此页标记为存在，更新PTE的PFN字段以记录新获取页的内存位置，并重试指令，将PTE更新到TLB中。</p><h3 id="21-4-内存满了怎么办"><a href="#21-4-内存满了怎么办" class="headerlink" title="21.4 内存满了怎么办"></a>21.4 内存满了怎么办</h3><p>OS希望先交换出一个或多个页，以便为操作系统即将交换新页留出空间。这被称为页交换策略（page-replacement policy）。</p><h3 id="21-5-页错误处理流程"><a href="#21-5-页错误处理流程" class="headerlink" title="21.5 页错误处理流程"></a>21.5 页错误处理流程</h3><ul><li>当TLB未命中会有三种情景：<ol><li>present and valid：TLB未命中处理程序从PTE中获取PFN，然后重试指令更新TLB，然后继续前面的流程</li><li>页不存在于内存中，抛出page fault</li><li>访问的是无效页的话，抛出segment fault</li></ol></li></ul><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-21/21.2.jpg" alt=""></p><p>那么page fault处理程序的流程是什么样呢？</p><ul><li>OS必须为换出的页找到一个物理帧</li><li>如果没有这样的物理帧，等待置换算法从内存中换出一些页</li><li>处理程序通过IO请求从swap space中读取页</li><li>OS更新页表并重试指令来更新TLB</li></ul><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/os/ostep/chapter-21/21.3.jpg" alt=""></p><h3 id="21-6-交换的时机"><a href="#21-6-交换的时机" class="headerlink" title="21.6 交换的时机"></a>21.6 交换的时机</h3><p>如果是等OS发现内存满了之后，才执行置换算法，有点不切实际。OS是更主动地预留了一小部分空闲内存。</p><p>为了保证有少量地空闲内存，大部分OS会设置高水位线（HW）和低水位线（LW），来帮助决定何时从内存中清除页。</p><p>如果OS发现有少于LW个页可用时，后台负责释放内存的线程会开始运行，直到有HW个可用物理页。这个后台线程有时被称为swap daemon或者page daemon。</p><h2 id="Chapter-22-超越物理内存：策略"><a href="#Chapter-22-超越物理内存：策略" class="headerlink" title="Chapter 22 超越物理内存：策略"></a>Chapter 22 超越物理内存：策略</h2><blockquote><p>Q： 如何决定置换哪个页？</p></blockquote><h3 id="22-1-缓存管理"><a href="#22-1-缓存管理" class="headerlink" title="22.1 缓存管理"></a>22.1 缓存管理</h3><p>选择置换策略时，我们的目标是让cache miss最少。程序的平均内存访问时间（Average Memory Access Time， AMAT），这是衡量硬件缓存的指标。可按照如下公式计算AMAT：</p><script type="math/tex; mode=display">AMAT = (P_{Hit} * T_M) + (P_{miss} * P_D)</script><p>其中$T<em>M$表示访问内存的成本，$T_D$表示访问磁盘的成本，$T</em>{Hit}$表示缓存命中率，$T_{miss}$表示缓存未命中率。</p><h3 id="22-2-最优替换策略"><a href="#22-2-最优替换策略" class="headerlink" title="22.2 最优替换策略"></a>22.2 最优替换策略</h3><p><strong>optional</strong>：是Belady发明的，最优替换策略能达到总体未命中数量最少。</p><p>基本策略：替换内存中在最远将来才会被访问的页。</p><p>这个策略不存在，因为无法预知用户使用哪个页面。</p><p>Belady异常：缓存大小变大时，命中率反而下降。</p><h3 id="22-3-简单策略：FIFO"><a href="#22-3-简单策略：FIFO" class="headerlink" title="22.3 简单策略：FIFO"></a>22.3 简单策略：FIFO</h3><p>一些系统使用FIFO替换策略，发生替换时，替换最先进入的页。</p><h3 id="22-4-随机"><a href="#22-4-随机" class="headerlink" title="22.4 随机"></a>22.4 随机</h3><p>随机置换页面。</p><h3 id="22-5-LRU"><a href="#22-5-LRU" class="headerlink" title="22.5 LRU"></a>22.5 LRU</h3><p>最近最久未使用</p><h3 id="22-9-考虑脏页"><a href="#22-9-考虑脏页" class="headerlink" title="22.9 考虑脏页"></a>22.9 考虑脏页</h3><p>如果内存中的页已经被修改，那么替换该页会引起写磁盘操作，性能开销大。因此可以优先选择未修改的页面。</p><h3 id="22-11-抖动"><a href="#22-11-抖动" class="headerlink" title="22.11 抖动"></a>22.11 抖动</h3><p>频繁换入换出。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;进程虚拟化&quot;&gt;&lt;a href=&quot;#进程虚拟化&quot; class=&quot;headerlink&quot; title=&quot;进程虚拟化&quot;&gt;&lt;/a&gt;进程虚拟化&lt;/h1&gt;&lt;h2 id=&quot;Chapter-4-进程&quot;&gt;&lt;a href=&quot;#Chapter-4-进程&quot; class=&quot;headerli</summary>
      
    
    
    
    <category term="计算机基础" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/"/>
    
    <category term="操作系统" scheme="https://l1n.wang/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%9F%BA%E7%A1%80/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
    
    <category term="操作系统" scheme="https://l1n.wang/tags/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
  </entry>
  
  <entry>
    <title>ThreadLocal解析</title>
    <link href="https://l1n.wang/2020/12/java-threadlocal/"/>
    <id>https://l1n.wang/2020/12/java-threadlocal/</id>
    <published>2020-12-03T03:17:38.000Z</published>
    <updated>2020-12-03T03:22:56.875Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><blockquote><p>This class provides thread-local variables. These variables differ from their normal counterparts in that each thread that accesses one (via its <code>get</code> or <code>set</code> method) has its own, independently initialized copy of the variable. <code>ThreadLocal</code> instances are typically private static fields in classes that wish to associate state with a thread (e.g., a user ID or Transaction ID).</p></blockquote><p>简而言之，<code>ThreadLocal</code>可以创建一个只能被当前线程访问或修改的变量。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><blockquote><p>Q：如何实现线程隔离？</p></blockquote><p>使用<code>Thread</code>对象中的<code>ThreadLocalMap</code>进行数据存储。也就是<code>ThreadLocal</code>将数据存储到当前的线程对象中，通过<code>Thread.currentThread()</code>来获取线程，再通过<code>getMap(t)</code>来获取<code>ThreadLocalMap</code>。具体内容通过阅读源码来逐步分析。</p><h2 id="get"><a href="#get" class="headerlink" title="get"></a>get</h2><p>返回当前线程存储的<code>ThreadLocal</code>值，如果不存在，会进行初始化并返回。通过<code>map.getEntry(this)</code>。</p><pre><code class="hljs java"><span class="hljs-comment">/**</span><span class="hljs-comment"> * Returns the value in the current thread&#x27;s copy of this</span><span class="hljs-comment"> * thread-local variable.  If the variable has no value for the</span><span class="hljs-comment"> * current thread, it is first initialized to the value returned</span><span class="hljs-comment"> * by an invocation of the &#123;<span class="hljs-doctag">@link</span> #initialValue&#125; method.</span><span class="hljs-comment"> *</span><span class="hljs-comment"> * <span class="hljs-doctag">@return</span> the current thread&#x27;s value of this thread-local</span><span class="hljs-comment"> */</span><span class="hljs-function"><span class="hljs-keyword">public</span> T <span class="hljs-title">get</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-comment">//获取当前对象</span>    Thread t = Thread.currentThread();    <span class="hljs-comment">//通过getMap获取ThreadLocalMap</span>    ThreadLocalMap map = getMap(t);    <span class="hljs-keyword">if</span> (map != <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-comment">//获取entry</span>        ThreadLocalMap.Entry e = map.getEntry(<span class="hljs-keyword">this</span>);        <span class="hljs-keyword">if</span> (e != <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span>            T result = (T)e.value;            <span class="hljs-keyword">return</span> result;        &#125;    &#125;    <span class="hljs-comment">//不存在则进行初始化</span>    <span class="hljs-keyword">return</span> setInitialValue();&#125;</code></pre><h2 id="getMap"><a href="#getMap" class="headerlink" title="getMap"></a>getMap</h2><p>返回指定线程的<code>ThreadLocalMap</code>。</p><pre><code class="hljs java"><span class="hljs-function">ThreadLocalMap <span class="hljs-title">getMap</span><span class="hljs-params">(Thread t)</span> </span>&#123;        <span class="hljs-keyword">return</span> t.threadLocals;&#125;</code></pre><p><code>Thread</code>中关于<code>ThreadLocalMap</code>的部分：</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Thread</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Runnable</span> </span>&#123;    <span class="hljs-comment">/* ThreadLocal values pertaining to this thread. This map is maintained</span><span class="hljs-comment">     * by the ThreadLocal class. */</span>    ThreadLocal.ThreadLocalMap threadLocals = <span class="hljs-keyword">null</span>;&#125;</code></pre><h2 id="setInitialValue"><a href="#setInitialValue" class="headerlink" title="setInitialValue"></a>setInitialValue</h2><p>get方法获取不到值时，通过该方法设置初始值。</p><pre><code class="hljs java"><span class="hljs-comment">/**</span><span class="hljs-comment"> * Variant of set() to establish initialValue. Used instead</span><span class="hljs-comment"> * of set() in case user has overridden the set() method.</span><span class="hljs-comment"> *</span><span class="hljs-comment"> * <span class="hljs-doctag">@return</span> the initial value</span><span class="hljs-comment"> */</span><span class="hljs-function"><span class="hljs-keyword">private</span> T <span class="hljs-title">setInitialValue</span><span class="hljs-params">()</span> </span>&#123;    T value = initialValue();    Thread t = Thread.currentThread();    ThreadLocalMap map = getMap(t);    <span class="hljs-keyword">if</span> (map != <span class="hljs-keyword">null</span>)        map.set(<span class="hljs-keyword">this</span>, value);    <span class="hljs-keyword">else</span>        createMap(t, value);    <span class="hljs-keyword">return</span> value;&#125;</code></pre><h2 id="createMap"><a href="#createMap" class="headerlink" title="createMap"></a>createMap</h2><p><code>ThreadLocalMap</code>不存在时，通过该方法创建，这里有一个疑惑：</p><blockquote><p> Q：为什么在<code>get</code>和<code>setInitialValue</code>进行两次为空检查才进行<code>createMap</code>？</p></blockquote><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">createMap</span><span class="hljs-params">(Thread t, T firstValue)</span> </span>&#123;        t.threadLocals = <span class="hljs-keyword">new</span> ThreadLocalMap(<span class="hljs-keyword">this</span>, firstValue);&#125;</code></pre><h2 id="set"><a href="#set" class="headerlink" title="set"></a>set</h2><p>通过调用<code>map.set(this,value)</code>实现。map中的key是当前<code>ThreadLocal</code>对象。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">set</span><span class="hljs-params">(T value)</span> </span>&#123;        Thread t = Thread.currentThread();        ThreadLocalMap map = getMap(t);        <span class="hljs-keyword">if</span> (map != <span class="hljs-keyword">null</span>)            map.set(<span class="hljs-keyword">this</span>, value);        <span class="hljs-keyword">else</span>            createMap(t, value);    &#125;</code></pre><h2 id="ThreadLocalMap"><a href="#ThreadLocalMap" class="headerlink" title="ThreadLocalMap"></a>ThreadLocalMap</h2><p>内部类和成员变量代码如下：</p><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ThreadLocalMap</span> </span>&#123;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * The entries in this hash map extend WeakReference, using</span><span class="hljs-comment">         * its main ref field as the key (which is always a</span><span class="hljs-comment">         * ThreadLocal object).  Note that null keys (i.e. entry.get()</span><span class="hljs-comment">         * == null) mean that the key is no longer referenced, so the</span><span class="hljs-comment">         * entry can be expunged from table.  Such entries are referred to</span><span class="hljs-comment">         * as &quot;stale entries&quot; in the code that follows.</span><span class="hljs-comment">         */</span>        <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Entry</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">WeakReference</span>&lt;<span class="hljs-title">ThreadLocal</span>&lt;?&gt;&gt; </span>&#123;            <span class="hljs-comment">/** The value associated with this ThreadLocal. */</span>            Object value;            Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;                <span class="hljs-keyword">super</span>(k);                value = v;            &#125;        &#125;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * The initial capacity -- MUST be a power of two.</span><span class="hljs-comment">         */</span>        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> INITIAL_CAPACITY = <span class="hljs-number">16</span>;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * The table, resized as necessary.</span><span class="hljs-comment">         * table.length MUST always be a power of two.</span><span class="hljs-comment">         */</span>        <span class="hljs-keyword">private</span> Entry[] table;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * The number of entries in the table.</span><span class="hljs-comment">         */</span>        <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> size = <span class="hljs-number">0</span>;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * The next size value at which to resize.</span><span class="hljs-comment">         */</span>        <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> threshold; <span class="hljs-comment">// Default to 0</span>&#125;</code></pre><h3 id="set-1"><a href="#set-1" class="headerlink" title="set"></a>set</h3><p>流程：</p><ul><li>计算下标，得到下标i</li><li>进行遍历，如果i对应的key和传入相等，直接返回。如果对象已被回收，调用<code>replaceStaleEntry()</code>并返回。</li><li>如果i对应的不相等，则从i开始从table数组完后找。</li><li>能找到则返回，找不到新建entry进行存储。</li><li>检查是否需要<code>rehash()</code></li></ul><pre><code class="hljs java"><span class="hljs-comment">/**</span><span class="hljs-comment">         * Set the value associated with key.</span><span class="hljs-comment">         *</span><span class="hljs-comment">         * <span class="hljs-doctag">@param</span> key the thread local object</span><span class="hljs-comment">         * <span class="hljs-doctag">@param</span> value the value to be set</span><span class="hljs-comment">         */</span>        <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">set</span><span class="hljs-params">(ThreadLocal&lt;?&gt; key, Object value)</span> </span>&#123;            <span class="hljs-comment">// We don&#x27;t use a fast path as with get() because it is at</span>            <span class="hljs-comment">// least as common to use set() to create new entries as</span>            <span class="hljs-comment">// it is to replace existing ones, in which case, a fast</span>            <span class="hljs-comment">// path would fail more often than not.</span>            Entry[] tab = table;            <span class="hljs-keyword">int</span> len = tab.length;            <span class="hljs-comment">//获取hash后的下标</span>            <span class="hljs-keyword">int</span> i = key.threadLocalHashCode &amp; (len-<span class="hljs-number">1</span>);<span class="hljs-comment">//遍历</span>            <span class="hljs-keyword">for</span> (Entry e = tab[i];                 e != <span class="hljs-keyword">null</span>;                 e = tab[i = nextIndex(i, len)]) &#123;                ThreadLocal&lt;?&gt; k = e.get();<span class="hljs-comment">//如果对应下标的ThreadLocal与当前的相等，直接更新</span>                <span class="hljs-keyword">if</span> (k == key) &#123;                    e.value = value;                    <span class="hljs-keyword">return</span>;                &#125;<span class="hljs-comment">//如果ThreadLocal对象已被回收，调用replaceStaleEntry</span>                <span class="hljs-keyword">if</span> (k == <span class="hljs-keyword">null</span>) &#123;                    replaceStaleEntry(key, value, i);                    <span class="hljs-keyword">return</span>;                &#125;            &#125;<span class="hljs-comment">//如果遍历一圈还是找不到，新建entry进行存储</span>            tab[i] = <span class="hljs-keyword">new</span> Entry(key, value);            <span class="hljs-keyword">int</span> sz = ++size;            <span class="hljs-comment">//检查是否需要rehash</span>            <span class="hljs-keyword">if</span> (!cleanSomeSlots(i, sz) &amp;&amp; sz &gt;= threshold)                rehash();        &#125;</code></pre><p>下标计算方式：</p><p>通过<code>AtomicInteger + 0x61c88647 &amp; (len-1)</code>来获取下标。</p><pre><code class="hljs java"><span class="hljs-keyword">int</span> i = key.threadLocalHashCode &amp; (len-<span class="hljs-number">1</span>);<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> threadLocalHashCode = nextHashCode();<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> AtomicInteger nextHashCode = <span class="hljs-keyword">new</span> AtomicInteger();<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> HASH_INCREMENT = <span class="hljs-number">0x61c88647</span>;<span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">nextHashCode</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">return</span> nextHashCode.getAndAdd(HASH_INCREMENT);&#125;</code></pre><h3 id="getEntry"><a href="#getEntry" class="headerlink" title="getEntry"></a>getEntry</h3><p>流程：</p><ul><li>计算下标，得到下标i</li><li>判断对应下标的entry是否存在，key是否相等，符合要求则直接返回。</li><li>不符合则调用<code>getEntryAfterMiss()</code></li></ul><pre><code class="hljs java"><span class="hljs-comment">/**</span><span class="hljs-comment"> * Get the entry associated with key.  This method</span><span class="hljs-comment"> * itself handles only the fast path: a direct hit of existing</span><span class="hljs-comment"> * key. It otherwise relays to getEntryAfterMiss.  This is</span><span class="hljs-comment"> * designed to maximize performance for direct hits, in part</span><span class="hljs-comment"> * by making this method readily inlinable.</span><span class="hljs-comment"> *</span><span class="hljs-comment"> * <span class="hljs-doctag">@param</span>  key the thread local object</span><span class="hljs-comment"> * <span class="hljs-doctag">@return</span> the entry associated with key, or null if no such</span><span class="hljs-comment"> */</span><span class="hljs-function"><span class="hljs-keyword">private</span> Entry <span class="hljs-title">getEntry</span><span class="hljs-params">(ThreadLocal&lt;?&gt; key)</span> </span>&#123;    <span class="hljs-comment">//计算下标</span>    <span class="hljs-keyword">int</span> i = key.threadLocalHashCode &amp; (table.length - <span class="hljs-number">1</span>);    Entry e = table[i];    <span class="hljs-keyword">if</span> (e != <span class="hljs-keyword">null</span> &amp;&amp; e.get() == key)        <span class="hljs-keyword">return</span> e;    <span class="hljs-keyword">else</span>        <span class="hljs-comment">//entry不存在或key不相等</span>        <span class="hljs-keyword">return</span> getEntryAfterMiss(key, i, e);&#125;</code></pre><h4 id="getEntryAfterMiss"><a href="#getEntryAfterMiss" class="headerlink" title="getEntryAfterMiss"></a>getEntryAfterMiss</h4><p>也就是对table数组进行遍历查找，找一圈还没有则返回<code>null</code>。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> Entry <span class="hljs-title">getEntryAfterMiss</span><span class="hljs-params">(ThreadLocal&lt;?&gt; key, <span class="hljs-keyword">int</span> i, Entry e)</span> </span>&#123;    Entry[] tab = table;    <span class="hljs-keyword">int</span> len = tab.length;    <span class="hljs-keyword">while</span> (e != <span class="hljs-keyword">null</span>) &#123;        ThreadLocal&lt;?&gt; k = e.get();        <span class="hljs-keyword">if</span> (k == key)            <span class="hljs-keyword">return</span> e;        <span class="hljs-keyword">if</span> (k == <span class="hljs-keyword">null</span>)            expungeStaleEntry(i);        <span class="hljs-keyword">else</span>            i = nextIndex(i, len);        e = tab[i];    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;&#125;</code></pre><h3 id="remove"><a href="#remove" class="headerlink" title="remove"></a>remove</h3><p>流程：</p><ul><li>计算下标</li><li>判断key是否相等，符合要求则调用<code>e.clear()</code>来清除key，并调用<code>expungeStaleEntry</code>清除value。</li></ul><pre><code class="hljs java"><span class="hljs-comment">/**</span><span class="hljs-comment">         * Remove the entry for key.</span><span class="hljs-comment">         */</span>        <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">remove</span><span class="hljs-params">(ThreadLocal&lt;?&gt; key)</span> </span>&#123;            Entry[] tab = table;            <span class="hljs-keyword">int</span> len = tab.length;            <span class="hljs-comment">//计算下标</span>            <span class="hljs-keyword">int</span> i = key.threadLocalHashCode &amp; (len-<span class="hljs-number">1</span>);            <span class="hljs-keyword">for</span> (Entry e = tab[i];                 e != <span class="hljs-keyword">null</span>;                 e = tab[i = nextIndex(i, len)]) &#123;                <span class="hljs-comment">//如果key相等</span>                <span class="hljs-keyword">if</span> (e.get() == key) &#123;                    e.clear();                    expungeStaleEntry(i);                    <span class="hljs-keyword">return</span>;                &#125;            &#125;        &#125;</code></pre><h1 id="内存泄露问题"><a href="#内存泄露问题" class="headerlink" title="内存泄露问题"></a>内存泄露问题</h1><h2 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h2><p>从上文可以看到，<strong>Entry</strong>继承自<strong>WeakReference</strong>，弱引用指向的对象会在下一次GC时被回收。</p><pre><code class="hljs java">Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;                <span class="hljs-keyword">super</span>(k);                value = v;&#125;</code></pre><p>​        在初始化时，将弱引用指向<code>ThreadLocal</code>实例。如果外部没有强引用指向ThreadLocal的话，那么Thread Local实例就没有一条引用链路可达，则会被回收。此时entry的key未null，但是有value，但是不能通过key找到value，这样就会存在<strong>内存泄漏问题</strong>。但是如果线程结束，以上内存都会被回收，也就不存在上述问题。</p><p>​        如果使用线程池去维护线程，线程池并不会主动销毁内部的线程，总会存在着强引用，那么还是会存在问题。</p><p>总结问题出现的原因：</p><ol><li>线程一直运行，不终止（线程池）</li><li>null-value：某个弱引用key被回收</li></ol><h2 id="如何解决"><a href="#如何解决" class="headerlink" title="如何解决"></a>如何解决</h2><p>上述的<code>getEntryAfterMiss()</code>如果判断到了<code>key == null</code>则会调用<code>expungeStaleEntry()</code>将value删除。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">expungeStaleEntry</span><span class="hljs-params">(<span class="hljs-keyword">int</span> staleSlot)</span> </span>&#123;    Entry[] tab = table;    <span class="hljs-keyword">int</span> len = tab.length;    <span class="hljs-comment">// expunge entry at staleSlot</span>    tab[staleSlot].value = <span class="hljs-keyword">null</span>;    tab[staleSlot] = <span class="hljs-keyword">null</span>;    size--;    <span class="hljs-comment">// Rehash until we encounter null</span>    Entry e;    <span class="hljs-keyword">int</span> i;    <span class="hljs-keyword">for</span> (i = nextIndex(staleSlot, len);         (e = tab[i]) != <span class="hljs-keyword">null</span>;         i = nextIndex(i, len)) &#123;        ThreadLocal&lt;?&gt; k = e.get();        <span class="hljs-keyword">if</span> (k == <span class="hljs-keyword">null</span>) &#123;            e.value = <span class="hljs-keyword">null</span>;            tab[i] = <span class="hljs-keyword">null</span>;            size--;        &#125; <span class="hljs-keyword">else</span> &#123;            <span class="hljs-keyword">int</span> h = k.threadLocalHashCode &amp; (len - <span class="hljs-number">1</span>);            <span class="hljs-keyword">if</span> (h != i) &#123;                tab[i] = <span class="hljs-keyword">null</span>;                <span class="hljs-comment">// Unlike Knuth 6.4 Algorithm R, we must scan until</span>                <span class="hljs-comment">// null because multiple entries could have been stale.</span>                <span class="hljs-keyword">while</span> (tab[h] != <span class="hljs-keyword">null</span>)                    h = nextIndex(h, len);                tab[h] = e;            &#125;        &#125;    &#125;    <span class="hljs-keyword">return</span> i;&#125;</code></pre><p>该方法在<code>set</code>、<code>get</code>、<code>remove</code>中都会调用。因此还有一个问题：如果创建了<code>ThreadLocal</code>但不调用以上方法，还是会存在问题。</p><p>所以最能解决办法的就是用完<code>ThreadLocal</code>之后显式执行<code>ThreadLocal.remove()</code>。</p><h2 id="为什么key不设置为强引用"><a href="#为什么key不设置为强引用" class="headerlink" title="为什么key不设置为强引用"></a>为什么key不设置为强引用</h2><p>既然弱引用会存在内存泄露问题，为什么不使用强引用呢？</p><p>如果将key设置为强引用，当<code>ThreadLocal</code>实例释放后，但是ThreadLocal强引用指向threadLocalMap，threadLocalMap.Entry又强引用指向ThreadLocal，这样会导致ThreadLocal无法回收。也就会出现更为严重的问题。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p>Java SE 8 Docs API</p><p><a href="https://www.jianshu.com/p/dde92ec37bd1">https://www.jianshu.com/p/dde92ec37bd1</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;blockquote&gt;
&lt;p&gt;This class provides thread-local variables. These variabl</summary>
      
    
    
    
    <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
    <category term="Java并发" scheme="https://l1n.wang/categories/Java/Java%E5%B9%B6%E5%8F%91/"/>
    
    
    <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
    <category term="并发" scheme="https://l1n.wang/tags/%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>几个Java并发工具类解析</title>
    <link href="https://l1n.wang/2020/11/juc-tools/"/>
    <id>https://l1n.wang/2020/11/juc-tools/</id>
    <published>2020-11-29T14:40:17.000Z</published>
    <updated>2020-11-29T14:41:12.698Z</updated>
    
    <content type="html"><![CDATA[<h1 id="CountDownLatch"><a href="#CountDownLatch" class="headerlink" title="CountDownLatch"></a>CountDownLatch</h1><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><blockquote><p>A synchronization aid that allows one or more threads to wait until a set of operations being performed in other threads completes.</p></blockquote><p>只有当N个线程执行完毕，并且进行<code>countDown</code>操作时，才允许<code>await</code>的线程继续执行。否则该线程挂起。</p><h2 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h2><p>参数<code>count</code>为计数值，传入<code>AQS</code>的实现类<code>Sync</code>设置成AQS的<code>state</code>。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">CountDownLatch</span><span class="hljs-params">(<span class="hljs-keyword">int</span> count)</span> </span>&#123;        <span class="hljs-keyword">if</span> (count &lt; <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException(<span class="hljs-string">&quot;count &lt; 0&quot;</span>);        <span class="hljs-keyword">this</span>.sync = <span class="hljs-keyword">new</span> Sync(count);&#125;</code></pre><h2 id="Sync"><a href="#Sync" class="headerlink" title="Sync"></a>Sync</h2><p>通过继承<code>AQS</code>从而完成同步的核心功能。</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Sync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractQueuedSynchronizer</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = <span class="hljs-number">4982264981922014374L</span>;    <span class="hljs-comment">//构造方法</span>        Sync(<span class="hljs-keyword">int</span> count) &#123;            setState(count);        &#125;        <span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">getCount</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> getState();        &#125;        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">int</span> <span class="hljs-title">tryAcquireShared</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-keyword">return</span> (getState() == <span class="hljs-number">0</span>) ? <span class="hljs-number">1</span> : -<span class="hljs-number">1</span>;        &#125;        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryReleaseShared</span><span class="hljs-params">(<span class="hljs-keyword">int</span> releases)</span> </span>&#123;            <span class="hljs-comment">// Decrement count; signal when transition to zero</span>            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-keyword">int</span> c = getState();                <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>)                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;                <span class="hljs-keyword">int</span> nextc = c-<span class="hljs-number">1</span>;                <span class="hljs-keyword">if</span> (compareAndSetState(c, nextc))                    <span class="hljs-keyword">return</span> nextc == <span class="hljs-number">0</span>;            &#125;        &#125;    &#125;</code></pre><h2 id="核心方法"><a href="#核心方法" class="headerlink" title="核心方法"></a>核心方法</h2><ul><li><strong>countDown</strong>：将count值减1</li><li><strong>await</strong>：调用<strong>await</strong>的线程会被挂起，直到<code>count</code>为0才继续执行，允许中断</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">await</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;        sync.acquireSharedInterruptibly(<span class="hljs-number">1</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">countDown</span><span class="hljs-params">()</span> </span>&#123;        sync.releaseShared(<span class="hljs-number">1</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">await</span><span class="hljs-params">(<span class="hljs-keyword">long</span> timeout, TimeUnit unit)</span></span><span class="hljs-function">        <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;        <span class="hljs-keyword">return</span> sync.tryAcquireSharedNanos(<span class="hljs-number">1</span>, unit.toNanos(timeout));&#125;</code></pre><h2 id="使用案例"><a href="#使用案例" class="headerlink" title="使用案例"></a>使用案例</h2><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Driver</span> </span>&#123; <span class="hljs-comment">// ...</span>   <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;     CountDownLatch startSignal = <span class="hljs-keyword">new</span> CountDownLatch(<span class="hljs-number">1</span>);     CountDownLatch doneSignal = <span class="hljs-keyword">new</span> CountDownLatch(N);     <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; N; ++i) <span class="hljs-comment">// create and start threads</span>       <span class="hljs-keyword">new</span> Thread(<span class="hljs-keyword">new</span> Worker(startSignal, doneSignal)).start();     doSomethingElse();            <span class="hljs-comment">// don&#x27;t let run yet</span>     startSignal.countDown();      <span class="hljs-comment">// let all threads proceed</span>     doSomethingElse();     doneSignal.await();           <span class="hljs-comment">// wait for all to finish</span>   &#125; &#125; <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Worker</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Runnable</span> </span>&#123;   <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> CountDownLatch startSignal;   <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> CountDownLatch doneSignal;   Worker(CountDownLatch startSignal, CountDownLatch doneSignal) &#123;      <span class="hljs-keyword">this</span>.startSignal = startSignal;      <span class="hljs-keyword">this</span>.doneSignal = doneSignal;   &#125;   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">run</span><span class="hljs-params">()</span> </span>&#123;      <span class="hljs-keyword">try</span> &#123;        startSignal.await();        doWork();        doneSignal.countDown();      &#125; <span class="hljs-keyword">catch</span> (InterruptedException ex) &#123;&#125; <span class="hljs-comment">// return;</span>   &#125;   <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">doWork</span><span class="hljs-params">()</span> </span>&#123; ... &#125; &#125;</code></pre><h1 id="CyclicBarrier"><a href="#CyclicBarrier" class="headerlink" title="CyclicBarrier"></a>CyclicBarrier</h1><h2 id="简介-1"><a href="#简介-1" class="headerlink" title="简介"></a>简介</h2><blockquote><p>A synchronization aid that allows a set of threads to all wait for each other to reach a common barrier point. CyclicBarriers are useful in programs involving a fixed sized party of threads that must occasionally wait for each other. The barrier is called <em>cyclic</em> because it can be re-used after the waiting threads are released.</p></blockquote><p>一组线程到达barrier时会被阻塞，直到最后一个线程到达屏障，被阻塞的线程才会继续执行。</p><h2 id="构造方法-1"><a href="#构造方法-1" class="headerlink" title="构造方法"></a>构造方法</h2><p>参数含义如下：</p><ul><li><strong>parties</strong>：拦截的线程数量</li><li><strong>barrierAction</strong>：所有线程到达<code>barrier</code>后执行的任务</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">CyclicBarrier</span><span class="hljs-params">(<span class="hljs-keyword">int</span> parties, Runnable barrierAction)</span> </span>&#123;        <span class="hljs-keyword">if</span> (parties &lt;= <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException();        <span class="hljs-keyword">this</span>.parties = parties;        <span class="hljs-keyword">this</span>.count = parties;        <span class="hljs-keyword">this</span>.barrierCommand = barrierAction;&#125;</code></pre><h2 id="成员属性"><a href="#成员属性" class="headerlink" title="成员属性"></a>成员属性</h2><ul><li><strong>lock</strong>：可重入锁，用于进行<code>dowait</code>时锁定</li><li><strong>parties</strong>：参与的线程数量</li><li><strong>trip</strong>：实际进行<code>await()</code>的<code>condition</code></li><li><strong>barrierCommand</strong>：最后一个线程到达时执行的任务</li><li><strong>count</strong>：等待进入屏障的线程数量</li><li><strong>generation</strong>：当前的generation<ul><li><strong>broken</strong>，表示当前屏障是否被破坏。</li></ul></li></ul><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Generation</span> </span>&#123;        <span class="hljs-keyword">boolean</span> broken = <span class="hljs-keyword">false</span>;    &#125;    <span class="hljs-comment">/** The lock for guarding barrier entry */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ReentrantLock lock = <span class="hljs-keyword">new</span> ReentrantLock();    <span class="hljs-comment">/** Condition to wait on until tripped */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Condition trip = lock.newCondition();    <span class="hljs-comment">/** The number of parties */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> parties;    <span class="hljs-comment">/* The command to run when tripped */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Runnable barrierCommand;    <span class="hljs-comment">/** The current generation */</span>    <span class="hljs-keyword">private</span> Generation generation = <span class="hljs-keyword">new</span> Generation();    <span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> count;</code></pre><h2 id="核心方法-1"><a href="#核心方法-1" class="headerlink" title="核心方法"></a>核心方法</h2><h3 id="await"><a href="#await" class="headerlink" title="await"></a>await</h3><p>可响应中断，通过调用<code>dowait(false, 0L)</code>实现</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">await</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException, BrokenBarrierException </span>&#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">return</span> dowait(<span class="hljs-keyword">false</span>, <span class="hljs-number">0L</span>);        &#125; <span class="hljs-keyword">catch</span> (TimeoutException toe) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(toe); <span class="hljs-comment">// cannot happen</span>        &#125;&#125;</code></pre><h4 id="dowait"><a href="#dowait" class="headerlink" title="dowait"></a>dowait</h4><p><code>await</code>的具体实现。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">dowait</span><span class="hljs-params">(<span class="hljs-keyword">boolean</span> timed, <span class="hljs-keyword">long</span> nanos)</span></span><span class="hljs-function">        <span class="hljs-keyword">throws</span> InterruptedException, BrokenBarrierException,</span><span class="hljs-function">               TimeoutException </span>&#123;        <span class="hljs-keyword">final</span> ReentrantLock lock = <span class="hljs-keyword">this</span>.lock;        lock.lock();        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-keyword">final</span> Generation g = generation;<span class="hljs-comment">//屏障被破坏，抛出异常</span>            <span class="hljs-keyword">if</span> (g.broken)                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BrokenBarrierException();<span class="hljs-comment">//检查中断</span>            <span class="hljs-keyword">if</span> (Thread.interrupted()) &#123;                <span class="hljs-comment">//损坏屏障，唤醒所有线程</span>                breakBarrier();                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> InterruptedException();            &#125;<span class="hljs-comment">//减少等待进入屏障的线程数量</span>            <span class="hljs-keyword">int</span> index = --count;            <span class="hljs-comment">//index == 0表示 所有进程都已经进入</span>            <span class="hljs-keyword">if</span> (index == <span class="hljs-number">0</span>) &#123;  <span class="hljs-comment">// tripped</span>                <span class="hljs-comment">//运行的动作标识</span>                <span class="hljs-keyword">boolean</span> ranAction = <span class="hljs-keyword">false</span>;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">//运行任务</span>                    <span class="hljs-keyword">final</span> Runnable command = barrierCommand;                    <span class="hljs-keyword">if</span> (command != <span class="hljs-keyword">null</span>)                        command.run();                    ranAction = <span class="hljs-keyword">true</span>;                    <span class="hljs-comment">//进入下一代</span>                    nextGeneration();                    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;                &#125; <span class="hljs-keyword">finally</span> &#123;                    <span class="hljs-comment">//如果没有改成功，损坏当前屏障</span>                    <span class="hljs-keyword">if</span> (!ranAction)                        breakBarrier();                &#125;            &#125;            <span class="hljs-comment">// loop until tripped, broken, interrupted, or timed out</span>            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">//如果没有设置等待时间</span>                    <span class="hljs-comment">//调用condition.await()进行等待</span>                    <span class="hljs-keyword">if</span> (!timed)                        trip.await();                    <span class="hljs-comment">//否则调用awaitNanos()进行等待</span>                    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nanos &gt; <span class="hljs-number">0L</span>)                        nanos = trip.awaitNanos(nanos);                &#125; <span class="hljs-keyword">catch</span> (InterruptedException ie) &#123;                    <span class="hljs-comment">//如果被中断，并且当前代的屏障没有被损坏</span>                    <span class="hljs-keyword">if</span> (g == generation &amp;&amp; ! g.broken) &#123;                        <span class="hljs-comment">//损坏当前屏障</span>                        breakBarrier();                        <span class="hljs-keyword">throw</span> ie;                    &#125; <span class="hljs-keyword">else</span> &#123;                        <span class="hljs-comment">//不是当前代，进行中断</span>                        Thread.currentThread().interrupt();                    &#125;                &#125;<span class="hljs-comment">//检查损坏标识</span>                <span class="hljs-keyword">if</span> (g.broken)                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> BrokenBarrierException();<span class="hljs-comment">//不等于当前代损坏表示</span>                <span class="hljs-keyword">if</span> (g != generation)                    <span class="hljs-keyword">return</span> index;                <span class="hljs-keyword">if</span> (timed &amp;&amp; nanos &lt;= <span class="hljs-number">0L</span>) &#123;                    breakBarrier();                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> TimeoutException();                &#125;            &#125;        &#125; <span class="hljs-keyword">finally</span> &#123;            lock.unlock();        &#125;    &#125;</code></pre><h3 id="nextGeneration"><a href="#nextGeneration" class="headerlink" title="nextGeneration"></a>nextGeneration</h3><p>线程进入屏障后会进行调用。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">nextGeneration</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">// signal completion of last generation</span>    <span class="hljs-comment">//唤醒所有线程</span>        trip.signalAll();        <span class="hljs-comment">// set up next generation</span>    <span class="hljs-comment">//恢复正在等待进入屏障的线程数量</span>        count = parties;        generation = <span class="hljs-keyword">new</span> Generation();&#125;</code></pre><h3 id="breakBarrier"><a href="#breakBarrier" class="headerlink" title="breakBarrier"></a>breakBarrier</h3><p>损坏当前屏障，会唤醒所有在屏障中的线程。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">breakBarrier</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-comment">//设置损坏标志</span>        generation.broken = <span class="hljs-keyword">true</span>;    <span class="hljs-comment">//恢复正在等待进入屏障的线程</span>        count = parties;    <span class="hljs-comment">//唤醒所有线程</span>        trip.signalAll();&#125;</code></pre><h2 id="使用案例-1"><a href="#使用案例-1" class="headerlink" title="使用案例"></a>使用案例</h2><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Solver</span> </span>&#123;   <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> N;   <span class="hljs-keyword">final</span> <span class="hljs-keyword">float</span>[][] data;   <span class="hljs-keyword">final</span> CyclicBarrier barrier;   <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Worker</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Runnable</span> </span>&#123;     <span class="hljs-keyword">int</span> myRow;     Worker(<span class="hljs-keyword">int</span> row) &#123; myRow = row; &#125;     <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">run</span><span class="hljs-params">()</span> </span>&#123;       <span class="hljs-keyword">while</span> (!done()) &#123;         processRow(myRow);         <span class="hljs-keyword">try</span> &#123;           barrier.await();         &#125; <span class="hljs-keyword">catch</span> (InterruptedException ex) &#123;           <span class="hljs-keyword">return</span>;         &#125; <span class="hljs-keyword">catch</span> (BrokenBarrierException ex) &#123;           <span class="hljs-keyword">return</span>;         &#125;       &#125;     &#125;   &#125;   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Solver</span><span class="hljs-params">(<span class="hljs-keyword">float</span>[][] matrix)</span> </span>&#123;     data = matrix;     N = matrix.length;     barrier = <span class="hljs-keyword">new</span> CyclicBarrier(N,                                 <span class="hljs-keyword">new</span> Runnable() &#123;                                   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">run</span><span class="hljs-params">()</span> </span>&#123;                                     mergeRows(...);                                   &#125;                                 &#125;);     <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; N; ++i)       <span class="hljs-keyword">new</span> Thread(<span class="hljs-keyword">new</span> Worker(i)).start();     waitUntilDone();   &#125; &#125;</code></pre><h1 id="Semaphore"><a href="#Semaphore" class="headerlink" title="Semaphore"></a>Semaphore</h1><h2 id="简介-2"><a href="#简介-2" class="headerlink" title="简介"></a>简介</h2><blockquote><p>A counting semaphore. Conceptually, a semaphore maintains a set of permits. Each <a href="https://docs.oracle.com/javase/8/docs/api/java/util/concurrent/Semaphore.html#acquire--"><code>acquire()</code></a> blocks if necessary until a permit is available, and then takes it. Each <a href="https://docs.oracle.com/javase/8/docs/api/java/util/concurrent/Semaphore.html#release--"><code>release()</code></a> adds a permit, potentially releasing a blocking acquirer. However, no actual permit objects are used; the <code>Semaphore</code> just keeps a count of the number available and acts accordingly.</p></blockquote><p>线程执行<code>acquire()</code>后，会判断<code>permit</code>是否可用，不可用则阻塞，可用则减去<code>permit</code>。线程执行<code>release()</code>后，会增加一个<code>permit</code>，并且释放一个阻塞线程。</p><p>Semaphores are often used to restrict the number of threads than can access some (physical or logical) resource. For example, here is a class that uses a semaphore to control access to a pool of items:</p><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Pool</span> </span>&#123;  <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> MAX_AVAILABLE = <span class="hljs-number">100</span>;  <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Semaphore available = <span class="hljs-keyword">new</span> Semaphore(MAX_AVAILABLE, <span class="hljs-keyword">true</span>);  <span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">getItem</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;    available.acquire();    <span class="hljs-keyword">return</span> getNextAvailableItem();  &#125;  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">putItem</span><span class="hljs-params">(Object x)</span> </span>&#123;    <span class="hljs-keyword">if</span> (markAsUnused(x))      available.release();  &#125;  <span class="hljs-comment">// Not a particularly efficient data structure; just for demo</span>  <span class="hljs-keyword">protected</span> Object[] items = ... whatever kinds of items being managed  <span class="hljs-keyword">protected</span> <span class="hljs-keyword">boolean</span>[] used = <span class="hljs-keyword">new</span> <span class="hljs-keyword">boolean</span>[MAX_AVAILABLE];  <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">synchronized</span> Object <span class="hljs-title">getNextAvailableItem</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; MAX_AVAILABLE; ++i) &#123;      <span class="hljs-keyword">if</span> (!used[i]) &#123;         used[i] = <span class="hljs-keyword">true</span>;         <span class="hljs-keyword">return</span> items[i];      &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>; <span class="hljs-comment">// not reached</span>  &#125;  <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">synchronized</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">markAsUnused</span><span class="hljs-params">(Object item)</span> </span>&#123;    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; MAX_AVAILABLE; ++i) &#123;      <span class="hljs-keyword">if</span> (item == items[i]) &#123;         <span class="hljs-keyword">if</span> (used[i]) &#123;           used[i] = <span class="hljs-keyword">false</span>;           <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;         &#125; <span class="hljs-keyword">else</span>           <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;      &#125;    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;  &#125;&#125;</code></pre><h2 id="构造方法-2"><a href="#构造方法-2" class="headerlink" title="构造方法"></a>构造方法</h2><p>两个构造方法：默认创建非公平策略的信号量，另一个构造方法可以选择公平策略的信号量。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Semaphore</span><span class="hljs-params">(<span class="hljs-keyword">int</span> permits)</span> </span>&#123;    sync = <span class="hljs-keyword">new</span> NonfairSync(permits);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">Semaphore</span><span class="hljs-params">(<span class="hljs-keyword">int</span> permits, <span class="hljs-keyword">boolean</span> fair)</span> </span>&#123;     sync = fair ? <span class="hljs-keyword">new</span> FairSync(permits) : <span class="hljs-keyword">new</span> NonfairSync(permits);&#125;</code></pre><h2 id="成员属性-1"><a href="#成员属性-1" class="headerlink" title="成员属性"></a>成员属性</h2><p><code>Semaphore</code>主要通过<code>sync</code>（AQS的实现类）来实现核心功能。</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Sync sync;</code></pre><h3 id="Sync-1"><a href="#Sync-1" class="headerlink" title="Sync"></a>Sync</h3><p><code>Sync</code>代码如下：</p><pre><code class="hljs java"><span class="hljs-keyword">abstract</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Sync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractQueuedSynchronizer</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = <span class="hljs-number">1192457210091910933L</span>;<span class="hljs-comment">//构造方法</span>        Sync(<span class="hljs-keyword">int</span> permits) &#123;            setState(permits);        &#125;<span class="hljs-comment">//返回permit</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">getPermits</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> getState();        &#125;<span class="hljs-comment">//共享模式下的非公平策略获取</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">nonfairTryAcquireShared</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-keyword">int</span> available = getState();                <span class="hljs-keyword">int</span> remaining = available - acquires;                <span class="hljs-keyword">if</span> (remaining &lt; <span class="hljs-number">0</span> ||                    compareAndSetState(available, remaining))                    <span class="hljs-keyword">return</span> remaining;            &#125;        &#125;<span class="hljs-comment">//共享模式下的释放</span>        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryReleaseShared</span><span class="hljs-params">(<span class="hljs-keyword">int</span> releases)</span> </span>&#123;            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-keyword">int</span> current = getState();                <span class="hljs-keyword">int</span> next = current + releases;                <span class="hljs-keyword">if</span> (next &lt; current) <span class="hljs-comment">// overflow</span>                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Maximum permit count exceeded&quot;</span>);                <span class="hljs-keyword">if</span> (compareAndSetState(current, next))                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;            &#125;        &#125;<span class="hljs-comment">//根据指定数量减少可用许可数量</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">reducePermits</span><span class="hljs-params">(<span class="hljs-keyword">int</span> reductions)</span> </span>&#123;            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-keyword">int</span> current = getState();                <span class="hljs-keyword">int</span> next = current - reductions;                <span class="hljs-keyword">if</span> (next &gt; current) <span class="hljs-comment">// underflow</span>                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Permit count underflow&quot;</span>);                <span class="hljs-keyword">if</span> (compareAndSetState(current, next))                    <span class="hljs-keyword">return</span>;            &#125;        &#125;<span class="hljs-comment">//permit不为0则更新permit，并返回permit</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">drainPermits</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-keyword">int</span> current = getState();                <span class="hljs-keyword">if</span> (current == <span class="hljs-number">0</span> || compareAndSetState(current, <span class="hljs-number">0</span>))                    <span class="hljs-keyword">return</span> current;            &#125;        &#125;    &#125;</code></pre><h3 id="NonfairSync"><a href="#NonfairSync" class="headerlink" title="NonfairSync"></a>NonfairSync</h3><p>非公平策略直接调用<code>tryAcquireShared</code>完成获取资源的操作。</p><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">NonfairSync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Sync</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">2694183684443567898L</span>;        NonfairSync(<span class="hljs-keyword">int</span> permits) &#123;            <span class="hljs-keyword">super</span>(permits);        &#125;        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">int</span> <span class="hljs-title">tryAcquireShared</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-keyword">return</span> nonfairTryAcquireShared(acquires);        &#125;    &#125;</code></pre><h3 id="FairSync"><a href="#FairSync" class="headerlink" title="FairSync"></a>FairSync</h3><p>公平策略中，获取共享状态时，会判断<code>Sync Queue</code>中是否有前驱元素。</p><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FairSync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Sync</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = <span class="hljs-number">2014338818796000944L</span>;        FairSync(<span class="hljs-keyword">int</span> permits) &#123;            <span class="hljs-keyword">super</span>(permits);        &#125;        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">int</span> <span class="hljs-title">tryAcquireShared</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-keyword">if</span> (hasQueuedPredecessors())                    <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;                <span class="hljs-keyword">int</span> available = getState();                <span class="hljs-keyword">int</span> remaining = available - acquires;                <span class="hljs-keyword">if</span> (remaining &lt; <span class="hljs-number">0</span> ||                    compareAndSetState(available, remaining))                    <span class="hljs-keyword">return</span> remaining;            &#125;        &#125;&#125;</code></pre><h2 id="核心方法-2"><a href="#核心方法-2" class="headerlink" title="核心方法"></a>核心方法</h2><h3 id="acquire"><a href="#acquire" class="headerlink" title="acquire"></a>acquire</h3><p>获取一个<code>permit</code>，在<code>permit</code>有效之前，将会阻塞，响应中断。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">acquire</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;    sync.acquireSharedInterruptibly(<span class="hljs-number">1</span>);&#125;</code></pre><h3 id="acquireUninterruptibly"><a href="#acquireUninterruptibly" class="headerlink" title="acquireUninterruptibly"></a>acquireUninterruptibly</h3><p>不接受中断的<code>acquire()</code>.</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">acquireUninterruptibly</span><span class="hljs-params">()</span> </span>&#123;    sync.acquireShared(<span class="hljs-number">1</span>);&#125;</code></pre><h3 id="release"><a href="#release" class="headerlink" title="release"></a>release</h3><p>释放一个<code>permits</code>。通过<code>AQS.releaseShared()</code>。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">release</span><span class="hljs-params">(<span class="hljs-keyword">int</span> permits)</span> </span>&#123;    <span class="hljs-keyword">if</span> (permits &lt; <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException();    sync.releaseShared(permits);&#125;</code></pre><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p>Java SE 8 Docs API</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;CountDownLatch&quot;&gt;&lt;a href=&quot;#CountDownLatch&quot; class=&quot;headerlink&quot; title=&quot;CountDownLatch&quot;&gt;&lt;/a&gt;CountDownLatch&lt;/h1&gt;&lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot;</summary>
      
    
    
    
    <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
    <category term="Java并发" scheme="https://l1n.wang/categories/Java/Java%E5%B9%B6%E5%8F%91/"/>
    
    
    <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
    <category term="并发" scheme="https://l1n.wang/tags/%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>线程池ThreadPoolExecutor解析</title>
    <link href="https://l1n.wang/2020/11/java-threadpoolexecutor/"/>
    <id>https://l1n.wang/2020/11/java-threadpoolexecutor/</id>
    <published>2020-11-26T06:05:34.000Z</published>
    <updated>2020-11-26T06:11:07.832Z</updated>
    
    <content type="html"><![CDATA[<h1 id="什么是线程池"><a href="#什么是线程池" class="headerlink" title="什么是线程池"></a>什么是线程池</h1><p><code>Thread Pool</code>是一种基于池化思想管理线程的工具，经常出现在多线程程序中。</p><p>线程池的优点：</p><ul><li>降低资源消耗：通过池化技术重复利用已创建线程。</li><li>提高响应速度：任务到达时，无需等待进程创建即可执行。</li><li>提高线程的可管理性：使用线程池进行统一的分配、调优和监控。</li><li>提供更多强大的功能：线程池具备可扩展性，允许开发人员向其中增加更多功能。</li></ul><h1 id="为什么用线程池"><a href="#为什么用线程池" class="headerlink" title="为什么用线程池"></a>为什么用线程池</h1><p>直接创建线程存在性能开销：</p><ul><li>Java中线程是基于内核线程实现的，线程的创建和销毁需要进行系统调用，性能开销较高。</li><li>Java8中，每个<code>Thread</code>都需要有一个内核线程的支持，这意味着每个<code>Thread</code>都需要消耗一定的内核资源。Java8中每个线程栈大小是1M，Java11中，对创建线程操作进行优化，创建一个线程只需要40KB。</li><li>线程切换引起<code>context switch</code>。</li></ul><p>​        使用线程池解决的核心问题就是<strong>资源管理问题</strong>，多线程环境下，不确定性会带来一些问题：频繁申请/销毁线程会带来额外的开销、存在资源耗尽的风险等。</p><p>​        使用池化思想将资源统一在一起管理的一种思想，可以最大化收益最小化风险，</p><h1 id="ThreadPoolExecutor"><a href="#ThreadPoolExecutor" class="headerlink" title="ThreadPoolExecutor"></a>ThreadPoolExecutor</h1><h2 id="继承关系"><a href="#继承关系" class="headerlink" title="继承关系"></a>继承关系</h2><p><code>ThreadPoolExecutor</code>类的继承关系如下：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/concurrency/pool/ThreadPoolExecutor.jpg" alt="ThreadPoolExecutor继承关系"></p><ul><li>Executor：顶层的<code>Executor</code>仅提供一个<code>execute()</code>接口，实现了提交任务与执行任务的解耦。</li><li>ExecutorService：继承自<code>Executor</code>，实现了添加了其他接口，例如：<ul><li>为一个或一批异步任务生成Future的方法</li><li>提供了管控线程池的方法，例如停止线程池运行。</li></ul></li><li>AbstractExecutorService：实现了<code>ExecutorService</code>，实现了除<code>execute()</code>以外的所有方法，将最重要的<code>execute()</code>交给<code>ThreadPoolExecutor</code>实现。</li></ul><h2 id="运行机制"><a href="#运行机制" class="headerlink" title="运行机制"></a>运行机制</h2><p><code>ThreadPoolExecutor</code>的基本运行机制如下图所示（图片来源：美团技术团队）：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/concurrency/pool/ThreadPoolExecutor%E6%89%A7%E8%A1%8C%E6%B5%81%E7%A8%8B.png" alt="ThreadPoolExecutor"></p><p>线程池内部相当于一个生产者消费者模型，将线程池分成两个部分：任务管理、线程管理。</p><ul><li>任务管理相当于生产者，任务提交后，线程池判断该任务的后续操作。<ol><li>直接申请线程执行该任务</li><li>存放到阻塞队列中等待</li><li>拒绝该任务。</li></ol></li><li>线程管理部分是消费者，根据任务请求进行线程分配工作，当线程执行完任务后会继续获取新的任务去执行，最终当线程获取不到任务时，线程会进行回收。</li></ul><p>以下三个问题需要解决：</p><ul><li>线程池如何维护自身状态？</li><li>线程池如何管理任务？</li><li>线程池如何管理线程？</li></ul><h2 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h2><p>核心的构造方法如下，主要参数有：</p><ul><li><strong>corePoolSize</strong>：核心线程数量</li><li><strong>maximumPoolSize</strong>：最大线程数量</li><li><strong>workQueue</strong>：保存等待执行任务的阻塞队列，当提交一个新的任务到线程池时，线程池根据当前状态决定后续处理。</li><li><strong>keepAliveTime</strong>：线程池维护线程所允许的时间。当线程池中的线程数量大于corePoolSize的时候，如果这时没有新的任务提交，核心线程外的线程不会立即销毁，而是会等待，直到等待的时间超过了keepAliveTime。</li><li><strong>threadFactory</strong>：它是<code>ThreadFactory</code>类型的变量，用来创建新线程。</li><li><strong>handler</strong>：它是<code>RejectedExecutionHandler</code>类型的变量，表示线程池的拒绝策略。如果阻塞队列满了并且没有空闲的线程，这时如果继续提交任务，就需要采取一种策略处理该任务。</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ThreadPoolExecutor</span><span class="hljs-params">(<span class="hljs-keyword">int</span> corePoolSize,</span></span><span class="hljs-function"><span class="hljs-params">                              <span class="hljs-keyword">int</span> maximumPoolSize,</span></span><span class="hljs-function"><span class="hljs-params">                              <span class="hljs-keyword">long</span> keepAliveTime,</span></span><span class="hljs-function"><span class="hljs-params">                              TimeUnit unit,</span></span><span class="hljs-function"><span class="hljs-params">                              BlockingQueue&lt;Runnable&gt; workQueue,</span></span><span class="hljs-function"><span class="hljs-params">                              ThreadFactory threadFactory,</span></span><span class="hljs-function"><span class="hljs-params">                              RejectedExecutionHandler handler)</span> </span>&#123;        <span class="hljs-keyword">if</span> (corePoolSize &lt; <span class="hljs-number">0</span> ||            maximumPoolSize &lt;= <span class="hljs-number">0</span> ||            maximumPoolSize &lt; corePoolSize ||            keepAliveTime &lt; <span class="hljs-number">0</span>)            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalArgumentException();        <span class="hljs-keyword">if</span> (workQueue == <span class="hljs-keyword">null</span> || threadFactory == <span class="hljs-keyword">null</span> || handler == <span class="hljs-keyword">null</span>)            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> NullPointerException();        <span class="hljs-keyword">this</span>.acc = System.getSecurityManager() == <span class="hljs-keyword">null</span> ?                <span class="hljs-keyword">null</span> :                AccessController.getContext();        <span class="hljs-keyword">this</span>.corePoolSize = corePoolSize;        <span class="hljs-keyword">this</span>.maximumPoolSize = maximumPoolSize;        <span class="hljs-keyword">this</span>.workQueue = workQueue;        <span class="hljs-keyword">this</span>.keepAliveTime = unit.toNanos(keepAliveTime);        <span class="hljs-keyword">this</span>.threadFactory = threadFactory;        <span class="hljs-keyword">this</span>.handler = handler;    &#125;</code></pre><h2 id="线程池的状态"><a href="#线程池的状态" class="headerlink" title="线程池的状态"></a>线程池的状态</h2><p>线程池的运行状态，由<code>AtomicInteger ctl</code>维护，其中分为两个参数：<code>runState</code>和<code>workerCount</code>，高3位存储<code>runState</code>，低29位存储<code>workerCount</code>，提供了位运算的方法来获取对应的参数。线程池的运行状态，通过内部进行调整。不需要套</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> AtomicInteger ctl = <span class="hljs-keyword">new</span> AtomicInteger(ctlOf(RUNNING, <span class="hljs-number">0</span>));<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> COUNT_BITS = Integer.SIZE - <span class="hljs-number">3</span>;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> CAPACITY   = (<span class="hljs-number">1</span> &lt;&lt; COUNT_BITS) - <span class="hljs-number">1</span>;<span class="hljs-comment">// runState is stored in the high-order bits</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> RUNNING    = -<span class="hljs-number">1</span> &lt;&lt; COUNT_BITS;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> SHUTDOWN   =  <span class="hljs-number">0</span> &lt;&lt; COUNT_BITS;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> STOP       =  <span class="hljs-number">1</span> &lt;&lt; COUNT_BITS;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> TIDYING    =  <span class="hljs-number">2</span> &lt;&lt; COUNT_BITS;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> TERMINATED =  <span class="hljs-number">3</span> &lt;&lt; COUNT_BITS;<span class="hljs-comment">//获取运行状态</span><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">runStateOf</span><span class="hljs-params">(<span class="hljs-keyword">int</span> c)</span>     </span>&#123; <span class="hljs-keyword">return</span> c &amp; ~CAPACITY; &#125;<span class="hljs-comment">//获取活动线程数</span><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">workerCountOf</span><span class="hljs-params">(<span class="hljs-keyword">int</span> c)</span>  </span>&#123; <span class="hljs-keyword">return</span> c &amp; CAPACITY; &#125;<span class="hljs-comment">//获取运行状态和活动线程数</span><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">ctlOf</span><span class="hljs-params">(<span class="hljs-keyword">int</span> rs, <span class="hljs-keyword">int</span> wc)</span> </span>&#123; <span class="hljs-keyword">return</span> rs | wc; &#125;</code></pre><div class="table-container"><table><thead><tr><th style="text-align:center">运行状态</th><th style="text-align:center">状态描述</th></tr></thead><tbody><tr><td style="text-align:center">RUNNING</td><td style="text-align:center">能接受新提交的任务，并且能处理阻塞队列中的任务</td></tr><tr><td style="text-align:center">SHUTDOWN</td><td style="text-align:center">关闭状态，不再接受新提交的任务，但是可以继续处理阻塞队列中的任务。</td></tr><tr><td style="text-align:center">STOP</td><td style="text-align:center">不能接受新的任务，也不处理队列中的任务，会中断正在处理任务的线程。</td></tr><tr><td style="text-align:center">TIDYING</td><td style="text-align:center">如果所有的任务都终止了，<code>workerCount</code>位0，线程池会调用<code>terminated()</code>进入TERMINATED状态。</td></tr><tr><td style="text-align:center">TERMINATED</td><td style="text-align:center">执行完<code>terminated()</code>方法后进入该状态。</td></tr></tbody></table></div><p>线程池的状态转换过程如下图所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/concurrency/pool/pool-status.jpg" alt="线程池状态转换过程"></p><h2 id="任务调度机制"><a href="#任务调度机制" class="headerlink" title="任务调度机制"></a>任务调度机制</h2><p>用户提交一个任务给线程池，线程池如何进行调度，如何对任务进行管理。这是这部分的核心问题。</p><p>调度工作是由<code>execute</code>方法完成，由此可以看出该方法的重要性。</p><p>其基本执行过程如下：</p><ol><li>首先检查线程池运行状态，如果不是<code>RUNNING</code>，则直接拒绝。</li><li>如果workerCount &lt; corePoolSize，则创建并启动一个线程来执行新任务。</li><li>如果workerCount &gt;= corePoolSize，且线程池内的阻塞队列未满，则将任务添加到阻塞队列中。</li><li>如果workerCount &gt;= corePoolSize &amp;&amp; workCount &lt; maximumPoolSize，且线程池内阻塞队列已满，则创建并启动一个线程来执行新任务</li><li>如果workerCount &gt;= maximumPoolSize，并且线程池内的阻塞队列已满，则根据拒绝策略来处理该任务。</li></ol><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">execute</span><span class="hljs-params">(Runnable command)</span> </span>&#123;        <span class="hljs-keyword">if</span> (command == <span class="hljs-keyword">null</span>)            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> NullPointerException();        <span class="hljs-keyword">int</span> c = ctl.get();    <span class="hljs-comment">//workerCount &lt; corePoolSize</span>        <span class="hljs-keyword">if</span> (workerCountOf(c) &lt; corePoolSize) &#123;            <span class="hljs-comment">//创建一个新的线程放入线程池，并将任务加到该线程。</span>            <span class="hljs-keyword">if</span> (addWorker(command, <span class="hljs-keyword">true</span>))                <span class="hljs-keyword">return</span>;            <span class="hljs-comment">//如果添加失败，更新c</span>            c = ctl.get();        &#125;    <span class="hljs-comment">//判断当前线程池是RUNNING状态，并且将任务添加到workQueue成功</span>        <span class="hljs-keyword">if</span> (isRunning(c) &amp;&amp; workQueue.offer(command)) &#123;            <span class="hljs-comment">//重新获取ctl的值</span>            <span class="hljs-keyword">int</span> recheck = ctl.get();            <span class="hljs-comment">//再次判断线程池的状态，如果不是运行状态，将任务移出队列成功后，进行拒绝</span>            <span class="hljs-keyword">if</span> (! isRunning(recheck) &amp;&amp; remove(command))                reject(command);            <span class="hljs-comment">//如果workCount == 0，则添加新线程</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (workerCountOf(recheck) == <span class="hljs-number">0</span>)                addWorker(<span class="hljs-keyword">null</span>, <span class="hljs-keyword">false</span>);        &#125;    <span class="hljs-comment">//1. 线程不是RUNNING状态 2.线程池是RUNNING状态，但是workerCount &gt;= corePoolSize &amp;&amp; workerCount已满</span>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!addWorker(command, <span class="hljs-keyword">false</span>))            <span class="hljs-comment">//失败则进行拒绝</span>            reject(command);    &#125;</code></pre><p>其执行流程如下图所示：</p><p><img src="https://blog-1251613845.cos.ap-shanghai.myqcloud.com/concurrency/pool/execute.jpg" alt="任务调度流程"></p><h3 id="addWorker"><a href="#addWorker" class="headerlink" title="addWorker"></a>addWorker</h3><p>该方法主要是在线程池中新建一个<code>worker</code>线程并执行任务，两个参数的含义如下：</p><ul><li><code>firstTask</code>：执行新增的线程执行的第一个任务</li><li><code>core</code>：检测标识<ul><li><code>true</code>：新增线程时会判断当前活动线程是否小于<code>corePoolSize</code></li><li><code>false</code>：新增线程时会判断当前活动线程是否少于<code>maximumPoolSize</code></li></ul></li></ul><p>方法的具体含义参考代码注释。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">addWorker</span><span class="hljs-params">(Runnable firstTask, <span class="hljs-keyword">boolean</span> core)</span> </span>&#123;        retry:        <span class="hljs-keyword">for</span> (;;) &#123;            <span class="hljs-comment">//获取线程池运行状态</span>            <span class="hljs-keyword">int</span> c = ctl.get();            <span class="hljs-keyword">int</span> rs = runStateOf(c);            <span class="hljs-comment">//如果rs &gt;= SHUTDOWN，则表示此时不再接收新任务</span>            <span class="hljs-comment">//再检查三个条件，其中一个不满足则添加失败</span>            <span class="hljs-comment">//1. rs == SHUTDOWN：关闭状态则不接受新任务</span>            <span class="hljs-comment">//2. firstTask为空</span>            <span class="hljs-comment">//3. 阻塞队列不为空</span>            <span class="hljs-comment">//队列中没有任务则不需要再添加线程</span>            <span class="hljs-keyword">if</span> (rs &gt;= SHUTDOWN &amp;&amp;                ! (rs == SHUTDOWN &amp;&amp;                   firstTask == <span class="hljs-keyword">null</span> &amp;&amp;                   ! workQueue.isEmpty()))                <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-comment">//获取线程数量</span>                <span class="hljs-keyword">int</span> wc = workerCountOf(c);                <span class="hljs-comment">//检查线程数量是否超过CAPACITY（二进制为29个1）</span>                <span class="hljs-comment">//根据传入的core判断与哪个参数进行对比</span>                <span class="hljs-keyword">if</span> (wc &gt;= CAPACITY ||                    wc &gt;= (core ? corePoolSize : maximumPoolSize))                    <span class="hljs-comment">//如果条件成立，则拒绝创建线程</span>                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;                <span class="hljs-comment">//CAS尝试增加workerCount，成功则跳出外层循环</span>                <span class="hljs-keyword">if</span> (compareAndIncrementWorkerCount(c))                    <span class="hljs-keyword">break</span> retry;                c = ctl.get();  <span class="hljs-comment">// Re-read ctl</span>                <span class="hljs-comment">//当前运行状态不等于rs，说明状态已改变，则返回内层循环继续执行</span>                <span class="hljs-keyword">if</span> (runStateOf(c) != rs)                    <span class="hljs-keyword">continue</span> retry;                <span class="hljs-comment">// else CAS failed due to workerCount change; retry inner loop</span>            &#125;        &#125;        <span class="hljs-keyword">boolean</span> workerStarted = <span class="hljs-keyword">false</span>;        <span class="hljs-keyword">boolean</span> workerAdded = <span class="hljs-keyword">false</span>;        Worker w = <span class="hljs-keyword">null</span>;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">//根据firstTask创建Worker对象</span>            w = <span class="hljs-keyword">new</span> Worker(firstTask);            <span class="hljs-comment">//获取worker的thread</span>            <span class="hljs-keyword">final</span> Thread t = w.thread;            <span class="hljs-keyword">if</span> (t != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">//上一个可重入锁</span>                <span class="hljs-keyword">final</span> ReentrantLock mainLock = <span class="hljs-keyword">this</span>.mainLock;                mainLock.lock();                <span class="hljs-keyword">try</span> &#123;                    <span class="hljs-comment">// Recheck while holding lock.</span>                    <span class="hljs-comment">// Back out on ThreadFactory failure or if</span>                    <span class="hljs-comment">// shut down before lock acquired.</span>                    <span class="hljs-keyword">int</span> rs = runStateOf(ctl.get());<span class="hljs-comment">//rs &lt; SHUTDOWN表示是RUNNING状态</span>                    <span class="hljs-comment">//或者是RUNING状态并且firstTask为null</span>                    <span class="hljs-keyword">if</span> (rs &lt; SHUTDOWN ||                        (rs == SHUTDOWN &amp;&amp; firstTask == <span class="hljs-keyword">null</span>)) &#123;                        <span class="hljs-keyword">if</span> (t.isAlive()) <span class="hljs-comment">// precheck that t is startable</span>                            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalThreadStateException();                        <span class="hljs-comment">//将worker加入工作线程集合中。</span>                        workers.add(w);                        <span class="hljs-keyword">int</span> s = workers.size();                        <span class="hljs-comment">// largestPoolSize记录着线程池中出现过的最大线程数量</span>                        <span class="hljs-comment">//如果添加之后的工作线程集合size &gt; largestPoolSize</span>                        <span class="hljs-keyword">if</span> (s &gt; largestPoolSize)                            <span class="hljs-comment">//更新线程池中出现的最大线程数量</span>                            largestPoolSize = s;                        workerAdded = <span class="hljs-keyword">true</span>;                    &#125;                &#125; <span class="hljs-keyword">finally</span> &#123;                    mainLock.unlock();                &#125;                <span class="hljs-keyword">if</span> (workerAdded) &#123;                    <span class="hljs-comment">//添加成功，启动线程执行任务</span>                    t.start();                    workerStarted = <span class="hljs-keyword">true</span>;                &#125;            &#125;        &#125; <span class="hljs-keyword">finally</span> &#123;            <span class="hljs-keyword">if</span> (! workerStarted)                addWorkerFailed(w);        &#125;        <span class="hljs-keyword">return</span> workerStarted;    &#125;</code></pre><h3 id="拒绝策略"><a href="#拒绝策略" class="headerlink" title="拒绝策略"></a>拒绝策略</h3><p>线程池提供了如下四种策略：</p><ol><li><strong>AbortPolicy</strong>：直接抛出异常，这是默认策略。</li><li><strong>CallerRunsPolicy</strong>：用调用者所在的线程来执行任务。</li><li><strong>DiscardOldestPolicy</strong>：丢弃阻塞队列中靠最前的任务，并执行当前任务。</li><li><strong>DiscardPolicy</strong>：直接丢弃任务。</li></ol><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">CallerRunsPolicy</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">RejectedExecutionHandler</span> </span>&#123;                <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">CallerRunsPolicy</span><span class="hljs-params">()</span> </span>&#123; &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">rejectedExecution</span><span class="hljs-params">(Runnable r, ThreadPoolExecutor e)</span> </span>&#123;            <span class="hljs-keyword">if</span> (!e.isShutdown()) &#123;                r.run();            &#125;        &#125;    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">AbortPolicy</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">RejectedExecutionHandler</span> </span>&#123;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">AbortPolicy</span><span class="hljs-params">()</span> </span>&#123; &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">rejectedExecution</span><span class="hljs-params">(Runnable r, ThreadPoolExecutor e)</span> </span>&#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> RejectedExecutionException(<span class="hljs-string">&quot;Task &quot;</span> + r.toString() +                                                 <span class="hljs-string">&quot; rejected from &quot;</span> +                                                 e.toString());        &#125;    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DiscardPolicy</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">RejectedExecutionHandler</span> </span>&#123;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">DiscardPolicy</span><span class="hljs-params">()</span> </span>&#123; &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">rejectedExecution</span><span class="hljs-params">(Runnable r, ThreadPoolExecutor e)</span> </span>&#123;        &#125;    &#125;    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DiscardOldestPolicy</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">RejectedExecutionHandler</span> </span>&#123;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">DiscardOldestPolicy</span><span class="hljs-params">()</span> </span>&#123; &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">rejectedExecution</span><span class="hljs-params">(Runnable r, ThreadPoolExecutor e)</span> </span>&#123;            <span class="hljs-keyword">if</span> (!e.isShutdown()) &#123;                e.getQueue().poll();                e.execute(r);            &#125;        &#125;    &#125;</code></pre><h2 id="任务阻塞机制"><a href="#任务阻塞机制" class="headerlink" title="任务阻塞机制"></a>任务阻塞机制</h2><p>任务阻塞机制是线程池管理任务的核心机制。线程池将任务和线程进行解耦，以生产者消费者模式，通过一个阻塞队列来实现的。任务缓存到阻塞队列，线程从阻塞队列中获取任务。</p><h2 id="Worker"><a href="#Worker" class="headerlink" title="Worker"></a>Worker</h2><blockquote><p> Q：线程池如何管理进程？</p></blockquote><p>线程池中每一个线程被封装成<code>Worker</code>对象，通过对<code>Worker</code>对象的管理，从而达到对进程管理的目的。下面这个<code>HashSet</code>存储的是<code>Worker</code>集合。</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashSet&lt;Worker&gt; workers = <span class="hljs-keyword">new</span> HashSet&lt;Worker&gt;();</code></pre><p><code>Worker</code>继承了<code>AQS</code>，使用<code>AQS</code>实现了独占锁的功能，从<code>tryAcquire</code>可以看出禁止重入。其主要含义如下：</p><ul><li>独占状态：表示当前线程正在执行任务，则不应该中断线程。</li><li>空闲状态：没有在处理任务，可以对线程中断。</li></ul><p>其它注意点：</p><ul><li>线程池在执行<code>shutdown</code>方法或<code>tryTerminate</code>方法时会调用<code>interruptIdleWorkers</code>方法来中断空闲的线程，<code>interruptIdleWorkers</code>方法会使用<code>tryLock</code>方法来判断线程池中的线程是否是空闲状态；</li><li>设置成不可重入的原因：不希望任务在运行时重新获得锁，从而调用一些会中断运行时线程的方法。</li></ul><p><code>Worker</code>代码如下：</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Worker</span></span><span class="hljs-class">        <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractQueuedSynchronizer</span></span><span class="hljs-class">        <span class="hljs-keyword">implements</span> <span class="hljs-title">Runnable</span></span><span class="hljs-class">    </span>&#123;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * This class will never be serialized, but we provide a</span><span class="hljs-comment">         * serialVersionUID to suppress a javac warning.</span><span class="hljs-comment">         */</span>        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = <span class="hljs-number">6138294804551838833L</span>;       <span class="hljs-comment">//worker持有的线程</span>        <span class="hljs-keyword">final</span> Thread thread;       <span class="hljs-comment">//初始化的任务，可以为null</span>        Runnable firstTask;        <span class="hljs-comment">//任务计数器</span>        <span class="hljs-keyword">volatile</span> <span class="hljs-keyword">long</span> completedTasks;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * Creates with given first task and thread from ThreadFactory.</span><span class="hljs-comment">         * <span class="hljs-doctag">@param</span> firstTask the first task (null if none)</span><span class="hljs-comment">         */</span>        Worker(Runnable firstTask) &#123;            <span class="hljs-comment">//AQS默认为0，设置为-1是为了禁止执行任务前对线程进行中断</span>            setState(-<span class="hljs-number">1</span>); <span class="hljs-comment">// inhibit interrupts until runWorker</span>            <span class="hljs-keyword">this</span>.firstTask = firstTask;            <span class="hljs-comment">//通过ThreadFactory创建线程</span>            <span class="hljs-keyword">this</span>.thread = getThreadFactory().newThread(<span class="hljs-keyword">this</span>);        &#125;        <span class="hljs-comment">/** Delegates main run loop to outer runWorker  */</span>        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">run</span><span class="hljs-params">()</span> </span>&#123;            runWorker(<span class="hljs-keyword">this</span>);        &#125;        <span class="hljs-comment">// Lock methods</span>        <span class="hljs-comment">//</span>        <span class="hljs-comment">// The value 0 represents the unlocked state.</span>        <span class="hljs-comment">// The value 1 represents the locked state.</span>        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isHeldExclusively</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> getState() != <span class="hljs-number">0</span>;        &#125;<span class="hljs-comment">//不可重入</span>        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> unused)</span> </span>&#123;            <span class="hljs-keyword">if</span> (compareAndSetState(<span class="hljs-number">0</span>, <span class="hljs-number">1</span>)) &#123;                setExclusiveOwnerThread(Thread.currentThread());                <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;            &#125;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;        &#125;        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryRelease</span><span class="hljs-params">(<span class="hljs-keyword">int</span> unused)</span> </span>&#123;            setExclusiveOwnerThread(<span class="hljs-keyword">null</span>);            setState(<span class="hljs-number">0</span>);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span>        </span>&#123; acquire(<span class="hljs-number">1</span>); &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryLock</span><span class="hljs-params">()</span>  </span>&#123; <span class="hljs-keyword">return</span> tryAcquire(<span class="hljs-number">1</span>); &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">()</span>      </span>&#123; release(<span class="hljs-number">1</span>); &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isLocked</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-keyword">return</span> isHeldExclusively(); &#125;        <span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">interruptIfStarted</span><span class="hljs-params">()</span> </span>&#123;            Thread t;            <span class="hljs-keyword">if</span> (getState() &gt;= <span class="hljs-number">0</span> &amp;&amp; (t = thread) != <span class="hljs-keyword">null</span> &amp;&amp; !t.isInterrupted()) &#123;                <span class="hljs-keyword">try</span> &#123;                    t.interrupt();                &#125; <span class="hljs-keyword">catch</span> (SecurityException ignore) &#123;                &#125;            &#125;        &#125;    &#125;</code></pre><h3 id="runWorker"><a href="#runWorker" class="headerlink" title="runWorker"></a>runWorker</h3><p>在<code>Worker</code>类中的<code>run()</code>调用该方法来执行任务，主要逻辑如下：</p><ol><li>while循环通过<code>getTask()</code>获取任务。</li><li>如果线程池正在停止，那么要保证当前线程是中断状态，否则要保证当前线程不是中断状态（因为需要继续运行）。</li><li>调用<code>task.run()</code>运行任务</li><li>如果阻塞队列中没有任务可执行则跳出循环，执行<code>processWorkerExit()</code></li></ol><p>其代码如下：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">runWorker</span><span class="hljs-params">(Worker w)</span> </span>&#123;    <span class="hljs-comment">//获取线程</span>        Thread wt = Thread.currentThread();    <span class="hljs-comment">//获取第一个任务</span>        Runnable task = w.firstTask;        w.firstTask = <span class="hljs-keyword">null</span>;    <span class="hljs-comment">//允许中断</span>        w.unlock();    <span class="hljs-comment">//是否因为异常退出循环</span>        <span class="hljs-keyword">boolean</span> completedAbruptly = <span class="hljs-keyword">true</span>;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">//task为空，则通过getTask来获取任务</span>            <span class="hljs-keyword">while</span> (task != <span class="hljs-keyword">null</span> || (task = getTask()) != <span class="hljs-keyword">null</span>) &#123;                <span class="hljs-comment">//禁止中断</span>                w.lock();                <span class="hljs-comment">// If pool is stopping, ensure thread is interrupted;</span>                <span class="hljs-comment">// if not, ensure thread is not interrupted.  This</span>                <span class="hljs-comment">// requires a recheck in second case to deal with</span>                <span class="hljs-comment">// shutdownNow race while clearing interrupt</span>                <span class="hljs-comment">//如果线程池正在停止，保证当前线程是中断状态</span>                <span class="hljs-comment">//如果不是，确保当前线程不是中断状态</span>                <span class="hljs-keyword">if</span> ((runStateAtLeast(ctl.get(), STOP) ||                     (Thread.interrupted() &amp;&amp;                      runStateAtLeast(ctl.get(), STOP))) &amp;&amp;                    !wt.isInterrupted())                    wt.interrupt();                <span class="hljs-keyword">try</span> &#123;                    beforeExecute(wt, task);                    Throwable thrown = <span class="hljs-keyword">null</span>;                    <span class="hljs-keyword">try</span> &#123;                        task.run();                    &#125; <span class="hljs-keyword">catch</span> (RuntimeException x) &#123;                        thrown = x; <span class="hljs-keyword">throw</span> x;                    &#125; <span class="hljs-keyword">catch</span> (Error x) &#123;                        thrown = x; <span class="hljs-keyword">throw</span> x;                    &#125; <span class="hljs-keyword">catch</span> (Throwable x) &#123;                        thrown = x; <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(x);                    &#125; <span class="hljs-keyword">finally</span> &#123;                        afterExecute(task, thrown);                    &#125;                &#125; <span class="hljs-keyword">finally</span> &#123;                    task = <span class="hljs-keyword">null</span>;                    w.completedTasks++;                    w.unlock();                &#125;            &#125;            completedAbruptly = <span class="hljs-keyword">false</span>;        &#125; <span class="hljs-keyword">finally</span> &#123;            processWorkerExit(w, completedAbruptly);        &#125;    &#125;</code></pre><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://tech.meituan.com/2020/04/02/java-pooling-pratice-in-meituan.html">https://tech.meituan.com/2020/04/02/java-pooling-pratice-in-meituan.html</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;什么是线程池&quot;&gt;&lt;a href=&quot;#什么是线程池&quot; class=&quot;headerlink&quot; title=&quot;什么是线程池&quot;&gt;&lt;/a&gt;什么是线程池&lt;/h1&gt;&lt;p&gt;&lt;code&gt;Thread Pool&lt;/code&gt;是一种基于池化思想管理线程的工具，经常出现在多线程程序中。&lt;</summary>
      
    
    
    
    <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
    <category term="Java并发" scheme="https://l1n.wang/categories/Java/Java%E5%B9%B6%E5%8F%91/"/>
    
    
    <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
    <category term="线程池" scheme="https://l1n.wang/tags/%E7%BA%BF%E7%A8%8B%E6%B1%A0/"/>
    
  </entry>
  
  <entry>
    <title>ReentrantReadWriteLock源码分析</title>
    <link href="https://l1n.wang/2020/11/ReentrantReadWriteLock/"/>
    <id>https://l1n.wang/2020/11/ReentrantReadWriteLock/</id>
    <published>2020-11-22T09:00:43.000Z</published>
    <updated>2020-11-22T09:01:43.438Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p><code>ReentrantReadWriteLock</code> 分为读锁和写锁两个实例，读锁是共享锁，可被多个线程同时使用，写锁是独占锁。持有写锁的线程可以继续获取读锁，反之不行。</p><p>Doug Lea 将持有写锁的线程，去获取读锁，然后读锁然后释放写锁的方式，从写锁降级为读锁的过程称为<strong>锁降级（Lock downgrading）</strong>。</p><p>但是，<strong>锁升级</strong>是不可以的。线程持有读锁的话，在没释放的情况下不能去获取写锁，因为会发生<strong>死锁</strong>。</p><h1 id="类的继承关系"><a href="#类的继承关系" class="headerlink" title="类的继承关系"></a>类的继承关系</h1><p><code>ReentrantReadWriteLock</code>实现了<code>ReadWriteLock</code>接口，该接口定义了两个方法，分别返回读锁和写锁。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ReentrantReadWriteLock</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ReadWriteLock</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span></span><span class="hljs-class">    </span><span class="hljs-class"><span class="hljs-title">public</span> <span class="hljs-title">interface</span> <span class="hljs-title">ReadWriteLock</span> </span>&#123;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Returns the lock used for reading.</span><span class="hljs-comment">     *</span><span class="hljs-comment">     * <span class="hljs-doctag">@return</span> the lock used for reading</span><span class="hljs-comment">     */</span>    <span class="hljs-function">Lock <span class="hljs-title">readLock</span><span class="hljs-params">()</span></span>;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Returns the lock used for writing.</span><span class="hljs-comment">     *</span><span class="hljs-comment">     * <span class="hljs-doctag">@return</span> the lock used for writing</span><span class="hljs-comment">     */</span>    <span class="hljs-function">Lock <span class="hljs-title">writeLock</span><span class="hljs-params">()</span></span>;&#125;</code></pre><h1 id="类成员属性"><a href="#类成员属性" class="headerlink" title="类成员属性"></a>类成员属性</h1><pre><code class="hljs java"><span class="hljs-comment">//读锁</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ReentrantReadWriteLock.ReadLock readerLock;<span class="hljs-comment">//写锁</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ReentrantReadWriteLock.WriteLock writerLock;<span class="hljs-comment">//Sync是AQS的实现类</span><span class="hljs-keyword">final</span> Sync sync;<span class="hljs-comment">//Unsafe实例</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> sun.misc.Unsafe UNSAFE;<span class="hljs-comment">//获取Thread.tid的内存偏移值</span><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> TID_OFFSET;<span class="hljs-keyword">static</span> &#123;        <span class="hljs-keyword">try</span> &#123;            UNSAFE = sun.misc.Unsafe.getUnsafe();            Class&lt;?&gt; tk = Thread.class;            TID_OFFSET = UNSAFE.objectFieldOffset                (tk.getDeclaredField(<span class="hljs-string">&quot;tid&quot;</span>));        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(e);        &#125;&#125;</code></pre><h1 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h1><p>默认的构造方法创建非公平策略的<code>ReentrantReadWriteLock</code>，传入<code>true</code>则可以创建公平策略的<code>ReentrantReadWriteLock</code>。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ReentrantReadWriteLock</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">this</span>(<span class="hljs-keyword">false</span>);    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Creates a new &#123;<span class="hljs-doctag">@code</span> ReentrantReadWriteLock&#125; with</span><span class="hljs-comment">     * the given fairness policy.</span><span class="hljs-comment">     *</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> fair &#123;<span class="hljs-doctag">@code</span> true&#125; if this lock should use a fair ordering policy</span><span class="hljs-comment">     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ReentrantReadWriteLock</span><span class="hljs-params">(<span class="hljs-keyword">boolean</span> fair)</span> </span>&#123;        sync = fair ? <span class="hljs-keyword">new</span> FairSync() : <span class="hljs-keyword">new</span> NonfairSync();        readerLock = <span class="hljs-keyword">new</span> ReadLock(<span class="hljs-keyword">this</span>);        writerLock = <span class="hljs-keyword">new</span> WriteLock(<span class="hljs-keyword">this</span>);    &#125;</code></pre><h1 id="内部类"><a href="#内部类" class="headerlink" title="内部类"></a>内部类</h1><p><code>ReentrantReadWriteLock</code>共有五个内部类，其基本结构如下：</p><ul><li>Sync：AQS的实现类<ul><li>FairSync：公平策略</li><li>NofairSync：非公平策略</li></ul></li><li>ReadLock：读锁</li><li>WriteLock：写锁</li></ul><h2 id="Sync"><a href="#Sync" class="headerlink" title="Sync"></a>Sync</h2><p>该类继承自<code>AQS</code>抽象类，<code>ReentrantReadWriteLock</code>的大部分操作都交给<code>Sync</code>对象进行处理。由此可见<code>AQS</code>对于上层并发工具的贡献。</p><h3 id="HoldCounter"><a href="#HoldCounter" class="headerlink" title="HoldCounter"></a>HoldCounter</h3><p>该类配合读锁使用，其中属性定义如下：</p><ul><li>count： 代表某个读线程重入的次数</li><li>tid：获取当前线程的TID属性</li></ul><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">HoldCounter</span> </span>&#123;            <span class="hljs-keyword">int</span> count = <span class="hljs-number">0</span>;            <span class="hljs-comment">// Use id, not reference, to avoid garbage retention</span>            <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> tid = getThreadId(Thread.currentThread());&#125;</code></pre><h3 id="ThreadLocalHoldCounter"><a href="#ThreadLocalHoldCounter" class="headerlink" title="ThreadLocalHoldCounter"></a>ThreadLocalHoldCounter</h3><p>该类继承自<code>ThreadLocal</code>，并重写了<code>initialValue()</code>，<code>ThreadLocal</code>可以将线程与对象相关联，<code>get</code>得到的值都是<code>initialValue()</code>生成的<code>HoldCounter</code>对象。</p><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ThreadLocalHoldCounter</span></span><span class="hljs-class">            <span class="hljs-keyword">extends</span> <span class="hljs-title">ThreadLocal</span>&lt;<span class="hljs-title">HoldCounter</span>&gt; </span>&#123;            <span class="hljs-function"><span class="hljs-keyword">public</span> HoldCounter <span class="hljs-title">initialValue</span><span class="hljs-params">()</span> </span>&#123;                <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> HoldCounter();            &#125;&#125;</code></pre><h3 id="类的成员属性"><a href="#类的成员属性" class="headerlink" title="类的成员属性"></a>类的成员属性</h3><pre><code class="hljs java"><span class="hljs-keyword">abstract</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Sync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractQueuedSynchronizer</span> </span>&#123;    <span class="hljs-comment">// 版本序列号</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = <span class="hljs-number">6317671515068378041L</span>;            <span class="hljs-comment">// 高16位为读锁，低16位为写锁</span>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> SHARED_SHIFT   = <span class="hljs-number">16</span>;    <span class="hljs-comment">// 读锁单位</span>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> SHARED_UNIT    = (<span class="hljs-number">1</span> &lt;&lt; SHARED_SHIFT);    <span class="hljs-comment">// 读锁最大数量</span>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> MAX_COUNT      = (<span class="hljs-number">1</span> &lt;&lt; SHARED_SHIFT) - <span class="hljs-number">1</span>;    <span class="hljs-comment">// 写锁最大数量</span>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> EXCLUSIVE_MASK = (<span class="hljs-number">1</span> &lt;&lt; SHARED_SHIFT) - <span class="hljs-number">1</span>;    <span class="hljs-comment">// 本地线程计数器</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> ThreadLocalHoldCounter readHolds;    <span class="hljs-comment">// 缓存的计数器</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> HoldCounter cachedHoldCounter;    <span class="hljs-comment">// 第一个读线程</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> Thread firstReader = <span class="hljs-keyword">null</span>;    <span class="hljs-comment">// 第一个读线程的计数</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> <span class="hljs-keyword">int</span> firstReaderHoldCount;&#125;</code></pre><h3 id="构造方法-1"><a href="#构造方法-1" class="headerlink" title="构造方法"></a>构造方法</h3><pre><code class="hljs java">Sync() &#123;    <span class="hljs-comment">//本地线程计数器</span>            readHolds = <span class="hljs-keyword">new</span> ThreadLocalHoldCounter();    <span class="hljs-comment">//设置AQS的state</span>            setState(getState()); <span class="hljs-comment">// ensures visibility of readHolds</span>&#125;</code></pre><h3 id="sharedCount"><a href="#sharedCount" class="headerlink" title="sharedCount"></a>sharedCount</h3><p>该方法将<code>c</code>无符号右移16位，得到的值为读锁的线程数量，因为<code>c</code>的高16位代表读锁，低16位代表写锁数量。</p><p>也可以通过方法命名看出来，读锁是共享模式，写锁是独占模式。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">sharedCount</span><span class="hljs-params">(<span class="hljs-keyword">int</span> c)</span>    </span>&#123; <span class="hljs-keyword">return</span> c &gt;&gt;&gt; SHARED_SHIFT; &#125;</code></pre><h3 id="exclusiveCount"><a href="#exclusiveCount" class="headerlink" title="exclusiveCount"></a>exclusiveCount</h3><p>该方法表示返回占有写锁的线程数量，通过<code>state</code>与<code>(1 &lt;&lt; 16) - 1</code>进行与运算，其等价于<code>state % 2 ^ 16</code>，因为写锁数量由<code>state</code>的低16位表示。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">static</span> <span class="hljs-keyword">int</span> <span class="hljs-title">exclusiveCount</span><span class="hljs-params">(<span class="hljs-keyword">int</span> c)</span> </span>&#123; <span class="hljs-keyword">return</span> c &amp; EXCLUSIVE_MASK; &#125;</code></pre><h3 id="tryRelease"><a href="#tryRelease" class="headerlink" title="tryRelease"></a>tryRelease</h3><p>此方法用于释放写锁，通过这个调用链，可以清楚的看出<code>AQS</code>在并发类中的重要性，这也体现出了<code>AQS</code>的设计精髓，通过模板模式，将具体操作延迟到子类去实现。</p><pre><code class="hljs java"><span class="hljs-comment">//WriteLock</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">()</span> </span>&#123;            sync.release(<span class="hljs-number">1</span>);&#125;<span class="hljs-comment">//AQS中定义</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">release</span><span class="hljs-params">(<span class="hljs-keyword">int</span> arg)</span> </span>&#123;        <span class="hljs-keyword">if</span> (tryRelease(arg)) &#123;            Node h = head;            <span class="hljs-keyword">if</span> (h != <span class="hljs-keyword">null</span> &amp;&amp; h.waitStatus != <span class="hljs-number">0</span>)                unparkSuccessor(h);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;&#125;<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryRelease</span><span class="hljs-params">(<span class="hljs-keyword">int</span> releases)</span> </span>&#123;    <span class="hljs-comment">//判断当前线程是否是独占线程</span>            <span class="hljs-keyword">if</span> (!isHeldExclusively())                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalMonitorStateException();    <span class="hljs-comment">//nextc为释放资源后的写锁资源数量</span>            <span class="hljs-keyword">int</span> nextc = getState() - releases;    <span class="hljs-comment">//判断释放后的写锁数量是否为0</span>            <span class="hljs-keyword">boolean</span> free = exclusiveCount(nextc) == <span class="hljs-number">0</span>;            <span class="hljs-keyword">if</span> (free)                <span class="hljs-comment">//为0说明当前没有线程独占</span>                setExclusiveOwnerThread(<span class="hljs-keyword">null</span>);            setState(nextc);            <span class="hljs-keyword">return</span> free;&#125;</code></pre><h3 id="tryReleaseShared"><a href="#tryReleaseShared" class="headerlink" title="tryReleaseShared"></a>tryReleaseShared</h3><p><code>ReadLock</code>释放锁的流程，与<code>WriteLock</code>释放类似。</p><pre><code class="hljs java"><span class="hljs-comment">//ReadLock</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">()</span> </span>&#123;            sync.releaseShared(<span class="hljs-number">1</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryReleaseShared</span><span class="hljs-params">(<span class="hljs-keyword">int</span> unused)</span> </span>&#123;    <span class="hljs-comment">//获取当前线程</span>            Thread current = Thread.currentThread();    <span class="hljs-comment">//如果当前线程是第一个读线程</span>            <span class="hljs-keyword">if</span> (firstReader == current) &#123;                <span class="hljs-comment">// assert firstReaderHoldCount &gt; 0;</span>                <span class="hljs-comment">//判断读线程占用资源数是否为1</span>                <span class="hljs-keyword">if</span> (firstReaderHoldCount == <span class="hljs-number">1</span>)                    <span class="hljs-comment">//为1则将第一个读线程置为空</span>                    firstReader = <span class="hljs-keyword">null</span>;                <span class="hljs-keyword">else</span>                    <span class="hljs-comment">//不然就--</span>                    firstReaderHoldCount--;            &#125; <span class="hljs-keyword">else</span> &#123;                <span class="hljs-comment">//到这段说明：当前线程不是第一个读线程</span>                HoldCounter rh = cachedHoldCounter;                <span class="hljs-comment">//如果计数器为空，或者计数器中tid存储的不是当前线程</span>                <span class="hljs-keyword">if</span> (rh == <span class="hljs-keyword">null</span> || rh.tid != getThreadId(current))                    <span class="hljs-comment">//将计数器设置为当前线程计数器</span>                    rh = readHolds.get();                <span class="hljs-comment">//获取count</span>                <span class="hljs-keyword">int</span> count = rh.count;                <span class="hljs-keyword">if</span> (count &lt;= <span class="hljs-number">1</span>) &#123;                    <span class="hljs-comment">//count &lt;= 1 则将ThreadLocal中的值删除</span>                    readHolds.remove();                    <span class="hljs-keyword">if</span> (count &lt;= <span class="hljs-number">0</span>)                        <span class="hljs-keyword">throw</span> unmatchedUnlockException();                &#125;                <span class="hljs-comment">//更新计数器</span>                --rh.count;            &#125;    <span class="hljs-comment">//CAS自旋进行更新state</span>            <span class="hljs-keyword">for</span> (;;) &#123;                <span class="hljs-keyword">int</span> c = getState();                <span class="hljs-keyword">int</span> nextc = c - SHARED_UNIT;                <span class="hljs-keyword">if</span> (compareAndSetState(c, nextc))                    <span class="hljs-comment">// Releasing the read lock has no effect on readers,</span>                    <span class="hljs-comment">// but it may allow waiting writers to proceed if</span>                    <span class="hljs-comment">// both read and write locks are now free.</span>                    <span class="hljs-keyword">return</span> nextc == <span class="hljs-number">0</span>;            &#125;        &#125;</code></pre><h3 id="tryAcquire"><a href="#tryAcquire" class="headerlink" title="tryAcquire"></a>tryAcquire</h3><p>此方法用于写线程获取写锁，基本流程如下：</p><ul><li>首先会获取state，判断是否为0<ul><li>若为0，表示此时没有读锁线程。再判断写线程是否应该被阻塞，而在非公平策略下总是不会被阻塞，在公平策略下会进行判断(判断同步队列中是否有等待时间更长的线程，若存在，则需要被阻塞，否则，无需阻塞)，之后在设置状态state，然后返回true。</li><li>若不为0，则表示此时存在读锁或写锁线程，若写锁线程数量为0或者当前线程不是独占锁线程，则返回false，表示不成功，否则，判断写锁线程的申请资源数量 + 现有的写线程数量是否大于了<code>MAX_COUNT</code>，若是，则抛出<code>Error</code>，否则，设置状态<code>state</code>，返回true，表示成功。</li></ul></li></ul><pre><code class="hljs java"><span class="hljs-comment">//WriteLock</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span> </span>&#123;            sync.acquire(<span class="hljs-number">1</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-comment">/*</span><span class="hljs-comment">             * Walkthrough:</span><span class="hljs-comment">             * 1. If read count nonzero or write count nonzero</span><span class="hljs-comment">             *    and owner is a different thread, fail.</span><span class="hljs-comment">             * 2. If count would saturate, fail. (This can only</span><span class="hljs-comment">             *    happen if count is already nonzero.)</span><span class="hljs-comment">             * 3. Otherwise, this thread is eligible for lock if</span><span class="hljs-comment">             *    it is either a reentrant acquire or</span><span class="hljs-comment">             *    queue policy allows it. If so, update state</span><span class="hljs-comment">             *    and set owner.</span><span class="hljs-comment">             */</span>            Thread current = Thread.currentThread();    <span class="hljs-comment">//c为state</span>            <span class="hljs-keyword">int</span> c = getState();    <span class="hljs-comment">//w为写锁的线程数量</span>            <span class="hljs-keyword">int</span> w = exclusiveCount(c);    <span class="hljs-comment">//c == 0代表当前没有读锁线程或写锁线程</span>            <span class="hljs-keyword">if</span> (c != <span class="hljs-number">0</span>) &#123;                <span class="hljs-comment">// (Note: if c != 0 and w == 0 then shared count != 0)</span>                <span class="hljs-comment">//写线程数量为0或者当前线程没有占有独占资源</span>                <span class="hljs-keyword">if</span> (w == <span class="hljs-number">0</span> || current != getExclusiveOwnerThread())                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;                <span class="hljs-comment">//如果申请的数量 + 现有的写线程 &gt; MAX_COUNT</span>                <span class="hljs-keyword">if</span> (w + exclusiveCount(acquires) &gt; MAX_COUNT)                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Maximum lock count exceeded&quot;</span>);                <span class="hljs-comment">// Reentrant acquire</span>                setState(c + acquires);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;            &#125;            <span class="hljs-keyword">if</span> (writerShouldBlock() ||                !compareAndSetState(c, c + acquires))                <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    <span class="hljs-comment">//设置独占线程</span>            setExclusiveOwnerThread(current);            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;</code></pre><h3 id="tryAcquireShared"><a href="#tryAcquireShared" class="headerlink" title="tryAcquireShared"></a>tryAcquireShared</h3><p>此方法用于读线程获取读锁，基本流程如下：</p><ul><li>首先判断写锁是否为0并且当前线程不占有独占锁，直接返回。</li><li>否则，判断读线程是否需要被阻塞并且读锁数量是否小于最大值并且比较设置状态成功<ul><li>若当前没有读锁，则设置第一个读线程firstReader和firstReaderHoldCount</li><li>若当前线程线程为第一个读线程，则增加firstReaderHoldCount</li><li>否则，将设置当前线程对应的HoldCounter对象的值。</li></ul></li><li>如果下列三个条件不满足(读线程是否应该被阻塞、小于最大值、比较设置成功)则会执行<code>fullTryAcquireShared</code>。</li></ul><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">tryAcquireShared</span><span class="hljs-params">(<span class="hljs-keyword">int</span> unused)</span> </span>&#123;            <span class="hljs-comment">/*</span><span class="hljs-comment">             * Walkthrough:</span><span class="hljs-comment">             * 1. If write lock held by another thread, fail.</span><span class="hljs-comment">             * 2. Otherwise, this thread is eligible for</span><span class="hljs-comment">             *    lock wrt state, so ask if it should block</span><span class="hljs-comment">             *    because of queue policy. If not, try</span><span class="hljs-comment">             *    to grant by CASing state and updating count.</span><span class="hljs-comment">             *    Note that step does not check for reentrant</span><span class="hljs-comment">             *    acquires, which is postponed to full version</span><span class="hljs-comment">             *    to avoid having to check hold count in</span><span class="hljs-comment">             *    the more typical non-reentrant case.</span><span class="hljs-comment">             * 3. If step 2 fails either because thread</span><span class="hljs-comment">             *    apparently not eligible or CAS fails or count</span><span class="hljs-comment">             *    saturated, chain to version with full retry loop.</span><span class="hljs-comment">             */</span>            Thread current = Thread.currentThread();            <span class="hljs-keyword">int</span> c = getState();    <span class="hljs-comment">//如果当前有写线程，并且独占线程不是当前线程</span>            <span class="hljs-keyword">if</span> (exclusiveCount(c) != <span class="hljs-number">0</span> &amp;&amp;                getExclusiveOwnerThread() != current)                <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;    <span class="hljs-comment">//读锁数量</span>            <span class="hljs-keyword">int</span> r = sharedCount(c);    <span class="hljs-comment">//如果当前线程需要被阻塞，并且读锁数量小于MAX_COUNT,并且CAS设置state成功</span>            <span class="hljs-keyword">if</span> (!readerShouldBlock() &amp;&amp;                r &lt; MAX_COUNT &amp;&amp;                compareAndSetState(c, c + SHARED_UNIT)) &#123;                <span class="hljs-comment">//读锁为0</span>                <span class="hljs-keyword">if</span> (r == <span class="hljs-number">0</span>) &#123;                    <span class="hljs-comment">//设置第一个读线程和计数器</span>                    firstReader = current;                    firstReaderHoldCount = <span class="hljs-number">1</span>;                &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (firstReader == current) &#123;                    <span class="hljs-comment">//当前线程为第一个读线程</span>                    <span class="hljs-comment">//计数器加1</span>                    firstReaderHoldCount++;                &#125; <span class="hljs-keyword">else</span> &#123;                    <span class="hljs-comment">//设置当前线程对应的HoldCounter对象的值</span>                    HoldCounter rh = cachedHoldCounter;                    <span class="hljs-keyword">if</span> (rh == <span class="hljs-keyword">null</span> || rh.tid != getThreadId(current))                        cachedHoldCounter = rh = readHolds.get();                    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (rh.count == <span class="hljs-number">0</span>)                        readHolds.set(rh);                    rh.count++;                &#125;                <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;            &#125;            <span class="hljs-keyword">return</span> fullTryAcquireShared(current);        &#125;</code></pre><h2 id="NofairSync"><a href="#NofairSync" class="headerlink" title="NofairSync"></a>NofairSync</h2><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">NonfairSync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Sync</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">8159625535654395037L</span>;        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">writerShouldBlock</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>; <span class="hljs-comment">// writers can always barge</span>        &#125;        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">readerShouldBlock</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-comment">/* As a heuristic to avoid indefinite writer starvation,</span><span class="hljs-comment">             * block if the thread that momentarily appears to be head</span><span class="hljs-comment">             * of queue, if one exists, is a waiting writer.  This is</span><span class="hljs-comment">             * only a probabilistic effect since a new reader will not</span><span class="hljs-comment">             * block if there is a waiting writer behind other enabled</span><span class="hljs-comment">             * readers that have not yet drained from the queue.</span><span class="hljs-comment">             */</span>            <span class="hljs-keyword">return</span> apparentlyFirstQueuedIsExclusive();        &#125;    &#125;</code></pre><h2 id="FairSync"><a href="#FairSync" class="headerlink" title="FairSync"></a>FairSync</h2><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FairSync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Sync</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">2274990926593161451L</span>;        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">writerShouldBlock</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> hasQueuedPredecessors();        &#125;        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">readerShouldBlock</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> hasQueuedPredecessors();        &#125;    &#125;</code></pre><h2 id="ReadLock"><a href="#ReadLock" class="headerlink" title="ReadLock"></a>ReadLock</h2><h3 id="基本信息"><a href="#基本信息" class="headerlink" title="基本信息"></a>基本信息</h3><p><code>ReadLock</code>拥有一个内部变量<code>sync</code>，构造方法用于初始化<code>sync</code>，可以联系<code>ReentrantReadWriteLock</code>的构造方法一起看。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ReadLock</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Lock</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">5992448646407690164L</span>;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Sync sync;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * Constructor for use by subclasses</span><span class="hljs-comment">         *</span><span class="hljs-comment">         * <span class="hljs-doctag">@param</span> lock the outer lock object</span><span class="hljs-comment">         * <span class="hljs-doctag">@throws</span> NullPointerException if the lock is null</span><span class="hljs-comment">         */</span>        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-title">ReadLock</span><span class="hljs-params">(ReentrantReadWriteLock lock)</span> </span>&#123;            sync = lock.sync;        &#125;&#125;</code></pre><h3 id="核心方法"><a href="#核心方法" class="headerlink" title="核心方法"></a>核心方法</h3><p>从核心方法<code>lock()</code>和<code>unlock()</code>中可以看出其具体实现都是交给<code>sync</code>进行实现。读操作由于是共享的，所以它使用的是<code>AQS</code>的共享模式实现的。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span> </span>&#123;            sync.acquireShared(<span class="hljs-number">1</span>);&#125;<span class="hljs-comment">//相应中断的lock</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lockInterruptibly</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;            sync.acquireSharedInterruptibly(<span class="hljs-number">1</span>);&#125;<span class="hljs-comment">//尝试获取lock</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryLock</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> sync.tryReadLock();&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryLock</span><span class="hljs-params">(<span class="hljs-keyword">long</span> timeout, TimeUnit unit)</span></span><span class="hljs-function">                <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;            <span class="hljs-keyword">return</span> sync.tryAcquireSharedNanos(<span class="hljs-number">1</span>, unit.toNanos(timeout));&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">()</span> </span>&#123;            sync.releaseShared(<span class="hljs-number">1</span>);&#125;</code></pre><h2 id="WriteLock"><a href="#WriteLock" class="headerlink" title="WriteLock"></a>WriteLock</h2><p><code>WriteLock</code>和<code>ReadLock</code>类似，不同的是，写操作是独占的，因此它使用<code>AQS</code>的独占模式实现。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">WriteLock</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Lock</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">4992448646407690164L</span>;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Sync sync;        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-title">WriteLock</span><span class="hljs-params">(ReentrantReadWriteLock lock)</span> </span>&#123;            sync = lock.sync;        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span> </span>&#123;            sync.acquire(<span class="hljs-number">1</span>);        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lockInterruptibly</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;            sync.acquireInterruptibly(<span class="hljs-number">1</span>);        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryLock</span><span class="hljs-params">( )</span> </span>&#123;            <span class="hljs-keyword">return</span> sync.tryWriteLock();        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryLock</span><span class="hljs-params">(<span class="hljs-keyword">long</span> timeout, TimeUnit unit)</span></span><span class="hljs-function">                <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;            <span class="hljs-keyword">return</span> sync.tryAcquireNanos(<span class="hljs-number">1</span>, unit.toNanos(timeout));        &#125;        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">()</span> </span>&#123;            sync.release(<span class="hljs-number">1</span>);        &#125;</code></pre><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ul><li><a href="https://juejin.cn/post/6844903663488483336">https://juejin.cn/post/6844903663488483336</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;p&gt;&lt;code&gt;ReentrantReadWriteLock&lt;/code&gt; 分为读锁和写锁两个实例，读锁是共享锁，可被多个线程同时使用，写锁是独</summary>
      
    
    
    
    <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
    <category term="Java并发" scheme="https://l1n.wang/categories/Java/Java%E5%B9%B6%E5%8F%91/"/>
    
    
    <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
    <category term="并发" scheme="https://l1n.wang/tags/%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>ReentrantLock源码分析</title>
    <link href="https://l1n.wang/2020/11/reentrantlock-code/"/>
    <id>https://l1n.wang/2020/11/reentrantlock-code/</id>
    <published>2020-11-20T04:40:08.000Z</published>
    <updated>2020-11-20T04:41:33.871Z</updated>
    
    <content type="html"><![CDATA[<h1 id="源码实现"><a href="#源码实现" class="headerlink" title="源码实现"></a>源码实现</h1><h2 id="类的继承关系"><a href="#类的继承关系" class="headerlink" title="类的继承关系"></a>类的继承关系</h2><p><code>ReentrantLock</code>实现了<code>Lock</code>接口，<code>Lock</code>接口定义了锁的通用方法。</p><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ReentrantLock</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Lock</span>, <span class="hljs-title">java</span>.<span class="hljs-title">io</span>.<span class="hljs-title">Serializable</span> </span></code></pre><h2 id="内部类"><a href="#内部类" class="headerlink" title="内部类"></a>内部类</h2><p><code>ReentrantLock</code>内部有三个内部类，其中<code>Sync</code>是其它两个类<code>NonfairSync</code>和<code>FairSync</code>的父类，分别代表着非公平策略和公平策略。</p><h3 id="Sync"><a href="#Sync" class="headerlink" title="Sync"></a>Sync</h3><p>继承自<code>AbstractQueuedSynchronizer</code>，实现了对<code>state</code>字段的修改操作。</p><pre><code class="hljs java"><span class="hljs-keyword">abstract</span> <span class="hljs-keyword">static</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Sync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">AbstractQueuedSynchronizer</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">5179523762034025860L</span>;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * Performs &#123;<span class="hljs-doctag">@link</span> Lock#lock&#125;. The main reason for subclassing</span><span class="hljs-comment">         * is to allow fast path for nonfair version.</span><span class="hljs-comment">         */</span>        <span class="hljs-function"><span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span></span>;        <span class="hljs-comment">/**</span><span class="hljs-comment">         * Performs non-fair tryLock.  tryAcquire is implemented in</span><span class="hljs-comment">         * subclasses, but both need nonfair try for trylock method.</span><span class="hljs-comment">         */</span>    <span class="hljs-comment">//非公平方式尝试获取锁</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">nonfairTryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-comment">//获取当前线程</span>            <span class="hljs-keyword">final</span> Thread current = Thread.currentThread();            <span class="hljs-comment">//获取AQS的state状态</span>            <span class="hljs-keyword">int</span> c = getState();            <span class="hljs-comment">//为0表示暂无线程占用锁</span>            <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>) &#123;                <span class="hljs-comment">//通过CAS设置state</span>                <span class="hljs-keyword">if</span> (compareAndSetState(<span class="hljs-number">0</span>, acquires)) &#123;                    <span class="hljs-comment">//设置成功之后，设置当前线程独占</span>                    setExclusiveOwnerThread(current);                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;                &#125;            &#125;            <span class="hljs-comment">//如果当前线程拥有锁，则表示进行重入</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (current == getExclusiveOwnerThread()) &#123;                <span class="hljs-comment">//添加重入次数</span>                <span class="hljs-keyword">int</span> nextc = c + acquires;                <span class="hljs-keyword">if</span> (nextc &lt; <span class="hljs-number">0</span>) <span class="hljs-comment">// overflow</span>                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Maximum lock count exceeded&quot;</span>);                setState(nextc);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;            &#125;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;        &#125;    <span class="hljs-comment">//尝试释放锁资源，全部释放则返回true</span>        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryRelease</span><span class="hljs-params">(<span class="hljs-keyword">int</span> releases)</span> </span>&#123;            <span class="hljs-comment">//c是释放后的资源量</span>            <span class="hljs-keyword">int</span> c = getState() - releases;            <span class="hljs-comment">//如果当前线程不是占有锁的线程，抛出异常</span>            <span class="hljs-keyword">if</span> (Thread.currentThread() != getExclusiveOwnerThread())                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalMonitorStateException();            <span class="hljs-comment">//free是全部释放的标识</span>            <span class="hljs-keyword">boolean</span> free = <span class="hljs-keyword">false</span>;            <span class="hljs-comment">//如果c = 0，说明全部释放资源，可重入环境</span>            <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>) &#123;                <span class="hljs-comment">//设置全部释放标识</span>                free = <span class="hljs-keyword">true</span>;                <span class="hljs-comment">//置空独占线程</span>                setExclusiveOwnerThread(<span class="hljs-keyword">null</span>);            &#125;            setState(c);            <span class="hljs-keyword">return</span> free;        &#125;    <span class="hljs-comment">//判断资源是否被当前线程占有</span>        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isHeldExclusively</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-comment">// While we must in general read state before owner,</span>            <span class="hljs-comment">// we don&#x27;t need to do so to check if current thread is owner</span>            <span class="hljs-keyword">return</span> getExclusiveOwnerThread() == Thread.currentThread();        &#125;    <span class="hljs-comment">//生成一个条件</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> ConditionObject <span class="hljs-title">newCondition</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ConditionObject();        &#125;        <span class="hljs-comment">// Methods relayed from outer class</span><span class="hljs-comment">//返回占用锁的线程</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> Thread <span class="hljs-title">getOwner</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> getState() == <span class="hljs-number">0</span> ? <span class="hljs-keyword">null</span> : getExclusiveOwnerThread();        &#125;<span class="hljs-comment">//如果锁被线程占有，则返回state</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> <span class="hljs-title">getHoldCount</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> isHeldExclusively() ? getState() : <span class="hljs-number">0</span>;        &#125;    <span class="hljs-comment">//判断锁是否被线程占有</span>    <span class="hljs-comment">//state不等于0则锁被线程占用</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isLocked</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-keyword">return</span> getState() != <span class="hljs-number">0</span>;        &#125;        <span class="hljs-comment">//自定义反序列化逻辑</span>        <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">readObject</span><span class="hljs-params">(java.io.ObjectInputStream s)</span></span><span class="hljs-function">            <span class="hljs-keyword">throws</span> java.io.IOException, ClassNotFoundException </span>&#123;            s.defaultReadObject();            setState(<span class="hljs-number">0</span>); <span class="hljs-comment">// reset to unlocked state</span>        &#125;    &#125;</code></pre><h3 id="NonfairSync"><a href="#NonfairSync" class="headerlink" title="NonfairSync"></a>NonfairSync</h3><p><code>NonfairSync</code>表示是非公平策略获取锁。</p><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">NonfairSync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Sync</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = <span class="hljs-number">7316153563782823691L</span>;        <span class="hljs-comment">//获得锁</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span> </span>&#123;            <span class="hljs-comment">//CAS操作设置state为1</span>            <span class="hljs-keyword">if</span> (compareAndSetState(<span class="hljs-number">0</span>, <span class="hljs-number">1</span>))                <span class="hljs-comment">//CAS设置成功，则设置独占进程为当前进程</span>                setExclusiveOwnerThread(Thread.currentThread());            <span class="hljs-keyword">else</span>                <span class="hljs-comment">//锁已经被占用，或者set失败</span>                <span class="hljs-comment">//独占方式进行获取</span>                acquire(<span class="hljs-number">1</span>);        &#125;        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-keyword">return</span> nonfairTryAcquire(acquires);        &#125;    &#125;</code></pre><h3 id="FairSync"><a href="#FairSync" class="headerlink" title="FairSync"></a>FairSync</h3><p>采用公平策略获取锁。</p><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FairSync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Sync</span> </span>&#123;        <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = -<span class="hljs-number">3000897897090466540L</span>;<span class="hljs-comment">//调用acquire方法，以独占方式获取锁</span>        <span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span> </span>&#123;            acquire(<span class="hljs-number">1</span>);        &#125;        <span class="hljs-comment">//尝试获取公平锁</span>        <span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> acquires)</span> </span>&#123;            <span class="hljs-comment">//获取当前线程</span>            <span class="hljs-keyword">final</span> Thread current = Thread.currentThread();            <span class="hljs-comment">//获取AQS的state</span>            <span class="hljs-keyword">int</span> c = getState();            <span class="hljs-comment">//如果当前没有线程占有锁</span>            <span class="hljs-keyword">if</span> (c == <span class="hljs-number">0</span>) &#123;                <span class="hljs-comment">//判断AQS Queue是否有线程在等待</span>                <span class="hljs-comment">//如果没有则直接通过CAS获取锁资源</span>                <span class="hljs-keyword">if</span> (!hasQueuedPredecessors() &amp;&amp;                    compareAndSetState(<span class="hljs-number">0</span>, acquires)) &#123;                    <span class="hljs-comment">//设置当前线程为独占线程</span>                    setExclusiveOwnerThread(current);                    <span class="hljs-comment">//获取成功</span>                    <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;                &#125;            &#125;            <span class="hljs-comment">//如果当前线程已经占有锁，则更新可重入信息</span>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (current == getExclusiveOwnerThread()) &#123;                <span class="hljs-comment">//更新可重入信息</span>                <span class="hljs-keyword">int</span> nextc = c + acquires;                <span class="hljs-comment">//检查边界</span>                <span class="hljs-keyword">if</span> (nextc &lt; <span class="hljs-number">0</span>)                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(<span class="hljs-string">&quot;Maximum lock count exceeded&quot;</span>);                setState(nextc);                <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;            &#125;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;        &#125;    &#125;</code></pre><h2 id="成员变量"><a href="#成员变量" class="headerlink" title="成员变量"></a>成员变量</h2><p><code>sync</code>代表当前<code>ReentrantLock</code>使用的获取策略。</p><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Sync sync;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> serialVersionUID = <span class="hljs-number">7373984872572414699L</span>;</code></pre><h2 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h2><ul><li>无参构造方法<ul><li>默认是非公平策略</li></ul></li><li>有参构造方法<ul><li>传入<code>true</code>使用公平策略。</li></ul></li></ul><pre><code class="hljs java"><span class="hljs-comment">/**</span><span class="hljs-comment">     * Creates an instance of &#123;<span class="hljs-doctag">@code</span> ReentrantLock&#125;.</span><span class="hljs-comment">     * This is equivalent to using &#123;<span class="hljs-doctag">@code</span> ReentrantLock(false)&#125;.</span><span class="hljs-comment">     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ReentrantLock</span><span class="hljs-params">()</span> </span>&#123;        sync = <span class="hljs-keyword">new</span> NonfairSync();    &#125;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Creates an instance of &#123;<span class="hljs-doctag">@code</span> ReentrantLock&#125; with the</span><span class="hljs-comment">     * given fairness policy.</span><span class="hljs-comment">     *</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> fair &#123;<span class="hljs-doctag">@code</span> true&#125; if this lock should use a fair ordering policy</span><span class="hljs-comment">     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ReentrantLock</span><span class="hljs-params">(<span class="hljs-keyword">boolean</span> fair)</span> </span>&#123;        sync = fair ? <span class="hljs-keyword">new</span> FairSync() : <span class="hljs-keyword">new</span> NonfairSync();    &#125;</code></pre><h2 id="核心方法"><a href="#核心方法" class="headerlink" title="核心方法"></a>核心方法</h2><h3 id="lock"><a href="#lock" class="headerlink" title="lock"></a>lock</h3><p>调用<code>sync.lock()</code></p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span> </span>&#123;        sync.lock();&#125;</code></pre><h3 id="lockInterruptibly"><a href="#lockInterruptibly" class="headerlink" title="lockInterruptibly"></a>lockInterruptibly</h3><p>响应中断的获取锁的方法，调用<code>AQS.acquireInterruptibly()</code>完成。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lockInterruptibly</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;        sync.acquireInterruptibly(<span class="hljs-number">1</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">acquireInterruptibly</span><span class="hljs-params">(<span class="hljs-keyword">int</span> arg)</span></span><span class="hljs-function">            <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;        <span class="hljs-keyword">if</span> (Thread.interrupted())            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> InterruptedException();        <span class="hljs-keyword">if</span> (!tryAcquire(arg))            doAcquireInterruptibly(arg);&#125;</code></pre><h3 id="tryLock"><a href="#tryLock" class="headerlink" title="tryLock"></a>tryLock</h3><p>非公平方式尝试获取锁，调用<code>sync.nonfairTryAcquire(1)</code>完成。重载方法提供了超时策略，同时响应中断。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryLock</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> sync.nonfairTryAcquire(<span class="hljs-number">1</span>);&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryLock</span><span class="hljs-params">(<span class="hljs-keyword">long</span> timeout, TimeUnit unit)</span></span><span class="hljs-function">            <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;        <span class="hljs-keyword">return</span> sync.tryAcquireNanos(<span class="hljs-number">1</span>, unit.toNanos(timeout));&#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryAcquireNanos</span><span class="hljs-params">(<span class="hljs-keyword">int</span> arg, <span class="hljs-keyword">long</span> nanosTimeout)</span></span><span class="hljs-function">            <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;        <span class="hljs-keyword">if</span> (Thread.interrupted())            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> InterruptedException();        <span class="hljs-keyword">return</span> tryAcquire(arg) ||            doAcquireNanos(arg, nanosTimeout);&#125;</code></pre><h3 id="unlock"><a href="#unlock" class="headerlink" title="unlock"></a>unlock</h3><p>调用<code>sync.release(1)</code>完成<code>unlock()</code>操作。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">()</span> </span>&#123;        sync.release(<span class="hljs-number">1</span>);&#125;</code></pre><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><code>ReentrantLock</code>的核心功能主要通过内部类<code>Sync</code>完成。而<code>Sync</code>继承自<code>AQS</code>，通过<code>AQS</code>中的<code>Sync Queue</code>完成对线程排队的功能。<code>ReentrantLock</code>的公平策略和非公平策略通过另外两个内部类<code>FairSync</code>、<code>NonfairSync</code>实现。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;源码实现&quot;&gt;&lt;a href=&quot;#源码实现&quot; class=&quot;headerlink&quot; title=&quot;源码实现&quot;&gt;&lt;/a&gt;源码实现&lt;/h1&gt;&lt;h2 id=&quot;类的继承关系&quot;&gt;&lt;a href=&quot;#类的继承关系&quot; class=&quot;headerlink&quot; title=&quot;类的继承关</summary>
      
    
    
    
    <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
    <category term="Java并发" scheme="https://l1n.wang/categories/Java/Java%E5%B9%B6%E5%8F%91/"/>
    
    
    <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
    <category term="并发" scheme="https://l1n.wang/tags/%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>《程序员练级攻略》 阅读总结</title>
    <link href="https://l1n.wang/2020/11/programmer-study-manual/"/>
    <id>https://l1n.wang/2020/11/programmer-study-manual/</id>
    <published>2020-11-17T06:41:20.000Z</published>
    <updated>2020-11-18T05:13:07.202Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>​        陈皓叔叔的这个专栏之前就有所耳闻并大致过了一遍，如今重新精读本专栏，认真学习，对整体的学习方向进行把握，并且进行总结。</p><p>如何入门？</p><p>学习Python，JavaScript，学习使用操作系统Linux，编程工具VS Code，Web开发等，这些更容易获得编程的成就感。</p><p>如何成为专业的程序员？</p><p>拥有程序员的自我修养。这是反映出程序员的工程师特质和价值观，决定了这条路你到底能走多远。<strong>有修养的程序员才可能成长为真正的工程师和架构师，没有修养的程序员智能沦为码农，这是码农和工程师的关键区分点。</strong></p><ul><li>英文能力</li><li>提问的能力</li><li>写代码的修养<ul><li><a href="https://book.douban.com/subject/4262627/">重构：改善既有代码的设计</a></li><li><a href="https://book.douban.com/subject/4199741/">代码整洁之道</a></li><li><a href="https://book.douban.com/subject/11614538/">程序员的职业素养</a></li><li>Code Review<ul><li>对自我成长非常有帮助</li><li>不做Code Review的公司没有必要呆</li></ul></li><li>Unit Test</li></ul></li><li>安全防范意识<ul><li>代码安全</li><li>防御性编程</li></ul></li><li>软件工程和上线规范<ul><li><a href="https://book.douban.com/subject/4187479/">完美软件：对软件测试的各种幻想</a></li><li><a href="https://book.douban.com/subject/25742200/">Google软件测试之道</a></li></ul></li><li>编程规范</li></ul><p>专业基础包括哪些？</p><ul><li>编程语言：推荐C、C++、Java这三个工业级的编程语言。</li><li>理论学科：数据结构和算法，计算机网络，计算机组成原理，操作系统，编译原理</li></ul><p>从业方向？</p><ul><li>如果你对操作系统、文件系统、数据库、网络等比较感兴趣，那么可以考虑从事底层方面的工作。</li><li>如果对分布式系统架构、微服务、DevOps、Cloud Native 等有热情，那么可以从事架构方面的工作。</li><li>如果是对大数据、机器学习、人工智能等比较关注，那么数据领域可以成为你一展身手的地方。</li><li>如果你对用户体验或者交互等更感兴趣，那么前端工程师也是个不错的选择。</li><li>此外，安全开发、运维开发、嵌入式开发等几大方向中，也为你提供了丰富多彩的发展空间。</li></ul><p>两个观点：</p><ul><li>各种技术方向不是鱼和熊掌，是可以兼得的。</li><li>很多技术都是相通的，关键是要学的深入本质。</li></ul><p>几个问题：</p><ul><li>理论和现实的差距：学院派的知识是成为专家的必经之路，<strong>这就是“工人”和“工程师</strong>”的差别，是“建筑工人”和“建筑架构师”的差别。</li><li>技术太多学不过来/技术能力的瓶颈：自己懒惰的借口，<strong>以绝大多数人努力的程度，和为自己不努力找借口的程度为参考，只要你坚持正常的学习就可以超过大多数人了。</strong></li></ul><p>学习建议？</p><ul><li>一定要坚持，要保持长时间学习，甚至终身学习的态度。</li><li>一定要动手，不管例子多么简单，建议至少自己动手敲一遍看看是否理解了里面的细枝末节。</li><li>一定要学会思考，思考为什么会这样，要学会举一反三。</li><li>不要乱买书，不要乱追新技术新名词，基础的东西经过很长时间积累，会在未来至少十年通用。</li></ul><h2 id="编程入门"><a href="#编程入门" class="headerlink" title="编程入门"></a>编程入门</h2><ul><li>入门语言Python</li><li>入门语言JavaScript</li><li>操作系统LInux</li><li>编程工具VS Code</li><li>Web入门</li></ul><h2 id="正式入门"><a href="#正式入门" class="headerlink" title="正式入门"></a>正式入门</h2><ul><li>编程技能<ul><li><a href="http://blog.thefirehoseproject.com/posts/learn-to-code-and-be-self-reliant/">如何快速提升自己的编程能力</a></li></ul></li><li>编程技巧<ul><li><a href="https://book.douban.com/subject/1477390/">代码大全</a></li></ul></li><li>编程语言<ul><li>Java</li></ul></li><li>操作系统<ul><li><a href="https://book.douban.com/subject/30359954/">鸟哥的Linux私房菜</a></li></ul></li><li>网络协议<ul><li>系统了解HTTP协议</li></ul></li><li>数据库设计<ul><li>基本的设计知识</li><li><a href="https://book.douban.com/subject/3354490/">MySQL必知必会</a></li></ul></li><li>前端<ul><li>Vue</li></ul></li><li>字符编码方面<ul><li><a href="https://en.wikipedia.org/wiki/Character_encoding">Wikipedia</a></li></ul></li></ul><p>为什么转成Java语言？</p><ul><li>Java语言的综合实力最强，是一个工业级的编程语言。</li><li>像 Python 和 JavaScript 这样的动态语言用着是很爽，但是，只有像 C、C++ 和 Java 这样的静态语言才可以让你真正地进阶。</li><li>对于一个合格的程序员，掌握几门语言是非常正常的事情。</li></ul><h3 id="编程工具"><a href="#编程工具" class="headerlink" title="编程工具"></a>编程工具</h3><ul><li>IDE</li><li>版本管理工具</li><li>前端调试</li><li>数据库设计工具</li></ul><h2 id="编程语言"><a href="#编程语言" class="headerlink" title="编程语言"></a>编程语言</h2><h3 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h3><ul><li><a href="https://book.douban.com/subject/34898994/">Java核心技术·卷 I 基础知识</a></li></ul><blockquote><p>这本书本来是 Sun 公司的官方用书，是一本 Java的入门参考书。对于 Java 初学者来说，是一本非常不错的值得时常翻阅的技术手册。书中有较多地方进行 Java 与 C++ 的比较，因为当时 Java 面世的时候，又被叫作 “C++ Killer”。而我在看这本书的时候，发现书中有很多 C++ 的东西，于是又去学习了C++。学习 C++ 的时候，发现有很多 C 的东西不懂，又顺着去学习了 C。然后，C -&gt;C++ -&gt; Java 整条线融汇贯通，这对我未来的技术成长有非常大的帮助。</p></blockquote><ul><li><a href="https://book.douban.com/subject/27047716/">Effective Java</a></li></ul><blockquote><p>这本书是模仿 Scott Meyers 的经典图书《Effective C++》的。Effective 这种书基本上都是各种经验之谈，所以，这是一本非常不错的书，你一定要读。这里需要推荐一下 Google Guava 库 ，这个库不但是 JDK 的升级库，其中有如：集合（collections）、缓存（caching）、原生类型支持（primitives support）、并发库（concurrency libraries）、通用注解（common annotations）、字符串处理（string processing）、I/O 等库，其还是Effective Java 这本书中的那些经验的实践代表。</p></blockquote><ul><li><a href="https://book.douban.com/subject/10484692/">Java并发编程实战</a></li></ul><blockquote><p>是一本完美的 Java 并发参考手册。书中从并发性和线程安全性的基本概念出发，介绍了如何使用类库提供的基本并发构建块，用于避免并发危险、构造线程安全的类及验证线程安全的规则，如何将小的线程安全类组合成更大的线程安全类，如何利用线程来提高并发应用程序的吞吐量，如何识别可并行执行的任务，如何提高单线程子系统的响应性，如何确保并发程序执行预期任务，如何提高并发代码的性能和可伸缩性等内容。最后介绍了一些高级主题，如显式锁、原子变量、非阻塞算法以及如何开发自定义的同步工具类。</p></blockquote><ul><li><a href="https://book.douban.com/subject/26740520/">Java性能权威指南</a></li></ul><blockquote><p>通过学习这本书，你可以比较大程度地提升性能测试的效果。其中包括：使用 JDK 中自带的工具收集 Java 应用的性能数据，理解 JIT 编译器的优缺点，调优JVM 垃圾收集器以减少对程序的影响，学习管理堆内存和 JVM 原生内存的方法，了解如何最大程度地优化 Java 线程及同步的性能，等等。看完这本书后，如果你还有余力，想了解更多的底层细节，那么，你有必要去读一下《深入理解 Java 虚拟机》。</p></blockquote><ul><li><a href="https://book.douban.com/subject/2130190/">Java编程思想</a></li></ul><blockquote><p>真是一本透着编程思想的书。上面的书让你从微观角度了解 Java，而这本书则可以让你从一个宏观角度了解 Java。这本书和 Java 核心技术的厚度差不多，但这本书的信息密度比较大。所以，读起来是非常耗大脑的，因为它会让你不断地思考。对于想学好 Java 的程序员来说，这是一本必读的书。</p></blockquote><p>学Java一定要学面向对象的设计模式，两本书：</p><ul><li><a href="https://book.douban.com/subject/2243615/">Head First 设计模式</a></li><li><a href="https://book.douban.com/subject/1052241/">设计模式</a></li></ul><p>学习设计模式，不能迷失在23个设计模式种，一定要明白两个原则：</p><ul><li><strong>Program to an ‘interface’, not an ‘implementation’</strong><ul><li>使用者不需要知道数据类型，结构，算法等细节。</li><li>使用者不需要知道实现细节，只需要知道提供的接口。</li><li>利用封装，抽象，动态绑定，多态等符合面向对象的特质和理念。</li></ul></li><li><strong>Favor ‘object composition’ over ‘class inheritance’</strong><ul><li>继承需要给子类暴露一些父类的设计和实现细节。</li><li>父类实现的改变会造成子类也需要改变。</li><li>我们以为继承主要是为了代码重用，实际上在子类中需要重新实现很多父类的方法。继承更多的应该是为了多态。</li></ul></li></ul><ul><li>Java字节码相关<ul><li>相关教程和工具</li></ul></li><li>JVM相关<ul><li>The Java Virtual Machine Specification Java SE 8 Edition</li><li>JSR-133</li><li>垃圾回收机制</li><li>垃圾回收调优</li></ul></li><li></li></ul><p>上面的这些知识融会贯通，已经是高级的Java程序员了。</p><h3 id="C-C-语言"><a href="#C-C-语言" class="headerlink" title="C/C++语言"></a>C/C++语言</h3><ul><li><a href="https://book.douban.com/subject/1139336/">C程序设计语言</a></li><li><a href="https://book.douban.com/subject/2280547/">C语言程序设计现代方法</a></li><li><a href="https://book.douban.com/subject/2778632/">C缺陷与陷阱</a></li><li><a href="https://book.douban.com/subject/25708312/">C++ Primer</a></li><li><a href="https://book.douban.com/subject/5387403/">Effective C++</a></li><li><a href="https://book.douban.com/subject/5908727/">More Effective C++</a></li><li><a href="https://book.douban.com/subject/10427315/">深度探索C++对象模型</a></li></ul><h3 id="Go"><a href="#Go" class="headerlink" title="Go"></a>Go</h3><ul><li><a href="https://gobyexample.com/">Go by Example</a></li><li><a href="https://book.douban.com/subject/27044219/">Go程序设计语言</a></li><li><a href="https://golang.org/doc/effective_go.html">Effective Go</a></li></ul><h2 id="理论学科"><a href="#理论学科" class="headerlink" title="理论学科"></a>理论学科</h2><h3 id="数据结构和算法"><a href="#数据结构和算法" class="headerlink" title="数据结构和算法"></a>数据结构和算法</h3><blockquote><p> 无论是做业务还是做底层系统，经常需要使用算法处理各种问题，例如：业务上需要算法比较两个数组中差异的布隆过滤器，或者是在监控系统实时计算过去一分钟的P99统计时的蓄水池算法，或者是数据库的B+树索引，还是Linux内核中的<code>epoll</code>或者<code>TreeMap</code>中的红黑树。还有做任务调度的背包问题等都会用到数据结构和算法。</p></blockquote><ul><li><p>算法书籍：</p><ul><li>《算法》</li><li>《算法图解》</li><li>《算法导论》</li><li>《编程珠玑》</li><li>《数据结构与算法分析》</li></ul></li></ul><h3 id="系统知识"><a href="#系统知识" class="headerlink" title="系统知识"></a>系统知识</h3><p>书籍：</p><ul><li>《深入理解计算机系统》</li><li>《Unix高级环境编程》</li><li>《Unix网络编程》</li><li>《TCP/IP详解 卷I协议》</li><li>《Linux C编程一站式学习》</li><li>《TCP/IP网络编程》</li><li>《图解TCP/IP》</li><li>《Wireshark数据包分析实战》</li></ul><h3 id="软件设计"><a href="#软件设计" class="headerlink" title="软件设计"></a>软件设计</h3><p>一些软件设计的相关原则：</p><ul><li><a href="https://en.wikipedia.org/wiki/Don%27t_repeat_yourself">Don’t repeat yourself</a></li><li><a href="https://en.wikipedia.org/wiki/KISS_principle">KISS principle</a></li><li>Program to an interface, not an implementation：设计模式中最根本的哲学，注重接口，而不是实现，依赖接口，而不是实现。</li><li><a href="https://en.wikipedia.org/wiki/You_Ain%27t_Gonna_Need_It">You aren’t gonna need it</a></li><li><a href="https://en.wikipedia.org/wiki/Law_of_Demeter">Law of Demeter</a></li><li><a href="https://zh.wikipedia.org/zh-hans/SOLID_(%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E8%AE%BE%E8%AE%A1">面向对象的SOLID原则</a>)</li><li><a href="http://en.wikipedia.org/wiki/Hollywood_Principle">Hollywood principle</a></li><li>高内聚低耦合</li><li>CoC（Convention over Configuration）- <strong>惯例优于配置原则</strong></li><li>SoC (Separation of Concerns) - 关注点分离，实现关注点分离的方法主要有两种，一种是标准化，另一种是抽象与包装</li><li>DbC（Design by Contract）：契约式设计</li><li>ADP（Acyclic Dependencies Principle）：无环依赖原则</li></ul><p>一些软件设计的读物：</p><ul><li>领域驱动设计</li><li>UNIX编程技术</li><li>Clean Architecture</li></ul><h3 id="Linux系统"><a href="#Linux系统" class="headerlink" title="Linux系统"></a>Linux系统</h3><h3 id="内存"><a href="#内存" class="headerlink" title="内存"></a>内存</h3><h3 id="计算机网络"><a href="#计算机网络" class="headerlink" title="计算机网络"></a>计算机网络</h3><ul><li>基础知识</li><li>网络调优</li><li>网络协议</li></ul><h3 id="I-O模型"><a href="#I-O模型" class="headerlink" title="I/O模型"></a>I/O模型</h3><ul><li>阻塞I/O</li><li>非阻塞I/O</li><li>I/O的多路复用（select、poll）</li><li>信号驱动的I/O（SIGIO）</li><li>异步I/O（POSIX的aio_functions）</li></ul><h3 id="Lock-Free编程相关"><a href="#Lock-Free编程相关" class="headerlink" title="Lock-Free编程相关"></a>Lock-Free编程相关</h3><p>如果想开发出一个高性能的程序，你就非常有必要学习<code>Lock-Free</code>的编程方式。</p><ul><li>相关的概念</li><li>论文</li><li>博客</li><li>现有的实现，C++/Java类库</li></ul><h3 id="其它"><a href="#其它" class="headerlink" title="其它"></a>其它</h3><ul><li>64位系统编程</li><li>提高OpenSSL的执行性能</li><li>压缩</li><li>SSD硬盘性能测试</li></ul><h3 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h3><ul><li>关系型数据库<ul><li>MySQL官方手册</li><li>高性能MySQL</li><li>MySQL技术内幕：InnoDB存储引擎</li><li>数据库的索引设计与优化</li></ul></li><li>非关系型数据库<ul><li>各类论文</li></ul></li></ul><h2 id="分布式架构"><a href="#分布式架构" class="headerlink" title="分布式架构"></a>分布式架构</h2><ul><li>服务调度<ul><li>涉及服务发现、配置管理、弹性伸缩、故障恢复等。</li></ul></li><li>资源调度<ul><li>涉及对底层资源的调度使用、如计算资源、网络资源和存储资源等。</li></ul></li><li>流量调度<ul><li>涉及路由、负载均衡、流量控制、熔断等。</li></ul></li><li>数据调度<ul><li>涉及数据副本、数据一致性、分布式事务、分库分表等。</li></ul></li><li>容错处理<ul><li>涉及隔离、幂等、重试、业务补偿、异步、降级等。</li></ul></li><li>自动化运维<ul><li>涉及持续集成、持续部署、全栈监控、调用链跟踪等。</li></ul></li></ul><p>几个注意点：</p><ul><li>分布式系统之所以复杂，就是因为它太容易出错了。这意味着，你要把处理错误的代码当成正常功能的代码来处理。</li><li>开发一个健壮的分布式系统的成本是单体系统的几百倍甚至几万倍。这意味着，我们要自己开发一个，需要能力很强的开发人员。</li><li>非常健壮的开源的分布式系统并不多，或者说基本没有。这意味着，如果你要用开源的，那么你需要 hold 得住其源码。</li><li>管理或是协调多个服务或机器是非常难的。这意味着，我们要去读很多很多的分布式系统的论文。</li><li>在分布式环境下，出了问题是很难 debug 的。这意味着，我们需要非常好的监控和跟踪系统，还需要经常做演练和测试。</li><li>在分布式环境下，你需要更科学地分析和统计。这意味着，我们要用 P90 这样的统计指标，而不是平均值，我们还需要做容量计划和评估。</li><li>在分布式环境下，需要应用服务化。这意味着，我们需要一个服务开发框架，比如 SOA或微服务。</li><li>在分布式环境下，故障不可怕，可怕的是影响面过大，时间过长。这意味着，我们需要花时间来开发我们的自动化运维平台。</li></ul><h3 id="分布式架构入门"><a href="#分布式架构入门" class="headerlink" title="分布式架构入门"></a>分布式架构入门</h3><ul><li>分布式理论<ul><li>各种理论算法</li><li>论文</li><li>一致性问题</li></ul></li><li>分布式事务</li><li>Paxos一致性算法</li><li>Raft一致性算法</li><li>Gossip一致性算法</li><li>分布式存储和数据库</li><li>分布式信息系统</li><li>日志和数据</li><li>分布式监控和跟踪</li><li>数据分析</li></ul><h3 id="分布式架构工程设计"><a href="#分布式架构工程设计" class="headerlink" title="分布式架构工程设计"></a>分布式架构工程设计</h3><ul><li>论文</li><li>分布式设计模式<ul><li>可用性</li><li>数据管理</li><li>设计和实现</li><li>消息</li><li>管理和监控</li><li>性能和扩展</li><li>系统弹力</li><li>安全</li></ul></li><li>设计与工程实践<ul><li>分布式系统的故障测试</li><li>弹性伸缩</li><li>一致性哈希</li><li>数据库分布式</li><li>缓存</li><li>消息队列</li><li>日志方面</li><li>性能方面</li><li>搜索方面</li><li>公司的架构实践</li></ul></li></ul><h2 id="容器化和自动化运维"><a href="#容器化和自动化运维" class="headerlink" title="容器化和自动化运维"></a>容器化和自动化运维</h2><h3 id="Docker"><a href="#Docker" class="headerlink" title="Docker"></a>Docker</h3><ul><li>基础知识</li><li>实践</li><li>底层技术细节</li></ul><h3 id="Kubernetes"><a href="#Kubernetes" class="headerlink" title="Kubernetes"></a>Kubernetes</h3><p>Kubernetes 是 Google 开源的容器集群管理系统，是 Google 多年大规模容器管理技术<br>Borg 的开源版本，也是 CNCF 最重要的项目之一，主要功能包括：</p><ul><li>基于容器的应用部署、维护和滚动升级；</li><li>负载均衡和服务发现；</li><li>跨机器和跨地区的集群调度；</li><li>自动伸缩；</li><li>无状态服务和有状态服务；</li><li>广泛的 Volume 支持；</li><li>插件机制保证扩展性。</li></ul><p>如何学习？</p><ul><li>Kubernetes handbook</li><li>Kubernetes指南</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;​        陈皓叔叔的这个专栏之前就有所耳闻并大致过了一遍，如今重新精读本专栏，认真学习，对整体的学习方向进行把握，并且进行总结。&lt;/</summary>
      
    
    
    
    <category term="随笔" scheme="https://l1n.wang/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
  </entry>
  
  <entry>
    <title>如何设计一个秒杀系统</title>
    <link href="https://l1n.wang/2020/11/seckill-summary/"/>
    <id>https://l1n.wang/2020/11/seckill-summary/</id>
    <published>2020-11-09T05:29:50.000Z</published>
    <updated>2020-11-18T11:03:02.537Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>  秒杀从规模上可以分为以下两类：</p><ul><li>大秒：类似双十一，商品数量规模大，价格低，流量超大的活动。</li><li>小秒：商家自己配置的一些时段类型的活动，由商家自己指定时间上架。</li></ul><h2 id="秒杀系统面对的问题"><a href="#秒杀系统面对的问题" class="headerlink" title="秒杀系统面对的问题"></a>秒杀系统面对的问题</h2><p>  秒杀系统本质上就是一个满足大并发、高性能和高可用的分布式系统。</p><ul><li>高并发环境下的系统稳定性：如何保证系统在面对巨大的流量情况下，不被打崩？<ul><li>两个问题<ul><li>并发读<ul><li>核心优化理念：减少用户到服务端来读数据。或者让他们读取更少的数据。</li></ul></li><li>并发写<ul><li>核心优化理念：在数据库层面独立出来一个库，做特殊的处理。</li></ul></li></ul></li><li>大流量会产生以下实际待解决问题<ul><li>Redis缓存击穿/雪崩/穿透等问题</li><li>关系型数据库性能问题，锁竞争对性能的消耗</li></ul></li></ul></li><li>保证数据的最终一致性：库存不能超卖。</li><li>大数据分析功能：分析本次秒杀活动的商业效益。</li><li>需要有一个兜底方案，以防最坏的情况发生。</li></ul><h1 id="设计"><a href="#设计" class="headerlink" title="设计"></a>设计</h1><h2 id="架构原则：-“4要1不要”"><a href="#架构原则：-“4要1不要”" class="headerlink" title="架构原则： “4要1不要”"></a>架构原则： “4要1不要”</h2><blockquote><p>架构是一种平衡的艺术，最好的架构一旦脱离了它所适应的场景，一切都将是空谈。</p></blockquote><ul><li><p>数据要尽量少</p><ul><li>用户请求的数据能少就少，包括上传给系统的数据和系统返回给用户的数据。</li></ul></li><li>HTTP请求数尽量少<ul><li>合并CSS，JS文件</li></ul></li><li>路径要尽量短<ul><li>用户发出请求到返回数据的过程中，经过的节点要尽量短<ul><li>通常，每经过一个节点，都会产生一个新的Socket连接。</li></ul></li><li>会减少时延</li><li>可以选择将多个相互强依赖的引用部署在一起，将RPC变成JVM内部的方法调用</li></ul></li><li>依赖要尽量少<ul><li>完成一次用户请求必须依赖的系统或服务要少（指的是强依赖）<ul><li>比如展示秒杀页面，它强依赖商品信息，用户信息，但是优惠券，成交列表等非必要模块是可以在紧急情况下去掉。</li></ul></li><li>对系统模块进行分级，0级，1级，2级等</li></ul></li><li>不要有单点<ul><li>系统中的单点是系统架构上的一个大忌，单点意味着没有备份，风险不可控。<ul><li>如何避免单点？<ul><li>避免将服务的状态和机器绑定（服务无状态化）<ul><li>把服务的配置动态化（使用配置中心Nacos等）</li><li>存储服务不好实现，因为数据持久化存储在机器的磁盘里面。文件存储可以通过冗余多个备份的方式来解决单点问题。</li></ul></li></ul></li></ul></li></ul></li></ul><h2 id="流量过滤"><a href="#流量过滤" class="headerlink" title="流量过滤"></a>流量过滤</h2><p>  本质：逐级过滤掉无效的流量。基本有以下一些解决方案：</p><ul><li>活动开始前前端页面的Button无法点击，防止活动尚未开始时，用户进行点击产生流量。<ul><li>同时后端需要做相关校验。避免用户直接请求秒杀接口。</li><li>秒杀url实现动态化，可以选择进行md5加密随机字符串，然后通过另一个接口校验秒杀接口的合法性</li></ul></li><li>错峰：前端添加验证码或者答题，防止瞬间产生超高的流量，增加题目辨别难度，避免以图像识别等技术进行破解。</li><li>校验：对参与活动的用户进行校验拦截。主要从以下几个方面进行判断<ul><li>用户白名单</li><li>用户终端校验：对用户终端类型进行判断</li><li>IP、MAC、ID校验</li><li>参与次数校验：避免多次参与活动</li><li>用户黑名单：避免羊毛党等</li></ul></li><li>限流：通过接口限流策略判断请求是否放行<ul><li>令牌桶算法</li></ul></li></ul><h2 id="性能优化"><a href="#性能优化" class="headerlink" title="性能优化"></a>性能优化</h2><p>  前面的流量过滤基本过滤掉大部分流量，但是系统性能还需进行优化，主要有以下的解决方案：</p><ul><li>动静分离</li><li>活动预热：将参加活动的商品独立出来，不和普通的商品库存共享服务，提前将数据缓存到<code>Redis</code>，查询全部走缓存，扣减库存视情况而定。</li><li>选择<code>Nginx</code></li><li>采用微服务架构部署，提高部署量，均摊请求。</li><li>秒杀是典型的读多写少的场景，考虑到单体redis的性能问题，可以考虑：<ul><li><code>Redis</code>集群</li><li>主从同步</li><li>读写分离</li><li>如果使用<code>Redis</code>集群，同时需要考虑保证多节点的数据一致性</li></ul></li><li>异步处理：采用消息队列<ul><li>异步，削峰，解耦</li></ul></li></ul><h3 id="动静分离"><a href="#动静分离" class="headerlink" title="动静分离"></a>动静分离</h3><p>  <strong>“动态数据”和“静态数据”的主要区别就是看页面中输出的数据是否和 URL、浏览者、时间、地域相关，以及是否含有 Cookie 等私密数据</strong>。</p><p>  简而言之，静态数据是能不经过后端请求，直接输出给用户的数据，</p><p>  如何对静态数据做缓存？</p><ul><li>把静态数据缓存到离用户最近的地方。<ul><li>用户浏览器</li><li>CDN<ul><li>失效问题</li><li>命中率问题</li><li>发布更新问题</li></ul></li><li>服务器端Cache</li></ul></li><li>静态化改造就是直接缓存HTTP连接。<ul><li>静态化改造是直接缓存 HTTP 连接而不是仅仅缓存数据。Web代理服务器直接根据请求<code>URL</code>，取出对应的HTTP响应头和响应体然后直接返回。</li></ul></li><li>选择缓存性能优秀的工具进行缓存服务，可以在Web服务器层面进行缓存，例如Nginx，Apache。</li></ul><h4 id="如何做动静分离的改造？"><a href="#如何做动静分离的改造？" class="headerlink" title="如何做动静分离的改造？"></a>如何做动静分离的改造？</h4><ul><li>URL唯一化，每个商品由ID来标识。item.xxx.com/item.htm?id=xxxx 就可以作为唯一的 URL 标识。<ul><li>URL唯一可以根据id作为key，查询之前缓存的HTTP连接。</li></ul></li><li>分离浏览者相关的因素。登录状态可以通过请求获取<ul><li>JWT</li></ul></li><li>分离时间因素。服务端输出的时间也通过请求获取。</li><li>异步化地域因素：详情页面上的地域信息做成异步方式获取。</li><li>去掉Cookie，可以通过Web服务器删除服务器输出的页面中的Cookie。</li></ul><h4 id="动态数据的处理方式"><a href="#动态数据的处理方式" class="headerlink" title="动态数据的处理方式"></a>动态数据的处理方式</h4><ul><li>ESI方案：在Web代理服务器上做动态内容请求，并将请求插入到静态页面中。当用户拿到页面时已经是一个完整的页面了。这种方式对服务端性能有些影响，但是用户体验较好。</li><li>CSI方案：即单独发起一个异步 JavaScript 请求，以向服务端获取动态内容。这种方式服务端性能更佳，但是用户端页面可能会延时，体验稍差。</li></ul><h2 id="解决超卖"><a href="#解决超卖" class="headerlink" title="解决超卖"></a>解决超卖</h2><p>  一般情况下，减库存有以下方式：</p><ul><li>下单减库存：是最简单的方式，也控制的最精准。但是存在恶意锁单等问题。</li><li>付款减库存：大并发的情况下，可能存在买家下单后无法付款的问题。</li><li>预扣库存：买家下单后，库存为其保留一定时间，超时未付款自动释放库存</li></ul><p>  如果扣库存逻辑较为简单，比如没有复杂的SKU库存和总库存这种联动关系的话，可以选择在<code>Redis</code>完成减库存。<code>Redis</code>定时更新到<code>MySQL</code>。</p><p>  复杂秒杀场景选择的方式：</p><ol><li>首先查询<code>Redis</code>缓存库存是否充足</li><li>先扣库存再落订单数据，以防订单生成没有了库存的超卖问题</li><li><p>扣库存先扣数据库，再扣<code>Redis</code>，并且两个操作需要在同一个事务中，一个执行失败全部回滚。</p><ul><li>数据库<code>MySQL</code>更新可以采取<strong>乐观锁</strong>方式，添加<code>version</code>字段进行处理，性能较悲观锁高。</li></ul><p>上述方案能一定程度解决问题，但是如果大量请求线程落在同一条库存记录上去<code>update</code>时，会造成<code>InnoDB</code>行锁的竞争问题，而并发度越高等待的线程会越多，TPS（Transaction Per Second）会下降，相应时间上升，数据库的吞吐量会严重受影响。</p><p>这时，需要做数据库层面的优化。</p></li></ol><h2 id="质量保障"><a href="#质量保障" class="headerlink" title="质量保障"></a>质量保障</h2><ul><li>熔断限流降级</li><li>监控：QPS监控，容器监控，CPU监控，IO监控等</li><li>提前压测</li></ul><h2 id="数据统计"><a href="#数据统计" class="headerlink" title="数据统计"></a>数据统计</h2><ul><li>埋点，检测</li><li>数据大盘，通过数据库数据配合监控系统（首选）</li><li>离线数据分析</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h1&gt;&lt;p&gt;  秒杀从规模上可以分为以下两类：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;大秒：类似双十一，商品数量规模大，价格低，流量超大的活动。&lt;/li&gt;
&lt;li&gt;</summary>
      
    
    
    
    <category term="系统设计" scheme="https://l1n.wang/categories/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/"/>
    
    
    <category term="系统设计" scheme="https://l1n.wang/tags/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/"/>
    
  </entry>
  
  <entry>
    <title>LockSupport源码分析</title>
    <link href="https://l1n.wang/2020/11/java-locksupport/"/>
    <id>https://l1n.wang/2020/11/java-locksupport/</id>
    <published>2020-11-08T04:46:28.000Z</published>
    <updated>2020-11-20T04:41:51.684Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><blockquote><p>Basic thread blocking primitives for creating locks and other synchronization classes.   — Java Doc</p></blockquote><p><code>LockSupport</code>是用来创建锁和其它同步类的基本线程阻塞原语。底层依赖<code>Unsafe</code>实现，我们可以在其它的并发同步工具类的实现中看到该类的使用。<code>LockSupport</code>提供了<code>park()</code>和<code>unpark()</code>方法来分别实现阻塞线程和唤醒线程，每个使用<code>LockSupport</code>的线程都有一个<code>permit</code>，该值默认为0，取值0,1。</p><ul><li><code>unpark()</code>：如果<code>permit</code>当前值为0，将其自增1。</li><li><code>park()</code>：如果当前<code>permit</code>为1，将其自减1并立即返回，如果为0，直接阻塞。</li></ul><p>这两个方法不会有<code>Thread.suspend</code> 和<code>Thread.resume</code>所可能引发的死锁问题，因为<code>permit</code>存在，调用 <code>park</code>的线程和另一个试图将其 <code>unpark</code>的线程之间的竞争将保持活性。</p><p>如果调用线程被中断，那么<code>park</code>将会返回。<code>park</code>方法可能在任何时间<strong>no reason</strong>地返回，因此通常在重新检查返回条件地循环里调用此方法。在某种意义上，<code>park</code>是<strong>busy wait</strong>（忙则等待）的一种优化，减少了自旋对性能的消耗。当时必须与<code>unpark</code>配合使用才会更加高效。</p><p><code>park</code>还提供了支持<code>blocker</code>参数的方法，<code>blocker</code>对象在线程受阻塞时被记录，用于允许监视和诊断工具确定线程被阻塞的原因。提供了<code>getBlocker(Thread t)</code>来访问<code>blocker</code>。</p><p>下面是<code>Java Docs</code>中的示例用法：一个先进先出非重入锁类的基本框架：</p><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">FIFOMutex</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> AtomicBoolean locked = <span class="hljs-keyword">new</span> AtomicBoolean(<span class="hljs-keyword">false</span>);    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Queue&lt;Thread&gt; waiters      = <span class="hljs-keyword">new</span> ConcurrentLinkedQueue&lt;Thread&gt;();     <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">lock</span><span class="hljs-params">()</span> </span>&#123;      <span class="hljs-keyword">boolean</span> wasInterrupted = <span class="hljs-keyword">false</span>;      Thread current = Thread.currentThread();      waiters.add(current);       <span class="hljs-comment">// Block while not first in queue or cannot acquire lock</span>      <span class="hljs-keyword">while</span> (waiters.peek() != current ||             !locked.compareAndSet(<span class="hljs-keyword">false</span>, <span class="hljs-keyword">true</span>)) &#123;        LockSupport.park(<span class="hljs-keyword">this</span>);        <span class="hljs-keyword">if</span> (Thread.interrupted()) <span class="hljs-comment">// ignore interrupts while waiting</span>          wasInterrupted = <span class="hljs-keyword">true</span>;      &#125;      waiters.remove();      <span class="hljs-keyword">if</span> (wasInterrupted)          <span class="hljs-comment">// reassert interrupt status on exit</span>        current.interrupt();    &#125;     <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">()</span> </span>&#123;      locked.set(<span class="hljs-keyword">false</span>);      LockSupport.unpark(waiters.peek());    &#125;  &#125;&#125;</code></pre><hr><h2 id="源码解读"><a href="#源码解读" class="headerlink" title="源码解读"></a>源码解读</h2><h3 id="成员变量"><a href="#成员变量" class="headerlink" title="成员变量"></a>成员变量</h3><ul><li>UNSAFE类：用于进行不安全的底层操作。</li><li>parkBlockerOffset：存储<code>Thread.parkBlocker</code>的内存偏移地址，记录线程被谁阻塞的。用于线程监控和分析工具用来定位原因的。可以通过<code>getBlocker</code>获取到阻塞的对象。</li><li>SEED：存储<code>Thread.threadLocalRandomSeed</code>的内存偏移地址</li><li>PROBE：存储<code>Thread.threadLocalRandomProbe</code>的内存偏移地址</li><li>SECONDARY：存储<code>Thread.threadLocalRandomSecondarySeed</code>的内存偏移地址</li></ul><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> sun.misc.Unsafe UNSAFE;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> parkBlockerOffset;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> SEED;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> PROBE;<span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> SECONDARY;</code></pre><h3 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h3><p>不允许实例化，只能通过调用静态方法来完成操作。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-title">LockSupport</span><span class="hljs-params">()</span> </span>&#123;&#125; <span class="hljs-comment">// Cannot be instantiated.</span></code></pre><h3 id="静态代码块"><a href="#静态代码块" class="headerlink" title="静态代码块"></a>静态代码块</h3><p>通过反射机制获取<code>Thread</code>类的<code>parkBlocker</code>字段，然后通过<code>UNSAFE.objectFieldOffset</code>获取到<code>parkBlocker</code>在内存的偏移量。</p><blockquote><p>Q：为什么不通过get/set方式获取某个字段？</p><p>A：parkBlocker在线程处于阻塞状态下才会被赋值，此时直接调用线程内的方法，线程不会作出回应的。</p></blockquote><pre><code class="hljs java"><span class="hljs-keyword">static</span> &#123;        <span class="hljs-keyword">try</span> &#123;            <span class="hljs-comment">//获取unsafe实例</span>            UNSAFE = sun.misc.Unsafe.getUnsafe();            Class&lt;?&gt; tk = Thread.class;            parkBlockerOffset = UNSAFE.objectFieldOffset                (tk.getDeclaredField(<span class="hljs-string">&quot;parkBlocker&quot;</span>));            SEED = UNSAFE.objectFieldOffset                (tk.getDeclaredField(<span class="hljs-string">&quot;threadLocalRandomSeed&quot;</span>));            PROBE = UNSAFE.objectFieldOffset                (tk.getDeclaredField(<span class="hljs-string">&quot;threadLocalRandomProbe&quot;</span>));            SECONDARY = UNSAFE.objectFieldOffset                (tk.getDeclaredField(<span class="hljs-string">&quot;threadLocalRandomSecondarySeed&quot;</span>));        &#125; <span class="hljs-keyword">catch</span> (Exception ex) &#123; <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> Error(ex); &#125;    &#125;</code></pre><h3 id="setBlocker"><a href="#setBlocker" class="headerlink" title="setBlocker"></a>setBlocker</h3><p>对给定的线程<code>t</code>的<code>parkBlocker</code>赋值。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setBlocker</span><span class="hljs-params">(Thread t, Object arg)</span> </span>&#123;        <span class="hljs-comment">// Even though volatile, hotspot doesn&#x27;t need a write barrier here.</span>        UNSAFE.putObject(t, parkBlockerOffset, arg);    &#125;</code></pre><h3 id="getBlocker"><a href="#getBlocker" class="headerlink" title="getBlocker"></a>getBlocker</h3><p>返回线程<code>t</code>的<code>parkBlocker</code>对象。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Object <span class="hljs-title">getBlocker</span><span class="hljs-params">(Thread t)</span> </span>&#123;        <span class="hljs-keyword">if</span> (t == <span class="hljs-keyword">null</span>)            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> NullPointerException();        <span class="hljs-keyword">return</span> UNSAFE.getObjectVolatile(t, parkBlockerOffset);    &#125;</code></pre><h3 id="park"><a href="#park" class="headerlink" title="park"></a>park</h3><p>park方法阻塞线程，发生以下情况时，当前线程会继续执行：</p><ul><li>其它线程调用<code>unpark</code>方法唤醒该线程</li><li>其它线程中断当前线程</li><li><strong>no reason</strong>地返回</li></ul><p>该方法有两个重载版本。</p><blockquote><p>Q：为什么调用两次setBlocker方法？</p></blockquote><p>A：调用<code>park</code>方法时，当前线程首先设置好<code>parkBlocker</code>字段，然后调用<code>UNSAFE.park</code>方法，此时，当前线程阻塞，第二个<code>setBlocker</code>无法执行，过了一段时间，该线程的<code>unpark</code>方法被调用，该线程拿到<code>permit</code>后执行，将该线程的<code>blocker</code>字段置空。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">park</span><span class="hljs-params">(Object blocker)</span> </span>&#123;        Thread t = Thread.currentThread();        setBlocker(t, blocker);        UNSAFE.park(<span class="hljs-keyword">false</span>, <span class="hljs-number">0L</span>);        setBlocker(t, <span class="hljs-keyword">null</span>);    &#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">park</span><span class="hljs-params">()</span> </span>&#123;        UNSAFE.park(<span class="hljs-keyword">false</span>, <span class="hljs-number">0L</span>);    &#125;</code></pre><h3 id="parkNanos"><a href="#parkNanos" class="headerlink" title="parkNanos"></a>parkNanos</h3><p>阻塞当前线程，最长不超过<code>nanos</code>纳秒</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parkNanos</span><span class="hljs-params">(<span class="hljs-keyword">long</span> nanos)</span> </span>&#123;        <span class="hljs-keyword">if</span> (nanos &gt; <span class="hljs-number">0</span>)            UNSAFE.park(<span class="hljs-keyword">false</span>, nanos);    &#125;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parkNanos</span><span class="hljs-params">(Object blocker, <span class="hljs-keyword">long</span> nanos)</span> </span>&#123;        <span class="hljs-keyword">if</span> (nanos &gt; <span class="hljs-number">0</span>) &#123;            Thread t = Thread.currentThread();            setBlocker(t, blocker);            UNSAFE.park(<span class="hljs-keyword">false</span>, nanos);            setBlocker(t, <span class="hljs-keyword">null</span>);        &#125;    &#125;</code></pre><h3 id="parkUntil"><a href="#parkUntil" class="headerlink" title="parkUntil"></a>parkUntil</h3><p>该方法表示在限定的时间内将阻塞。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">parkUntil</span><span class="hljs-params">(<span class="hljs-keyword">long</span> deadline)</span> </span>&#123;        UNSAFE.park(<span class="hljs-keyword">true</span>, deadline);    &#125;</code></pre><h3 id="unpark"><a href="#unpark" class="headerlink" title="unpark"></a>unpark</h3><p>如果给定线程的<code>permit</code>不可用，则将其置为可用，如果该线程阻塞，则将它解除阻塞状态。否则，保证下一次调用<code>park</code>不会受阻塞。如果给定线程尚未启动，则无法保证该操作有任何效果。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unpark</span><span class="hljs-params">(Thread thread)</span> </span>&#123;    <span class="hljs-keyword">if</span> (thread != <span class="hljs-keyword">null</span>)        UNSAFE.unpark(thread);&#125;</code></pre><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://docs.oracle.com/javase/8/docs/api/java/util/concurrent/locks/LockSupport.html#park--">Java SE Doc</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;blockquote&gt;
&lt;p&gt;Basic thread blocking primitives for creating locks and o</summary>
      
    
    
    
    <category term="Java" scheme="https://l1n.wang/categories/Java/"/>
    
    <category term="Java并发" scheme="https://l1n.wang/categories/Java/Java%E5%B9%B6%E5%8F%91/"/>
    
    
    <category term="Java" scheme="https://l1n.wang/tags/Java/"/>
    
    <category term="并发" scheme="https://l1n.wang/tags/%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
</feed>
